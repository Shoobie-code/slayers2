local m = game:GetService("Players") or game.Players;
local p = game:GetService("ReplicatedStorage") or game.ReplicatedStorage;
local q = game:GetService("RunService") or game.RunService;
local a = workspace;
local c = m.LocalPlayer;
if _G.Slayers2 then
	local m = _G.Slayers2;
	_G.Slayers2 = nil;
	m.Running = false;
	pcall(function()
		if m.drawConn then
			m.drawConn:Disconnect();
		end;
	end);
	pcall(function()
		if m.tickConn then
			m.tickConn:Disconnect();
		end;
	end);
	pcall(function()
		if m.fishConn then
			m.fishConn:Disconnect();
		end;
	end);
	pcall(function()
		if m.bmConn then
			m.bmConn:Disconnect();
		end;
	end);
	pcall(function()
		if m.deferConn then
			m.deferConn:Disconnect();
		end;
	end);
	m.fishConn, m.tickConn, m.drawConn, m.bmConn = nil, nil, nil, nil;
	m.deferConn = nil;
	m.fish, m.bar, m.route, m.collect, m.follow = nil, nil, nil, nil, nil;
	m.soul, m.dlg, m.dlgClick, m.crow, m.lootPhase = nil, nil, nil, nil, nil;
	pcall(function()
		mouse1release();
	end);
	pcall(function()
		mouse2release();
	end);
	pcall(function()
		keyrelease(84);
	end);
	m.pool = nil;
	if true then
 
	end;
	pcall(function()
		if m.win then
			m.win:Destroy();
		end;
	end);
end;
local Z;
do
	local m = rawget(_G, "UILib");
	if type(m) == "table" then
		local p = tonumber(rawget(m, "_now"));
		if p and ((tick() - p) > 3 and type(rawget(m, "_rehome")) ~= "function") then
			_G.UI_RELOAD = true;
		end;
	end;
	local p;
	pcall(function()
		p = readfile("UILib.lua");
	end);
	if type(p) ~= "string" or #p < 1000 then
		p = nil;
		pcall(function()
			p = game:HttpGet("https://raw.githubusercontent.com/Shoobie-code/linoria-inspired/refs/heads/main/main");
		end);
		if type(p) == "string" and #p > 1000 then
			pcall(writefile, "UILib.lua", p);
		else
			p = nil;
		end;
	end;
	if type(p) == "string" then
		pcall(function()
			(loadstring(p))();
		end);
	end;
	_G.UI_RELOAD = nil;
	Z = _G.UILib;
end;
if not Z then
	notify("UILib not found in workspace", "Slayers 2", 6);
	return;
end;
local W = {
		Running = true,
		pool = {},
		drawConn = nil,
		tickConn = nil,
		win = nil,
		Mode = "off",
		FarmKind = "boss",
		CrowSlot = 2,
		WeapSlot = 1,
		crow = nil,
		SelectedMob = "Bandit",
		SelectedBoss = { Akazo = true },
		SelectedQuest = "[Auto-Detect Active Quest]",
		QuestSel = { "[Auto-Detect Active Quest]" },
		Stance = "Under Target",
		Offset = 3.8,
		AttackDelay = .3,
		Retarget = 1.0,
		HealthGuard = false,
		GuardPct = .45,
		Smooth = true,
		MoveSpeed = 60,
		WarpAbove = 40,
		camT = 0,
		AimLock = true,
		SkillGap = .3,
		atkCount = 0,
		SkipBlocking = true,
		Reach = 18,
		blockedT = 0,
		AimGain = .55,
		rmbDown = false,
		HoldT = 2.0,
		SettleT = .6,
		AutoLoot = true,
		AutoSouls = true,
		LootRange = 300,
		LootWait = 5,
		lootPhase = nil,
		LootBoss = true,
		RouteTap = .05,
		collect = nil,
		pendingLoot = nil,
		seen = {},
		seenDirty = false,
		seenT = 0,
		chestTap = {},
		clearKey = nil,
		warnedFocus = 0,
		AutoTrain = false,
		NoClip = false,
		AntiAFK = false,
		EspMobs = false,
		EspBosses = false,
		EspChests = false,
		EspCrystals = false,
		EspLily = false,
		EspHorses = false,
		EspLevers = false,
		EspMuzan = false,
		EspFruits = false,
		QuestPick = "normal",
		QuestGap = 30,
		AutoFish = false,
		FishLead = .12,
		FishPause = 1.0,
		FishTimeout = 30,
		BossOrder = { "Akazo" },
		npcPosCache = {},
		acceptAt = nil,
		EspDist = 1200,
		PanicVK = 113,
	};
_G.Slayers2 = W;
local S = {};
S.Presets = {
		Combat = true,
		["Regular Katana"] = true,
		["Insect Katana"] = true,
		["Sound Katanas"] = true,
		Scythe = true,
		["Bladed Wagasa"] = true,
		Gauntlet = true,
		Soryu = true,
		["Tai Chi"] = true,
		Spear = true,
		Tanto = true,
		["War Fans"] = true,
		Shotgun = true,
		Sickles = true,
		Claws = true,
		["Blood Manipulation"] = true,
		Cutlass = true,
		["Axe and Mace"] = true,
		Reaper = true,
		["Obi Manipulation"] = true,
		Tamari = true,
		Dream = true,
	};
do
	local m = p:FindFirstChild("Assets");
	m = m and m:FindFirstChild("Animations");
	for m, p in ipairs(m and m:GetChildren() or {}) do
		local q = string.match(p.Name, "^(.*)_Combat_Anims$");
		if q and q ~= "" then
			S.Presets[q] = true;
		end;
	end;
end;
S.StyleWords = {
		cleaver = "Sound Katanas",
		fan = "War Fans",
		sickle = "Sickles",
		claw = "Claws",
		wagasa = "Bladed Wagasa",
	};
S.StyleMap = {};
do
	local m = {
			Gauntlets = "Gauntlet",
			Warfans = "War Fans",
			["Axe & Mace"] = "Axe and Mace",
			Katanas = "Regular Katana",
		};
	local function q(p)
		local q = string.gsub(p, "%s*Category%s*$", "");
		q = string.gsub(q, "^%s*(.-)%s*$", "%1");
		q = m[q] or q;
		if S.Presets[q] then
			return q;
		end;
		return nil;
	end;
	local a = p:FindFirstChild("ItemAssets");
	a = a and a:FindFirstChild("Tools");
	for m, p in ipairs(a and a:GetChildren() or {}) do
		local a = q(p.Name);
		for m, p in ipairs(p:GetChildren()) do
			local q = p.Name;
			local c = string.lower(q);
			local Z;
			if S.Presets[q] then
				Z = q;
			elseif string.find(c, "sound", 1, true) or string.find(c, "cleaver", 1, true) then
				Z = "Sound Katanas";
			elseif string.find(c, "insect", 1, true) then
				Z = "Insect Katana";
			else
				local m = string.match(q, "[-]%s*(.+)$");
				local p = string.match(q, "^(.+)%s+Orb$");
				if m and S.Presets[m] then
					Z = m;
				elseif p and S.Presets[p] then
					Z = p;
				else
					Z = a;
				end;
			end;
			if Z and S.Presets[Z] then
				S.StyleMap[c] = Z;
			end;
		end;
	end;
end;
do
	local m = p:FindFirstChild("CAM");
	m = m and m:FindFirstChild("Global");
	m = m and m:FindFirstChild("Powers");
	m = m and m:FindFirstChild("FightingStyles");
	if m then
		for m, p in ipairs(m:GetChildren()) do
			S.Presets[p.Name] = true;
		end;
	end;
end;
S.Spawns = {
		Bandit = Vector3.new(-287, 1224, -1008),
		Zuko = Vector3.new(-450, 1245, -950),
		Civilian = Vector3.new(-650, 1245, -1100),
		["*Civilian*"] = Vector3.new(-650, 1245, -1100),
		Mizunoto = Vector3.new(-888, 965, -26),
		["Beast Born Demon"] = Vector3.new(-850, 970, -120),
		["Blood Hounded Demon"] = Vector3.new(-870, 970, -100),
		["Bear Cub"] = Vector3.new(540, 1122, -1023),
		["Mother Bear"] = Vector3.new(550, 1122, -1035),
		Hoyuzo = Vector3.new(480, 1122, -1010),
		["Hoyuzo Subordinate"] = Vector3.new(470, 1122, -1010),
		Kaiden = Vector3.new(510, 1122, -1000),
		["Kaiden Subordinate"] = Vector3.new(500, 1122, -1000),
		["Lesser Demon"] = Vector3.new(-715, 220, 456),
		["Greater Demon"] = Vector3.new(-650, 235, 380),
		["High Demon"] = Vector3.new(518, 1223, -1785),
		["Ice Profound Demon"] = Vector3.new(312, 1223, -1778),
		["Fire Profound Demon"] = Vector3.new(230, 1254, -1983),
		["Kanoe Demon Slayer"] = Vector3.new(373, 1286, -2031),
		["Mizunoe Demon Slayer"] = Vector3.new(-2626.7, 284, -191.2),
		Fujiko = Vector3.new(-2626.7, 284, -191.2),
		Akazo = Vector3.new(-1132.6, 1381.1, -1742.5),
		Gyutai = Vector3.new(-263.1, 1043.5, -1150.5),
		Datai = Vector3.new(-163.2, 1043.5, -1136.9),
		Saneri = Vector3.new(-375.2, 1093.6, -438.8),
		Obari = Vector3.new(742.85, 1124.07, -1018.62),
		["Serpent Trainee"] = Vector3.new(-270.3, 1292.49, -1533.8),
		Reaper = Vector3.new(99.86, 1043.49, -576.31),
		Yahari = Vector3.new(823.2, 1019.1, -640.0),
		Sumari = Vector3.new(395.4, 1018.5, -622.7),
		Nezura = Vector3.new(-1456.63, 278.45, 927.69),
		Muzan = Vector3.new(2466.37, 1079.63, 2334.54),
	};
S.Mobs = {
		"All Regular Mobs",
		"Bandit",
		"Civilian",
		"*Civilian*",
		"Mizunoto",
		"Village Spy",
		"Karu Village Bandit",
		"Beast Born Demon",
		"Blood Hounded Demon",
		"Lesser Demon",
		"Greater Demon",
		"High Demon",
		"Ice Profound Demon",
		"Fire Profound Demon",
		"Rogue Demon",
		"Lost",
		"Iceveil Road Pikeman",
		"Iceveil Road Bandit",
		"Iceveil Road Marauder",
		"Bear Cub",
		"Mother Bear",
		"Hoyuzo Subordinate",
		"Kaiden Subordinate",
		"Kanoe Demon Slayer",
		"Mizunoe Demon Slayer",
		"Cache Lancer",
		"Cache Prowler",
		"Grove Raider",
		"Lancer Captain",
		"Prowler Captain",
		"Raid Captain",
		"Serpent Trainee",
		"Water Trainee",
		"Thunder Trainee",
		"Flame Trainee",
		"Wind Trainee",
		"Stone Trainee",
		"Insect Trainee",
		"Sound Trainee",
		"Soryu Trainee",
		"Tai Chi Trainee",
		"Reaper Trainee",
	};
S.Bosses = {
		"All Bosses",
		"Akazo",
		"Gyutai",
		"Datai",
		"Domae",
		"Reaper",
		"Nezura",
		"Yahari",
		"Sumari",
		"Enru",
		"Yeti Demon",
		"Small Yeti",
		"Hand Demon",
		"Zuko",
		"Kaiden",
		"Hoyuzo",
		"Mother Bear",
		"Fujiko",
		"Shinora",
		"Obari",
		"Saneri",
		"Giyen",
		"Tengai",
		"Zentaro",
		"Gyorei",
		"Rengu",
		"Muzan",
	};
S.BossNames = {};
for m = 1, #S.Bosses, 1 do
	S.BossNames[string.lower(S.Bosses[m])] = true;
end;
S.BossNames["all bosses"] = nil;
S.Zones = {
		["Windy Peak (Village)"] = Vector3.new(-650, 1245, -1100),
		["Windy Peak (Bandit Camp)"] = Vector3.new(-287, 1224, -1008),
		["Mistfall Harbor (Docks)"] = Vector3.new(-888, 965, -26),
		["Mistfall Harbor (Crystal)"] = Vector3.new(135.8, 873.7, 734.1),
		["Bamboo Grove (Bear Woods)"] = Vector3.new(540, 1122, -1023),
		["Bamboo Grove Sanctuary"] = Vector3.new(657.4, 1021.2, 139.7),
		["Butterfly Estate"] = Vector3.new(-1809.3, 311.8, -101.1),
		["Hidden Mist Village"] = Vector3.new(1793.08, 661.3, -231.72),
		["Iceveil Valley"] = Vector3.new(485.3, 1222.9, -1813.4),
		["Iceveil Settlement"] = Vector3.new(-154.62, 1351.5, -2556.21),
		["Final Selection Plains"] = Vector3.new(-2626.7, 284, -191.2),
		["Workout Grounds"] = Vector3.new(-311.6, 1071.6, -579.6),
	};
S.BossTP = {
		Akazo = Vector3.new(-1132.6, 1381.1, -1742.5),
		Gyutai = Vector3.new(-263.1, 1043.5, -1150.5),
		Datai = Vector3.new(-163.2, 1043.5, -1136.9),
		Saneri = Vector3.new(-383.03, 1096.07, -405.25),
		Zuko = Vector3.new(-288.51, 1226.7, -1027.99),
		Kaiden = Vector3.new(558.56, 1148.67, -1322.1),
		Hoyuzo = Vector3.new(758.57, 1003.51, -1406.92),
		["Mother Bear"] = Vector3.new(550, 1122, -1035),
		Fujiko = Vector3.new(-2626.7, 284, -191.2),
		["Hand Demon"] = Vector3.new(-2626.7, 284, -191.2),
		["Yeti Demon"] = Vector3.new(485.3, 1222.9, -1813.4),
		["Small Yeti"] = Vector3.new(485.3, 1222.9, -1813.4),
		Domae = Vector3.new(-297.34, 1352.99, -3448.14),
		Reaper = Vector3.new(104.73, 1045.5, -579.95),
		Enru = Vector3.new(819.73, 798.15, 557.06),
		Shinora = Vector3.new(-463.59, 967.0, -7.85),
		Obari = Vector3.new(742.85, 1124.07, -1018.62),
		Giyen = Vector3.new(405.07, 1020.5, -74.58),
		Tengai = Vector3.new(-132.74, 1351.48, -2616.27),
		Zentaro = Vector3.new(1329.34, 823.5, -1022.86),
		Gyorei = Vector3.new(2569.72, 1091.5, -730.2),
		Nezura = Vector3.new(-1456.63, 278.45, 927.69),
		Yahari = Vector3.new(809.68, 1021.01, -632.32),
		Sumari = Vector3.new(400.17, 1020.5, -596.86),
		["Serpent Trainee"] = Vector3.new(-270.3, 1292.49, -1533.8),
		["Reaper Trainee Kuzan"] = Vector3.new(-1231.91, 1376.61, -3013.45),
		Muzan = Vector3.new(2466.37, 1079.63, 2334.54),
	};
S.Trainers = {
		["Fisherman Jeso"] = Vector3.new(-192.0, 807.0, 602.7),
		["Alchemist Meku"] = Vector3.new(-138.1, 798.9, 518.7),
		["Dock Master Sofen"] = Vector3.new(-160.8, 798.8, 703.3),
		["Estate Worker Niko"] = Vector3.new(341.1, 939.0, 580.6),
		Ginzo = Vector3.new(273.8, 944.0, 528.2),
		Elara = Vector3.new(427.8, 943.6, 507.4),
		Jugg = Vector3.new(487.7, 876.5, 1007.8),
		Liv = Vector3.new(657.4, 1021.2, 139.7),
		["Serpent Trainer Obari"] = Vector3.new(37.0, 1307.5, -1179.5),
		["Soryu Trainee Goki"] = Vector3.new(-424.5, 289.1, 540.4),
		["Lamplighter Isamu"] = Vector3.new(1082.2, 1425.7, -749.0),
	};
S.Stations = {
		"Boulder Push",
		"Squat Rack",
		"Aim Training",
		"Boulder Split",
		"Cup Game",
		"Meditation",
		"Pushups",
		"Parkour Dungeon",
	};
S.Deliver = {
		["Deliver Niko\'s Supply Box"] = { opt = "Ill deliver the supply box(Lv 70)", tasks = { ["Deliver to Shiori"] = { npc = "Shiori", pos = Vector3.new(-1814.34, 311.8, -101.07), answer = "Niko sent it up" }, ["Report back to Niko"] = { npc = "Estate Worker Niko", pos = Vector3.new(341.07, 938.91, 580.59), answer = "Shiori says thank you" } } },
		["Restock the Infirmary"] = { opt = "Ill restock the infirmary(Lv 70)", tasks = { ["Return to Shiori"] = { npc = "Shiori", pos = Vector3.new(-1814.34, 311.8, -101.07), answer = "The infirmary is stocked" } } },
		["The Full Pantry"] = { opt = "Ill stock the reserves(Lv 75)", tasks = { ["Return to Shiori"] = { npc = "Shiori", pos = Vector3.new(-1814.34, 311.8, -101.07), answer = "The stores are stocked" } } },
		["Locate Ren\'s Lost Nichirin"] = { opt = "Ill look for your blade(Lv 75)", tasks = { ["Return to Ren"] = { npc = "Ren", pos = Vector3.new(-1795, 311.8, -85) } } },
		["The Morning Haul"] = { opt = "Ill fill your crates(Lv 45)", tasks = { ["Return to Runo"] = { npc = "Angler Runo", pos = Vector3.new(-561.1, 796.25, 683.7) } } },
		["The Good Catch"] = { opt = "Ill land the good catch(Lv 60)", tasks = { ["Return to Runo"] = { npc = "Angler Runo", pos = Vector3.new(-561.1, 796.25, 683.7) } } },
		["Retrieve Ginzo\'s Jewelry Box"] = { opt = "Ill find the jewelry box(Lv 45)", tasks = { ["Return to Ginzo"] = { npc = "Ginzo", pos = Vector3.new(273.8, 941.5, 528.19) } } },
		["Earn a Fishing Permit"] = { opt = "Ill find the permit stamp(Lv 45)", tasks = { ["Return to Sofen"] = { npc = "Dock Master Sofen", pos = Vector3.new(-160.81, 796.25, 703.29) } } },
		["Acquire Bear Meat"] = { opt = "Ill restock the pantry(Lv 10)", tasks = { ["Bring bear meat back to Lucy"] = { npc = "Lucy", pos = Vector3.new(-615.5, 1258.5, -1177.5), answer = "Hand over the meat" } } },
		["Deliver Package to Elara"] = { opt = "Ill deliver the package", tasks = { ["Package delivered"] = { npc = "Elara", pos = Vector3.new(427.82, 941.0, 507.45) } } },
		["Report to Noote"] = { opt = "Ill bring him the notes", tasks = { ["Speak with Noote"] = { npc = "Noote", pos = Vector3.new(-515.5, 1243.2, -1251.2) } } },
		["Deliver the Coded Letter"] = { opt = "Ill get this letter delivered", tasks = { ["Deliver the Letter to Chaka"] = { npc = "Chaka", pos = Vector3.new(471, 1146, -1260) } } },
	};
S.DeliverGivers = { { Npc = "Estate Worker Niko", Pos = Vector3.new(341.1, 938.9, 580.6), QuestOption = "Ill deliver the supply box(Lv 70)" }, { Npc = "MoldySugar", Pos = Vector3.new(-701.8, 1243.3, -982.7), QuestOption = "Ill deliver the package" }, { Npc = "Noote", Pos = Vector3.new(-515.5, 1243.2, -1251.2), QuestOption = "Ill get this letter delivered" } };
S.Quests = {
		["[Auto-Detect Active Quest]"] = { Auto = true },
		["[Auto] All delivery quests"] = { Auto = true, AutoDeliver = true },
		["[Windy Peak] Krue: 3 Bandits"] = {
			Npc = "Krue",
			Pos = Vector3.new(-425.9, 1240.7, -952.5),
			Target = "Bandit",
			QuestOption = "Ill take 3 bandits",
		},
		["[Windy Peak] Krue: Bandit Leader Zuko"] = {
			Npc = "Krue",
			Pos = Vector3.new(-425.9, 1240.7, -952.5),
			Target = "Zuko",
			BossOption = "Ill take the bandit boss(Lv 7)",
		},
		["[Windy Peak] Kazu: Village Spies"] = {
			Npc = "Kazu",
			Pos = Vector3.new(-626, 1242.5, -1138),
			Target = "*Civilian*",
			Alt = "Village Spy",
			QuestOption = "Ill help clear them out",
		},
		["[Windy Peak] Kazu: Notes to Noote"] = { Npc = "Kazu", Pos = Vector3.new(-626, 1242.5, -1138), QuestOption = "Ill bring him the notes" },
		["[Windy Peak] Lucy: Bear Meat"] = {
			Npc = "Lucy",
			Pos = Vector3.new(-615.5, 1260.2, -1177.5),
			Target = "Mother Bear",
			Alt = "Bear Cub",
		},
		["[Mistfall] Niko: Eliminate Mizunoto"] = { Npc = "Estate Worker Niko", Pos = Vector3.new(341.1, 938.0, 581.9), Target = "Mizunoto" },
		["[Mistfall] Niko: 5 Broken Blades"] = { Npc = "Estate Worker Niko", Pos = Vector3.new(341.1, 938.0, 581.9), Target = "Mizunoto" },
		["[Mistfall] Ginzo: Lost Jewelry Box"] = { Npc = "Ginzo", Pos = Vector3.new(273.8, 944.0, 528.2), Target = "Mizunoto" },
		["[Bamboo] Betty: Betty\'s Gemstone"] = { Npc = "Betty", Pos = Vector3.new(714.0, 1125.1, -808.0), QuestOption = "Ill look for it(Lv 10)" },
		["[Bamboo] Wagwan: Hoyuzo\'s guard"] = {
			Npc = "Wagwan",
			Pos = Vector3.new(723.76, 1019.2, -801.98),
			Target = "Hoyuzo Subordinate",
			QuestOption = "I will clear out his guards(Lv 40)",
			BossOption = "I will take care of Hoyuzo(Lv 50)",
			BossTarget = "Hoyuzo",
		},
		["[Bamboo] Liv: Lucky Penny / coins"] = {
			Npc = "Liv",
			Pos = Vector3.new(657.4, 1021.2, 139.7),
			QuestOption = "Ill look for the penny(Lv 14)",
			BossOption = "Ill find the coins(Lv 21)",
		},
		["[Hidden Mist] Togane: the forge"] = { Npc = "Blacksmith Togane", Pos = Vector3.new(1793.08, 661.3, -231.72), QuestOption = "Ill find the forge(Lv 65)" },
		["[Bamboo] Tom: Wood & Bamboo"] = {
			Npc = "Tom",
			Pos = Vector3.new(508.5, 1124.3, -970.5),
			Target = "Bear Cub",
			Alt = "Mother Bear",
			QuestOption = "Ill drive the bears back(Lv 10)",
			BossOption = "Ill fell the Mother Bear(Lv 18)",
			BossTarget = "Mother Bear",
		},
		["[Bamboo] Chaka: Kaiden and the bandits"] = {
			Npc = "Chaka",
			Pos = Vector3.new(471, 1148.5, -1260),
			Target = "Kaiden Subordinate",
			QuestOption = "Ill clear out his subordinates(Lv 26)",
			BossOption = "Ill deal with Kaiden(Lv 34)",
			BossTarget = "Kaiden",
		},
		["[Butterfly] Shiori: Restock Infirmary"] = {
			Npc = "Shiori",
			Pos = Vector3.new(-1814.3, 311.8, -101.1),
			Target = "Lesser Demon",
			Alt = "Greater Demon",
			QuestOption = "Ill restock the infirmary(Lv 70)",
			BossOption = "Ill stock the reserves(Lv 75)",
		},
		["[Butterfly] Goro: Lesser / Greater Demons"] = {
			Npc = "Demon Slayer Goro",
			Pos = Vector3.new(-871.97, 237.25, 318.47),
			Target = "Lesser Demon",
			QuestOption = "Ill thin them out(Lv 75)",
			BossOption = "Ill go up after the greater ones(Lv 83)",
			BossTarget = "Greater Demon",
		},
		["[Butterfly] Ren: Lost Nichirin"] = {
			Npc = "Ren",
			Pos = Vector3.new(-1795, 311.8, -85),
			Target = "Greater Demon",
			Alt = "Lesser Demon",
			QuestOption = "Ill look for your blade(Lv 75)",
		},
		["[Mistfall] Niko: Supply Box (delivery)"] = { Npc = "Estate Worker Niko", Pos = Vector3.new(341.1, 938.9, 580.6), QuestOption = "Ill deliver the supply box(Lv 70)" },
		["[Windy Peak] MoldySugar: Package (delivery)"] = { Npc = "MoldySugar", Pos = Vector3.new(-701.8, 1243.3, -982.7), QuestOption = "Ill deliver the package" },
		["[Windy Peak] Noote: Coded Letter (delivery)"] = { Npc = "Noote", Pos = Vector3.new(-515.5, 1243.2, -1251.2), QuestOption = "Ill get this letter delivered" },
		["[Iceveil] Tomoi: Ice Demons"] = {
			Npc = "Wounded Slayer Tomoi",
			Pos = Vector3.new(485.3, 1222.9, -1813.4),
			Target = "Ice Profound Demon",
			Alt = "High Demon",
		},
		["[Iceveil] Tomoi: Fire Demons"] = {
			Npc = "Wounded Slayer Tomoi",
			Pos = Vector3.new(485.3, 1222.9, -1813.4),
			Target = "Fire Profound Demon",
			Alt = "High Demon",
		},
		["[Final Selection] Exam"] = {
			Npc = "Demon Mokuro",
			Pos = Vector3.new(-2626.7, 284, -191.2),
			Target = "Mizunoe Demon Slayer",
			Alt = "Fujiko",
		},
		["[Demon Only] Mokuro: Mizunoto slayer"] = {
			Npc = "Demon Mokuro",
			Pos = Vector3.new(-2626.7, 284, -191.2),
			Target = "Mizunoto Demon Slayer",
			BossOption = "Ill break their watch(Lv 75)",
		},
	};
local w = {};
W.F = w;
w.remote = nil;
do
	local m = p:FindFirstChild("Communication");
	local q = m and m:FindFirstChild("ServerAndClient");
	local a = q and q:FindFirstChild("Signals");
	local c = a and a:FindFirstChild("SignalEvent");
	w.remote = c and c:FindFirstChild("Event");
end;
function w.logErr(m, p)
	local q = os.date("%H:%M:%S") .. (" " .. (tostring(m) .. (": " .. tostring(p))));
	warn("[Slayers 2] " .. q);
	if (os.clock() - ((W.errT or 0))) < 1 then
		return;
	end;
	W.errT = os.clock();
	pcall(function()
		appendfile("slayers2_errors.txt", q .. string.char(10));
	end);
end;
local R = {};
function w.defer(m, p, a)
	if type(m) ~= "function" then
		return;
	end;
	R[#R + 1] = { m, p, a };
	if W.deferConn then
		return;
	end;
	W.deferConn = q.Heartbeat:Connect(function()
			local m = W.deferConn;
			W.deferConn = nil;
			pcall(function()
				if m then
					m:Disconnect();
				end;
			end);
			local p = R;
			R = {};
			if not W.Running then
				return;
			end;
			for m = 1, #p, 1 do
				local q, a = pcall(p[m][1], p[m][2], p[m][3]);
				if not q then
					w.logErr("ui", a);
				end;
			end;
		end);
end;
function w.guard(m)
	if type(m) ~= "table" and type(m) ~= "userdata" then
		return m;
	end;
	return setmetatable({}, { __index = function(p, q)
			local a = m[q];
			if type(a) ~= "function" then
				return a;
			end;
			return function(p, q, ...)
				if type(q) == "table" and type(q.callback) == "function" then
					local m = q.callback;
					q.callback = function(p, q)
							w.defer(m, p, q);
						end;
				end;
				return a(m, q, ...);
			end;
		end });
end;
function w.mark(m)
	W.where = m;
end;
function w.markWrite(m)
	if m - ((W.markT or 0)) < 1 then
		return;
	end;
	W.markT = m;
	pcall(function()
		writefile("slayers2_where.txt", os.date("%H:%M:%S") .. (" " .. tostring(W.where or "?")));
	end);
end;
function w.markPrev()
	local m;
	pcall(function()
		if isfile("slayers2_where.txt") then
			m = readfile("slayers2_where.txt");
		end;
	end);
	if type(m) == "string" and #m > 0 then
		pcall(function()
			appendfile("slayers2_errors.txt", os.date("%H:%M:%S") .. (" previous run died during: " .. (m .. string.char(10))));
		end);
		warn("[Slayers 2] previous run died during: " .. m);
	end;
	pcall(function()
		delfile("slayers2_where.txt");
	end);
end;
function w.notify(m, p)
	pcall(function()
		Z:Notify({ title = "Slayers 2", text = tostring(m), duration = p or 3 });
	end);
end;
function w.char()
	local m = c.Character;
	if not m then
		return nil, nil, nil;
	end;
	local p = m:FindFirstChild("HumanoidRootPart");
	local q = m:FindFirstChildOfClass("Humanoid");
	if not p or not q then
		return nil, nil, nil;
	end;
	return m, p, q;
end;
local y = .25;
function w.addr(m)
	local p, q = pcall(function()
			return m.Address;
		end);
	if p and type(q) == "number" then
		return q;
	end;
	return nil;
end;
function w.attached(m, p)
	if m == nil then
		return false;
	end;
	local q, a = pcall(function()
			local q = m.Parent;
			if q == nil then
				return false;
			end;
			if p == nil then
				return true;
			end;
			return q.Address == p;
		end);
	return q and a == true;
end;
function w.npcAlive(m, p)
	if not m or m.dead or m.stale then
		return false;
	end;
	local q = os.clock();
	local a = q - ((m.chkT or 0));
	if a < .005 then
		return true;
	end;
	if a > ((p or y)) then
		m.stale = true;
		return false;
	end;
	if w.attached(m.Model, m.PAddr) then
		m.chkT = q;
		return true;
	end;
	m.dead = true;
	return false;
end;
local N = {};
function w.refreshPlayers()
	N = {};
	local p = m:GetPlayers();
	for m = 1, #p, 1 do
		N[p[m].Name] = true;
	end;
end;
w.refreshPlayers();
function w.isBoss(m)
	if m:FindFirstChild("BossInfo") then
		return true;
	end;
	return S.BossNames[string.lower(m.Name)] == true;
end;
function w.scan(m, p, q)
	local c = {};
	local Z = nil;
	if type(m) == "table" then
		Z = m;
		m = nil;
	end;
	local W = m and string.lower(m) or nil;
	local S = (not Z) and (((m == nil) or (m == "All Regular Mobs") or (m == "All Bosses")));
	local R = { a:FindFirstChild("Humanoids"), a:FindFirstChild("Debree") };
	for m = 1, #R, 1 do
		local a = R[m] and R[m]:FindFirstChild("Regions");
		if a then
			local m = a:GetChildren();
			for a = 1, #m, 1 do
				local R = m[a]:FindFirstChild("ActiveNpcs");
				if R then
					local m = R:GetChildren();
					for a = 1, #m, 1 do
						local R = m[a];
						local y = R.Name;
						if not N[y] then
							local m = false;
							if Z then
								m = Z[string.lower(y)] == true;
							elseif S then
								m = true;
							elseif W then
								local p = string.lower(y);
								if q then
									m = (p == W);
								else
									m = (p == W) or (string.find(p, W, 1, true) ~= nil);
								end;
							end;
							if m then
								local a = w.isBoss(R);
								if q and (p and not a) then
									m = false;
								end;
							end;
							if m then
								local m = w.isBoss(R);
								if p == nil or not S or p == m then
									local p = R:FindFirstChild(y) or R:FindFirstChildOfClass("Model");
									if p then
										local q = p:FindFirstChildOfClass("Humanoid");
										local a = p:FindFirstChild("HumanoidRootPart");
										if q and (a and q.Health > 0) then
											local Z = {
													Model = p,
													Hum = q,
													Root = a,
													Name = y,
													Boss = m,
												};
											Z.exactName = W ~= nil and string.lower(y) == W;
											Z.PAddr = w.addr(R);
											Z.chkT = os.clock();
											c[#c + 1] = Z;
											pcall(function()
												w.noteSeen(y, a.Position);
											end);
										end;
									end;
								end;
							end;
						end;
					end;
				end;
			end;
		end;
	end;
	if W and not q then
		local m = {};
		for p = 1, #c, 1 do
			if c[p].exactName then
				m[#m + 1] = c[p];
			end;
		end;
		if #m > 0 then
			return m;
		end;
	end;
	return c;
end;
local O = { key = "", t = -99, list = {} };
function w.setKey(m)
	local p = {};
	for m in pairs(m) do
		p[#p + 1] = m;
	end;
	table.sort(p);
	return table.concat(p, "|");
end;
function w.scanCached(m, p, q, a, c)
	local Z;
	if type(m) == "table" then
		Z = w.setKey(m);
	else
		Z = tostring(m);
	end;
	Z = Z .. (tostring(p) .. tostring(c));
	if O.key == Z and (q - O.t) < ((a or 1.0)) then
		return O.list;
	end;
	O.key = Z;
	O.t = q;
	O.list = w.scan(m, p, c);
	return O.list;
end;
function w.dropCache()
	O.key = "";
	O.t = -99;
end;
function w.bossSet()
	local m = W.SelectedBoss;
	local p = false;
	local q, a = {}, 0;
	if type(m) == "string" then
		if m == "All Bosses" then
			p = true;
		else
			q[string.lower(m)] = true;
			a = 1;
		end;
	else
		for m, c in pairs(m or {}) do
			if c then
				if m == "All Bosses" then
					p = true;
				else
					q[string.lower(m)] = true;
					a = a + 1;
				end;
			end;
		end;
	end;
	if p then
		q, a = {}, 0;
		for m = 1, #S.Bosses, 1 do
			local p = S.Bosses[m];
			if p ~= "All Bosses" then
				q[string.lower(p)] = true;
				a = a + 1;
			end;
		end;
	end;
	if a == 0 then
		return nil, false;
	end;
	return q, p;
end;
function w.updateBossOrder(m)
	local p = {};
	if type(m) == "table" then
		for m, q in pairs(m) do
			if q then
				p[m] = true;
			end;
		end;
	elseif type(m) == "string" then
		p[m] = true;
	end;
	local q = {};
	if p["All Bosses"] then
		for m, p in ipairs(S.Bosses) do
			if p ~= "All Bosses" then
				q[#q + 1] = p;
			end;
		end;
	else
		for m, a in ipairs(W.BossOrder or {}) do
			if p[a] then
				q[#q + 1] = a;
				p[a] = nil;
			end;
		end;
		for m, a in ipairs(S.Bosses) do
			if p[a] then
				q[#q + 1] = a;
			end;
		end;
	end;
	W.BossOrder = q;
	W.spawnIdx = 0;
end;
function w.bossLabel()
	local m, p = w.bossSet();
	if p then
		return "all bosses";
	end;
	if not m then
		return "none selected";
	end;
	local q = {};
	for m in pairs(m) do
		q[#q + 1] = m;
	end;
	table.sort(q);
	return table.concat(q, ", ");
end;
S.SharedOK = { ["-2627,-192"] = true, ["485,-1814"] = true };
local Q = {};
do
	local m = {};
	for p, q in ipairs({ S.Spawns, S.BossTP }) do
		for p, q in pairs(q) do
			local a = math.floor(q.X) .. ("," .. math.floor(q.Z));
			m[a] = m[a] or {};
			m[a][string.lower(p)] = true;
		end;
	end;
	for m, p in pairs(m) do
		local q = 0;
		for m in pairs(p) do
			q = q + 1;
		end;
		if q > 1 and not S.SharedOK[m] then
			for m in pairs(p) do
				Q[m] = true;
			end;
		end;
	end;
end;
local function s(m)
	local p = string.lower(m);
	local q = W.seen[p];
	if q then
		return Vector3.new(q[1], q[2], q[3]);
	end;
	if Q[p] then
		return nil;
	end;
	for q, a in ipairs({ S.Spawns, S.BossTP }) do
		local c = a[m];
		if c then
			return c;
		end;
		for m, q in pairs(a) do
			if string.lower(m) == p then
				return q;
			end;
		end;
	end;
	return nil;
end;
function w.wantSpawn(m)
	if type(m) ~= "table" then
		return s(m);
	end;
	local p = {};
	if W.Mode == "boss" and W.BossOrder then
		for q, a in ipairs(W.BossOrder) do
			local c = string.lower(a);
			if m[c] and s(c) then
				p[#p + 1] = c;
			end;
		end;
	end;
	if #p == 0 then
		for m in pairs(m) do
			if s(m) then
				p[#p + 1] = m;
			end;
		end;
		table.sort(p);
	end;
	if #p == 0 then
		return nil;
	end;
	W.spawnIdx = ((W.spawnIdx or 0)) % #p + 1;
	return s(p[W.spawnIdx]);
end;
local F = "slayers2_seen.json";
function w.seenLoad()
	local m;
	if not pcall(function()
		m = readfile(F);
	end) or type(m) ~= "string" then
		return;
	end;
	local p, q = pcall(function()
			return (game:GetService("HttpService")):JSONDecode(m);
		end);
	if p and type(q) == "table" then
		W.seen = q;
	end;
end;
function w.seenSave(m)
	if not W.seenDirty then
		return;
	end;
	if m and (m - ((W.seenT or 0))) < 30 then
		return;
	end;
	W.seenT = m or 0;
	W.seenDirty = false;
	pcall(function()
		writefile(F, (game:GetService("HttpService")):JSONEncode(W.seen));
	end);
end;
function w.noteSeen(m, p)
	local q = string.lower(m);
	local a = W.seen[q];
	if a and (math.abs(a[1] - p.X) < 8 and math.abs(a[3] - p.Z) < 8) then
		return;
	end;
	W.seen[q] = { p.X, p.Y, p.Z };
	W.seenDirty = true;
end;
function w.nearest(m, p)
	local q, a = nil, 1000000000.0;
	for c = 1, #m, 1 do
		local Z = m[c];
		if w.npcAlive(Z, .6) then
			local m, c = pcall(function()
					return Z.Root.Position;
				end);
			if m and c then
				local m = ((c - p)).Magnitude;
				if m < a then
					a = m;
					q = Z;
				end;
			end;
		end;
	end;
	return q, a;
end;
function w.tpTo(m, p)
	local q, a = w.char();
	if not a or not m then
		return false;
	end;
	pcall(function()
		a.CFrame = CFrame.new(m.X, m.Y + ((p or 2.5)), m.Z);
		a.AssemblyLinearVelocity = Vector3.new(0, 0, 0);
	end);
	return true;
end;
function w.findNamed(m)
	local p = string.lower(m or "");
	if p == "" then
		return nil;
	end;
	local q = nil;
	local c = { a:FindFirstChild("Humanoids"), a:FindFirstChild("Debree") };
	for m = 1, #c, 1 do
		local a = c[m] and c[m]:FindFirstChild("Regions");
		if a then
			for m, a in ipairs(a:GetChildren()) do
				for m, a in ipairs({ a:FindFirstChild("ActiveNpcs"), a:FindFirstChild("StationaryNpcs") }) do
					for m, a in ipairs(a and a:GetChildren() or {}) do
						local c = string.lower(a.Name);
						local Z = (c == p);
						if Z or (not q and string.find(c, p, 1, true)) then
							local m = a:FindFirstChildOfClass("Model") or a;
							local p = m:FindFirstChild("HumanoidRootPart") or m:FindFirstChildWhichIsA("BasePart");
							if p then
								if Z then
									return p.Position;
								end;
								q = p.Position;
							end;
						end;
					end;
				end;
			end;
		end;
	end;
	return q;
end;
function w.npcPos(m, p, q)
	local a = W.npcPosCache[m];
	if a and (p - a.t) < ((q or 2.0)) then
		return a.pos;
	end;
	local c = w.findNamed(m);
	W.npcPosCache[m] = { pos = c, t = p };
	return c;
end;
function w.followNpc(m, p, q)
	local a, c = w.char();
	local Z = w.npcPos(m, q, .1) or p;
	local S = W.follow;
	if S and S.name == m then
		S.t = q;
		S.fb = p;
	else
		W.follow = { name = m, fb = p, t = q };
	end;
	if not c or not Z then
		return math.huge;
	end;
	local R = c.Position;
	local y, N = R.X - Z.X, R.Z - Z.Z;
	local O = math.sqrt(y * y + N * N);
	if math.abs(R.Y - Z.Y) > 15 then
		return math.max(O, 99);
	end;
	return O;
end;
function w.followStep(m)
	local p = W.follow;
	if not p then
		return;
	end;
	if m - p.t > .6 then
		W.follow = nil;
		return;
	end;
	local q, a = w.char();
	local c = w.npcPos(p.name, m, .1) or p.fb;
	if not a or not c then
		return;
	end;
	if p.lastPos and p.lastPos ~= c then
		local q = m - ((p.lastT or m));
		if q > .02 then
			local a, Z = ((c.X - p.lastPos.X)) / q, ((c.Z - p.lastPos.Z)) / q;
			if a * a + Z * Z < 900 then
				p.vx, p.vz = a, Z;
			end;
			p.lastPos, p.lastT = c, m;
		end;
	elseif not p.lastPos then
		p.lastPos, p.lastT = c, m;
	end;
	local Z, S = p.vx or 0, p.vz or 0;
	if W.dlg or (W.dlgClick ~= nil) then
		w.aimRelease();
	else
		pcall(w.aimStep, Vector3.new(c.X, c.Y + 2, c.Z), true);
	end;
	local R = a.Position;
	local y, N = R.X - c.X, R.Z - c.Z;
	local O = math.sqrt(y * y + N * N);
	local Q, s = 1, 0;
	if O > .5 then
		Q, s = y / O, N / O;
	end;
	local F = (c.X + Q * 2.5) + Z * .2;
	local K = (c.Z + s * 2.5) + S * .2;
	local x, e, L = F - R.X, c.Y - R.Y, K - R.Z;
	local z = math.sqrt((x * x + e * e) + L * L);
	if z > W.WarpAbove then
		w.travelTo(Vector3.new(F, c.Y - 3, K), m);
		return;
	end;
	local M, o, l = Z + x * 6, e * 6, S + L * 6;
	local d = math.sqrt((M * M + o * o) + l * l);
	if d > W.MoveSpeed then
		local m = W.MoveSpeed / d;
		M, o, l = M * m, o * m, l * m;
	end;
	if z < .6 and Z * Z + S * S < 1 then
		M, o, l = 0, 0, 0;
	end;
	pcall(function()
		a.AssemblyLinearVelocity = Vector3.new(M, o, l);
	end);
end;
S.MuzanRoute = {
		Vector3.new(55.0, 826.5, 781.5),
		Vector3.new(197.2, 871.4, 812.2),
		Vector3.new(330.0, 873.0, 824.0),
		Vector3.new(182.4, 873.0, 703.7),
		Vector3.new(187.6, 914.8, 558.3),
		Vector3.new(237.8, 940.5, 389.6),
		Vector3.new(227.8, 963.5, 250.1),
		Vector3.new(98.9, 963.5, 210.2),
		Vector3.new(232.7, 963.5, 267.0),
		Vector3.new(212.2, 940.5, 452.1),
		Vector3.new(181.8, 909.0, 590.1),
		Vector3.new(183.6, 873.0, 714.7),
		Vector3.new(115.8, 826.5, 808.6),
		Vector3.new(1920.6, 599.0, -881.0),
		Vector3.new(1816.3, 599.0, -719.9),
		Vector3.new(1748.2, 599.0, -549.1),
		Vector3.new(1771.7, 599.0, -425.8),
		Vector3.new(1787.4, 599.0, -295.5),
		Vector3.new(1810.5, 599.0, -145.7),
		Vector3.new(1667.8, 599.0, -92.7),
		Vector3.new(1811.8, 599.0, -146.8),
		Vector3.new(1776.7, 599.0, -332.5),
		Vector3.new(1772.2, 599.0, -452.5),
		Vector3.new(1762.0, 599.0, -632.5),
		Vector3.new(1877.4, 599.0, -794.8),
		Vector3.new(-687.0, 1380.5, -2606.5),
		Vector3.new(-531.8, 1368.2, -2594.2),
		Vector3.new(-410.5, 1348.5, -2603.5),
		Vector3.new(-392.3, 1348.5, -2794.1),
		Vector3.new(-413.6, 1380.5, -2992.6),
		Vector3.new(-463.5, 1380.5, -3136.3),
		Vector3.new(-451.1, 1350.0, -3301.2),
		Vector3.new(-398.7, 1350.0, -3438.5),
		Vector3.new(-251.6, 1350.0, -3452.8),
		Vector3.new(-191.2, 1350.0, -3327.3),
		Vector3.new(-182.9, 1380.5, -3188.0),
		Vector3.new(-171.5, 1380.5, -2996.0),
		Vector3.new(-173.1, 1373.7, -2862.2),
		Vector3.new(-163.6, 1348.5, -2702.0),
		Vector3.new(-172.5, 1348.5, -2548.0),
		Vector3.new(-362.7, 1348.5, -2551.9),
		Vector3.new(-499.4, 1352.7, -2594.5),
		Vector3.new(-643.2, 1380.5, -2607.4),
	};
function w.muzanSpots()
	local m = {};
	for p, q in ipairs({ a:FindFirstChild("Humanoids"), a:FindFirstChild("Debree") }) do
		local a = q and q:FindFirstChild("Regions");
		for p, q in ipairs(a and a:GetChildren() or {}) do
			for p, a in ipairs({ q:FindFirstChild("ActiveNpcs"), q:FindFirstChild("StationaryNpcs") }) do
				for p, a in ipairs(a and a:GetChildren() or {}) do
					if string.find(string.lower(a.Name), "muzan", 1, true) then
						local p = a:FindFirstChild(a.Name) or a:FindFirstChildOfClass("Model");
						local c = a:FindFirstChild("HumanoidRootPart") or (p and p:FindFirstChild("HumanoidRootPart")) or a:FindFirstChildWhichIsA("BasePart");
						local Z;
						if c then
							pcall(function()
								Z = c.Position;
							end);
						end;
						m[#m + 1] = {
								name = a.Name,
								pos = Z,
								region = q.Name,
								npc = true,
							};
					end;
				end;
			end;
		end;
	end;
	return m;
end;
function w.findMuzan(m)
	local p, q = w.char();
	local a, c;
	for m, p in ipairs(w.muzanSpots()) do
		local Z = ((q and p.pos)) and ((p.pos - q.Position)).Magnitude or nil;
		if not a or (Z and (c and Z < c)) then
			a, c = p, Z;
		end;
	end;
	if a and a.pos then
		if m then
			w.tpTo(Vector3.new(a.pos.X + 5, a.pos.Y, a.pos.Z), 3);
			w.notify("at muzan" .. ((a.region and " (" .. (a.region .. ")") or "")), 4);
		else
			w.notify(string.format("muzan at %d, %d, %d - %d studs", a.pos.X, a.pos.Y, a.pos.Z, c or 0), 6);
		end;
		return a;
	end;
	if a then
		w.notify("muzan is walking in " .. (tostring(a.region) .. " - travel there to see him"), 6);
		return a;
	end;
	local Z, W;
	for m, p in ipairs(S.MuzanRoute) do
		local a = q and ((p - q.Position)).Magnitude or 0;
		if not W or a < W then
			Z, W = p, a;
		end;
	end;
	if m and Z then
		w.tpTo(Z, 3);
	end;
	w.notify("muzan is not walking the map - waiting spot set on his patrol route", 6);
	return nil;
end;
S.BlackMarketer = { Vector3.new(-132.68, 804, 101.24), Vector3.new(2161.96, 823.46, -900.68) };
function w.tpBlackMarketer()
	if W.bmConn then
		pcall(function()
			W.bmConn:Disconnect();
		end);
		W.bmConn = nil;
	end;
	local function m(m)
		w.tpTo(Vector3.new(m.X + 4, m.Y, m.Z), 2.5);
		w.notify("at the Black Marketer", 3);
	end;
	local p = w.findNamed("Black Marketer");
	if p then
		m(p);
		return;
	end;
	local a, c = 1, os.clock();
	w.tpTo(S.BlackMarketer[1], 3);
	W.bmConn = q.Heartbeat:Connect(function()
			local p, q = pcall(function()
					local p = os.clock();
					if p - c < 1.5 then
						return;
					end;
					local q = w.findNamed("Black Marketer");
					if q then
						if W.bmConn then
							W.bmConn:Disconnect();
							W.bmConn = nil;
						end;
						m(q);
					elseif a < #S.BlackMarketer then
						a = a + 1;
						c = p;
						w.tpTo(S.BlackMarketer[a], 3);
					else
						if W.bmConn then
							W.bmConn:Disconnect();
							W.bmConn = nil;
						end;
						w.notify("Black Marketer not at either spawn", 4);
					end;
				end);
			if not p then
				if W.bmConn then
					pcall(function()
						W.bmConn:Disconnect();
					end);
					W.bmConn = nil;
				end;
				w.logErr("black marketer", q);
			end;
		end);
end;
function w.tpNamed(m, p)
	local q = w.findNamed(m);
	local a = q or p;
	if a then
		w.tpTo(a, 2.5);
		w.notify(((q and "Reached " or "Teleported to ")) .. m, 2);
	else
		w.notify("No location for " .. tostring(m), 2);
	end;
end;
local K = { t = -99, style = "Combat", item = nil };
function w.style()
	local m = os.clock();
	if m - K.t < 2.0 then
		return K.style, K.item;
	end;
	K.t = m;
	local p, q = w.styleRaw();
	K.style, K.item = p, q;
	return p, q;
end;
function w.styleRaw()
	local m = c.Character;
	if not m then
		return "Combat", nil;
	end;
	local q = p:FindFirstChild("Assets");
	q = q and q:FindFirstChild("Animations");
	local a = nil;
	local Z = m:GetChildren();
	for m = 1, #Z, 1 do
		if Z[m].ClassName == "Tool" then
			a = Z[m];
			break;
		end;
	end;
	if not a then
		local p = m:FindFirstChild("Tool_Accessories");
		if p then
			a = (p:GetChildren())[1];
		end;
	end;
	local W = nil;
	local w = p:FindFirstChild("CAM");
	local R = w and (w.Client and w.Client.Controllers);
	R = R and R:FindFirstChild("Skills_Provider");
	R = R and R:FindFirstChild("CurPower");
	if R and (type(R.Value) == "string" and (R.Value ~= "" and q)) then
		for m in string.gmatch(R.Value, "[^,]+") do
			if q:FindFirstChild(m .. "_Combat_Anims") then
				W = m;
				break;
			end;
		end;
	end;
	if not W and (a and (q and q:FindFirstChild(a.Name .. "_Combat_Anims"))) then
		W = a.Name;
	end;
	if not W and a then
		W = a.Name;
	end;
	if not W then
		W = "Combat";
	end;
	if S.Presets[W] then
		return W, nil;
	end;
	local y = S.StyleMap[string.lower(W)];
	if y then
		return y, nil;
	end;
	local N = string.lower(W);
	local O;
	for m, p in pairs(S.StyleWords) do
		if string.find(N, m, 1, true) and S.Presets[p] then
			return p, nil;
		end;
	end;
	for m in pairs(S.Presets) do
		if m ~= "Combat" then
			local p = string.lower(m);
			local q = string.find(N, p, 1, true) ~= nil;
			if not q and string.sub(p, -1) == "s" then
				q = (N == string.sub(p, 1, -2)) or string.find(N, string.sub(p, 1, -2), 1, true) ~= nil;
			end;
			if not q then
				q = (N .. "s" == p);
			end;
			if q and ((not O or #m > #O)) then
				O = m;
			end;
		end;
	end;
	if O then
		return O, nil;
	end;
	return "Regular Katana", W;
end;
function w.targetBlocking()
	if not W.SkipBlocking then
		return false;
	end;
	local m = W.cur;
	if not m or not m.Model or not w.npcAlive(m) then
		return false;
	end;
	local p;
	local q = pcall(function()
			p = m.Model:FindFirstChild("Blocking");
		end);
	return q and p ~= nil;
end;
function w.curBlocking()
	local m = W.cur;
	if not m or not m.Model or not w.npcAlive(m) then
		return false;
	end;
	local p;
	local q = pcall(function()
			p = m.Model:FindFirstChild("Blocking");
		end);
	return q and p ~= nil;
end;
function w.attack(m)
	if not w.remote then
		return;
	end;
	local p = c.Character;
	if not p then
		return;
	end;
	m = m or os.clock();
	if m < ((W.atkNext or 0)) then
		return;
	end;
	local q = W.cur;
	if q and W.Reach > 0 then
		local m;
		local a = pcall(function()
				m = ((q.Root.Position - p.HumanoidRootPart.Position)).Magnitude;
			end);
		if a and (m and m > W.Reach) then
			return;
		end;
	end;
	if w.targetBlocking() then
		W.blockedT = m;
		if W.cur then
			W.cur.t = m;
		end;
		return;
	end;
	local a = tonumber(p:GetAttribute("last_combo")) or 0;
	if W.atkSent then
		if a == W.atkSent then
			W.atkMiss = 0;
		else
			W.atkMiss = ((W.atkMiss or 0)) + 1;
		end;
	end;
	local Z = a + 1;
	if Z > 5 or Z < 1 then
		Z = 1;
	end;
	local S, R = w.style();
	pcall(function()
		w.remote:FireServer("Combat_Service", S, Z, false, 0, false, R);
	end);
	W.atkSent = Z;
	W.atkCount = ((W.atkCount or 0)) + 1;
	local y = W.AttackDelay;
	if Z == 1 then
		y = y * 2.0;
	end;
	if ((W.atkMiss or 0)) > 0 then
		y = y + .15 * W.atkMiss;
	end;
	if y > 1.6 then
		y = 1.6;
	end;
	W.atkNext = m + y;
end;
function w.goalFor(m)
	local p = m.Position;
	local q = W.Offset;
	if W.Stance == "On Top" then
		return p + Vector3.new(0, q, 0);
	elseif W.Stance == "Above (Hover)" then
		return p + Vector3.new(0, q + 4, 0);
	elseif W.Stance == "Behind" then
		local a;
		if pcall(function()
			a = m.CFrame.LookVector;
		end) and a then
			local m, c = a.X, a.Z;
			local Z = math.sqrt(m * m + c * c);
			if Z > .001 then
				return Vector3.new(p.X - (m / Z) * q, p.Y + .8, p.Z - (c / Z) * q);
			end;
		end;
		return p + Vector3.new(0, .8, q);
	end;
	return p - Vector3.new(0, q, 0);
end;
function w.steer()
	local m = W.cur;
	if not m or not w.npcAlive(m) then
		return;
	end;
	local p, q = w.char();
	if not q then
		return;
	end;
	local a, c;
	local Z = pcall(function()
			c = m.Root.Position;
			a = w.goalFor(m.Root);
		end);
	if not Z or not a then
		return;
	end;
	local S = q.Position;
	local R, y, N = a.X - S.X, a.Y - S.Y, a.Z - S.Z;
	local O = math.sqrt((R * R + y * y) + N * N);
	if W.Smooth and O > W.WarpAbove then
		pcall(function()
			q.CFrame = CFrame.new(a.X, a.Y + 2, a.Z);
			q.AssemblyLinearVelocity = Vector3.new(0, 0, 0);
		end);
		return;
	end;
	if not W.Smooth then
		pcall(function()
			q.CFrame = CFrame.lookAt(a, c);
			q.AssemblyLinearVelocity = Vector3.new(0, 0, 0);
		end);
		return;
	end;
	if O < .4 then
		pcall(function()
			q.AssemblyLinearVelocity = Vector3.new(0, 0, 0);
		end);
		if c then
			local m, p = c.X - S.X, c.Z - S.Z;
			if (m * m + p * p) > .02 then
				pcall(function()
					q.CFrame = CFrame.lookAt(S, Vector3.new(c.X, S.Y, c.Z));
				end);
			end;
		end;
		return;
	end;
	local Q = O * 5.0;
	if Q > W.MoveSpeed then
		Q = W.MoveSpeed;
	end;
	local s = Q / O;
	pcall(function()
		q.AssemblyLinearVelocity = Vector3.new(R * s, y * s, N * s);
	end);
end;
local x = .001747;
function w.camYaw()
	local m = a.CurrentCamera;
	if not m then
		return nil;
	end;
	local p;
	local q = pcall(function()
			p = m.CFrame.LookVector;
		end);
	if not q or not p then
		return nil;
	end;
	return math.atan2(-p.X, -p.Z);
end;
local e = -0.001747;
local L = -0.001345;
function w.aimRelease()
	local m = W.rmbDown;
	if not m and ismouse2pressed then
		local p, q = pcall(ismouse2pressed);
		m = p and q;
	end;
	if m then
		pcall(function()
			mouse2release();
		end);
	end;
	W.rmbDown = false;
end;
function w.aimStep(m, p)
	if (not W.AimLock and not p) or not m then
		return w.aimRelease();
	end;
	if W.win and W.win:IsVisible() then
		return w.aimRelease();
	end;
	if isrbxactive then
		local m, p = pcall(isrbxactive);
		if m and not p then
			return w.aimRelease();
		end;
	end;
	local q = a.CurrentCamera;
	if not q then
		return w.aimRelease();
	end;
	local c;
	if not pcall(function()
		c = q.CFrame;
	end) or not c then
		return;
	end;
	local Z, S = c.Position, c.LookVector;
	local R = m.X - Z.X;
	local y = m.Y - Z.Y;
	local N = m.Z - Z.Z;
	local O = math.sqrt(R * R + N * N);
	if O < .01 then
		return;
	end;
	local Q = math.atan2(-R, -N) - math.atan2(-S.X, -S.Z);
	while Q > math.pi do
		Q = Q - 2 * math.pi;
	end;
	while Q < -math.pi do
		Q = Q + 2 * math.pi;
	end;
	local s = math.atan2(y, O) - math.asin(S.Y);
	if math.abs(Q) < .008 and math.abs(s) < .008 then
		return;
	end;
	if not W.rmbDown then
		pcall(function()
			mouse2press();
		end);
		W.rmbDown = true;
	end;
	local F = (Q / e) * W.AimGain;
	local K = (s / L) * W.AimGain;
	if F > 900 then
		F = 900;
	elseif F < -900 then
		F = -900;
	end;
	if K > 600 then
		K = 600;
	elseif K < -600 then
		K = -600;
	end;
	pcall(function()
		mousemoverel(F, K);
	end);
end;
function w.syncSkills()
	if not W.Skills or not W.win then
		return;
	end;
	for m = 1, #W.Skills, 1 do
		local p = W.Skills[m];
		local q;
		if pcall(function()
			q = W.win:GetValue("sk_" .. (p.name .. "_on"));
		end) and q ~= nil then
			p.on = q and true or false;
		end;
	end;
end;
function w.skillsRelease()
	for m = 1, #((W.Skills or {})), 1 do
		local p = W.Skills[m];
		if p.vk and p.vk > 0 then
			pcall(function()
				keyrelease(p.vk);
			end);
		end;
		p.rel = 0;
	end;
end;
function w.skillStep(m, p)
	if not W.Skills then
		return;
	end;
	if (m - ((W.skillSyncT or 0))) > 1.0 then
		W.skillSyncT = m;
		pcall(w.syncSkills);
	end;
	local q;
	for a = 1, #W.Skills, 1 do
		local c = W.Skills[a];
		if c.rel > 0 then
			if m >= c.rel then
				pcall(function()
					keyrelease(c.vk);
				end);
				c.rel = 0;
				c.next = m + W.SkillGap;
			end;
		elseif c.on and (p and (p <= c.dist and (m >= ((c.next or 0)) and ((not c.breaker or ((function()
			if q == nil then
				local m, p = pcall(w.curBlocking);
				q = m and p == true;
			end;
			return q;
		end))()))))) then
			c.rel = m + c.hold;
			pcall(function()
				keypress(c.vk);
			end);
		end;
	end;
end;
function w.travelTo(m, p)
	local q, a = w.char();
	if not a then
		return;
	end;
	local c = a.Position;
	local Z, S, R = m.X - c.X, ((m.Y + 3)) - c.Y, m.Z - c.Z;
	local y = math.sqrt((Z * Z + S * S) + R * R);
	if y > W.WarpAbove or not W.Smooth then
		pcall(function()
			a.CFrame = CFrame.new(m.X, m.Y + 3, m.Z);
			a.AssemblyLinearVelocity = Vector3.new(0, 0, 0);
		end);
		return;
	end;
	if y < 1 then
		return;
	end;
	local N = y * 5.0;
	if N > W.MoveSpeed then
		N = W.MoveSpeed;
	end;
	local O = N / y;
	pcall(function()
		a.AssemblyLinearVelocity = Vector3.new(Z * O, S * O, R * O);
	end);
end;
function w.focused(m)
	if not isrbxactive then
		return true;
	end;
	local p, q = pcall(isrbxactive);
	if not p then
		return true;
	end;
	if q then
		return true;
	end;
	if m and (m - ((W.warnedFocus or 0))) > 15 then
		W.warnedFocus = m;
		w.notify("roblox not focused - T pickups paused", 4);
	end;
	return false;
end;
local function z(m)
	local p = string.lower(m);
	return string.find(p, "mound", 1, true) ~= nil or string.find(p, "snow", 1, true) ~= nil;
end;
function w.openChests()
	local m = {};
	local p = a:FindFirstChild("Chests");
	if not p then
		return m;
	end;
	for p, q in ipairs(p:GetChildren()) do
		if not q:FindFirstChild("SealVFX") and not z(q.Name) then
			local p = q;
			if q.ClassName ~= "Part" and q.ClassName ~= "MeshPart" then
				p = q:FindFirstChild("RootPart") or q:FindFirstChildWhichIsA("BasePart");
			end;
			if p then
				m[#m + 1] = { Name = q.Name, Root = p, Node = q };
			end;
		end;
	end;
	return m;
end;
function w.collectStart(m, p)
	if not m or #m == 0 then
		return false;
	end;
	W.collect = {
			list = m,
			i = 1,
			phase = "move",
			t = os.clock(),
		};
	w.notify(((p or "collecting")) .. (": " .. (#m .. " item(s)")), 3);
	return true;
end;
function w.collectStop()
	W.collect = nil;
	w.aimRelease();
	pcall(function()
		keyrelease(84);
	end);
end;
function w.collectStep(m)
	local p = W.collect;
	if not p then
		return false;
	end;
	local q, a = w.char();
	if not a then
		return true;
	end;
	if not w.focused(m) then
		p.blocked = p.blocked or m;
		if (m - p.blocked) > 6 then
			w.collectStop();
			return false;
		end;
		return true;
	end;
	p.blocked = nil;
	local c = p.list[p.i];
	if not c then
		w.collectStop();
		w.notify("loot collected", 2);
		return false;
	end;
	local Z;
	if not pcall(function()
		Z = c.get();
	end) or not Z then
		p.i = p.i + 1;
		p.phase = "move";
		p.t = m;
		pcall(function()
			keyrelease(84);
		end);
		return true;
	end;
	pcall(w.aimStep, Vector3.new(Z.X, Z.Y + 1.5, Z.Z), true);
	local S, R, y = Z.X, Z.Y + ((c.lift or 2.5)), Z.Z;
	if c.side then
		S = S + 6;
		R = Z.Y + 3;
	end;
	local N = a.Position;
	local O, Q, s = S - N.X, R - N.Y, y - N.Z;
	local F = math.sqrt((O * O + Q * Q) + s * s);
	if p.phase == "move" then
		if F > W.WarpAbove then
			pcall(function()
				a.CFrame = CFrame.new(Z.X, Z.Y + 3, Z.Z);
				a.AssemblyLinearVelocity = Vector3.new(0, 0, 0);
			end);
		elseif F > ((c.reach or 3.5)) then
			local m = F * 5.0;
			if m > W.MoveSpeed then
				m = W.MoveSpeed;
			end;
			local p = m / F;
			pcall(function()
				a.AssemblyLinearVelocity = Vector3.new(O * p, Q * p, s * p);
			end);
		else
			pcall(function()
				a.AssemblyLinearVelocity = Vector3.new(0, 0, 0);
			end);
			p.settle = p.settle or (m + W.SettleT);
			if m < p.settle then
				return true;
			end;
			p.settle = nil;
			p.phase = "hold";
			p.t = m;
			pcall(function()
				keypress(84);
			end);
		end;
		if m - p.t > 10 then
			p.i = p.i + 1;
			p.phase = "move";
			p.t = m;
			p.settle = nil;
		end;
	else
		pcall(function()
			a.AssemblyLinearVelocity = Vector3.new(0, 0, 0);
		end);
		local q = c.hold or (c.tap and .2 or W.HoldT);
		if m - p.t >= q then
			pcall(function()
				keyrelease(84);
			end);
			local q = false;
			if c.soul and ((c.tries or 0)) < 2 then
				local m;
				if pcall(function()
					m = c.get();
				end) and m then
					local p = ((m - a.Position)).Magnitude;
					if p < ((c.reach or 6)) + 3 then
						c.tries = ((c.tries or 0)) + 1;
						q = true;
					end;
				end;
			end;
			if not q then
				p.i = p.i + 1;
			end;
			p.phase = "move";
			p.t = m;
			p.settle = nil;
		end;
	end;
	return true;
end;
function w.soulsNear(m, p)
	local q = {};
	local c = a:FindFirstChild("Debree");
	if not c then
		return q;
	end;
	local function Z(a)
		local c = string.lower(a.Name);
		if not string.match(c, "soul$") then
			return;
		end;
		if string.find(c, "host", 1, true) or string.find(c, "grab", 1, true) then
			return;
		end;
		local Z = a;
		if a.ClassName ~= "Part" and a.ClassName ~= "MeshPart" then
			Z = a:FindFirstChild("Root") or a:FindFirstChildWhichIsA("BasePart");
		end;
		if not Z then
			return;
		end;
		for m = 1, #q, 1 do
			if q[m].root == Z then
				return;
			end;
		end;
		local S;
		if pcall(function()
			S = ((Z.Position - m)).Magnitude;
		end) and (S and S <= p) then
			q[#q + 1] = {
					root = Z,
					get = function()
						return Z.Position;
					end,
					soul = true,
					hold = math.max(W.HoldT or 2, 2.5),
					lift = 0,
					reach = 6,
				};
		end;
	end;
	for m, p in ipairs(c:GetChildren()) do
		Z(p);
		if p.ClassName == "Folder" or p.ClassName == "Model" then
			for m, p in ipairs(p:GetChildren()) do
				Z(p);
			end;
		end;
	end;
	return q;
end;
function w.soulStep(m)
	if not W.AutoSouls then
		return false;
	end;
	local p, q = w.char();
	if not q then
		return false;
	end;
	local a = W.soul;
	if m - ((a and a.chk or 0)) < .25 then
		if not a or not a.pos then
			return false;
		end;
	end;
	local c = w.soulsNear(q.Position, W.LootRange);
	if #c == 0 then
		if a then
			if a.held then
				pcall(function()
					keyrelease(84);
				end);
			end;
			W.soul = nil;
		end;
		return false;
	end;
	local Z, S;
	for m = 1, #c, 1 do
		local p;
		if pcall(function()
			p = c[m].get();
		end) and p then
			local m = ((p - q.Position)).Magnitude;
			if not S or m < S then
				Z, S = p, m;
			end;
		end;
	end;
	if not Z then
		return false;
	end;
	if not a then
		a = { t = m, tries = 0 };
		W.soul = a;
	end;
	a.chk = m;
	a.pos = Z;
	if S > 6 then
		a.held = false;
		pcall(function()
			keyrelease(84);
		end);
		w.travelTo(Vector3.new(Z.X, Z.Y - 3, Z.Z), m);
		if m - a.t > 20 then
			W.soul = nil;
		end;
		return true;
	end;
	pcall(function()
		q.AssemblyLinearVelocity = Vector3.new(0, 0, 0);
	end);
	pcall(w.aimStep, Z, true);
	if not w.focused(m) then
		return true;
	end;
	local R = math.max(W.HoldT or 2, 2.5);
	if not a.held then
		if m - ((a.rel or 0)) < .15 then
			return true;
		end;
		a.held = true;
		a.pressT = m;
		pcall(function()
			keypress(84);
		end);
	elseif m - ((a.pressT or m)) > R then
		a.held = false;
		a.rel = m;
		a.tries = a.tries + 1;
		pcall(function()
			keyrelease(84);
		end);
		if a.tries >= 6 then
			W.soul = nil;
		end;
	end;
	return true;
end;
function w.partOf(m, p)
	local q = m.ClassName;
	if q == "Part" or q == "MeshPart" then
		return m;
	end;
	return (p and m:FindFirstChild(p)) or m:FindFirstChildWhichIsA("BasePart");
end;
function w.refind(m, p)
	if not m or not p then
		return nil;
	end;
	local q = m:GetChildren();
	for m = 1, #q, 1 do
		if w.addr(q[m]) == p then
			return q[m];
		end;
	end;
	return nil;
end;
function w.pickup(m, p, q, a, c, Z)
	local W = Z or {};
	W.folderFn, W.prefer = p, c;
	W.node, W.part = q, a;
	W.addr, W.pAddr = w.addr(q), w.addr(m);
	W.seen = os.clock();
	W.get = function()
			return w.pickupPos(W);
		end;
	return W;
end;
function w.pickupPos(m)
	if m.gone then
		return nil;
	end;
	local p = os.clock();
	if p - ((m.seen or 0)) > y then
		local p = m.folderFn();
		local q = w.refind(p, m.addr);
		local a = q and w.partOf(q, m.prefer);
		if not a then
			m.gone = true;
			return nil;
		end;
		m.node, m.part, m.pAddr = q, a, w.addr(p);
	elseif not w.attached(m.node, m.pAddr) then
		m.gone = true;
		return nil;
	end;
	m.seen = p;
	local q, a = pcall(function()
			return m.part.Position;
		end);
	if q and a then
		return a;
	end;
	return nil;
end;
local function M()
	return a:FindFirstChild("LootDrops");
end;
local function o()
	return a:FindFirstChild("Chests");
end;
function w.lootNear(m, p)
	local q = {};
	local a = os.clock();
	local c = M();
	if c then
		for a, Z in ipairs(c:GetChildren()) do
			local W = w.partOf(Z);
			if W then
				local a;
				if pcall(function()
					a = ((W.Position - m)).Magnitude;
				end) and (a and a <= p) then
					q[#q + 1] = w.pickup(c, M, Z, W);
				end;
			end;
		end;
	end;
	local Z = o();
	for c, S in ipairs(w.openChests()) do
		local R;
		if pcall(function()
			R = ((S.Root.Position - m)).Magnitude;
		end) and (R and R <= p) then
			local m = S.Root.Position;
			local p = S.Name .. ("@" .. (math.floor(m.X) .. ("," .. math.floor(m.Z))));
			local c = W.chestTap[p];
			if not c or (a - c) > 45 then
				W.chestTap[p] = a;
				table.insert(q, 1, w.pickup(Z, o, S.Node, S.Root, "RootPart", { tap = true, side = true }));
			end;
		end;
	end;
	return q;
end;
function w.crowBossFromText(m)
	local p = string.lower(tostring(m or ""));
	if p == "" or not string.find(p, "defeat", 1, true) then
		return nil;
	end;
	if string.find(p, "mother bear", 1, true) then
		return nil;
	end;
	for m, q in ipairs(S.Bosses) do
		if q ~= "All Bosses" and (q ~= "Mother Bear" and string.find(p, string.lower(q), 1, true)) then
			return q;
		end;
	end;
	return nil;
end;
function w.crowChoices()
	local m = os.clock();
	local p = W.crowPicks;
	if p and (m - p.t) < .35 then
		return p.list;
	end;
	local q = {};
	local a = c:FindFirstChild("PlayerGui");
	if not a then
		W.crowPicks = { t = m, list = q };
		return q;
	end;
	local function Z(m, p)
		if p > 8 then
			return;
		end;
		for m, a in ipairs(m:GetChildren()) do
			local c = a.ClassName;
			if c == "TextButton" or c == "TextLabel" or c == "ImageButton" then
				local m;
				pcall(function()
					m = a.Text;
				end);
				local p = w.crowBossFromText(m);
				if p then
					local m;
					pcall(function()
						m = a.AbsolutePosition;
					end);
					if not ((m and (m.X > 0 and m.Y > 0))) then
						local p = a.Parent;
						for q = 1, 4, 1 do
							if not p then
								break;
							end;
							local a;
							pcall(function()
								a = p.AbsolutePosition;
							end);
							if a and (a.X > 0 and a.Y > 0) then
								m = a;
								break;
							end;
							p = p.Parent;
						end;
					end;
					if m and (m.X > 0 and m.Y > 0) then
						local c, Z = 0, 0;
						local W = w.absSize and w.absSize(a);
						if W then
							c = tonumber(W.X) or 0;
							Z = tonumber(W.Y) or 0;
						end;
						local S, R, y, N;
						local O = a.Parent;
						for m = 1, 4, 1 do
							if not O then
								break;
							end;
							local p = O.ClassName;
							if p == "TextButton" or p == "ImageButton" then
								local m;
								pcall(function()
									m = O.AbsolutePosition;
								end);
								local p = w.absSize and w.absSize(O);
								if m and (m.X > 0 and m.Y > 0) then
									S, R = m.X, m.Y;
									y = p and tonumber(p.X) or 0;
									N = p and tonumber(p.Y) or 0;
								end;
								break;
							end;
							O = O.Parent;
						end;
						q[#q + 1] = {
								x = m.X,
								y = m.Y,
								w = c,
								h = Z,
								bx = S,
								by = R,
								bw = y,
								bh = N,
								boss = p,
							};
					end;
				end;
			end;
			Z(a, p + 1);
		end;
	end;
	Z(a, 0);
	table.sort(q, function(m, p)
		return m.y < p.y;
	end);
	local S, R = {}, {};
	for m, p in ipairs(q) do
		local q = p.boss .. (":" .. math.floor(p.y / 20));
		if not S[q] then
			S[q] = true;
			R[#R + 1] = p;
		end;
	end;
	W.crowPicks = { t = m, list = R };
	return R;
end;
function w.crowDump()
	if W.crowDumped then
		return;
	end;
	W.crowDumped = true;
	local m = { os.date("%H:%M:%S") .. " crow menu dump" };
	local function p(p, q)
		local a, c, Z, W, S, R = "?", "?", nil, nil, nil, nil;
		pcall(function()
			a = p.Name;
			c = p.ClassName;
		end);
		pcall(function()
			Z = p.Text;
		end);
		pcall(function()
			W = p.AbsolutePosition;
		end);
		pcall(function()
			S = p.Visible;
			R = p.Active;
		end);
		local y = w.absSize and w.absSize(p);
		m[#m + 1] = string.rep(" ", q) .. (c .. (" " .. (a .. (" pos=" .. (((W and (math.floor(W.X) .. ("," .. math.floor(W.Y))) or "?")) .. (" size=" .. (((y and (math.floor(tonumber(y.X) or 0) .. ("x" .. math.floor(tonumber(y.Y) or 0))) or "?")) .. (" vis=" .. (tostring(S) .. (" active=" .. (tostring(R) .. ((Z and (Z ~= "" and (" text=" .. string.sub(tostring(Z), 1, 40))) or "")))))))))))));
	end;
	local q = c:FindFirstChild("PlayerGui");
	local function a(q, c)
		if c > 9 then
			return;
		end;
		for q, Z in ipairs(q:GetChildren()) do
			local W;
			pcall(function()
				W = Z.Text;
			end);
			if w.crowBossFromText(W) then
				local q, a = {}, Z;
				for m = 1, 6, 1 do
					if not a then
						break;
					end;
					q[#q + 1] = a;
					a = a.Parent;
				end;
				m[#m + 1] = "--- offer: " .. tostring(W);
				for m = #q, 1, -1 do
					p(q[m], ((#q - m)) * 2);
				end;
				for m, q in ipairs(Z.Parent and Z.Parent:GetChildren() or {}) do
					p(q, 14);
				end;
			end;
			a(Z, c + 1);
		end;
	end;
	if q then
		pcall(a, q, 0);
	end;
	pcall(function()
		writefile("crow_ui.txt", table.concat(m, string.char(10)));
	end);
	w.notify("crow: wrote crow_ui.txt", 4);
end;
function w.crowPoint(m, p)
	p = p or 1;
	if p <= 1 and (((m.w or 0)) > 4 and ((m.h or 0)) > 4) then
		return m.x + m.w * .5, m.y + m.h * .5;
	end;
	if p <= 2 then
		return m.x + 40, m.y + 14;
	end;
	if p <= 3 and (m.bx and ((m.bw or 0)) > 4) then
		return m.bx + m.bw * .5, m.by + ((m.bh or 20)) * .5;
	end;
	if p <= 4 then
		return m.x + 120, m.y + 14;
	end;
	return m.x + 8, m.y + 8;
end;
function w.equipSlot(m, p)
	local q = 48 + math.max(1, math.min(5, math.floor(m or 1)));
	pcall(function()
		keypress(q);
	end);
	W.crowKeyUp = { vk = q, t = p + .15 };
end;
function w.heldItem()
	local m = c.Character;
	if not m then
		return nil;
	end;
	local p = m:GetChildren();
	for m = 1, #p, 1 do
		if p[m].ClassName == "Tool" then
			return p[m].Name;
		end;
	end;
	local q = m:FindFirstChild("Tool_Accessories");
	local a = q and (q:GetChildren())[1];
	return a and a.Name or nil;
end;
function w.weaponHeld()
	local m = w.heldItem();
	if not m then
		return false;
	end;
	local p = string.lower(m);
	if S.StyleMap[p] then
		return true;
	end;
	if S.Presets[m] and m ~= "Combat" then
		return true;
	end;
	for m in pairs(S.StyleWords) do
		if string.find(p, m, 1, true) then
			return true;
		end;
	end;
	return string.find(p, "katana", 1, true) ~= nil;
end;
function w.crowClose(m)
	w.equipSlot(W.WeapSlot or 1, m);
end;
function w.crowUse(m)
	w.equipSlot(W.CrowSlot or 2, m);
	W.crow.clickAt = m + .6;
end;
function w.crowStep(m)
	local p = W.crow;
	if not p then
		p = { phase = "idle", t = m };
		W.crow = p;
	end;
	if not w.focused(m) then
		return nil;
	end;
	if not w.scrOffset(m) then
		return nil;
	end;
	if W.crowKeyUp and m >= W.crowKeyUp.t then
		local m = W.crowKeyUp.vk;
		W.crowKeyUp = nil;
		pcall(function()
			keyrelease(m);
		end);
	end;
	if p.clickAt and m >= p.clickAt then
		p.clickAt = nil;
		w.dlgClickAt(((W.crowClickX or 960)) - ((W.scrOffX or 0)), ((W.crowClickY or 540)) - ((W.scrOffY or 0)));
	end;
	local q = w.activeQuest();
	if q and (not q.Complete and p.phase ~= "fight") then
		local a = w.questTarget(q);
		if not a then
			for m = 1, #((q.Tasks or {})), 1 do
				a = a or w.codeToMob(q.Tasks[m].Code);
			end;
		end;
		if a and S.BossNames[string.lower(a)] then
			p.target = a;
			p.quest = q.Name;
			p.phase = "fight";
			p.lastAlive = m;
			w.notify("crow: already on " .. (q.Name .. (" - hunting " .. a)), 4);
			return a;
		end;
	end;
	if p.phase == "idle" then
		if m < ((p.nextAt or 0)) then
			return nil;
		end;
		p.prevQuest = q and q.Name or nil;
		w.crowUse(m);
		p.phase = "menu";
		p.menuAt = m + 3.5;
		p.deadline = m + 10;
		return nil;
	end;
	if p.phase == "menu" then
		if m < ((p.menuAt or 0)) then
			return nil;
		end;
		local q = w.crowChoices();
		if #q > 0 then
			local a = q[1];
			p.aim = (p.aim or (W.crowAim or 1));
			local c, Z = w.crowPoint(a, p.aim);
			w.dlgClickAt(c, Z);
			p.pick = a;
			p.target = a.boss;
			p.phase = "quest";
			p.deadline = m + 5;
			p.sawBoss = false;
			w.logErr("crow", "click " .. (a.boss .. (" aim " .. (tostring(p.aim) .. (" at " .. (math.floor(c) .. ("," .. math.floor(Z))))))));
			w.notify("crow: taking " .. a.boss, 3);
		elseif m >= ((p.deadline or 0)) then
			p.phase = "idle";
			p.nextAt = m + 2;
			w.notify("crow: no boss offer found, retrying", 4);
		end;
		return nil;
	end;
	if p.phase == "quest" then
		local a = q and q.Name ~= p.prevQuest;
		if q and (not q.Complete and ((a or (p.target and w.questMatchesEntry(q.Name, p.target))))) then
			W.crowAim = p.aim or 1;
			p.quest = q.Name;
			p.phase = "fight";
			p.lastAlive = m;
			p.eqT = 0;
			if #w.crowChoices() > 0 then
				w.crowClose(m);
			end;
			w.notify("crow: hunting " .. tostring(p.target), 3);
		elseif m >= ((p.deadline or 0)) then
			if #w.crowChoices() > 0 and ((p.aim or 1)) < 5 then
				pcall(w.crowDump);
				p.aim = ((p.aim or 1)) + 1;
				p.phase = "menu";
				p.menuAt = m + .4;
				p.deadline = m + 8;
				return nil;
			end;
			w.crowClose(m);
			p.aim = nil;
			p.target = nil;
			p.phase = "idle";
			p.nextAt = m + 3;
		end;
		return nil;
	end;
	if not w.weaponHeld() and m - ((p.eqT or 0)) > 1.5 then
		p.eqT = m;
		w.equipSlot(W.WeapSlot or 1, m);
	end;
	if not q or (q.Name == p.quest and q.Complete) then
		p.target, p.quest, p.phase, p.nextAt = nil, nil, "idle", m + 1;
		w.notify("crow: boss quest done, next one", 3);
		return nil;
	end;
	return p.target;
end;
function w.scrOffset(m)
	if W.scrOffX then
		return true;
	end;
	local p = c:GetMouse();
	if not W.scrCalT then
		W.scrMx, W.scrMy = p.X, p.Y;
		pcall(function()
			mousemoveabs(700, 450);
		end);
		W.scrCalT = m;
		return false;
	end;
	if m - W.scrCalT < .12 then
		return false;
	end;
	W.scrOffX, W.scrOffY = 700 - p.X, 450 - p.Y;
	pcall(function()
		mousemoveabs(W.scrMx + W.scrOffX, W.scrMy + W.scrOffY);
	end);
	return true;
end;
function w.dlgRoot()
	local m = c:FindFirstChild("PlayerGui");
	local p = m and m:FindFirstChild("ComponentsHolder");
	return p and p:FindFirstChild("DialogueFrame") or nil;
end;
local function l(m, p)
	p = p or 0;
	if p > 6 then
		return nil;
	end;
	for m, q in ipairs(m:GetChildren()) do
		local a, c;
		pcall(function()
			a = q.Text;
		end);
		pcall(function()
			c = q.AbsolutePosition;
		end);
		if a and (a ~= "" and c) then
			return a, c;
		end;
		local Z, W = l(q, p + 1);
		if Z then
			return Z, W;
		end;
	end;
	return nil;
end;
function w.dlgAnswers(m)
	local p = {};
	local q = m and m:FindFirstChild("ButtonHolder", true);
	if not q then
		local function p(m, q)
			if q > 5 then
				return nil;
			end;
			for m, a in ipairs(m:GetChildren()) do
				if a.Name == "ButtonHolder" then
					return a;
				end;
				local c = p(a, q + 1);
				if c then
					return c;
				end;
			end;
		end;
		q = m and p(m, 0) or nil;
	end;
	if not q then
		return p;
	end;
	for m, q in ipairs(q:GetChildren()) do
		local a = l(q, 0);
		local c;
		pcall(function()
			c = q.AbsolutePosition;
		end);
		if c and ((c.X > 0 or c.Y > 0)) then
			p[#p + 1] = { text = a or q.Name, x = c.X, y = c.Y };
		end;
	end;
	return p;
end;
function w.dlgContinue(m)
	local p;
	local function q(m, p)
		if p > 5 then
			return nil;
		end;
		for m, a in ipairs(m:GetChildren()) do
			if a.Name == "DialogueHolder" then
				return a;
			end;
			local c = q(a, p + 1);
			if c then
				return c;
			end;
		end;
	end;
	p = m and q(m, 0) or nil;
	if not p then
		return nil;
	end;
	local a, c = l(p, 0);
	if c and c.X < 0 then
		return nil;
	end;
	return c;
end;
function w.dlgClickAt(m, p)
	W.crowPicks = nil;
	W.dlgClick = {
			x = m + ((W.scrOffX or 0)),
			y = p + ((W.scrOffY or 0)),
			st = "move",
			t = os.clock(),
		};
end;
function w.dlgTalk(m, p)
	local q = W.dlg;
	if not q or q.want ~= m then
		q = { want = m, t = p, tries = 0 };
		W.dlg = q;
	end;
	if not w.focused(p) then
		return "working";
	end;
	if not w.scrOffset(p) then
		return "working";
	end;
	local a = w.dlgRoot();
	if not a then
		if q.opened then
			W.dlg = nil;
			return "done";
		end;
		if p - q.t > 20 then
			W.dlg = nil;
			return "failed";
		end;
		if p - ((q.openT or 0)) > 2.5 then
			q.openT = p;
			q.tries = q.tries + 1;
			pcall(function()
				keypress(84);
			end);
			W.dlgKeyUp = p + .15;
		end;
		return "working";
	end;
	q.opened = true;
	if not m then
		return "done";
	end;
	if p - ((q.clickT or 0)) < .8 then
		return "working";
	end;
	q.clickT = p;
	for p, a in ipairs(w.dlgAnswers(a)) do
		if a.text == m then
			w.dlgClickAt(a.x + 40, a.y + 14);
			q.clicked = true;
			return "working";
		end;
	end;
	local c = w.dlgContinue(a);
	if c then
		w.dlgClickAt(c.X + 12, c.Y + 8);
	end;
	if p - q.t > 25 then
		W.dlg = nil;
		return "failed";
	end;
	return "working";
end;
function w.dlgRelease(m)
	local p = W.dlgClick;
	if p then
		if p.st == "move" then
			pcall(function()
				mousemoveabs(p.x, p.y);
			end);
			p.st, p.t = "press", m;
		elseif p.st == "press" and m - p.t > .1 then
			pcall(function()
				mouse1press();
			end);
			W.dlgUp = m + .08;
			p.st, p.t = "down", m;
		elseif p.st == "down" and m - p.t > .3 then
			W.dlgClick = nil;
		end;
	end;
	if W.dlgUp and m >= W.dlgUp then
		W.dlgUp = nil;
		pcall(function()
			mouse1release();
		end);
	end;
	if W.crowKeyUp and m >= W.crowKeyUp.t then
		local m = W.crowKeyUp.vk;
		W.crowKeyUp = nil;
		pcall(function()
			keyrelease(m);
		end);
	end;
	if W.dlgKeyUp and m >= W.dlgKeyUp then
		W.dlgKeyUp = nil;
		pcall(function()
			keyrelease(84);
		end);
	end;
end;
function w.acoPath(m, p)
	local q = #p;
	if q == 0 then
		return {};
	end;
	if q == 1 then
		return { 1 };
	end;
	local a = { [0] = m };
	for m = 1, q, 1 do
		a[m] = p[m];
	end;
	local c = {};
	for m = 0, q, 1 do
		c[m] = {};
		for p = 0, q, 1 do
			local q, Z = a[m], a[p];
			local W, S, w = q.X - Z.X, q.Y - Z.Y, q.Z - Z.Z;
			c[m][p] = math.max(math.sqrt((W * W + S * S) + w * w), .01);
		end;
	end;
	local function Z(m)
		local p, q = 0, 0;
		for a = 1, #m, 1 do
			p = p + c[q][m[a]];
			q = m[a];
		end;
		return p;
	end;
	local W, S, w = {}, {}, 0;
	for m = 1, q, 1 do
		local p, a = nil, math.huge;
		for m = 1, q, 1 do
			if not S[m] and c[w][m] < a then
				a = c[w][m];
				p = m;
			end;
		end;
		S[p] = true;
		W[#W + 1] = p;
		w = p;
	end;
	local R = Z(W);
	local y = 1 / ((q * R));
	local N = {};
	for m = 0, q, 1 do
		N[m] = {};
		for p = 0, q, 1 do
			N[m][p] = y;
		end;
	end;
	local O, Q, s, F = 1, 3, .2, .9;
	local K = math.min(20, q + 5);
	local x = q <= 8 and 40 or (q <= 20 and 30 or 15);
	for m = 1, x, 1 do
		for m = 1, K, 1 do
			local p, a, S = {}, {}, 0;
			for m = 1, q, 1 do
				local Z;
				if math.random() < F then
					local m = -1;
					for q = 1, q, 1 do
						if not p[q] then
							local p = N[S][q] ^ O * ((1 / c[S][q])) ^ Q;
							if p > m then
								m = p;
								Z = q;
							end;
						end;
					end;
				else
					local m, a = 0, {};
					for q = 1, q, 1 do
						if not p[q] then
							a[q] = N[S][q] ^ O * ((1 / c[S][q])) ^ Q;
							m = m + a[q];
						end;
					end;
					local W = math.random() * m;
					for m = 1, q, 1 do
						if a[m] then
							W = W - a[m];
							if W <= 0 then
								Z = m;
								break;
							end;
						end;
					end;
					if not Z then
						for m = 1, q, 1 do
							if not p[m] then
								Z = m;
								break;
							end;
						end;
					end;
				end;
				N[S][Z] = ((1 - s)) * N[S][Z] + s * y;
				p[Z] = true;
				a[#a + 1] = Z;
				S = Z;
			end;
			local w = Z(a);
			if w < R then
				R = w;
				W = a;
			end;
		end;
		local p = 0;
		for m = 1, #W, 1 do
			local q = W[m];
			N[p][q] = ((1 - s)) * N[p][q] + s / R;
			p = q;
		end;
	end;
	return W, R;
end;
function w.routeStart(m)
	local p, q = w.char();
	if not q or not m or #m == 0 then
		return false;
	end;
	local a, c = {}, {};
	for p = 1, #m, 1 do
		local q, Z = pcall(m[p].get);
		if q and Z then
			a[#a + 1] = Z;
			c[#c + 1] = m[p];
		end;
	end;
	if #a == 0 then
		return false;
	end;
	local Z, S = w.acoPath(q.Position, a);
	local R = {};
	for m = 1, #Z, 1 do
		R[m] = c[Z[m]];
	end;
	W.route = {
			list = R,
			i = 1,
			t = os.clock(),
			tDown = false,
			nextT = 0,
		};
	w.notify(string.format("boss drops: %d, path %.0f studs", #R, S or 0), 3);
	return true;
end;
function w.routeStop()
	local m = W.route;
	W.route = nil;
	if m and m.tDown then
		pcall(function()
			keyrelease(84);
		end);
	end;
end;
function w.routeStep(m)
	local p = W.route;
	if not p then
		return false;
	end;
	local q, a = w.char();
	if not a then
		return true;
	end;
	if w.focused(m) and m >= p.nextT then
		p.nextT = m + W.RouteTap;
		p.tDown = not p.tDown;
		pcall(function()
			if p.tDown then
				keypress(84);
			else
				keyrelease(84);
			end;
		end);
	end;
	local c = p.list[p.i];
	if not c then
		w.routeStop();
		return false;
	end;
	local Z;
	if not pcall(function()
		Z = c.get();
	end) or not Z then
		p.i = p.i + 1;
		p.t = m;
		p.arrive = nil;
		return true;
	end;
	local S = a.Position;
	local R, y, N = Z.X - S.X, ((Z.Y + 2.5)) - S.Y, Z.Z - S.Z;
	local O = math.sqrt((R * R + y * y) + N * N);
	if O <= 3 then
		p.i = p.i + 1;
		p.t = m;
		if not p.list[p.i] then
			pcall(function()
				a.AssemblyLinearVelocity = Vector3.new(0, 0, 0);
			end);
		end;
		return true;
	end;
	if O > W.WarpAbove then
		pcall(function()
			a.CFrame = CFrame.new(Z.X, Z.Y + 3, Z.Z);
			a.AssemblyLinearVelocity = Vector3.new(0, 0, 0);
		end);
	else
		local m = W.MoveSpeed;
		if not p.list[p.i + 1] then
			m = math.min(O * 5.0, W.MoveSpeed);
		end;
		local q = m / O;
		pcall(function()
			a.AssemblyLinearVelocity = Vector3.new(R * q, y * q, N * q);
		end);
	end;
	if m - p.t > 5 then
		p.i = p.i + 1;
		p.t = m;
	end;
	return true;
end;
function w.activeQuest()
	local m = p:FindFirstChild("Player_Service");
	local q = m and m:FindFirstChild("Data");
	local a = q and q:FindFirstChild(c.Name);
	if not a then
		return nil;
	end;
	local Z = a:FindFirstChild("slotEquipped");
	Z = Z and Z.Value or 1;
	local W = a:FindFirstChild("slots");
	local S = W and W:FindFirstChild("Slot" .. tostring(math.floor(Z)));
	local w = S and S:FindFirstChild("Quests");
	local R = w and w:FindFirstChild("Holder");
	if not R then
		return nil;
	end;
	local y = nil;
	local N = R:GetChildren();
	for m = 1, #N, 1 do
		local p = N[m]:FindFirstChild("Tasks");
		if p then
			local q, a = {}, true;
			local c = p:GetChildren();
			for m = 1, #c, 1 do
				local p = c[m]:FindFirstChild("Value");
				local Z = c[m]:FindFirstChild("Max");
				local W;
				local S = c[m]:FindFirstChild("Code");
				if S then
					local m = S.Value;
					if type(m) == "string" and string.match(m, "^[%w ]+$") then
						W = m;
					end;
				end;
				q[#q + 1] = {
						Name = c[m].Name,
						Value = p and p.Value or 0,
						Max = Z and Z.Value or 1,
						Code = W,
					};
				if ((p and p.Value or 0)) < ((Z and Z.Value or 1)) then
					a = false;
				end;
			end;
			local Z = N[m]:FindFirstChild("QuestString");
			local W = nil;
			if Z then
				local m = Z.Value;
				if type(m) == "string" and #m > 0 then
					local p = true;
					for q = 1, #m, 1 do
						local a = string.byte(m, q);
						if a < 32 or a > 126 then
							p = false;
							break;
						end;
					end;
					if p then
						W = m;
					end;
				end;
			end;
			local S = {
					Name = N[m].Name,
					Desc = W,
					Id = N[m].Name,
					Tasks = q,
					Complete = a,
				};
			if not a then
				return S;
			end;
			if not y then
				y = S;
			end;
		end;
	end;
	return y;
end;
local d = {
		"guard",
		"guards",
		"subordinate",
		"subordinates",
		"minion",
		"minions",
		"underling",
		"underlings",
		"escort",
	};
function w.questPickups()
	local m = {};
	for p, q in ipairs(a:GetChildren()) do
		if string.match(q.Name, "%d+$") then
			local p;
			if q.ClassName == "Part" or q.ClassName == "MeshPart" then
				p = q;
			else
				p = q:FindFirstChildWhichIsA("BasePart");
			end;
			if p then
				local a = false;
				for m, p in ipairs(q:GetDescendants()) do
					if p.ClassName == "ProximityPrompt" then
						a = true;
						break;
					end;
				end;
				if a then
					m[#m + 1] = { Name = q.Name, Root = p, Node = q };
				end;
			end;
		end;
	end;
	return m;
end;
local G = {
		"defeated",
		"remaining",
		"slain",
		"killed",
		"eliminated",
		"defeat",
		"hunted",
		"fell ",
	};
local r = {
		"found",
		"collected",
		"delivered",
		"escorted",
		"caught",
		"return",
		"retrieve",
		"recovered",
		"gathered",
	};
function w.taskKind(m)
	if not m then
		return "unknown";
	end;
	local p = " " .. (string.lower(m) .. " ");
	for m, q in ipairs(r) do
		if string.find(p, q, 1, true) then
			return "fetch";
		end;
	end;
	for m, q in ipairs(G) do
		if string.find(p, q, 1, true) then
			return "kill";
		end;
	end;
	return "unknown";
end;
function w.questMatchesEntry(m, p)
	if not m or not p then
		return false;
	end;
	local function q(m)
		local p = {};
		for m in string.gmatch(string.lower(m), "%a+") do
			if #m >= 4 then
				p[m] = true;
			end;
		end;
		return p;
	end;
	local a, c = q(m), q(p);
	for m in pairs(a) do
		if c[m] then
			return true;
		end;
	end;
	return false;
end;
function w.codeToMob(m)
	if not m or m == "" then
		return nil;
	end;
	local p = string.gsub(m, "(%l)(%u)", "%1 %2");
	p = string.gsub(p, " Sub$", " Subordinate");
	local q = string.lower(p);
	local a = string.gsub(q, " ", "");
	for m, p in ipairs({ S.Mobs, S.Bosses }) do
		for m = 1, #p, 1 do
			local c = string.lower(p[m]);
			if c == q or string.gsub(c, " ", "") == a then
				return p[m];
			end;
		end;
	end;
	return nil;
end;
function w.questCompleted(m)
	local q = p:FindFirstChild("Player_Service");
	local a = q and q:FindFirstChild("Data");
	local Z = a and a:FindFirstChild(c.Name);
	if not Z then
		return false;
	end;
	local W = Z:FindFirstChild("slotEquipped");
	local S = Z:FindFirstChild("slots");
	S = S and S:FindFirstChild("Slot" .. tostring(math.floor(W and W.Value or 1)));
	local w = S and S:FindFirstChild("Quests");
	local R = w and w:FindFirstChild("Completed");
	return R ~= nil and R:FindFirstChild(m) ~= nil;
end;
function w.questLastAge()
	local m = p:FindFirstChild("Player_Service");
	local q = m and m:FindFirstChild("Data");
	local a = q and q:FindFirstChild(c.Name);
	if not a then
		return nil;
	end;
	local Z = a:FindFirstChild("slotEquipped");
	local W = a:FindFirstChild("slots");
	W = W and W:FindFirstChild("Slot" .. tostring(math.floor(Z and Z.Value or 1)));
	local S = W and W:FindFirstChild("Quests");
	local w = S and S:FindFirstChild("LastTime");
	if not w then
		return nil;
	end;
	return os.time() - w.Value;
end;
function w.optLevel(m)
	if type(m) ~= "string" then
		return nil;
	end;
	local p = string.match(m, "%(Lv%s*(%d+)%)");
	return p and tonumber(p) or nil;
end;
function w.questOpt(m)
	if not m then
		return nil;
	end;
	if W.QuestPick == "boss" then
		return m.BossOption or m.QuestOption;
	end;
	return m.QuestOption or m.BossOption;
end;
function w.pickQuest(m)
	W.qBack = W.qBack or {};
	W.qSent = W.qSent or {};
	local p;
	local q = W.QuestSel or {};
	local a = #q;
	local c = 1;
	if W.lastDoneKey then
		local m = S.Quests[W.lastDoneKey];
		local p = m and ((m.QuestOption or m.BossOption));
		for m = 1, a, 1 do
			local a = q[m];
			local Z = S.Quests[a];
			local w = a == W.lastDoneKey;
			if not w and (Z and (Z.AutoDeliver and p)) then
				for m, q in ipairs(S.DeliverGivers) do
					if q.QuestOption == p then
						w = true;
						break;
					end;
				end;
			end;
			if w then
				c = m + 1;
				break;
			end;
		end;
	end;
	for Z = 0, a - 1, 1 do
		local R = q[((((c - 1) + Z)) % math.max(a, 1)) + 1];
		local y = S.Quests[R];
		local N = W.qSent[R];
		if N and m - N > 6 then
			W.qSent[R] = nil;
			W.qBack[R] = m + 60;
		end;
		if y and y.AutoDeliver then
			local q, a = w.pickDelivery(m);
			if q then
				return y, R;
			end;
			if a and ((not p or a < p)) then
				p = a;
			end;
		elseif y and (not y.Auto and y.Pos) then
			local q = w.questOpt(y);
			if q and not w.questCompleted(q) then
				local q = W.qBack[R];
				if not q or m >= q then
					return y, R;
				end;
				if not p or q < p then
					p = q;
				end;
			end;
		end;
	end;
	return nil, p;
end;
function w.entryForQuest(m)
	if not m then
		return nil;
	end;
	if m.Desc then
		for p, q in pairs(S.Quests) do
			if q.QuestOption == m.Desc or q.BossOption == m.Desc then
				return q, p;
			end;
		end;
	end;
	for p, q in ipairs(W.QuestSel or {}) do
		local a = S.Quests[q];
		if a and (not a.Auto and w.questMatchesEntry(m.Name, q)) then
			return a, q;
		end;
	end;
	return nil;
end;
function w.pickDelivery(m)
	W.delivBack = W.delivBack or {};
	local p;
	for q, a in ipairs(S.DeliverGivers) do
		local c = a.QuestOption;
		local Z = W.delivSent and W.delivSent[c];
		if Z and (m - Z) > 6 then
			W.delivSent[c] = nil;
			local p = ((W.delivFails or {}))[c] or 0;
			W.delivFails = W.delivFails or {};
			W.delivFails[c] = p + 1;
			W.delivBack[c] = m + 8;
		end;
		if not w.questCompleted(c) then
			local q = W.delivBack[c];
			if not q or m >= q then
				return a;
			end;
			if not p or q < p then
				p = q;
			end;
		end;
	end;
	return nil, p;
end;
function w.deliverStep(m, p, q, a, c)
	if W.cur then
		W.cur = nil;
		w.aimRelease();
		pcall(w.skillsRelease);
		if w.clickRelease then
			pcall(w.clickRelease);
		end;
	end;
	local Z = m .. ("|" .. p);
	if W.deliverKey ~= Z then
		W.deliverKey = Z;
		W.deliverTries = 0;
		W.deliverAt = nil;
	end;
	local S = w.followNpc(q.npc, q.pos, c);
	if S > 8 then
		W.deliverAt = nil;
		return;
	end;
	W.deliverAt = W.deliverAt or (c + W.SettleT);
	if c < W.deliverAt then
		return;
	end;
	if not w.remote then
		local m = w.dlgTalk(q.answer, c);
		if m == "failed" and c - ((W.deliverWarn or 0)) > 12 then
			W.deliverWarn = c;
			w.notify("could not hand \'" .. (p .. ("\' to " .. (q.npc .. " - do it by hand"))), 6);
		end;
		return;
	end;
	if (c - ((W.deliverT or 0))) < 3 then
		return;
	end;
	W.deliverT = c;
	W.deliverTries = ((W.deliverTries or 0)) + 1;
	if W.deliverTries > 4 then
		if c - ((W.deliverWarn or 0)) > 10 then
			W.deliverWarn = c;
			w.notify("\'" .. (p .. "\' not accepted - missing the item?"), 6);
		end;
		return;
	end;
	pcall(function()
		w.remote:FireServer("QuestProgress", m, p);
	end);
	w.notify("handing in: " .. p, 3);
end;
function w.questTarget(m)
	if not m then
		return nil;
	end;
	local p = string.lower(m.Name or "");
	if m.Desc then
		p = p .. (" " .. string.lower(m.Desc));
	end;
	for q = 1, #m.Tasks, 1 do
		p = p .. (" " .. string.lower(m.Tasks[q].Name));
	end;
	p = " " .. (string.gsub(p, "[^%a]+", " ") .. " ");
	local q = false;
	for m, a in ipairs(d) do
		if string.find(p, " " .. (a .. " "), 1, true) then
			q = true;
			break;
		end;
	end;
	local a = { lost = true };
	local function c(m)
		local q, a = 0, 0;
		for m in string.gmatch(string.lower(m), "%a+") do
			a = a + 1;
			if #m >= 4 then
				if string.find(p, " " .. (m .. " "), 1, true) or string.find(p, " " .. (m .. "s "), 1, true) then
					q = q + 1;
				end;
			end;
		end;
		return q, a;
	end;
	local Z, W, w = nil, 0, -1;
	local R = { S.Mobs, S.Bosses };
	for m = 1, #R, 1 do
		local p = R[m];
		for m = 1, #p, 1 do
			local S = p[m];
			if S ~= "All Regular Mobs" and (S ~= "All Bosses" and not a[string.lower(S)]) then
				local m = c(S);
				if m > 0 then
					local p = string.find(string.lower(S), "subordinate", 1, true) ~= nil;
					local a = m * 10;
					if q and p then
						a = a + 5;
					end;
					if (not q) and (not p) then
						a = a + 5;
					end;
					if m > W or (m == W and a > w) then
						Z, W, w = S, m, a;
					end;
				end;
			end;
		end;
	end;
	return Z;
end;
function w.chests()
	local m = {};
	local p = a:FindFirstChild("Chests");
	if not p then
		return m;
	end;
	local q = p:GetChildren();
	for p = 1, #q, 1 do
		local a = q[p];
		local c;
		if a.ClassName == "Part" or a.ClassName == "MeshPart" then
			c = a;
		else
			c = a:FindFirstChild("RootPart") or a:FindFirstChildWhichIsA("BasePart");
		end;
		if c then
			m[#m + 1] = { Name = a.Name, Pos = c.Position };
		end;
	end;
	return m;
end;
local I = "__s2pool_slayers2";
local T = 48;
do
	local m = rawget(_G, I);
	_G[I] = nil;
	local p;
	if type(m) == "table" and (#m == T and (type(m[1]) == "table" and (m[1].box and m[1].tag))) then
		m[1].box.Visible = false;
		m[1].tag.Visible = false;
		p = m;
	end;
	if p then
		W.pool = p;
		for m = 1, T, 1 do
			local q = p[m];
			q.vis = false;
			q.box.Visible = false;
			q.tag.Visible = false;
		end;
		_G[I] = p;
	else
		for m = 1, T, 1 do
			local p = Drawing.new("Square");
			p.Thickness = 1;
			p.Filled = false;
			p.Visible = false;
			local q = Drawing.new("Text");
			q.Size = 13;
			q.Center = true;
			q.Outline = true;
			q.Visible = false;
			W.pool[m] = { box = p, tag = q };
		end;
		_G[I] = W.pool;
	end;
end;
local u = {};
W.espList = function()
		return u;
	end;
local X = {
		Color3.fromRGB(230, 230, 230),
		Color3.fromRGB(255, 90, 90),
		Color3.fromRGB(255, 215, 90),
		Color3.fromRGB(255, 130, 200),
		Color3.fromRGB(170, 130, 255),
		Color3.fromRGB(140, 255, 200),
		Color3.fromRGB(255, 240, 90),
		Color3.fromRGB(255, 40, 70),
		Color3.fromRGB(120, 255, 80),
	};
function w.espOn()
	return W.EspMobs or W.EspBosses or W.EspChests or W.EspCrystals or W.EspLily or W.EspHorses or W.EspLevers or W.EspMuzan or W.EspFruits;
end;
function w.espClear()
	local m = W.pool;
	if not m then
		return;
	end;
	for p = 1, #m, 1 do
		local q = m[p];
		q.box.Visible = false;
		q.tag.Visible = false;
		q.vis, q.bx, q.by, q.bw, q.bh, q.tx, q.ty, q.text, q.col = false, nil, nil, nil, nil, nil, nil, nil, nil;
	end;
	u = {};
	w.npcCache = nil;
end;
function w.espBuild()
	local m, p = w.char();
	if not p then
		u = {};
		return;
	end;
	local q = p.Position;
	local c = W.EspDist;
	local Z = {};
	if W.EspMobs or W.EspBosses then
		local m = os.clock();
		if not w.npcCache or (m - w.npcCacheT) > 2.0 then
			w.npcCache = w.scan(nil, nil);
			w.npcCacheT = m;
		end;
		local p = w.npcCache;
		for m = 1, #p, 1 do
			if #Z >= T then
				break;
			end;
			local a = p[m];
			local S = (a.Boss and W.EspBosses) or ((not a.Boss) and W.EspMobs);
			if S and w.npcAlive(a, .6) then
				local m, p = nil, 0;
				pcall(function()
					p = a.Hum.Health;
					m = ((a.Root.Position - q)).Magnitude;
				end);
				if m and (p > 0 and m <= c) then
					Z[#Z + 1] = {
							kind = "m",
							npc = a,
							root = a.Root,
							text = a.Boss and (a.Name .. (" " .. (math.floor(p) .. "hp"))) or (a.Name .. (" " .. (math.floor(m) .. "m"))),
							col = a.Boss and 2 or 1,
						};
				end;
			end;
		end;
	end;
	if W.EspChests then
		local m = w.chests();
		for p = 1, #m, 1 do
			if #Z >= T then
				break;
			end;
			local a = ((m[p].Pos - q)).Magnitude;
			if a <= c then
				Z[#Z + 1] = {
						kind = "p",
						pos = m[p].Pos,
						text = m[p].Name,
						col = 3,
					};
			end;
		end;
	end;
	if W.EspCrystals or W.EspLily then
		local m = a:FindFirstChild("Debree");
		if m then
			if W.EspLily then
				local p = m:GetChildren();
				for m = 1, #p, 1 do
					if #Z >= T then
						break;
					end;
					if string.find(string.lower(p[m].Name), "spider lily", 1, true) then
						local a = p[m]:FindFirstChildWhichIsA("BasePart");
						if a then
							local m = ((a.Position - q)).Magnitude;
							if m <= c then
								Z[#Z + 1] = {
										kind = "p",
										pos = a.Position,
										text = "Spider Lily",
										col = 4,
									};
							end;
						end;
					end;
				end;
			end;
			local p = m:FindFirstChild("Regions");
			if W.EspCrystals and p then
				local m = p:GetChildren();
				for p = 1, #m, 1 do
					if #Z >= T then
						break;
					end;
					local a = m[p]:GetChildren();
					for W = 1, #a, 1 do
						if string.find(string.lower(a[W].Name), "spawncrystal", 1, true) then
							local S = a[W]:FindFirstChild("Root") or a[W]:FindFirstChildWhichIsA("BasePart");
							if S then
								local a = ((S.Position - q)).Magnitude;
								if a <= c then
									Z[#Z + 1] = {
											kind = "p",
											pos = S.Position,
											text = m[p].Name,
											col = 5,
										};
								end;
							end;
							break;
						end;
					end;
				end;
			end;
		end;
	end;
	if W.EspHorses then
		local m = w.scan("horse", false);
		for p = 1, #m, 1 do
			if #Z >= T then
				break;
			end;
			local a = ((m[p].Root.Position - q)).Magnitude;
			if a <= c then
				Z[#Z + 1] = {
						kind = "p",
						pos = m[p].Root.Position,
						text = "Horse",
						col = 6,
					};
			end;
		end;
	end;
	if W.EspLevers and #Z < T then
		local m = a:FindFirstChild("Map");
		local p = m and m:FindFirstChild("Puzzles");
		if p then
			for m, p in ipairs(p:GetDescendants()) do
				if #Z >= T then
					break;
				end;
				if ((p.ClassName == "MeshPart" or p.ClassName == "Part")) and string.find(string.lower(p.Name), "lever", 1, true) then
					local m = ((p.Position - q)).Magnitude;
					if m <= c then
						Z[#Z + 1] = {
								kind = "p",
								pos = p.Position,
								text = "LEVER " .. (math.floor(m) .. "m"),
								col = 7,
							};
					end;
				end;
			end;
		end;
	end;
	if W.EspMuzan and #Z < T then
		local m = {};
		for p, q in ipairs({ a:FindFirstChild("Humanoids"), a:FindFirstChild("Debree") }) do
			local a = q and q:FindFirstChild("Regions");
			for p, q in ipairs(a and a:GetChildren() or {}) do
				m[#m + 1] = q:FindFirstChild("ActiveNpcs");
				m[#m + 1] = q:FindFirstChild("StationaryNpcs");
			end;
		end;
		for m, p in ipairs(m) do
			if #Z >= T then
				break;
			end;
			for m, p in ipairs(p and p:GetChildren() or {}) do
				if #Z >= T then
					break;
				end;
				if string.find(string.lower(p.Name), "muzan", 1, true) then
					local m = p:FindFirstChild(p.Name) or p:FindFirstChildOfClass("Model");
					local a = p:FindFirstChild("HumanoidRootPart") or (m and m:FindFirstChild("HumanoidRootPart")) or p:FindFirstChildWhichIsA("BasePart");
					local W = p:FindFirstChildOfClass("Humanoid") or (m and m:FindFirstChildOfClass("Humanoid"));
					if a then
						local m = ((a.Position - q)).Magnitude;
						if m <= c then
							local q = "";
							if W then
								q = " " .. (math.floor(W.Health) .. ("/" .. math.floor(W.MaxHealth)));
							end;
							Z[#Z + 1] = {
									kind = "p",
									pos = a.Position,
									text = p.Name .. (q .. (" " .. (math.floor(m) .. "m"))),
									col = 8,
								};
						end;
					end;
				end;
			end;
		end;
	end;
	if W.EspFruits and #Z < T then
		local m = {
				"apple",
				"banana",
				"grape",
				"fruit",
				"peach",
				"mango",
				"melon",
				"berry",
				"cherry",
				"kiwi",
				"orange",
				"pineapple",
				"coconut",
			};
		for p, a in ipairs(a:GetChildren()) do
			if #Z >= T then
				break;
			end;
			local W = string.lower(a.Name);
			local S = false;
			for m, p in ipairs(m) do
				if string.find(W, p, 1, true) then
					S = true;
					break;
				end;
			end;
			if S then
				local m = a;
				if a.ClassName ~= "Part" and a.ClassName ~= "MeshPart" then
					m = a:FindFirstChildWhichIsA("BasePart");
				end;
				if m then
					local p = ((m.Position - q)).Magnitude;
					if p <= c then
						Z[#Z + 1] = {
								kind = "p",
								pos = m.Position,
								text = a.Name .. (" " .. (math.floor(p) .. "m")),
								col = 9,
							};
					end;
				end;
			end;
		end;
	end;
	u = Z;
end;
local B = { off = 260, at = 0 };
function B.calibrate()
	B.at = tick();
	local m, p = pcall(function()
			return workspace.CurrentCamera.ViewportSize;
		end);
	if not ((m and (p and p.X > 1))) then
		return;
	end;
	local q, a = pcall(function()
			return (game:GetService("Players")).LocalPlayer.PlayerGui:GetChildren();
		end);
	for m, q in ipairs(q and a or {}) do
		local a, c = pcall(function()
				return q.Address;
			end);
		if a and (type(c) == "number" and c > 0) then
			for m = 128, 512, 4 do
				local q, a = pcall(memory_read, "float", c + m);
				local Z, W = pcall(memory_read, "float", (c + m) + 4);
				if q and (Z and (type(a) == "number" and (type(W) == "number" and (math.abs(a - p.X) < 1 and (W > p.Y - 120 and W <= p.Y))))) then
					B.off = m;
					return;
				end;
			end;
		end;
	end;
end;
local function E(m)
	local p, q = pcall(function()
			return m.AbsoluteSize;
		end);
	if p and (q and ((q.X >= .5 or q.Y >= .5))) then
		return q;
	end;
	local a, c = pcall(function()
			return m.Address;
		end);
	if not ((a and (type(c) == "number" and c > 0))) then
		return q;
	end;
	local function Z()
		local m, p = pcall(memory_read, "float", c + B.off);
		local q, a = pcall(memory_read, "float", (c + B.off) + 4);
		if m and (q and (type(p) == "number" and (type(a) == "number" and (p == p and (a == a and (p >= 0 and (a >= 0 and (p < 100000.0 and a < 100000.0)))))))) then
			return { X = p, Y = a };
		end;
	end;
	local W = Z();
	if W and ((W.X >= .5 or W.Y >= .5)) then
		return W;
	end;
	if tick() - B.at > 1 then
		pcall(B.calibrate);
		return Z() or q;
	end;
	return W or q;
end;
w.absSize = E;
local function U(m, p, q, a, c, Z, W, S, w)
	if m.bx ~= p or m.by ~= q then
		m.bx, m.by = p, q;
		m.box.Position = Vector2.new(p, q);
	end;
	if m.bw ~= a or m.bh ~= c then
		m.bw, m.bh = a, c;
		m.box.Size = Vector2.new(a, c);
	end;
	if m.col ~= w then
		m.col = w;
		local p = X[w] or X[1];
		m.box.Color = p;
		m.tag.Color = p;
	end;
	if m.tx ~= Z or m.ty ~= W then
		m.tx, m.ty = Z, W;
		m.tag.Position = Vector2.new(Z, W);
	end;
	if m.text ~= S then
		m.text = S;
		m.tag.Text = S;
	end;
	if not m.vis then
		m.vis = true;
		m.box.Visible = true;
		m.tag.Visible = true;
	end;
end;
function w.espDraw()
	local m = W.pool;
	if not m then
		return;
	end;
	local p = 0;
	for q = 1, #u, 1 do
		if p >= T then
			break;
		end;
		local a = u[q];
		if a.kind == "m" then
			local q = w.npcAlive(a.npc, .6) and a.root.Position;
			local c, Z, W, S;
			if q then
				c, Z = WorldToScreen(q + Vector3.new(0, 2.8, 0));
				W, S = WorldToScreen(q - Vector3.new(0, 3.2, 0));
			end;
			if Z and S then
				p = p + 1;
				local q = W.Y - c.Y;
				local Z = q * .5;
				U(m[p], c.X - Z * .5, c.Y, Z, q, c.X, c.Y - 15, a.text, a.col);
			end;
		else
			local q, c = WorldToScreen(a.pos);
			if c then
				p = p + 1;
				U(m[p], q.X - 7, q.Y - 7, 14, 14, q.X, q.Y - 17, a.text, a.col);
			end;
		end;
	end;
	for p = p + 1, T, 1 do
		local q = m[p];
		if q.vis then
			q.vis = false;
			q.box.Visible = false;
			q.tag.Visible = false;
		end;
	end;
end;
function w.espSync()
	if w.espOn() then
		if not W.drawConn then
			local m = 0;
			W.drawConn = q.RenderStepped:Connect(function()
					if not W.Running or not W.pool then
						return;
					end;
					local p = os.clock();
					if p - m < .0166 then
						return;
					end;
					m = p;
					w.mark("espdraw");
					local q, a = pcall(w.espDraw);
					if not q and p - ((W.drawErrT or 0)) > 5 then
						W.drawErrT = p;
						w.logErr("esp", a);
					end;
				end);
		end;
	else
		if W.drawConn then
			pcall(function()
				W.drawConn:Disconnect();
			end);
			W.drawConn = nil;
		end;
		w.espClear();
	end;
end;
local g = {
		One = 49,
		Two = 50,
		Three = 51,
		Four = 52,
		Five = 53,
		Six = 54,
		Seven = 55,
		Eight = 56,
		Nine = 57,
		Zero = 48,
	};
function w.onMenu()
	if not W.win then
		return false;
	end;
	local m, p = pcall(function()
			return W.win:IsVisible();
		end);
	if not m or not p then
		return false;
	end;
	return Z ~= nil and Z._overUI == true;
end;
function w.inputHeld()
	return W.lmbDown == true or W.rmbDown == true or W.dlgUp ~= nil or W.dlgClick ~= nil or (W.fish ~= nil and W.fish.lmb == true) or (W.bar ~= nil and W.bar.lmb == true);
end;
function w.trainBar()
	return W.AutoTrain == true and not w.remote;
end;
function w.fishWantsInput()
	local m = W.bar;
	if m and ((m.active or m.lmb)) then
		return true;
	end;
	local p = W.fish;
	if not W.AutoFish or not p then
		return false;
	end;
	return p.lmb == true or p.keyUp ~= nil or p.phase == "game" or os.clock() < ((p.inputUntil or 0));
end;
function w.wantsGameInput()
	return w.fishWantsInput() or W.dlg ~= nil or W.Mode ~= "off" or W.collect ~= nil or W.route ~= nil or W.AutoTrain == true;
end;
function w.fishInputReady(m)
	local p = W.fish;
	p.inputUntil = m + .6;
	return not ((Z and Z._inputSent == false));
end;
function w.waterAreas(m)
	local p = W.waterC;
	if p and m - p.t < 5 then
		return p.list;
	end;
	local q = {};
	local c = a:FindFirstChild("Map");
	c = c and c:FindFirstChild("Map");
	local Z = c and c:FindFirstChild("WatersFunctions");
	local function S(m, p)
		if p > 3 then
			return;
		end;
		for m, a in ipairs(m:GetChildren()) do
			if a.Name == "TouchPart" then
				local m, p;
				pcall(function()
					m = a.Position;
					p = a.Size;
				end);
				if m and (p and p.X > 4) then
					q[#q + 1] = {
							top = m.Y + p.Y * .5,
							x1 = m.X - p.X * .5,
							x2 = m.X + p.X * .5,
							z1 = m.Z - p.Z * .5,
							z2 = m.Z + p.Z * .5,
						};
				end;
			end;
			S(a, p + 1);
		end;
	end;
	if Z then
		S(Z, 0);
	end;
	W.waterC = { t = m, list = q };
	return q;
end;
function w.waterPoint(m, p)
	local q, a = w.char();
	if not a then
		return nil;
	end;
	local c = a.Position;
	local Z = w.waterAreas(m);
	local W;
	pcall(function()
		W = a.CFrame.LookVector;
	end);
	local S = {};
	if W then
		local m = math.sqrt(W.X * W.X + W.Z * W.Z);
		if m > .01 then
			S[1] = { W.X / m, W.Z / m };
		end;
	end;
	for m = 0, 7, 1 do
		local p = (m * math.pi) / 4;
		S[#S + 1] = { math.cos(p), math.sin(p) };
	end;
	local R = ((p or 0)) % #S;
	for m = 1, #S, 1 do
		local p = S[((((m - 1) + R)) % #S) + 1];
		for m, q in ipairs({ 10, 16, 22 }) do
			local a, W = c.X + p[1] * q, c.Z + p[2] * q;
			for m, p in ipairs(Z) do
				local q = c.Y - p.top;
				if a > p.x1 and (a < p.x2 and (W > p.z1 and (W < p.z2 and (q > -4 and q < 45)))) then
					return Vector3.new(a, p.top, W);
				end;
			end;
		end;
	end;
	local y = S[(R % #S) + 1];
	return Vector3.new(c.X + y[1] * 16, c.Y - 2, c.Z + y[2] * 16);
end;
function w.fishRod(m)
	local p = W.fishRodC;
	if m and (p and m - p.t < .5) then
		return p.v;
	end;
	local q = w.fishRodScan();
	if m then
		W.fishRodC = { t = m, v = q };
	end;
	return q;
end;
function w.fishRodScan()
	local m = c.Character;
	local p = m and m:FindFirstChild("Tool_Accessories");
	if not p then
		return nil;
	end;
	for m, p in ipairs(p:GetChildren()) do
		if string.find(p.Name, "Fishing Rod", 1, true) then
			return p;
		end;
	end;
	return nil;
end;
function w.fishRodKey()
	local m = nil;
	if not m then
		local q = p:FindFirstChild("Player_Service");
		local a = q and q:FindFirstChild("Data");
		m = a and a:FindFirstChild(c.Name);
		local Z = m and m:FindFirstChild("slotEquipped");
		local W = m and m:FindFirstChild("slots");
		m = W and W:FindFirstChild("Slot" .. tostring(math.floor(Z and Z.Value or 1)));
	end;
	local q = m and m:FindFirstChild("Inventory");
	local a = q and q:FindFirstChild("Toolbar");
	local Z = q and q:FindFirstChild("Inventory");
	if not a or not Z then
		return nil;
	end;
	local W = {};
	for m, p in ipairs(Z:GetChildren()) do
		if string.find(p.Name, "Fishing Rod", 1, true) then
			local m = p:FindFirstChild("Id");
			if m then
				W[m.Value] = true;
			end;
		end;
	end;
	for m, p in ipairs(a:GetChildren()) do
		if W[p.Value] and g[p.Name] then
			return g[p.Name];
		end;
	end;
	return nil;
end;
function w.fishGame(m)
	local p = c:FindFirstChild("PlayerGui");
	local q = p and p:FindFirstChild("Misc");
	if not q then
		return nil;
	end;
	local a = q:GetChildren();
	if #a == 0 then
		return nil;
	end;
	for m, p in ipairs(a) do
		for m, p in ipairs(p:GetChildren()) do
			local q = p:FindFirstChild("tracker");
			if q then
				local m = p:FindFirstChild("Bar");
				if m then
					return q, m, p;
				end;
			end;
		end;
	end;
	return nil;
end;
function w.barMouse(m)
	local p = W.bar;
	if m and w.onMenu() then
		m = false;
	end;
	if not p or p.lmb == m then
		return;
	end;
	p.lmb = m;
	pcall(function()
		if m then
			mouse1press();
		else
			mouse1release();
		end;
	end);
end;
function w.barStep(m, p)
	local q = W.bar;
	if not q then
		q = { lmb = false };
		W.bar = q;
	end;
	local a, c, S;
	if q.active or m - ((q.chk or 0)) >= .1 then
		q.chk = m;
		a, c, S = w.fishGame(m);
	end;
	if not a then
		if q.active then
			q.active = false;
			w.barMouse(false);
		end;
		return false;
	end;
	if not q.active then
		q.active = true;
		q.prevBy = nil;
		q.games = ((q.games or 0)) + 1;
	end;
	if not w.focused(m) then
		w.barMouse(false);
		return true;
	end;
	local R, y;
	pcall(function()
		R = a.AbsolutePosition.Y + (E(a)).Y * .5;
		y = c.AbsolutePosition.Y + (E(c)).Y * .5;
	end);
	if R and y then
		local m = 0;
		if q.prevBy and p > 0 then
			m = ((y - q.prevBy)) / p;
		end;
		q.prevBy = y;
		w.barMouse((y + m * W.FishLead) > R and not ((Z and Z._inputSent == false)));
	end;
	return true;
end;
function w.fishFind(m, p)
	local q = a:FindFirstChild("Debree");
	local c, Z = w.char();
	if not q or not Z then
		return nil;
	end;
	local W = #m;
	local S, R = nil, p;
	for p, q in ipairs(q:GetChildren()) do
		if string.sub(q.Name, 1, W) == m then
			local m = q;
			if q.ClassName ~= "Part" and q.ClassName ~= "MeshPart" then
				m = q:FindFirstChildWhichIsA("BasePart");
			end;
			local p, a = pcall(function()
					return m.Position;
				end);
			if p and a then
				local m = ((a - Z.Position)).Magnitude;
				if m < R then
					R = m;
					S = a;
				end;
			end;
		end;
	end;
	return S, R;
end;
function w.fishMouse(m)
	local p = W.fish;
	if m and w.onMenu() then
		m = false;
	end;
	if not p or p.lmb == m then
		return;
	end;
	p.lmb = m;
	pcall(function()
		if m then
			mouse1press();
		else
			mouse1release();
		end;
	end);
end;
function w.fishCast(m)
	if not w.remote then
		return false;
	end;
	pcall(function()
		w.remote:FireServer("Tool_Mouse", "Up", m);
	end);
	return true;
end;
function w.fishStep(m)
	local p = W.fish;
	if not p then
		return;
	end;
	if not W.AutoFish then
		return;
	end;
	local q = m - ((p.last or m));
	p.last = m;
	if w.barStep(m, q) then
		p.phase = "game";
		return;
	end;
	if p.phase == "game" then
		p.badCasts = 0;
		p.phase = "catch";
		p.t = m;
		p.tries = 0;
		p.bites = ((p.bites or 0)) + 1;
		p.holdAt = nil;
		return;
	end;
	if m - ((p.slow or 0)) < .1 then
		return;
	end;
	p.slow = m;
	local a, c = w.char();
	if not c then
		return;
	end;
	if not w.fishRod(m) then
		if m - ((p.eqT or 0)) > 3 then
			p.eqT = m;
			local q = w.fishRodKey();
			if q and (w.focused(m) and not w.fishInputReady(m)) then
				p.eqT = m - 2.8;
			elseif q and w.focused(m) then
				pcall(function()
					keypress(q);
				end);
				p.keyUp = { vk = q, t = m + .08 };
			elseif m - ((p.warn or 0)) > 10 then
				p.warn = m;
				w.notify("auto fish: put a fishing rod on the hotbar", 5);
			end;
		end;
		p.phase = "cast";
		p.t = m;
		return;
	end;
	if p.phase == "catch" then
		if p.keyUp then
			return;
		end;
		if m - ((p.catchChk or 0)) < .4 then
			return;
		end;
		p.catchChk = m;
		local q = w.fishFind("FishingCatch_", 14);
		if not q then
			if m - p.t > 2.5 or p.tries > 0 then
				if p.tries > 0 then
					p.caught = ((p.caught or 0)) + 1;
				end;
				p.phase = "cast";
				p.t = m;
			end;
			return;
		end;
		if m < ((p.holdAt or 0)) or not w.focused(m) then
			return;
		end;
		if p.tries >= 6 then
			w.notify("auto fish: could not pick up the catch", 4);
			p.phase = "cast";
			p.t = m;
			return;
		end;
		if not w.fishInputReady(m) then
			return;
		end;
		p.tries = p.tries + 1;
		local a = math.max(W.HoldT or 3, 3);
		pcall(function()
			keypress(84);
		end);
		p.keyUp = { vk = 84, t = m + a };
		p.holdAt = (m + a) + .4;
		return;
	end;
	if p.phase == "cast" then
		if m - p.t < W.FishPause then
			return;
		end;
		if w.fishFind("FishingCatch_", 12) then
			p.phase = "catch";
			p.t = m;
			p.tries = 0;
			p.holdAt = nil;
			return;
		end;
		local q = w.waterPoint(m, p.dir or 0);
		if not q then
			return;
		end;
		p.water = q;
		if w.fishCast(q) == false then
			return;
		end;
		p.phase = "wait";
		p.t = m;
		return;
	end;
	if p.phase == "wait" then
		if m - ((p.bobChk or 0)) < 1 then
			return;
		end;
		p.bobChk = m;
		local q = w.fishFind("FishingLine_", 120);
		if q then
			p.sawBob = true;
		end;
		if (not q and m - p.t > 4) or m - p.t > W.FishTimeout then
			if p.sawBob and (not q and m - p.t < 8) then
				p.badCasts = ((p.badCasts or 0)) + 1;
				p.dir = ((p.dir or 0)) + 1;
				if p.badCasts % 6 == 0 then
					w.notify("auto fish: casts keep coming back - move closer to open water", 6);
				end;
			else
				p.badCasts = 0;
			end;
			if q then
				w.fishCast(p.water);
			end;
			p.phase = "cast";
			p.t = m;
			p.sawBob = false;
		end;
		return;
	end;
	if p.phase ~= "idle" then
		p.phase = "cast";
		p.t = m;
	end;
end;
function w.fishSync()
	if W.AutoFish or w.trainBar() then
		if W.AutoFish and not W.fish then
			W.fish = { phase = "cast", t = os.clock(), lmb = false };
		end;
		if not W.fishConn then
			local m = 0;
			local p = os.clock();
			W.fishConn = q.RenderStepped:Connect(function()
					local q, a = pcall(function()
							if not W.Running or not ((W.AutoFish or w.trainBar())) then
								return;
							end;
							local q = os.clock();
							local a = W.fish;
							if a and (a.mouseUp and q >= a.mouseUp) then
								a.mouseUp = nil;
								w.fishMouse(false);
							end;
							if a and (a.keyUp and q >= a.keyUp.t) then
								local m = a.keyUp.vk;
								a.keyUp = nil;
								pcall(function()
									keyrelease(m);
								end);
							end;
							if q - m < .0166 then
								return;
							end;
							m = q;
							local c = q - p;
							p = q;
							local Z, S;
							if W.AutoFish then
								w.mark("fish/" .. tostring(W.fish and W.fish.phase or "?"));
								w.markWrite(q);
								Z, S = pcall(w.fishStep, q);
							else
								w.mark("barminigame");
								w.markWrite(q);
								Z, S = pcall(w.barStep, q, c);
							end;
							if not Z and q - ((W.fishErrT or 0)) > 5 then
								W.fishErrT = q;
								w.logErr("fish", S);
							end;
						end);
					if not q then
						w.logErr("fish loop", a);
					end;
				end);
		end;
	else
		if W.fishConn then
			pcall(function()
				W.fishConn:Disconnect();
			end);
			W.fishConn = nil;
		end;
		if W.fish then
			w.fishMouse(false);
			if W.fish.keyUp then
				pcall(function()
					keyrelease(W.fish.keyUp.vk);
				end);
				W.fish.keyUp = nil;
			end;
			W.fish.phase = "cast";
		end;
		if W.bar then
			w.barMouse(false);
			W.bar.active = false;
		end;
	end;
end;
pcall(function()
	Z:SetTheme("monochrome");
	Z:SetFont("Monospace", 13);
	Z:SetAccentBar(false);
end);
local Y = Z:CreateWindow({
		title = "slayers 2",
		subtitle = "matcha",
		id = "slayers2",
		size = { 560, 600 },
		columns = 2,
		toggleKey = "RSHIFT",
		folder = "UI/slayers2",
		config = "slayers2.json",
		inputGuard = function()
			return w.inputHeld();
		end,
		captureInput = function()
			return not w.wantsGameInput();
		end,
		onUnload = function()
			W.Running = false;
			W.Mode = "off";
			pcall(function()
				if W.F then
					W.F.aimRelease();
					W.F.skillsRelease();
				end;
			end);
			pcall(function()
				if W.drawConn then
					W.drawConn:Disconnect();
				end;
			end);
			pcall(function()
				if W.tickConn then
					W.tickConn:Disconnect();
				end;
			end);
			pcall(function()
				if W.fishConn then
					W.fishConn:Disconnect();
				end;
			end);
			pcall(function()
				if W.deferConn then
					W.deferConn:Disconnect();
				end;
			end);
			W.deferConn = nil;
			pcall(function()
				if W.fish and W.fish.lmb then
					mouse1release();
				end;
			end);
			pcall(function()
				if W.bar and W.bar.lmb then
					mouse1release();
				end;
			end);
			pcall(function()
				mouse1release();
			end);
			pcall(function()
				mouse2release();
			end);
			pcall(function()
				keyrelease(84);
			end);
			pcall(w.skillsRelease);
			W.soul, W.dlg, W.dlgClick, W.crow, W.collect, W.route = nil, nil, nil, nil, nil, nil;
			local m = W.pool;
			W.pool = nil;
			_G[I] = nil;
			if m then
				for p = 1, #m, 1 do
					pcall(function()
						m[p].box.Visible = false;
						m[p].box:Remove();
					end);
					pcall(function()
						m[p].tag.Visible = false;
						m[p].tag:Remove();
					end);
				end;
			end;
			pcall(function()
				delfile("slayers2_where.txt");
			end);
			_G.Slayers2 = nil;
		end,
	});
W.win = Y;
local H = Y:AddTab("farm");
local v = Y:AddTab("teleport");
local D = Y:AddTab("skills");
local k = Y:AddTab("esp");
local C = Y:AddTab("misc");
Y:AddSettingsTab("menu");
local P = w.guard(H:AddGroup("target", 1));
P:AddToggle({
	text = "autofarm",
	flag = "autofarm",
	default = false,
	save = false,
	callback = function(m)
		W.Mode = m and ((W.FarmKind or "boss")) or "off";
		W.crow = nil;
		W.cur = nil;
		w.dropCache();
		w.sync();
	end,
});
P:AddDropdown({
	text = "mode",
	flag = "farmkind",
	values = {
		"mob",
		"boss",
		"crow",
		"quest",
	},
	default = "boss",
	callback = function(m)
		W.FarmKind = m;
		if W.Mode ~= "off" then
			W.Mode = m;
			W.cur = nil;
			w.dropCache();
			w.sync();
		end;
	end,
});
P:AddDropdown({
	text = "mob",
	flag = "mob",
	values = S.Mobs,
	default = "Bandit",
	search = true,
	callback = function(m)
		W.SelectedMob = m;
	end,
});
P:AddDropdown({
	text = "boss",
	flag = "boss",
	values = S.Bosses,
	default = { "Akazo" },
	multi = true,
	search = true,
	callback = function(m)
		W.SelectedBoss = m;
		w.updateBossOrder(m);
		W.cur = nil;
		w.dropCache();
	end,
});
do
	local m, p, q = {}, {}, {};
	for a, c in pairs(S.Quests) do
		local Z, W = a, nil;
		if not c.Auto then
			local m, p = w.optLevel(c.QuestOption), w.optLevel(c.BossOption);
			if m and (p and m ~= p) then
				Z = a .. (" (Lv " .. (math.min(m, p) .. ("/" .. (math.max(m, p) .. ")"))));
			elseif m or p then
				Z = a .. (" (Lv " .. (((m or p)) .. ")"));
			end;
			W = math.min(m or 999, p or 999);
		end;
		m[#m + 1] = Z;
		p[Z] = a;
		q[Z] = c.Auto and -1 or W;
	end;
	table.sort(m, function(m, p)
		if q[m] ~= q[p] then
			return q[m] < q[p];
		end;
		return m < p;
	end);
	local a = {};
	for m, q in ipairs(m) do
		a[p[q]] = m;
	end;
	S.QuestByLabel = p;
	P:AddDropdown({
		text = "quest",
		flag = "quests",
		values = m,
		default = { m[1] },
		multi = true,
		search = true,
		callback = function(m)
			local q = {};
			local function c(m)
				local a = p[m] or (S.Quests[m] and m);
				if a then
					q[#q + 1] = a;
				end;
			end;
			if type(m) == "table" then
				for m, p in pairs(m) do
					if p == true then
						c(m);
					elseif type(p) == "string" then
						c(p);
					end;
				end;
			elseif type(m) == "string" then
				c(m);
			end;
			table.sort(q, function(m, p)
				return ((a[m] or 999)) < ((a[p] or 999));
			end);
			W.QuestSel = q;
			W.SelectedQuest = q[1] or "[Auto-Detect Active Quest]";
			W.acceptTries = 0;
			W.acceptAt = nil;
			W.acceptKey = nil;
		end,
	});
end;
P:AddDropdown({
	text = "quest type",
	flag = "questpick",
	values = { "normal", "boss" },
	default = "normal",
	callback = function(m)
		W.QuestPick = m;
		W.acceptTries = 0;
		W.acceptAt = nil;
	end,
});
P:AddSlider({
	text = "crow slot",
	flag = "crowslot",
	min = 1,
	max = 5,
	step = 1,
	default = 2,
	callback = function(m)
		W.CrowSlot = m;
	end,
});
P:AddSlider({
	text = "weapon slot",
	flag = "weapslot",
	min = 1,
	max = 5,
	step = 1,
	default = 1,
	callback = function(m)
		W.WeapSlot = m;
	end,
});
P:AddSlider({
	text = "quest gap",
	flag = "questgap",
	min = 10,
	max = 120,
	step = 5,
	default = 30,
	suffix = "s",
	callback = function(m)
		W.QuestGap = m;
	end,
});
local A = w.guard(H:AddGroup("fight", 2));
A:AddDropdown({
	text = "stance",
	flag = "stance",
	values = {
		"Under Target",
		"Behind",
		"On Top",
		"Above (Hover)",
	},
	default = "Under Target",
	callback = function(m)
		W.Stance = m;
	end,
});
A:AddSlider({
	text = "offset",
	flag = "offset",
	min = 1,
	max = 15,
	step = .1,
	default = 3.8,
	callback = function(m)
		W.Offset = m;
	end,
});
A:AddSlider({
	text = "attack delay",
	flag = "atkdelay",
	min = .05,
	max = 1,
	step = .05,
	default = .3,
	suffix = "s",
	callback = function(m)
		W.AttackDelay = m;
	end,
});
A:AddSlider({
	text = "swing within",
	flag = "reach",
	min = 5,
	max = 40,
	default = 18,
	suffix = "studs",
	callback = function(m)
		W.Reach = m;
	end,
});
A:AddToggle({
	text = "hold when blocking",
	flag = "skipblock",
	default = true,
	callback = function(m)
		W.SkipBlocking = m;
	end,
});
A:AddToggle({
	text = "aimlock",
	flag = "aimlock",
	default = true,
	callback = function(m)
		W.AimLock = m;
		if not m then
			w.aimRelease();
		end;
	end,
});
A:AddSlider({
	text = "aim gain",
	flag = "aimgain",
	min = .1,
	max = 1,
	step = .05,
	default = .55,
	callback = function(m)
		W.AimGain = m;
	end,
});
A:AddToggle({
	text = "smooth movement",
	flag = "smooth",
	default = true,
	callback = function(m)
		W.Smooth = m;
	end,
});
A:AddSlider({
	text = "move speed",
	flag = "movespeed",
	min = 10,
	max = 150,
	default = 60,
	suffix = "s/s",
	callback = function(m)
		W.MoveSpeed = m;
	end,
});
A:AddSlider({
	text = "warp above",
	flag = "warpabove",
	min = 10,
	max = 200,
	default = 40,
	suffix = "studs",
	callback = function(m)
		W.WarpAbove = m;
	end,
});
A:AddToggle({
	text = "health guard",
	flag = "hguard",
	default = false,
	callback = function(m)
		W.HealthGuard = m;
	end,
});
A:AddSlider({
	text = "guard at",
	flag = "hpct",
	min = .1,
	max = .9,
	step = .05,
	default = .45,
	callback = function(m)
		W.GuardPct = m;
	end,
});
local h = {};
for m in pairs(S.Zones) do
	h[#h + 1] = m;
end;
table.sort(h);
local V = {};
for m in pairs(S.BossTP) do
	V[#V + 1] = m;
end;
table.sort(V);
local n = {};
for m in pairs(S.Trainers) do
	n[#n + 1] = m;
end;
table.sort(n);
local J = w.guard(v:AddGroup("regions", 1));
J:AddButton({ text = "black market", callback = function()
		w.tpBlackMarketer();
	end });
J:AddButton({ text = "find muzan", callback = function()
		w.findMuzan(true);
	end });
local i = h[1];
J:AddDropdown({
	text = "region",
	flag = "zone",
	values = h,
	default = i,
	search = true,
	callback = function(m)
		i = m;
	end,
});
J:AddButton({ text = "travel", callback = function()
		w.tpTo(S.Zones[i], 3);
	end });
local t = V[1];
J:AddDropdown({
	text = "boss",
	flag = "bosstp",
	values = V,
	default = t,
	search = true,
	callback = function(m)
		t = m;
	end,
});
J:AddButton({ text = "travel to boss", callback = function()
		w.tpNamed(t, S.BossTP[t]);
	end });
local b = w.guard(v:AddGroup("npcs", 2));
local f = n[1];
b:AddDropdown({
	text = "trainer",
	flag = "trainer",
	values = n,
	default = f,
	search = true,
	callback = function(m)
		f = m;
	end,
});
b:AddButton({ text = "travel to trainer", callback = function()
		w.tpNamed(f, S.Trainers[f]);
	end });
local j = S.Stations[1];
b:AddDropdown({
	text = "station",
	flag = "station",
	values = S.Stations,
	default = j,
	callback = function(m)
		j = m;
	end,
});
b:AddButton({ text = "travel to station", callback = function()
		local m = a:FindFirstChild("Training");
		local p = m and m:FindFirstChild(j);
		local q = p and p:FindFirstChildWhichIsA("BasePart", true);
		if q then
			w.tpTo(q.Position, 3);
			w.notify("at " .. j, 2);
		else
			w.notify(j .. " not loaded", 2);
		end;
	end });
b:AddButton({ text = "nearest chest", callback = function()
		local m, p = w.char();
		if not p then
			return;
		end;
		local q = w.chests();
		local a, c = nil, 1000000000.0;
		for m = 1, #q, 1 do
			local Z = ((q[m].Pos - p.Position)).Magnitude;
			if Z < c then
				c = Z;
				a = q[m];
			end;
		end;
		if a then
			w.tpTo(a.Pos, 3);
			w.notify(a.Name, 2);
		else
			w.notify("no chests up", 2);
		end;
	end });
S.SkillKeys = {
		{ "z", 90 },
		{ "x", 88 },
		{ "c", 67 },
		{ "v", 86 },
		{ "b", 66 },
		{ "k", 75 },
		{ "n", 78 },
	};
do
	local m = w.guard(D:AddGroup("timing", 1));
	m:AddSlider({
		text = "gap between presses",
		flag = "sk_gap",
		min = .05,
		max = 5,
		step = .05,
		default = .3,
		suffix = "s",
		callback = function(m)
			W.SkillGap = m;
		end,
	});
end;
W.Skills = {};
for m = 1, #S.SkillKeys, 1 do
	local p, q = S.SkillKeys[m][1], S.SkillKeys[m][2];
	local a = w.guard(D:AddGroup(p, (m <= 3) and 1 or 2));
	local c = {
			on = false,
			breaker = false,
			hold = .08,
			dist = 15,
			vk = q,
			rel = 0,
			next = 0,
			name = p,
		};
	W.Skills[m] = c;
	a:AddToggle({
		text = "enabled",
		flag = "sk_" .. (p .. "_on"),
		default = false,
		callback = function(m)
			c.on = m;
		end,
	});
	a:AddToggle({
		text = "block breaker",
		flag = "sk_" .. (p .. "_breaker"),
		default = false,
		callback = function(m)
			c.breaker = m;
		end,
	});
	a:AddSlider({
		text = "hold",
		flag = "sk_" .. (p .. "_hold"),
		min = .02,
		max = 3,
		step = .02,
		default = .08,
		suffix = "s",
		callback = function(m)
			c.hold = m;
		end,
	});
	a:AddSlider({
		text = "range",
		flag = "sk_" .. (p .. "_dist"),
		min = 1,
		max = 120,
		default = 15,
		suffix = "studs",
		callback = function(m)
			c.dist = m;
		end,
	});
end;
local mY = w.guard(k:AddGroup("targets", 1));
mY:AddToggle({
	text = "mobs",
	flag = "espmob",
	default = false,
	callback = function(m)
		W.EspMobs = m;
		w.sync();
	end,
});
mY:AddToggle({
	text = "bosses",
	flag = "espboss",
	default = false,
	callback = function(m)
		W.EspBosses = m;
		w.sync();
	end,
});
mY:AddToggle({
	text = "chests",
	flag = "espchest",
	default = false,
	callback = function(m)
		W.EspChests = m;
		w.sync();
	end,
});
local pY = w.guard(k:AddGroup("world", 2));
pY:AddToggle({
	text = "spawn crystals",
	flag = "espcry",
	default = false,
	callback = function(m)
		W.EspCrystals = m;
		w.sync();
	end,
});
pY:AddToggle({
	text = "spider lily",
	flag = "esplily",
	default = false,
	callback = function(m)
		W.EspLily = m;
		w.sync();
	end,
});
pY:AddToggle({
	text = "horses",
	flag = "esphorse",
	default = false,
	callback = function(m)
		W.EspHorses = m;
		w.sync();
	end,
});
pY:AddToggle({
	text = "levers",
	flag = "esplever",
	default = false,
	callback = function(m)
		W.EspLevers = m;
		w.sync();
	end,
});
pY:AddToggle({
	text = "muzan",
	flag = "espmuzan",
	default = false,
	callback = function(m)
		W.EspMuzan = m;
		w.sync();
	end,
});
pY:AddToggle({
	text = "fruits",
	flag = "espfruit",
	default = false,
	callback = function(m)
		W.EspFruits = m;
		w.sync();
	end,
});
pY:AddSlider({
	text = "distance",
	flag = "espdist",
	min = 100,
	max = 3000,
	default = 1200,
	suffix = "m",
	callback = function(m)
		W.EspDist = m;
	end,
});
local qY = w.guard(C:AddGroup("automation", 1));
qY:AddToggle({
	text = "auto pick up loot",
	flag = "autoloot",
	default = true,
	callback = function(m)
		W.AutoLoot = m;
	end,
});
qY:AddToggle({
	text = "collect boss drops",
	flag = "lootboss",
	default = true,
	callback = function(m)
		W.LootBoss = m;
	end,
});
qY:AddToggle({
	text = "claim souls",
	flag = "autosouls",
	default = true,
	callback = function(m)
		W.AutoSouls = m;
		w.sync();
	end,
});
qY:AddSlider({
	text = "settle before press",
	flag = "settlet",
	min = 0,
	max = 2,
	step = .1,
	default = .6,
	suffix = "s",
	callback = function(m)
		W.SettleT = m;
	end,
});
qY:AddSlider({
	text = "wait for drops",
	flag = "lootwait",
	min = 1,
	max = 20,
	step = .5,
	default = 5,
	suffix = "s",
	callback = function(m)
		W.LootWait = m;
	end,
});
qY:AddSlider({
	text = "hold T for",
	flag = "holdt",
	min = .5,
	max = 4,
	step = .1,
	default = 2.0,
	suffix = "s",
	callback = function(m)
		W.HoldT = m;
	end,
});
qY:AddToggle({
	text = "auto train",
	flag = "autotrain",
	default = false,
	callback = function(m)
		W.AutoTrain = m;
		w.sync();
	end,
});
qY:AddToggle({
	text = "noclip",
	flag = "noclip",
	default = false,
	callback = function(m)
		W.NoClip = m;
		w.sync();
	end,
});
qY:AddToggle({
	text = "anti afk",
	flag = "antiafk",
	default = false,
	callback = function(m)
		W.AntiAFK = m;
		w.sync();
	end,
});
local aY = w.guard(C:AddGroup("panic", 2));
aY:AddKeybind({
	text = "panic stop",
	flag = "panickey",
	default = "F2",
	mode = "always",
	onClick = function()
		w.panic();
	end,
	changed = function(m)
 
	end,
});
aY:AddButton({ text = "stop everything", callback = function()
		w.panic();
	end });
aY:AddButton({ text = "unload", confirm = true, callback = function()
		Y:Unload();
	end });
local cY = w.guard(C:AddGroup("fishing", 2));
cY:AddToggle({
	text = "auto fish",
	flag = "autofish",
	default = false,
	callback = function(m)
		W.AutoFish = m;
		w.fishSync();
	end,
});
cY:AddSlider({
	text = "bar lead",
	flag = "fishlead",
	min = 0,
	max = .4,
	step = .01,
	default = .12,
	suffix = "s",
	callback = function(m)
		W.FishLead = m;
	end,
});
cY:AddSlider({
	text = "no bite recast",
	flag = "fishtimeout",
	min = 10,
	max = 60,
	step = 1,
	default = 30,
	suffix = "s",
	callback = function(m)
		W.FishTimeout = m;
	end,
});
function w.panic()
	W.Mode = "off";
	W.AutoTrain = false;
	W.AutoFish = false;
	pcall(function()
		Y:SetValue("autofarm", false);
		Y:SetValue("autotrain", false);
		Y:SetValue("autofish", false);
	end);
	pcall(w.fishSync);
	pcall(w.skillsRelease);
	pcall(function()
		keyrelease(84);
	end);
	W.cur = nil;
	w.aimRelease();
	pcall(w.skillsRelease);
	W.pendingLoot = nil;
	W.lootPhase = nil;
	W.follow = nil;
	W.dlg = nil;
	W.soul = nil;
	W.crow = nil;
	w.routeStop();
	w.collectStop();
	w.dropCache();
	w.sync();
	w.notify("stopped", 3);
end;
local ZY = {
		farm = 0,
		esp = 0,
		chest = 0,
		train = 0,
		clip = 0,
		afk = 0,
		zx = 0,
		guard = 0,
		key = 0,
		soul = 0,
	};
local function WY(m)
	if W.Mode == "off" then
		return;
	end;
	local p, q, c = w.char();
	if not p or c.Health <= 0 then
		return;
	end;
	if W.HealthGuard and c.Health < c.MaxHealth * W.GuardPct then
		if m - ZY.guard > 1 then
			ZY.guard = m;
			pcall(function()
				q.CFrame = q.CFrame + Vector3.new(0, 60, 0);
				q.AssemblyLinearVelocity = Vector3.new(0, 0, 0);
			end);
		end;
		return;
	end;
	local Z, R;
	if W.Mode == "mob" then
		Z, R = W.SelectedMob, false;
	elseif W.Mode == "crow" then
		local p = w.crowStep(m);
		if not p then
			if W.cur then
				W.cur = nil;
				w.aimRelease();
			end;
			return;
		end;
		Z, R = p, true;
	elseif W.Mode == "boss" then
		local m = w.bossSet();
		Z, R = (m or "All Bosses"), true;
	else
		local p = w.activeQuest();
		if not p then
			if W.questInfoKey then
				W.lastDoneKey = W.questInfoKey;
				W.questInfoKey = nil;
			end;
			local p, q = w.pickQuest(m);
			if not p then
				local p, q = w.pickQuest(m);
				if W.cur then
					W.cur = nil;
					w.aimRelease();
				end;
				if m - ((ZY.qwarn or 0)) > 12 then
					ZY.qwarn = m;
					if q then
						w.notify("ticked quests cooling down - retry in " .. (math.ceil(q - m) .. "s"), 5);
					else
						w.notify("no quest to take - tick some in the quest list (or they are all done)", 6);
					end;
				end;
				return;
			end;
			if W.acceptKey ~= q then
				W.acceptKey = q;
				W.acceptTries = 0;
				W.acceptAt = nil;
				W.atGiver = false;
			end;
			if p and p.AutoDeliver then
				local q, a = w.pickDelivery(m);
				if not q then
					if W.cur then
						W.cur = nil;
						w.aimRelease();
					end;
					if a then
						for p, q in ipairs(S.DeliverGivers) do
							if not w.questCompleted(q.QuestOption) then
								w.followNpc(q.Npc, q.Pos, m);
								break;
							end;
						end;
					end;
					if m - ((ZY.qwarn or 0)) > 15 then
						ZY.qwarn = m;
						if a then
							w.notify("delivery quests cooling down - retry in " .. (math.ceil(a - m) .. "s"), 5);
						else
							w.notify("every delivery quest is done", 6);
						end;
					end;
					return;
				end;
				if W.delivGiver ~= q.QuestOption then
					W.delivGiver = q.QuestOption;
					W.acceptTries = 0;
					W.acceptAt = nil;
					W.atGiver = false;
				end;
				if not ((W.delivSent and W.delivSent[q.QuestOption])) then
					W.acceptTries = 0;
				end;
				p = q;
			end;
			if not ((p and p.Pos)) then
				if m - ((ZY.qwarn or 0)) > 8 then
					ZY.qwarn = m;
					w.notify("no active quest - pick one in the quest list to accept it", 5);
				end;
				return;
			end;
			if W.cur then
				W.cur = nil;
				w.aimRelease();
				pcall(w.skillsRelease);
				if w.clickRelease then
					pcall(w.clickRelease);
				end;
			end;
			local a = w.followNpc(p.Npc, p.Pos, m);
			local c = W.atGiver and 12 or 8;
			if a > c then
				W.atGiver = false;
				W.acceptAt = nil;
				return;
			end;
			W.atGiver = true;
			local Z = w.questOpt(p);
			if not Z then
				if m - ((ZY.qwarn or 0)) > 8 then
					ZY.qwarn = m;
					w.notify("at " .. (tostring(p.Npc) .. (": this quest has no " .. (W.QuestPick .. " option"))), 5);
				end;
				return;
			end;
			if w.questCompleted(Z) then
				if m - ((ZY.qwarn or 0)) > 10 then
					ZY.qwarn = m;
					w.notify("already completed: " .. (Z .. " - pick another quest"), 6);
				end;
				return;
			end;
			W.acceptAt = W.acceptAt or (m + W.SettleT);
			if m < W.acceptAt then
				return;
			end;
			W.hadQuest = false;
			local R = W.lastAcceptAt and (m - W.lastAcceptAt);
			if not R and p.QuestOption then
				local m = w.questLastAge();
				if m then
					R = m;
				end;
			end;
			if R and R < W.QuestGap then
				if m - ((ZY.qwarn or 0)) > 10 then
					ZY.qwarn = m;
					w.notify("next quest in " .. (math.ceil(W.QuestGap - R) .. "s"), 3);
				end;
				return;
			end;
			if not w.remote then
				local q = w.dlgTalk(Z, m);
				if q == "failed" and m - ((ZY.qwarn or 0)) > 12 then
					ZY.qwarn = m;
					w.notify("could not take \'" .. (Z .. ("\' from " .. (tostring(p.Npc) .. " - do it by hand"))), 6);
				end;
				return;
			end;
			if w.remote and (m - ((ZY.accept or 0))) > 8 then
				ZY.accept = m;
				W.acceptTries = ((W.acceptTries or 0)) + 1;
				if W.acceptTries > 3 then
					if m - ((ZY.qwarn or 0)) > 10 then
						ZY.qwarn = m;
						w.notify("server keeps refusing \'" .. (Z .. "\' (level or quest limit?) - skipping it"), 6);
					end;
					if q then
						W.qBack = W.qBack or {};
						W.qBack[q] = m + 120;
					end;
					W.acceptTries = 0;
					return;
				end;
				pcall(function()
					w.remote:FireServer("AddQuest", Z);
				end);
				if q and not p.AutoDeliver then
					W.qSent = W.qSent or {};
					W.qSent[q] = W.qSent[q] or m;
				end;
				if p.QuestOption == Z and S.DeliverGivers then
					W.delivSent = W.delivSent or {};
					W.delivSent[Z] = W.delivSent[Z] or m;
				end;
				w.notify("accepting: " .. Z, 3);
			end;
			return;
		end;
		W.acceptAt = nil;
		W.acceptTries = 0;
		W.atGiver = false;
		W.dlg = nil;
		if not W.hadQuest then
			W.lastAcceptAt = m;
		end;
		W.hadQuest = true;
		if p.Desc and W.delivSent then
			W.delivSent[p.Desc] = nil;
			if W.delivFails then
				W.delivFails[p.Desc] = nil;
			end;
		end;
		local c, y = w.entryForQuest(p);
		W.questInfo = c;
		W.questDesc = p.Desc;
		W.questInfoKey = y;
		if y and W.qSent then
			W.qSent[y] = nil;
		end;
		if y and W.qBack then
			W.qBack[y] = nil;
		end;
		if p.Complete then
			if W.cur then
				W.cur = nil;
				w.aimRelease();
				pcall(w.skillsRelease);
				if w.clickRelease then
					pcall(w.clickRelease);
				end;
			end;
			local p = c;
			if p and p.Pos then
				w.followNpc(p.Npc, p.Pos, m);
			end;
			return;
		end;
		local N = c;
		local O;
		for m = 1, #p.Tasks, 1 do
			if ((p.Tasks[m].Value or 0)) < ((p.Tasks[m].Max or 1)) then
				O = p.Tasks[m];
				break;
			end;
		end;
		local Q = S.Deliver[p.Name];
		local s = Q and (O and Q.tasks[O.Name]);
		if s then
			w.deliverStep(Q.opt, O.Name, s, q, m);
			return;
		end;
		W.deliverKey = nil;
		local F = w.taskKind(O and O.Name);
		Z = nil;
		if F ~= "fetch" then
			local m = {};
			local function q(p)
				if type(p) == "string" and p ~= "" then
					m[string.lower(p)] = true;
				end;
			end;
			q(w.codeToMob(O and O.Code));
			q(w.questTarget(p));
			if F == "kill" and (N and not N.Auto) then
				local m = p.Desc and N.BossOption == p.Desc;
				if m == nil then
					m = (W.QuestPick == "boss");
				end;
				q(m and ((N.BossTarget or N.Target)) or N.Target);
			end;
			local a = 0;
			for m in pairs(m) do
				a = a + 1;
			end;
			if a > 1 then
				Z = m;
			elseif a == 1 then
				for m in pairs(m) do
					Z = m;
				end;
			end;
		end;
		if not Z then
			local m = w.questPickups();
			if #m > 0 then
				local p = {};
				for q = 1, #m, 1 do
					p[#p + 1] = w.pickup(a, function()
							return a;
						end, m[q].Node, m[q].Root, nil, { tap = true });
				end;
				w.collectStart(p, "quest pickup: " .. m[1].Name);
				return;
			end;
			if F == "kill" and (N and (not N.Auto and N.Target)) then
				Z = N.Target;
			end;
		end;
		if not Z and F == "fetch" then
			if m - ((ZY.qwarn or 0)) > 10 then
				ZY.qwarn = m;
				w.notify("\'" .. (tostring(O and O.Name) .. "\' is not a kill objective and no prop is spawned - nothing to farm"), 6);
			end;
			return;
		end;
		R = false;
		if not Z then
			if m - ((ZY.qwarn or 0)) > 8 then
				ZY.qwarn = m;
				w.notify("nothing to farm or pick up in: " .. tostring(p.Name), 5);
			end;
			return;
		end;
	end;
	local y = (type(Z) == "table") and w.setKey(Z) or tostring(Z);
	local N = W.cur;
	if N and N.wantKey == y then
		local p = nil;
		local q = w.npcAlive(N) and pcall(function()
				p = N.Hum.Health;
			end);
		if not N.stale and (((not q or not p or p <= 0)) and (W.LootBoss and (N.Boss and N.lastPos))) then
			W.pendingLoot = { pos = N.lastPos, t = m };
		end;
		if p and p > 0 then
			if N.lastHp == nil or p < N.lastHp then
				N.lastHp = p;
				N.t = m;
			end;
			pcall(function()
				N.lastPos = N.Root.Position;
			end);
			if (m - N.t) < 8.0 then
				w.attack(m);
				return;
			end;
			w.notify("target stuck, re-picking", 2);
		end;
		W.cur = nil;
		w.dropCache();
	end;
	local O = w.scanCached(Z, R, m, .5, W.Mode == "boss" or W.Mode == "crow");
	if #O == 0 and W.Mode == "quest" then
		local m = W.questInfo;
		local p = m and (m.BossOption and W.questDesc == m.BossOption);
		if m and (m.Alt and not p) then
			O = w.scan(m.Alt, false);
		end;
	end;
	if #O == 0 then
		if m - ZY.guard > W.Retarget * 3 then
			ZY.guard = m;
			local p = w.wantSpawn(Z);
			if p then
				w.tpTo(p, 3);
			elseif m - ((ZY.nodest or 0)) > 12 then
				ZY.nodest = m;
				local p = (type(Z) == "table") and w.bossLabel() or tostring(Z);
				w.notify("no known spot for " .. (p .. " - find it once and it is remembered"), 6);
			end;
		end;
		return;
	end;
	local Q = w.nearest(O, q.Position);
	if not Q then
		w.dropCache();
		return;
	end;
	Q.want = Z;
	Q.wantKey = y;
	Q.t = m;
	if Q.Head == nil then
		Q.MAddr = w.addr(Q.Model);
		pcall(function()
			Q.Head = Q.Model:FindFirstChild("Head") or false;
		end);
	end;
	W.cur = Q;
	w.attack(m);
end;
local function SY()
	if not W.Running then
		return;
	end;
	local m = os.clock();
	w.mark("tick");
	w.markWrite(m);
	if W.AutoSouls and (w.farming() and not W.route) then
		w.mark("souls");
		local p, q = pcall(w.soulStep, m);
		if p and q then
			return;
		end;
	end;
	if W.route then
		w.mark("route");
		local p, q = pcall(w.routeStep, m);
		if p and q then
			return;
		end;
		if not p then
			w.routeStop();
		end;
	end;
	if W.collect then
		w.mark("collect");
		if pcall(w.collectStep, m) then
			return;
		end;
	end;
	if ((W.AutoLoot or W.AutoSouls)) and (w.farming() and (not W.collect and (not W.lootPhase and (not W.route and (m - ((ZY.loot or 0))) > 2.0)))) then
		ZY.loot = m;
		local p, q = w.char();
		if q then
			if W.AutoLoot then
				local m = w.lootNear(q.Position, W.LootRange);
				if #m > 0 then
					w.collectStart(m, "loot");
				end;
			end;
		end;
	end;
	if W.pendingLoot and (m - W.pendingLoot.t) > 1.0 then
		W.lootPhase = { pos = W.pendingLoot.pos, t0 = m, last = m };
		W.pendingLoot = nil;
		w.notify("boss down, collecting drops", 3);
	end;
	if W.lootPhase then
		w.mark("bossloot");
		local p = W.lootPhase;
		if m - ((p.scanT or 0)) < .25 then
			w.travelTo(p.pos, m);
			return;
		end;
		p.scanT = m;
		local q = w.lootNear(p.pos, 90);
		local a, c = {}, {};
		for m = 1, #q, 1 do
			if q[m].tap then
				a[#a + 1] = q[m];
			else
				c[#c + 1] = q[m];
			end;
		end;
		if #a > 0 or #c > 0 then
			p.sawChest = true;
		end;
		if #a > 0 then
			p.last = m;
			w.collectStart(a, "boss chest");
			return;
		end;
		if #c > 0 and ((p.passes or 0)) < 4 then
			p.last = m;
			p.passes = ((p.passes or 0)) + 1;
			if w.routeStart(c) then
				return;
			end;
		end;
		local Z = (m - p.last) > W.LootWait;
		if not p.sawChest then
			Z = (m - p.t0) > 25;
		end;
		if (m - p.t0) > 60 then
			Z = true;
		end;
		if Z then
			if not p.sawChest then
				w.notify("no boss chest appeared, moving on", 4);
			end;
			W.lootPhase = nil;
			w.notify("drops done, next boss", 2);
		else
			w.travelTo(p.pos, m);
			return;
		end;
	end;
	if m - ZY.key >= .1 then
		ZY.key = m;
		if iskeypressed and iskeypressed(W.PanicVK) then
			if W.Mode ~= "off" or W.AutoTrain or W.collect then
				w.panic();
			end;
		end;
	end;
	pcall(w.dlgRelease, m);
	if W.follow and not W.cur then
		pcall(w.followStep, m);
	end;
	if W.Mode ~= "off" and (W.cur and w.npcAlive(W.cur)) then
		pcall(w.steer);
		local p;
		pcall(function()
			local m = W.cur;
			local q = m.Head;
			if q and not w.attached(q, m.MAddr) then
				q = nil;
				m.Head = false;
			end;
			p = (q and q.Position) or m.Root.Position;
		end);
		pcall(w.aimStep, p);
		local q, a = w.char();
		if a and p then
			local q = ((p - a.Position)).Magnitude;
			pcall(w.skillStep, m, q);
		end;
	else
		w.aimRelease();
		pcall(w.skillsRelease);
	end;
	if m - ZY.farm >= W.AttackDelay then
		ZY.farm = m;
		w.mark("farm/" .. tostring(W.Mode));
		pcall(WY, m);
	end;
	if w.espOn() and m - ZY.esp >= .35 then
		ZY.esp = m;
		w.mark("espbuild");
		pcall(w.espBuild);
	end;
	if W.AutoTrain and (not W.AutoFish and (not W.collect and m - ZY.train >= 1.5)) then
		ZY.train = m;
		pcall(function()
			local q = p:FindFirstChild("Player_Service");
			local a = q and q:FindFirstChild("Values");
			local Z = a and a:FindFirstChild(c.Name);
			if not Z then
				return;
			end;
			if Z:FindFirstChild("Training") then
				if w.remote then
					w.remote:FireServer("training_signaler", "Stop", true);
				else
					W.trainSince = W.trainSince or m;
					local p = W.bar;
					if not ((p and p.active)) and (m - W.trainSince > 5 and m - ((W.trainWarn or 0)) > 30) then
						W.trainWarn = m;
						w.notify("click mode only plays the Squat bar - finish this station yourself", 6);
					end;
				end;
			else
				W.trainSince = nil;
			end;
		end);
	end;
	if W.NoClip and m - ZY.clip >= .25 then
		ZY.clip = m;
		pcall(function()
			local m = c.Character;
			if not m then
				return;
			end;
			local p = m:GetChildren();
			for m = 1, #p, 1 do
				local q = p[m];
				if ((q.ClassName == "Part" or q.ClassName == "MeshPart")) and q.Name ~= "HumanoidRootPart" then
					q.CanCollide = false;
				end;
			end;
		end);
	end;
	if W.Mode ~= "off" and (m - ((ZY.survey or 0))) > 10 then
		ZY.survey = m;
		pcall(function()
			w.scan(nil, nil);
		end);
	end;
	pcall(w.seenSave, m);
	if W.AntiAFK and m - ZY.afk >= 120 then
		ZY.afk = m;
		pcall(function()
			mousemoverel(2, 0);
			mousemoverel(-2, 0);
		end);
	end;
end;
function w.farming()
	return W.Mode ~= "off" or W.lootPhase ~= nil or W.pendingLoot ~= nil;
end;
function w.busy()
	return W.Mode ~= "off" or W.collect ~= nil or W.route ~= nil or W.lootPhase ~= nil or W.pendingLoot ~= nil or W.AutoTrain or W.NoClip or W.AntiAFK or w.espOn();
end;
function w.sync()
	w.espSync();
	pcall(w.fishSync);
	pcall(w.syncSkills);
	if w.busy() then
		if not W.tickConn then
			W.tickConn = q.Heartbeat:Connect(function()
					local m, p = pcall(SY);
					if not m then
						w.logErr("tick", p);
					end;
				end);
		end;
	elseif W.tickConn then
		pcall(function()
			W.tickConn:Disconnect();
		end);
		W.tickConn = nil;
		w.aimRelease();
		pcall(w.skillsRelease);
		W.cur = nil;
		w.dropCache();
	end;
end;
pcall(w.markPrev);
pcall(w.seenLoad);
pcall(function()
	Y:LoadSettings();
end);
w.sync();
w.notify("loaded - rshift for the menu", 4);
print("[Slayers 2] loaded");
