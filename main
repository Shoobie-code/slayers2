local D = game:GetService("Players") or game.Players;
local Q = game:GetService("ReplicatedStorage") or game.ReplicatedStorage;
local f = game:GetService("RunService") or game.RunService;
local u = workspace;
local h = D.LocalPlayer;
if _G.Slayers2 then
	local D = _G.Slayers2;
	_G.Slayers2 = nil;
	D.Running = false;
	pcall(function()
		if D.drawConn then
			D.drawConn:Disconnect();
		end;
	end);
	pcall(function()
		if D.tickConn then
			D.tickConn:Disconnect();
		end;
	end);
	pcall(function()
		if D.fishConn then
			D.fishConn:Disconnect();
		end;
	end);
	pcall(function()
		if D.bmConn then
			D.bmConn:Disconnect();
		end;
	end);
	pcall(function()
		if D.deferConn then
			D.deferConn:Disconnect();
		end;
	end);
	D.fishConn, D.tickConn, D.drawConn, D.bmConn = nil, nil, nil, nil;
	D.deferConn = nil;
	D.fish, D.bar, D.route, D.collect, D.follow = nil, nil, nil, nil, nil;
	D.soul, D.dlg, D.dlgClick, D.crow, D.lootPhase = nil, nil, nil, nil, nil;
	pcall(function()
		mouse1release();
	end);
	pcall(function()
		mouse2release();
	end);
	pcall(function()
		keyrelease(84);
	end);
	D.pool = nil;
	if true then
 
	end;
	pcall(function()
		if D.win then
			D.win:Destroy();
		end;
	end);
end;
local P;
do
	local D = rawget(_G, "UILib");
	if type(D) == "table" then
		local Q = tonumber(rawget(D, "_now"));
		if Q and (tick() - Q) > 3 then
			_G.UI_RELOAD = true;
		end;
	end;
	local Q;
	pcall(function()
		Q = readfile("UILib.lua");
	end);
	if type(Q) ~= "string" or #Q < 1000 then
		Q = nil;
		pcall(function()
			Q = game:HttpGet("https://raw.githubusercontent.com/Shoobie-code/linoria-inspired/refs/heads/main/main");
		end);
		if type(Q) == "string" and #Q > 1000 then
			pcall(writefile, "UILib.lua", Q);
		else
			Q = nil;
		end;
	end;
	if type(Q) == "string" then
		pcall(function()
			(loadstring(Q))();
		end);
	end;
	_G.UI_RELOAD = nil;
	P = _G.UILib;
end;
if not P then
	notify("UILib not found in workspace", "Slayers 2", 6);
	return;
end;
local m = {
		Running = true,
		pool = {},
		drawConn = nil,
		tickConn = nil,
		win = nil,
		Mode = "off",
		FarmKind = "boss",
		CrowSlot = 2,
		crow = nil,
		SelectedMob = "Bandit",
		SelectedBoss = { Akazo = true },
		SelectedQuest = "[Auto-Detect Active Quest]",
		QuestSel = { "[Auto-Detect Active Quest]" },
		Stance = "Under Target",
		Offset = 3.8,
		AttackDelay = .3,
		Retarget = 1.0,
		HealthGuard = false,
		GuardPct = .45,
		Smooth = true,
		MoveSpeed = 60,
		WarpAbove = 40,
		camT = 0,
		AimLock = true,
		SkillGap = .3,
		atkCount = 0,
		SkipBlocking = true,
		Reach = 18,
		blockedT = 0,
		AimGain = .55,
		rmbDown = false,
		HoldT = 2.0,
		SettleT = .6,
		AutoLoot = true,
		AutoSouls = true,
		LootRange = 300,
		LootWait = 5,
		lootPhase = nil,
		LootBoss = true,
		RouteTap = .05,
		collect = nil,
		pendingLoot = nil,
		seen = {},
		seenDirty = false,
		seenT = 0,
		chestTap = {},
		clearKey = nil,
		warnedFocus = 0,
		AutoTrain = false,
		NoClip = false,
		AntiAFK = false,
		EspMobs = false,
		EspBosses = false,
		EspChests = false,
		EspCrystals = false,
		EspLily = false,
		EspHorses = false,
		EspLevers = false,
		EspMuzan = false,
		EspFruits = false,
		QuestPick = "normal",
		QuestGap = 30,
		AutoFish = false,
		FishLead = .12,
		FishPause = 1.0,
		FishTimeout = 30,
		BossOrder = { "Akazo" },
		npcPosCache = {},
		acceptAt = nil,
		EspDist = 1200,
		PanicVK = 113,
	};
_G.Slayers2 = m;
local S = {};
S.Presets = {
		Combat = true,
		["Regular Katana"] = true,
		["Insect Katana"] = true,
		["Sound Katanas"] = true,
		Scythe = true,
		["Bladed Wagasa"] = true,
		Gauntlet = true,
		Soryu = true,
		["Tai Chi"] = true,
		Spear = true,
		Tanto = true,
		["War Fans"] = true,
		Shotgun = true,
		Sickles = true,
		Claws = true,
		["Blood Manipulation"] = true,
	};
do
	local D = Q:FindFirstChild("Assets");
	D = D and D:FindFirstChild("Animations");
	for D, Q in ipairs(D and D:GetChildren() or {}) do
		local f = string.match(Q.Name, "^(.*)_Combat_Anims$");
		if f and f ~= "" then
			S.Presets[f] = true;
		end;
	end;
end;
S.StyleWords = {
		cleaver = "Sound Katanas",
		fan = "War Fans",
		sickle = "Sickles",
		claw = "Claws",
		wagasa = "Bladed Wagasa",
	};
do
	local D = Q:FindFirstChild("CAM");
	D = D and D:FindFirstChild("Global");
	D = D and D:FindFirstChild("Powers");
	D = D and D:FindFirstChild("FightingStyles");
	if D then
		for D, Q in ipairs(D:GetChildren()) do
			S.Presets[Q.Name] = true;
		end;
	end;
end;
S.Spawns = {
		Bandit = Vector3.new(-287, 1224, -1008),
		Zuko = Vector3.new(-450, 1245, -950),
		Civilian = Vector3.new(-650, 1245, -1100),
		["*Civilian*"] = Vector3.new(-650, 1245, -1100),
		Mizunoto = Vector3.new(-888, 965, -26),
		["Beast Born Demon"] = Vector3.new(-850, 970, -120),
		["Blood Hounded Demon"] = Vector3.new(-870, 970, -100),
		["Bear Cub"] = Vector3.new(540, 1122, -1023),
		["Mother Bear"] = Vector3.new(550, 1122, -1035),
		Hoyuzo = Vector3.new(480, 1122, -1010),
		["Hoyuzo Subordinate"] = Vector3.new(470, 1122, -1010),
		Kaiden = Vector3.new(510, 1122, -1000),
		["Kaiden Subordinate"] = Vector3.new(500, 1122, -1000),
		["Lesser Demon"] = Vector3.new(-715, 220, 456),
		["Greater Demon"] = Vector3.new(-650, 235, 380),
		["High Demon"] = Vector3.new(518, 1223, -1785),
		["Ice Profound Demon"] = Vector3.new(312, 1223, -1778),
		["Fire Profound Demon"] = Vector3.new(230, 1254, -1983),
		["Kanoe Demon Slayer"] = Vector3.new(373, 1286, -2031),
		["Mizunoe Demon Slayer"] = Vector3.new(-2626.7, 284, -191.2),
		Fujiko = Vector3.new(-2626.7, 284, -191.2),
		Akazo = Vector3.new(-1132.6, 1381.1, -1742.5),
		Gyutai = Vector3.new(-263.1, 1043.5, -1150.5),
		Datai = Vector3.new(-163.2, 1043.5, -1136.9),
		Saneri = Vector3.new(-375.2, 1093.6, -438.8),
		Obari = Vector3.new(742.85, 1124.07, -1018.62),
		["Serpent Trainee"] = Vector3.new(-270.3, 1292.49, -1533.8),
		Reaper = Vector3.new(99.86, 1043.49, -576.31),
		Yahari = Vector3.new(823.2, 1019.1, -640.0),
		Sumari = Vector3.new(395.4, 1018.5, -622.7),
		Nezura = Vector3.new(-1456.63, 278.45, 927.69),
		Muzan = Vector3.new(2466.37, 1079.63, 2334.54),
	};
S.Mobs = {
		"All Regular Mobs",
		"Bandit",
		"Civilian",
		"*Civilian*",
		"Mizunoto",
		"Village Spy",
		"Karu Village Bandit",
		"Beast Born Demon",
		"Blood Hounded Demon",
		"Lesser Demon",
		"Greater Demon",
		"High Demon",
		"Ice Profound Demon",
		"Fire Profound Demon",
		"Rogue Demon",
		"Lost",
		"Iceveil Road Pikeman",
		"Iceveil Road Bandit",
		"Iceveil Road Marauder",
		"Bear Cub",
		"Mother Bear",
		"Hoyuzo Subordinate",
		"Kaiden Subordinate",
		"Kanoe Demon Slayer",
		"Mizunoe Demon Slayer",
		"Cache Lancer",
		"Cache Prowler",
		"Grove Raider",
		"Lancer Captain",
		"Prowler Captain",
		"Raid Captain",
		"Serpent Trainee",
		"Water Trainee",
		"Thunder Trainee",
		"Flame Trainee",
		"Wind Trainee",
		"Stone Trainee",
		"Insect Trainee",
		"Sound Trainee",
		"Soryu Trainee",
		"Tai Chi Trainee",
		"Reaper Trainee",
	};
S.Bosses = {
		"All Bosses",
		"Akazo",
		"Gyutai",
		"Datai",
		"Domae",
		"Reaper",
		"Nezura",
		"Yahari",
		"Sumari",
		"Enru",
		"Yeti Demon",
		"Small Yeti",
		"Hand Demon",
		"Zuko",
		"Kaiden",
		"Hoyuzo",
		"Mother Bear",
		"Fujiko",
		"Shinora",
		"Obari",
		"Saneri",
		"Giyen",
		"Tengai",
		"Zentaro",
		"Gyorei",
		"Rengu",
		"Muzan",
	};
S.BossNames = {};
for D = 1, #S.Bosses, 1 do
	S.BossNames[string.lower(S.Bosses[D])] = true;
end;
S.BossNames["all bosses"] = nil;
S.Zones = {
		["Windy Peak (Village)"] = Vector3.new(-650, 1245, -1100),
		["Windy Peak (Bandit Camp)"] = Vector3.new(-287, 1224, -1008),
		["Mistfall Harbor (Docks)"] = Vector3.new(-888, 965, -26),
		["Mistfall Harbor (Crystal)"] = Vector3.new(135.8, 873.7, 734.1),
		["Bamboo Grove (Bear Woods)"] = Vector3.new(540, 1122, -1023),
		["Bamboo Grove Sanctuary"] = Vector3.new(657.4, 1021.2, 139.7),
		["Butterfly Estate"] = Vector3.new(-1809.3, 311.8, -101.1),
		["Hidden Mist Village"] = Vector3.new(1793.08, 661.3, -231.72),
		["Iceveil Valley"] = Vector3.new(485.3, 1222.9, -1813.4),
		["Iceveil Settlement"] = Vector3.new(-154.62, 1351.5, -2556.21),
		["Final Selection Plains"] = Vector3.new(-2626.7, 284, -191.2),
		["Workout Grounds"] = Vector3.new(-311.6, 1071.6, -579.6),
	};
S.BossTP = {
		Akazo = Vector3.new(-1132.6, 1381.1, -1742.5),
		Gyutai = Vector3.new(-263.1, 1043.5, -1150.5),
		Datai = Vector3.new(-163.2, 1043.5, -1136.9),
		Saneri = Vector3.new(-383.03, 1096.07, -405.25),
		Zuko = Vector3.new(-288.51, 1226.7, -1027.99),
		Kaiden = Vector3.new(558.56, 1148.67, -1322.1),
		Hoyuzo = Vector3.new(758.57, 1003.51, -1406.92),
		["Mother Bear"] = Vector3.new(550, 1122, -1035),
		Fujiko = Vector3.new(-2626.7, 284, -191.2),
		["Hand Demon"] = Vector3.new(-2626.7, 284, -191.2),
		["Yeti Demon"] = Vector3.new(485.3, 1222.9, -1813.4),
		["Small Yeti"] = Vector3.new(485.3, 1222.9, -1813.4),
		Domae = Vector3.new(-297.34, 1352.99, -3448.14),
		Reaper = Vector3.new(104.73, 1045.5, -579.95),
		Enru = Vector3.new(819.73, 798.15, 557.06),
		Shinora = Vector3.new(-463.59, 967.0, -7.85),
		Obari = Vector3.new(742.85, 1124.07, -1018.62),
		Giyen = Vector3.new(405.07, 1020.5, -74.58),
		Tengai = Vector3.new(-132.74, 1351.48, -2616.27),
		Zentaro = Vector3.new(1329.34, 823.5, -1022.86),
		Gyorei = Vector3.new(2569.72, 1091.5, -730.2),
		Nezura = Vector3.new(-1456.63, 278.45, 927.69),
		Yahari = Vector3.new(809.68, 1021.01, -632.32),
		Sumari = Vector3.new(400.17, 1020.5, -596.86),
		["Serpent Trainee"] = Vector3.new(-270.3, 1292.49, -1533.8),
		["Reaper Trainee Kuzan"] = Vector3.new(-1231.91, 1376.61, -3013.45),
		Muzan = Vector3.new(2466.37, 1079.63, 2334.54),
	};
S.Trainers = {
		["Fisherman Jeso"] = Vector3.new(-192.0, 807.0, 602.7),
		["Alchemist Meku"] = Vector3.new(-138.1, 798.9, 518.7),
		["Dock Master Sofen"] = Vector3.new(-160.8, 798.8, 703.3),
		["Estate Worker Niko"] = Vector3.new(341.1, 939.0, 580.6),
		Ginzo = Vector3.new(273.8, 944.0, 528.2),
		Elara = Vector3.new(427.8, 943.6, 507.4),
		Jugg = Vector3.new(487.7, 876.5, 1007.8),
		Liv = Vector3.new(657.4, 1021.2, 139.7),
		["Serpent Trainer Obari"] = Vector3.new(37.0, 1307.5, -1179.5),
		["Soryu Trainee Goki"] = Vector3.new(-424.5, 289.1, 540.4),
		["Lamplighter Isamu"] = Vector3.new(1082.2, 1425.7, -749.0),
	};
S.Stations = {
		"Boulder Push",
		"Squat Rack",
		"Aim Training",
		"Boulder Split",
		"Cup Game",
		"Meditation",
		"Pushups",
		"Parkour Dungeon",
	};
S.Deliver = {
		["Deliver Niko\'s Supply Box"] = { opt = "Ill deliver the supply box(Lv 70)", tasks = { ["Deliver to Shiori"] = { npc = "Shiori", pos = Vector3.new(-1814.34, 311.8, -101.07), answer = "Niko sent it up" }, ["Report back to Niko"] = { npc = "Estate Worker Niko", pos = Vector3.new(341.07, 938.91, 580.59), answer = "Shiori says thank you" } } },
		["Restock the Infirmary"] = { opt = "Ill restock the infirmary(Lv 70)", tasks = { ["Return to Shiori"] = { npc = "Shiori", pos = Vector3.new(-1814.34, 311.8, -101.07), answer = "The infirmary is stocked" } } },
		["The Full Pantry"] = { opt = "Ill stock the reserves(Lv 75)", tasks = { ["Return to Shiori"] = { npc = "Shiori", pos = Vector3.new(-1814.34, 311.8, -101.07), answer = "The stores are stocked" } } },
		["Locate Ren\'s Lost Nichirin"] = { opt = "Ill look for your blade(Lv 75)", tasks = { ["Return to Ren"] = { npc = "Ren", pos = Vector3.new(-1795, 311.8, -85) } } },
		["The Morning Haul"] = { opt = "Ill fill your crates(Lv 45)", tasks = { ["Return to Runo"] = { npc = "Angler Runo", pos = Vector3.new(-561.1, 796.25, 683.7) } } },
		["The Good Catch"] = { opt = "Ill land the good catch(Lv 60)", tasks = { ["Return to Runo"] = { npc = "Angler Runo", pos = Vector3.new(-561.1, 796.25, 683.7) } } },
		["Retrieve Ginzo\'s Jewelry Box"] = { opt = "Ill find the jewelry box(Lv 45)", tasks = { ["Return to Ginzo"] = { npc = "Ginzo", pos = Vector3.new(273.8, 941.5, 528.19) } } },
		["Earn a Fishing Permit"] = { opt = "Ill find the permit stamp(Lv 45)", tasks = { ["Return to Sofen"] = { npc = "Dock Master Sofen", pos = Vector3.new(-160.81, 796.25, 703.29) } } },
		["Acquire Bear Meat"] = { opt = "Ill restock the pantry(Lv 10)", tasks = { ["Bring bear meat back to Lucy"] = { npc = "Lucy", pos = Vector3.new(-615.5, 1258.5, -1177.5), answer = "Hand over the meat" } } },
		["Deliver Package to Elara"] = { opt = "Ill deliver the package", tasks = { ["Package delivered"] = { npc = "Elara", pos = Vector3.new(427.82, 941.0, 507.45) } } },
		["Report to Noote"] = { opt = "Ill bring him the notes", tasks = { ["Speak with Noote"] = { npc = "Noote", pos = Vector3.new(-515.5, 1243.2, -1251.2) } } },
		["Deliver the Coded Letter"] = { opt = "Ill get this letter delivered", tasks = { ["Deliver the Letter to Chaka"] = { npc = "Chaka", pos = Vector3.new(471, 1146, -1260) } } },
	};
S.DeliverGivers = { { Npc = "Estate Worker Niko", Pos = Vector3.new(341.1, 938.9, 580.6), QuestOption = "Ill deliver the supply box(Lv 70)" }, { Npc = "MoldySugar", Pos = Vector3.new(-701.8, 1243.3, -982.7), QuestOption = "Ill deliver the package" }, { Npc = "Noote", Pos = Vector3.new(-515.5, 1243.2, -1251.2), QuestOption = "Ill get this letter delivered" } };
S.Quests = {
		["[Auto-Detect Active Quest]"] = { Auto = true },
		["[Auto] All delivery quests"] = { Auto = true, AutoDeliver = true },
		["[Windy Peak] Krue: 3 Bandits"] = {
			Npc = "Krue",
			Pos = Vector3.new(-425.9, 1240.7, -952.5),
			Target = "Bandit",
			QuestOption = "Ill take 3 bandits",
		},
		["[Windy Peak] Krue: Bandit Leader Zuko"] = {
			Npc = "Krue",
			Pos = Vector3.new(-425.9, 1240.7, -952.5),
			Target = "Zuko",
			BossOption = "Ill take the bandit boss(Lv 7)",
		},
		["[Windy Peak] Kazu: Village Spies"] = {
			Npc = "Kazu",
			Pos = Vector3.new(-626, 1242.5, -1138),
			Target = "*Civilian*",
			Alt = "Village Spy",
			QuestOption = "Ill help clear them out",
		},
		["[Windy Peak] Kazu: Notes to Noote"] = { Npc = "Kazu", Pos = Vector3.new(-626, 1242.5, -1138), QuestOption = "Ill bring him the notes" },
		["[Windy Peak] Lucy: Bear Meat"] = {
			Npc = "Lucy",
			Pos = Vector3.new(-615.5, 1260.2, -1177.5),
			Target = "Mother Bear",
			Alt = "Bear Cub",
		},
		["[Mistfall] Niko: Eliminate Mizunoto"] = { Npc = "Estate Worker Niko", Pos = Vector3.new(341.1, 938.0, 581.9), Target = "Mizunoto" },
		["[Mistfall] Niko: 5 Broken Blades"] = { Npc = "Estate Worker Niko", Pos = Vector3.new(341.1, 938.0, 581.9), Target = "Mizunoto" },
		["[Mistfall] Ginzo: Lost Jewelry Box"] = { Npc = "Ginzo", Pos = Vector3.new(273.8, 944.0, 528.2), Target = "Mizunoto" },
		["[Bamboo] Betty: Betty\'s Gemstone"] = { Npc = "Betty", Pos = Vector3.new(714.0, 1125.1, -808.0), QuestOption = "Ill look for it(Lv 10)" },
		["[Bamboo] Wagwan: Hoyuzo\'s guard"] = {
			Npc = "Wagwan",
			Pos = Vector3.new(723.76, 1019.2, -801.98),
			Target = "Hoyuzo Subordinate",
			QuestOption = "I will clear out his guards(Lv 40)",
			BossOption = "I will take care of Hoyuzo(Lv 50)",
			BossTarget = "Hoyuzo",
		},
		["[Bamboo] Liv: Lucky Penny / coins"] = {
			Npc = "Liv",
			Pos = Vector3.new(657.4, 1021.2, 139.7),
			QuestOption = "Ill look for the penny(Lv 14)",
			BossOption = "Ill find the coins(Lv 21)",
		},
		["[Hidden Mist] Togane: the forge"] = { Npc = "Blacksmith Togane", Pos = Vector3.new(1793.08, 661.3, -231.72), QuestOption = "Ill find the forge(Lv 65)" },
		["[Bamboo] Tom: Wood & Bamboo"] = {
			Npc = "Tom",
			Pos = Vector3.new(508.5, 1124.3, -970.5),
			Target = "Bear Cub",
			Alt = "Mother Bear",
			QuestOption = "Ill drive the bears back(Lv 10)",
			BossOption = "Ill fell the Mother Bear(Lv 18)",
			BossTarget = "Mother Bear",
		},
		["[Bamboo] Chaka: Kaiden and the bandits"] = {
			Npc = "Chaka",
			Pos = Vector3.new(471, 1148.5, -1260),
			Target = "Kaiden Subordinate",
			QuestOption = "Ill clear out his subordinates(Lv 26)",
			BossOption = "Ill deal with Kaiden(Lv 34)",
			BossTarget = "Kaiden",
		},
		["[Butterfly] Shiori: Restock Infirmary"] = {
			Npc = "Shiori",
			Pos = Vector3.new(-1814.3, 311.8, -101.1),
			Target = "Lesser Demon",
			Alt = "Greater Demon",
			QuestOption = "Ill restock the infirmary(Lv 70)",
			BossOption = "Ill stock the reserves(Lv 75)",
		},
		["[Butterfly] Goro: Lesser / Greater Demons"] = {
			Npc = "Demon Slayer Goro",
			Pos = Vector3.new(-871.97, 237.25, 318.47),
			Target = "Lesser Demon",
			QuestOption = "Ill thin them out(Lv 75)",
			BossOption = "Ill go up after the greater ones(Lv 83)",
			BossTarget = "Greater Demon",
		},
		["[Butterfly] Ren: Lost Nichirin"] = {
			Npc = "Ren",
			Pos = Vector3.new(-1795, 311.8, -85),
			Target = "Greater Demon",
			Alt = "Lesser Demon",
			QuestOption = "Ill look for your blade(Lv 75)",
		},
		["[Mistfall] Niko: Supply Box (delivery)"] = { Npc = "Estate Worker Niko", Pos = Vector3.new(341.1, 938.9, 580.6), QuestOption = "Ill deliver the supply box(Lv 70)" },
		["[Windy Peak] MoldySugar: Package (delivery)"] = { Npc = "MoldySugar", Pos = Vector3.new(-701.8, 1243.3, -982.7), QuestOption = "Ill deliver the package" },
		["[Windy Peak] Noote: Coded Letter (delivery)"] = { Npc = "Noote", Pos = Vector3.new(-515.5, 1243.2, -1251.2), QuestOption = "Ill get this letter delivered" },
		["[Iceveil] Tomoi: Ice Demons"] = {
			Npc = "Wounded Slayer Tomoi",
			Pos = Vector3.new(485.3, 1222.9, -1813.4),
			Target = "Ice Profound Demon",
			Alt = "High Demon",
		},
		["[Iceveil] Tomoi: Fire Demons"] = {
			Npc = "Wounded Slayer Tomoi",
			Pos = Vector3.new(485.3, 1222.9, -1813.4),
			Target = "Fire Profound Demon",
			Alt = "High Demon",
		},
		["[Final Selection] Exam"] = {
			Npc = "Demon Mokuro",
			Pos = Vector3.new(-2626.7, 284, -191.2),
			Target = "Mizunoe Demon Slayer",
			Alt = "Fujiko",
		},
		["[Demon Only] Mokuro: Mizunoto slayer"] = {
			Npc = "Demon Mokuro",
			Pos = Vector3.new(-2626.7, 284, -191.2),
			Target = "Mizunoto Demon Slayer",
			BossOption = "Ill break their watch(Lv 75)",
		},
	};
local x = {};
m.F = x;
x.remote = nil;
do
	local D = Q:FindFirstChild("Communication");
	local f = D and D:FindFirstChild("ServerAndClient");
	local u = f and f:FindFirstChild("Signals");
	local h = u and u:FindFirstChild("SignalEvent");
	x.remote = h and h:FindFirstChild("Event");
end;
function x.logErr(D, Q)
	local f = os.date("%H:%M:%S") .. (" " .. (tostring(D) .. (": " .. tostring(Q))));
	warn("[Slayers 2] " .. f);
	if (os.clock() - ((m.errT or 0))) < 1 then
		return;
	end;
	m.errT = os.clock();
	pcall(function()
		appendfile("slayers2_errors.txt", f .. string.char(10));
	end);
end;
local C = {};
function x.defer(D, Q, u)
	if type(D) ~= "function" then
		return;
	end;
	C[#C + 1] = { D, Q, u };
	if m.deferConn then
		return;
	end;
	m.deferConn = f.Heartbeat:Connect(function()
			local D = m.deferConn;
			m.deferConn = nil;
			pcall(function()
				if D then
					D:Disconnect();
				end;
			end);
			local Q = C;
			C = {};
			if not m.Running then
				return;
			end;
			for D = 1, #Q, 1 do
				local f, u = pcall(Q[D][1], Q[D][2], Q[D][3]);
				if not f then
					x.logErr("ui", u);
				end;
			end;
		end);
end;
function x.guard(D)
	if type(D) ~= "table" and type(D) ~= "userdata" then
		return D;
	end;
	return setmetatable({}, { __index = function(Q, f)
			local u = D[f];
			if type(u) ~= "function" then
				return u;
			end;
			return function(Q, f, ...)
				if type(f) == "table" and type(f.callback) == "function" then
					local D = f.callback;
					f.callback = function(Q, f)
							x.defer(D, Q, f);
						end;
				end;
				return u(D, f, ...);
			end;
		end });
end;
function x.mark(D)
	m.where = D;
end;
function x.markWrite(D)
	if D - ((m.markT or 0)) < 1 then
		return;
	end;
	m.markT = D;
	pcall(function()
		writefile("slayers2_where.txt", os.date("%H:%M:%S") .. (" " .. tostring(m.where or "?")));
	end);
end;
function x.markPrev()
	local D;
	pcall(function()
		if isfile("slayers2_where.txt") then
			D = readfile("slayers2_where.txt");
		end;
	end);
	if type(D) == "string" and #D > 0 then
		pcall(function()
			appendfile("slayers2_errors.txt", os.date("%H:%M:%S") .. (" previous run died during: " .. (D .. string.char(10))));
		end);
		warn("[Slayers 2] previous run died during: " .. D);
	end;
	pcall(function()
		delfile("slayers2_where.txt");
	end);
end;
function x.notify(D, Q)
	pcall(function()
		P:Notify({ title = "Slayers 2", text = tostring(D), duration = Q or 3 });
	end);
end;
function x.char()
	local D = h.Character;
	if not D then
		return nil, nil, nil;
	end;
	local Q = D:FindFirstChild("HumanoidRootPart");
	local f = D:FindFirstChildOfClass("Humanoid");
	if not Q or not f then
		return nil, nil, nil;
	end;
	return D, Q, f;
end;
local I = .25;
function x.addr(D)
	local Q, f = pcall(function()
			return D.Address;
		end);
	if Q and type(f) == "number" then
		return f;
	end;
	return nil;
end;
function x.attached(D, Q)
	if D == nil then
		return false;
	end;
	local f, u = pcall(function()
			local f = D.Parent;
			if f == nil then
				return false;
			end;
			if Q == nil then
				return true;
			end;
			return f.Address == Q;
		end);
	return f and u == true;
end;
function x.npcAlive(D, Q)
	if not D or D.dead or D.stale then
		return false;
	end;
	local f = os.clock();
	local u = f - ((D.chkT or 0));
	if u < .005 then
		return true;
	end;
	if u > ((Q or I)) then
		D.stale = true;
		return false;
	end;
	if x.attached(D.Model, D.PAddr) then
		D.chkT = f;
		return true;
	end;
	D.dead = true;
	return false;
end;
local U = {};
function x.refreshPlayers()
	U = {};
	local Q = D:GetPlayers();
	for D = 1, #Q, 1 do
		U[Q[D].Name] = true;
	end;
end;
x.refreshPlayers();
function x.isBoss(D)
	if D:FindFirstChild("BossInfo") then
		return true;
	end;
	return S.BossNames[string.lower(D.Name)] == true;
end;
function x.scan(D, Q, f)
	local h = {};
	local P = nil;
	if type(D) == "table" then
		P = D;
		D = nil;
	end;
	local m = D and string.lower(D) or nil;
	local S = (not P) and (((D == nil) or (D == "All Regular Mobs") or (D == "All Bosses")));
	local C = { u:FindFirstChild("Humanoids"), u:FindFirstChild("Debree") };
	for D = 1, #C, 1 do
		local u = C[D] and C[D]:FindFirstChild("Regions");
		if u then
			local D = u:GetChildren();
			for u = 1, #D, 1 do
				local C = D[u]:FindFirstChild("ActiveNpcs");
				if C then
					local D = C:GetChildren();
					for u = 1, #D, 1 do
						local C = D[u];
						local I = C.Name;
						if not U[I] then
							local D = false;
							if P then
								D = P[string.lower(I)] == true;
							elseif S then
								D = true;
							elseif m then
								local Q = string.lower(I);
								if f then
									D = (Q == m);
								else
									D = (Q == m) or (string.find(Q, m, 1, true) ~= nil);
								end;
							end;
							if D then
								local u = x.isBoss(C);
								if f and (Q and not u) then
									D = false;
								end;
							end;
							if D then
								local D = x.isBoss(C);
								if Q == nil or not S or Q == D then
									local Q = C:FindFirstChild(I) or C:FindFirstChildOfClass("Model");
									if Q then
										local f = Q:FindFirstChildOfClass("Humanoid");
										local u = Q:FindFirstChild("HumanoidRootPart");
										if f and (u and f.Health > 0) then
											local P = {
													Model = Q,
													Hum = f,
													Root = u,
													Name = I,
													Boss = D,
												};
											P.exactName = m ~= nil and string.lower(I) == m;
											P.PAddr = x.addr(C);
											P.chkT = os.clock();
											h[#h + 1] = P;
											pcall(function()
												x.noteSeen(I, u.Position);
											end);
										end;
									end;
								end;
							end;
						end;
					end;
				end;
			end;
		end;
	end;
	if m and not f then
		local D = {};
		for Q = 1, #h, 1 do
			if h[Q].exactName then
				D[#D + 1] = h[Q];
			end;
		end;
		if #D > 0 then
			return D;
		end;
	end;
	return h;
end;
local Y = { key = "", t = -99, list = {} };
function x.setKey(D)
	local Q = {};
	for D in pairs(D) do
		Q[#Q + 1] = D;
	end;
	table.sort(Q);
	return table.concat(Q, "|");
end;
function x.scanCached(D, Q, f, u, h)
	local P;
	if type(D) == "table" then
		P = x.setKey(D);
	else
		P = tostring(D);
	end;
	P = P .. (tostring(Q) .. tostring(h));
	if Y.key == P and (f - Y.t) < ((u or 1.0)) then
		return Y.list;
	end;
	Y.key = P;
	Y.t = f;
	Y.list = x.scan(D, Q, h);
	return Y.list;
end;
function x.dropCache()
	Y.key = "";
	Y.t = -99;
end;
function x.bossSet()
	local D = m.SelectedBoss;
	local Q = false;
	local f, u = {}, 0;
	if type(D) == "string" then
		if D == "All Bosses" then
			Q = true;
		else
			f[string.lower(D)] = true;
			u = 1;
		end;
	else
		for D, h in pairs(D or {}) do
			if h then
				if D == "All Bosses" then
					Q = true;
				else
					f[string.lower(D)] = true;
					u = u + 1;
				end;
			end;
		end;
	end;
	if Q then
		f, u = {}, 0;
		for D = 1, #S.Bosses, 1 do
			local Q = S.Bosses[D];
			if Q ~= "All Bosses" then
				f[string.lower(Q)] = true;
				u = u + 1;
			end;
		end;
	end;
	if u == 0 then
		return nil, false;
	end;
	return f, Q;
end;
function x.updateBossOrder(D)
	local Q = {};
	if type(D) == "table" then
		for D, f in pairs(D) do
			if f then
				Q[D] = true;
			end;
		end;
	elseif type(D) == "string" then
		Q[D] = true;
	end;
	local f = {};
	if Q["All Bosses"] then
		for D, Q in ipairs(S.Bosses) do
			if Q ~= "All Bosses" then
				f[#f + 1] = Q;
			end;
		end;
	else
		for D, u in ipairs(m.BossOrder or {}) do
			if Q[u] then
				f[#f + 1] = u;
				Q[u] = nil;
			end;
		end;
		for D, u in ipairs(S.Bosses) do
			if Q[u] then
				f[#f + 1] = u;
			end;
		end;
	end;
	m.BossOrder = f;
	m.spawnIdx = 0;
end;
function x.bossLabel()
	local D, Q = x.bossSet();
	if Q then
		return "all bosses";
	end;
	if not D then
		return "none selected";
	end;
	local f = {};
	for D in pairs(D) do
		f[#f + 1] = D;
	end;
	table.sort(f);
	return table.concat(f, ", ");
end;
S.SharedOK = { ["-2627,-192"] = true, ["485,-1814"] = true };
local R = {};
do
	local D = {};
	for Q, f in ipairs({ S.Spawns, S.BossTP }) do
		for Q, f in pairs(f) do
			local u = math.floor(f.X) .. ("," .. math.floor(f.Z));
			D[u] = D[u] or {};
			D[u][string.lower(Q)] = true;
		end;
	end;
	for D, Q in pairs(D) do
		local f = 0;
		for D in pairs(Q) do
			f = f + 1;
		end;
		if f > 1 and not S.SharedOK[D] then
			for D in pairs(Q) do
				R[D] = true;
			end;
		end;
	end;
end;
local function i(D)
	local Q = string.lower(D);
	local f = m.seen[Q];
	if f then
		return Vector3.new(f[1], f[2], f[3]);
	end;
	if R[Q] then
		return nil;
	end;
	for f, u in ipairs({ S.Spawns, S.BossTP }) do
		local h = u[D];
		if h then
			return h;
		end;
		for D, f in pairs(u) do
			if string.lower(D) == Q then
				return f;
			end;
		end;
	end;
	return nil;
end;
function x.wantSpawn(D)
	if type(D) ~= "table" then
		return i(D);
	end;
	local Q = {};
	if m.Mode == "boss" and m.BossOrder then
		for f, u in ipairs(m.BossOrder) do
			local h = string.lower(u);
			if D[h] and i(h) then
				Q[#Q + 1] = h;
			end;
		end;
	end;
	if #Q == 0 then
		for D in pairs(D) do
			if i(D) then
				Q[#Q + 1] = D;
			end;
		end;
		table.sort(Q);
	end;
	if #Q == 0 then
		return nil;
	end;
	m.spawnIdx = ((m.spawnIdx or 0)) % #Q + 1;
	return i(Q[m.spawnIdx]);
end;
local J = "slayers2_seen.json";
function x.seenLoad()
	local D;
	if not pcall(function()
		D = readfile(J);
	end) or type(D) ~= "string" then
		return;
	end;
	local Q, f = pcall(function()
			return (game:GetService("HttpService")):JSONDecode(D);
		end);
	if Q and type(f) == "table" then
		m.seen = f;
	end;
end;
function x.seenSave(D)
	if not m.seenDirty then
		return;
	end;
	if D and (D - ((m.seenT or 0))) < 30 then
		return;
	end;
	m.seenT = D or 0;
	m.seenDirty = false;
	pcall(function()
		writefile(J, (game:GetService("HttpService")):JSONEncode(m.seen));
	end);
end;
function x.noteSeen(D, Q)
	local f = string.lower(D);
	local u = m.seen[f];
	if u and (math.abs(u[1] - Q.X) < 8 and math.abs(u[3] - Q.Z) < 8) then
		return;
	end;
	m.seen[f] = { Q.X, Q.Y, Q.Z };
	m.seenDirty = true;
end;
function x.nearest(D, Q)
	local f, u = nil, 1000000000.0;
	for h = 1, #D, 1 do
		local P = D[h];
		if x.npcAlive(P, .6) then
			local D, h = pcall(function()
					return P.Root.Position;
				end);
			if D and h then
				local D = ((h - Q)).Magnitude;
				if D < u then
					u = D;
					f = P;
				end;
			end;
		end;
	end;
	return f, u;
end;
function x.tpTo(D, Q)
	local f, u = x.char();
	if not u or not D then
		return false;
	end;
	pcall(function()
		u.CFrame = CFrame.new(D.X, D.Y + ((Q or 2.5)), D.Z);
		u.AssemblyLinearVelocity = Vector3.new(0, 0, 0);
	end);
	return true;
end;
function x.findNamed(D)
	local Q = string.lower(D or "");
	if Q == "" then
		return nil;
	end;
	local f = nil;
	local h = { u:FindFirstChild("Humanoids"), u:FindFirstChild("Debree") };
	for D = 1, #h, 1 do
		local u = h[D] and h[D]:FindFirstChild("Regions");
		if u then
			for D, u in ipairs(u:GetChildren()) do
				for D, u in ipairs({ u:FindFirstChild("ActiveNpcs"), u:FindFirstChild("StationaryNpcs") }) do
					for D, u in ipairs(u and u:GetChildren() or {}) do
						local h = string.lower(u.Name);
						local P = (h == Q);
						if P or (not f and string.find(h, Q, 1, true)) then
							local D = u:FindFirstChildOfClass("Model") or u;
							local Q = D:FindFirstChild("HumanoidRootPart") or D:FindFirstChildWhichIsA("BasePart");
							if Q then
								if P then
									return Q.Position;
								end;
								f = Q.Position;
							end;
						end;
					end;
				end;
			end;
		end;
	end;
	return f;
end;
function x.npcPos(D, Q, f)
	local u = m.npcPosCache[D];
	if u and (Q - u.t) < ((f or 2.0)) then
		return u.pos;
	end;
	local h = x.findNamed(D);
	m.npcPosCache[D] = { pos = h, t = Q };
	return h;
end;
function x.followNpc(D, Q, f)
	local u, h = x.char();
	local P = x.npcPos(D, f, .1) or Q;
	local S = m.follow;
	if S and S.name == D then
		S.t = f;
		S.fb = Q;
	else
		m.follow = { name = D, fb = Q, t = f };
	end;
	if not h or not P then
		return math.huge;
	end;
	local C = h.Position;
	local I, U = C.X - P.X, C.Z - P.Z;
	local Y = math.sqrt(I * I + U * U);
	if math.abs(C.Y - P.Y) > 15 then
		return math.max(Y, 99);
	end;
	return Y;
end;
function x.followStep(D)
	local Q = m.follow;
	if not Q then
		return;
	end;
	if D - Q.t > .6 then
		m.follow = nil;
		return;
	end;
	local f, u = x.char();
	local h = x.npcPos(Q.name, D, .1) or Q.fb;
	if not u or not h then
		return;
	end;
	if Q.lastPos and Q.lastPos ~= h then
		local f = D - ((Q.lastT or D));
		if f > .02 then
			local u, P = ((h.X - Q.lastPos.X)) / f, ((h.Z - Q.lastPos.Z)) / f;
			if u * u + P * P < 900 then
				Q.vx, Q.vz = u, P;
			end;
			Q.lastPos, Q.lastT = h, D;
		end;
	elseif not Q.lastPos then
		Q.lastPos, Q.lastT = h, D;
	end;
	local P, S = Q.vx or 0, Q.vz or 0;
	if m.dlg or (m.dlgClick ~= nil) then
		x.aimRelease();
	else
		pcall(x.aimStep, Vector3.new(h.X, h.Y + 2, h.Z), true);
	end;
	local C = u.Position;
	local I, U = C.X - h.X, C.Z - h.Z;
	local Y = math.sqrt(I * I + U * U);
	local R, i = 1, 0;
	if Y > .5 then
		R, i = I / Y, U / Y;
	end;
	local J = (h.X + R * 2.5) + P * .2;
	local q = (h.Z + i * 2.5) + S * .2;
	local t, e, p = J - C.X, h.Y - C.Y, q - C.Z;
	local H = math.sqrt((t * t + e * e) + p * p);
	if H > m.WarpAbove then
		x.travelTo(Vector3.new(J, h.Y - 3, q), D);
		return;
	end;
	local a, z, j = P + t * 6, e * 6, S + p * 6;
	local A = math.sqrt((a * a + z * z) + j * j);
	if A > m.MoveSpeed then
		local D = m.MoveSpeed / A;
		a, z, j = a * D, z * D, j * D;
	end;
	if H < .6 and P * P + S * S < 1 then
		a, z, j = 0, 0, 0;
	end;
	pcall(function()
		u.AssemblyLinearVelocity = Vector3.new(a, z, j);
	end);
end;
S.MuzanRoute = {
		Vector3.new(55.0, 826.5, 781.5),
		Vector3.new(197.2, 871.4, 812.2),
		Vector3.new(330.0, 873.0, 824.0),
		Vector3.new(182.4, 873.0, 703.7),
		Vector3.new(187.6, 914.8, 558.3),
		Vector3.new(237.8, 940.5, 389.6),
		Vector3.new(227.8, 963.5, 250.1),
		Vector3.new(98.9, 963.5, 210.2),
		Vector3.new(232.7, 963.5, 267.0),
		Vector3.new(212.2, 940.5, 452.1),
		Vector3.new(181.8, 909.0, 590.1),
		Vector3.new(183.6, 873.0, 714.7),
		Vector3.new(115.8, 826.5, 808.6),
		Vector3.new(1920.6, 599.0, -881.0),
		Vector3.new(1816.3, 599.0, -719.9),
		Vector3.new(1748.2, 599.0, -549.1),
		Vector3.new(1771.7, 599.0, -425.8),
		Vector3.new(1787.4, 599.0, -295.5),
		Vector3.new(1810.5, 599.0, -145.7),
		Vector3.new(1667.8, 599.0, -92.7),
		Vector3.new(1811.8, 599.0, -146.8),
		Vector3.new(1776.7, 599.0, -332.5),
		Vector3.new(1772.2, 599.0, -452.5),
		Vector3.new(1762.0, 599.0, -632.5),
		Vector3.new(1877.4, 599.0, -794.8),
		Vector3.new(-687.0, 1380.5, -2606.5),
		Vector3.new(-531.8, 1368.2, -2594.2),
		Vector3.new(-410.5, 1348.5, -2603.5),
		Vector3.new(-392.3, 1348.5, -2794.1),
		Vector3.new(-413.6, 1380.5, -2992.6),
		Vector3.new(-463.5, 1380.5, -3136.3),
		Vector3.new(-451.1, 1350.0, -3301.2),
		Vector3.new(-398.7, 1350.0, -3438.5),
		Vector3.new(-251.6, 1350.0, -3452.8),
		Vector3.new(-191.2, 1350.0, -3327.3),
		Vector3.new(-182.9, 1380.5, -3188.0),
		Vector3.new(-171.5, 1380.5, -2996.0),
		Vector3.new(-173.1, 1373.7, -2862.2),
		Vector3.new(-163.6, 1348.5, -2702.0),
		Vector3.new(-172.5, 1348.5, -2548.0),
		Vector3.new(-362.7, 1348.5, -2551.9),
		Vector3.new(-499.4, 1352.7, -2594.5),
		Vector3.new(-643.2, 1380.5, -2607.4),
	};
function x.muzanSpots()
	local D = {};
	for Q, f in ipairs({ u:FindFirstChild("Humanoids"), u:FindFirstChild("Debree") }) do
		local u = f and f:FindFirstChild("Regions");
		for Q, f in ipairs(u and u:GetChildren() or {}) do
			for Q, u in ipairs({ f:FindFirstChild("ActiveNpcs"), f:FindFirstChild("StationaryNpcs") }) do
				for Q, u in ipairs(u and u:GetChildren() or {}) do
					if string.find(string.lower(u.Name), "muzan", 1, true) then
						local Q = u:FindFirstChild(u.Name) or u:FindFirstChildOfClass("Model");
						local h = u:FindFirstChild("HumanoidRootPart") or (Q and Q:FindFirstChild("HumanoidRootPart")) or u:FindFirstChildWhichIsA("BasePart");
						local P;
						if h then
							pcall(function()
								P = h.Position;
							end);
						end;
						D[#D + 1] = {
								name = u.Name,
								pos = P,
								region = f.Name,
								npc = true,
							};
					end;
				end;
			end;
		end;
	end;
	return D;
end;
function x.findMuzan(D)
	local Q, f = x.char();
	local u, h;
	for D, Q in ipairs(x.muzanSpots()) do
		local P = ((f and Q.pos)) and ((Q.pos - f.Position)).Magnitude or nil;
		if not u or (P and (h and P < h)) then
			u, h = Q, P;
		end;
	end;
	if u and u.pos then
		if D then
			x.tpTo(Vector3.new(u.pos.X + 5, u.pos.Y, u.pos.Z), 3);
			x.notify("at muzan" .. ((u.region and " (" .. (u.region .. ")") or "")), 4);
		else
			x.notify(string.format("muzan at %d, %d, %d - %d studs", u.pos.X, u.pos.Y, u.pos.Z, h or 0), 6);
		end;
		return u;
	end;
	if u then
		x.notify("muzan is walking in " .. (tostring(u.region) .. " - travel there to see him"), 6);
		return u;
	end;
	local P, m;
	for D, Q in ipairs(S.MuzanRoute) do
		local u = f and ((Q - f.Position)).Magnitude or 0;
		if not m or u < m then
			P, m = Q, u;
		end;
	end;
	if D and P then
		x.tpTo(P, 3);
	end;
	x.notify("muzan is not walking the map - waiting spot set on his patrol route", 6);
	return nil;
end;
S.BlackMarketer = { Vector3.new(-132.68, 804, 101.24), Vector3.new(2161.96, 823.46, -900.68) };
function x.tpBlackMarketer()
	if m.bmConn then
		pcall(function()
			m.bmConn:Disconnect();
		end);
		m.bmConn = nil;
	end;
	local function D(D)
		x.tpTo(Vector3.new(D.X + 4, D.Y, D.Z), 2.5);
		x.notify("at the Black Marketer", 3);
	end;
	local Q = x.findNamed("Black Marketer");
	if Q then
		D(Q);
		return;
	end;
	local u, h = 1, os.clock();
	x.tpTo(S.BlackMarketer[1], 3);
	m.bmConn = f.Heartbeat:Connect(function()
			local Q, f = pcall(function()
					local Q = os.clock();
					if Q - h < 1.5 then
						return;
					end;
					local f = x.findNamed("Black Marketer");
					if f then
						if m.bmConn then
							m.bmConn:Disconnect();
							m.bmConn = nil;
						end;
						D(f);
					elseif u < #S.BlackMarketer then
						u = u + 1;
						h = Q;
						x.tpTo(S.BlackMarketer[u], 3);
					else
						if m.bmConn then
							m.bmConn:Disconnect();
							m.bmConn = nil;
						end;
						x.notify("Black Marketer not at either spawn", 4);
					end;
				end);
			if not Q then
				if m.bmConn then
					pcall(function()
						m.bmConn:Disconnect();
					end);
					m.bmConn = nil;
				end;
				x.logErr("black marketer", f);
			end;
		end);
end;
function x.tpNamed(D, Q)
	local f = x.findNamed(D);
	local u = f or Q;
	if u then
		x.tpTo(u, 2.5);
		x.notify(((f and "Reached " or "Teleported to ")) .. D, 2);
	else
		x.notify("No location for " .. tostring(D), 2);
	end;
end;
local q = { t = -99, style = "Combat", item = nil };
function x.style()
	local D = os.clock();
	if D - q.t < 2.0 then
		return q.style, q.item;
	end;
	q.t = D;
	local Q, f = x.styleRaw();
	q.style, q.item = Q, f;
	return Q, f;
end;
function x.styleRaw()
	local D = h.Character;
	if not D then
		return "Combat", nil;
	end;
	local f = Q:FindFirstChild("Assets");
	f = f and f:FindFirstChild("Animations");
	local u = nil;
	local P = D:GetChildren();
	for D = 1, #P, 1 do
		if P[D].ClassName == "Tool" then
			u = P[D];
			break;
		end;
	end;
	if not u then
		local Q = D:FindFirstChild("Tool_Accessories");
		if Q then
			u = (Q:GetChildren())[1];
		end;
	end;
	local m = nil;
	local x = Q:FindFirstChild("CAM");
	local C = x and (x.Client and x.Client.Controllers);
	C = C and C:FindFirstChild("Skills_Provider");
	C = C and C:FindFirstChild("CurPower");
	if C and (type(C.Value) == "string" and (C.Value ~= "" and f)) then
		for D in string.gmatch(C.Value, "[^,]+") do
			if f:FindFirstChild(D .. "_Combat_Anims") then
				m = D;
				break;
			end;
		end;
	end;
	if not m and (u and (f and f:FindFirstChild(u.Name .. "_Combat_Anims"))) then
		m = u.Name;
	end;
	if not m and u then
		m = u.Name;
	end;
	if not m then
		m = "Combat";
	end;
	if S.Presets[m] then
		return m, nil;
	end;
	local I = string.lower(m);
	local U;
	for D, Q in pairs(S.StyleWords) do
		if string.find(I, D, 1, true) and S.Presets[Q] then
			return Q, nil;
		end;
	end;
	for D in pairs(S.Presets) do
		if D ~= "Combat" then
			local Q = string.lower(D);
			local f = string.find(I, Q, 1, true) ~= nil;
			if not f and string.sub(Q, -1) == "s" then
				f = (I == string.sub(Q, 1, -2)) or string.find(I, string.sub(Q, 1, -2), 1, true) ~= nil;
			end;
			if not f then
				f = (I .. "s" == Q);
			end;
			if f and ((not U or #D > #U)) then
				U = D;
			end;
		end;
	end;
	if U then
		return U, nil;
	end;
	return "Regular Katana", m;
end;
function x.targetBlocking()
	if not m.SkipBlocking then
		return false;
	end;
	local D = m.cur;
	if not D or not D.Model or not x.npcAlive(D) then
		return false;
	end;
	local Q;
	local f = pcall(function()
			Q = D.Model:FindFirstChild("Blocking");
		end);
	return f and Q ~= nil;
end;
function x.curBlocking()
	local D = m.cur;
	if not D or not D.Model or not x.npcAlive(D) then
		return false;
	end;
	local Q;
	local f = pcall(function()
			Q = D.Model:FindFirstChild("Blocking");
		end);
	return f and Q ~= nil;
end;
function x.attack(D)
	if not x.remote then
		return;
	end;
	local Q = h.Character;
	if not Q then
		return;
	end;
	D = D or os.clock();
	if D < ((m.atkNext or 0)) then
		return;
	end;
	local f = m.cur;
	if f and m.Reach > 0 then
		local D;
		local u = pcall(function()
				D = ((f.Root.Position - Q.HumanoidRootPart.Position)).Magnitude;
			end);
		if u and (D and D > m.Reach) then
			return;
		end;
	end;
	if x.targetBlocking() then
		m.blockedT = D;
		if m.cur then
			m.cur.t = D;
		end;
		return;
	end;
	local u = tonumber(Q:GetAttribute("last_combo")) or 0;
	if m.atkSent then
		if u == m.atkSent then
			m.atkMiss = 0;
		else
			m.atkMiss = ((m.atkMiss or 0)) + 1;
		end;
	end;
	local P = u + 1;
	if P > 5 or P < 1 then
		P = 1;
	end;
	local S, C = x.style();
	pcall(function()
		x.remote:FireServer("Combat_Service", S, P, false, 0, false, C);
	end);
	m.atkSent = P;
	m.atkCount = ((m.atkCount or 0)) + 1;
	local I = m.AttackDelay;
	if P == 1 then
		I = I * 2.0;
	end;
	if ((m.atkMiss or 0)) > 0 then
		I = I + .15 * m.atkMiss;
	end;
	if I > 1.6 then
		I = 1.6;
	end;
	m.atkNext = D + I;
end;
function x.goalFor(D)
	local Q = D.Position;
	local f = m.Offset;
	if m.Stance == "On Top" then
		return Q + Vector3.new(0, f, 0);
	elseif m.Stance == "Above (Hover)" then
		return Q + Vector3.new(0, f + 4, 0);
	elseif m.Stance == "Behind" then
		local u;
		if pcall(function()
			u = D.CFrame.LookVector;
		end) and u then
			local D, h = u.X, u.Z;
			local P = math.sqrt(D * D + h * h);
			if P > .001 then
				return Vector3.new(Q.X - (D / P) * f, Q.Y + .8, Q.Z - (h / P) * f);
			end;
		end;
		return Q + Vector3.new(0, .8, f);
	end;
	return Q - Vector3.new(0, f, 0);
end;
function x.steer()
	local D = m.cur;
	if not D or not x.npcAlive(D) then
		return;
	end;
	local Q, f = x.char();
	if not f then
		return;
	end;
	local u, h;
	local P = pcall(function()
			h = D.Root.Position;
			u = x.goalFor(D.Root);
		end);
	if not P or not u then
		return;
	end;
	local S = f.Position;
	local C, I, U = u.X - S.X, u.Y - S.Y, u.Z - S.Z;
	local Y = math.sqrt((C * C + I * I) + U * U);
	if m.Smooth and Y > m.WarpAbove then
		pcall(function()
			f.CFrame = CFrame.new(u.X, u.Y + 2, u.Z);
			f.AssemblyLinearVelocity = Vector3.new(0, 0, 0);
		end);
		return;
	end;
	if not m.Smooth then
		pcall(function()
			f.CFrame = CFrame.lookAt(u, h);
			f.AssemblyLinearVelocity = Vector3.new(0, 0, 0);
		end);
		return;
	end;
	if Y < .4 then
		pcall(function()
			f.AssemblyLinearVelocity = Vector3.new(0, 0, 0);
		end);
		if h then
			local D, Q = h.X - S.X, h.Z - S.Z;
			if (D * D + Q * Q) > .02 then
				pcall(function()
					f.CFrame = CFrame.lookAt(S, Vector3.new(h.X, S.Y, h.Z));
				end);
			end;
		end;
		return;
	end;
	local R = Y * 5.0;
	if R > m.MoveSpeed then
		R = m.MoveSpeed;
	end;
	local i = R / Y;
	pcall(function()
		f.AssemblyLinearVelocity = Vector3.new(C * i, I * i, U * i);
	end);
end;
local t = .001747;
function x.camYaw()
	local D = u.CurrentCamera;
	if not D then
		return nil;
	end;
	local Q;
	local f = pcall(function()
			Q = D.CFrame.LookVector;
		end);
	if not f or not Q then
		return nil;
	end;
	return math.atan2(-Q.X, -Q.Z);
end;
local e = -0.001747;
local p = -0.001345;
function x.aimRelease()
	local D = m.rmbDown;
	if not D and ismouse2pressed then
		local Q, f = pcall(ismouse2pressed);
		D = Q and f;
	end;
	if D then
		pcall(function()
			mouse2release();
		end);
	end;
	m.rmbDown = false;
end;
function x.aimStep(D, Q)
	if (not m.AimLock and not Q) or not D then
		return x.aimRelease();
	end;
	if m.win and m.win:IsVisible() then
		return x.aimRelease();
	end;
	if isrbxactive then
		local D, Q = pcall(isrbxactive);
		if D and not Q then
			return x.aimRelease();
		end;
	end;
	local f = u.CurrentCamera;
	if not f then
		return x.aimRelease();
	end;
	local h;
	if not pcall(function()
		h = f.CFrame;
	end) or not h then
		return;
	end;
	local P, S = h.Position, h.LookVector;
	local C = D.X - P.X;
	local I = D.Y - P.Y;
	local U = D.Z - P.Z;
	local Y = math.sqrt(C * C + U * U);
	if Y < .01 then
		return;
	end;
	local R = math.atan2(-C, -U) - math.atan2(-S.X, -S.Z);
	while R > math.pi do
		R = R - 2 * math.pi;
	end;
	while R < -math.pi do
		R = R + 2 * math.pi;
	end;
	local i = math.atan2(I, Y) - math.asin(S.Y);
	if math.abs(R) < .008 and math.abs(i) < .008 then
		return;
	end;
	if not m.rmbDown then
		pcall(function()
			mouse2press();
		end);
		m.rmbDown = true;
	end;
	local J = (R / e) * m.AimGain;
	local q = (i / p) * m.AimGain;
	if J > 900 then
		J = 900;
	elseif J < -900 then
		J = -900;
	end;
	if q > 600 then
		q = 600;
	elseif q < -600 then
		q = -600;
	end;
	pcall(function()
		mousemoverel(J, q);
	end);
end;
function x.syncSkills()
	if not m.Skills or not m.win then
		return;
	end;
	for D = 1, #m.Skills, 1 do
		local Q = m.Skills[D];
		local f;
		if pcall(function()
			f = m.win:GetValue("sk_" .. (Q.name .. "_on"));
		end) and f ~= nil then
			Q.on = f and true or false;
		end;
	end;
end;
function x.skillsRelease()
	for D = 1, #((m.Skills or {})), 1 do
		local Q = m.Skills[D];
		if Q.vk and Q.vk > 0 then
			pcall(function()
				keyrelease(Q.vk);
			end);
		end;
		Q.rel = 0;
	end;
end;
function x.skillStep(D, Q)
	if not m.Skills then
		return;
	end;
	if (D - ((m.skillSyncT or 0))) > 1.0 then
		m.skillSyncT = D;
		pcall(x.syncSkills);
	end;
	local f;
	for u = 1, #m.Skills, 1 do
		local h = m.Skills[u];
		if h.rel > 0 then
			if D >= h.rel then
				pcall(function()
					keyrelease(h.vk);
				end);
				h.rel = 0;
				h.next = D + m.SkillGap;
			end;
		elseif h.on and (Q and (Q <= h.dist and (D >= ((h.next or 0)) and ((not h.breaker or ((function()
			if f == nil then
				local D, Q = pcall(x.curBlocking);
				f = D and Q == true;
			end;
			return f;
		end))()))))) then
			h.rel = D + h.hold;
			pcall(function()
				keypress(h.vk);
			end);
		end;
	end;
end;
function x.travelTo(D, Q)
	local f, u = x.char();
	if not u then
		return;
	end;
	local h = u.Position;
	local P, S, C = D.X - h.X, ((D.Y + 3)) - h.Y, D.Z - h.Z;
	local I = math.sqrt((P * P + S * S) + C * C);
	if I > m.WarpAbove or not m.Smooth then
		pcall(function()
			u.CFrame = CFrame.new(D.X, D.Y + 3, D.Z);
			u.AssemblyLinearVelocity = Vector3.new(0, 0, 0);
		end);
		return;
	end;
	if I < 1 then
		return;
	end;
	local U = I * 5.0;
	if U > m.MoveSpeed then
		U = m.MoveSpeed;
	end;
	local Y = U / I;
	pcall(function()
		u.AssemblyLinearVelocity = Vector3.new(P * Y, S * Y, C * Y);
	end);
end;
function x.focused(D)
	if not isrbxactive then
		return true;
	end;
	local Q, f = pcall(isrbxactive);
	if not Q then
		return true;
	end;
	if f then
		return true;
	end;
	if D and (D - ((m.warnedFocus or 0))) > 15 then
		m.warnedFocus = D;
		x.notify("roblox not focused - T pickups paused", 4);
	end;
	return false;
end;
local function H(D)
	local Q = string.lower(D);
	return string.find(Q, "mound", 1, true) ~= nil or string.find(Q, "snow", 1, true) ~= nil;
end;
function x.openChests()
	local D = {};
	local Q = u:FindFirstChild("Chests");
	if not Q then
		return D;
	end;
	for Q, f in ipairs(Q:GetChildren()) do
		if not f:FindFirstChild("SealVFX") and not H(f.Name) then
			local Q = f;
			if f.ClassName ~= "Part" and f.ClassName ~= "MeshPart" then
				Q = f:FindFirstChild("RootPart") or f:FindFirstChildWhichIsA("BasePart");
			end;
			if Q then
				D[#D + 1] = { Name = f.Name, Root = Q, Node = f };
			end;
		end;
	end;
	return D;
end;
function x.collectStart(D, Q)
	if not D or #D == 0 then
		return false;
	end;
	m.collect = {
			list = D,
			i = 1,
			phase = "move",
			t = os.clock(),
		};
	x.notify(((Q or "collecting")) .. (": " .. (#D .. " item(s)")), 3);
	return true;
end;
function x.collectStop()
	m.collect = nil;
	x.aimRelease();
	pcall(function()
		keyrelease(84);
	end);
end;
function x.collectStep(D)
	local Q = m.collect;
	if not Q then
		return false;
	end;
	local f, u = x.char();
	if not u then
		return true;
	end;
	if not x.focused(D) then
		Q.blocked = Q.blocked or D;
		if (D - Q.blocked) > 6 then
			x.collectStop();
			return false;
		end;
		return true;
	end;
	Q.blocked = nil;
	local h = Q.list[Q.i];
	if not h then
		x.collectStop();
		x.notify("loot collected", 2);
		return false;
	end;
	local P;
	if not pcall(function()
		P = h.get();
	end) or not P then
		Q.i = Q.i + 1;
		Q.phase = "move";
		Q.t = D;
		pcall(function()
			keyrelease(84);
		end);
		return true;
	end;
	pcall(x.aimStep, Vector3.new(P.X, P.Y + 1.5, P.Z), true);
	local S, C, I = P.X, P.Y + ((h.lift or 2.5)), P.Z;
	if h.side then
		S = S + 6;
		C = P.Y + 3;
	end;
	local U = u.Position;
	local Y, R, i = S - U.X, C - U.Y, I - U.Z;
	local J = math.sqrt((Y * Y + R * R) + i * i);
	if Q.phase == "move" then
		if J > m.WarpAbove then
			pcall(function()
				u.CFrame = CFrame.new(P.X, P.Y + 3, P.Z);
				u.AssemblyLinearVelocity = Vector3.new(0, 0, 0);
			end);
		elseif J > ((h.reach or 3.5)) then
			local D = J * 5.0;
			if D > m.MoveSpeed then
				D = m.MoveSpeed;
			end;
			local Q = D / J;
			pcall(function()
				u.AssemblyLinearVelocity = Vector3.new(Y * Q, R * Q, i * Q);
			end);
		else
			pcall(function()
				u.AssemblyLinearVelocity = Vector3.new(0, 0, 0);
			end);
			Q.settle = Q.settle or (D + m.SettleT);
			if D < Q.settle then
				return true;
			end;
			Q.settle = nil;
			Q.phase = "hold";
			Q.t = D;
			pcall(function()
				keypress(84);
			end);
		end;
		if D - Q.t > 10 then
			Q.i = Q.i + 1;
			Q.phase = "move";
			Q.t = D;
			Q.settle = nil;
		end;
	else
		pcall(function()
			u.AssemblyLinearVelocity = Vector3.new(0, 0, 0);
		end);
		local f = h.hold or (h.tap and .2 or m.HoldT);
		if D - Q.t >= f then
			pcall(function()
				keyrelease(84);
			end);
			local f = false;
			if h.soul and ((h.tries or 0)) < 2 then
				local D;
				if pcall(function()
					D = h.get();
				end) and D then
					local Q = ((D - u.Position)).Magnitude;
					if Q < ((h.reach or 6)) + 3 then
						h.tries = ((h.tries or 0)) + 1;
						f = true;
					end;
				end;
			end;
			if not f then
				Q.i = Q.i + 1;
			end;
			Q.phase = "move";
			Q.t = D;
			Q.settle = nil;
		end;
	end;
	return true;
end;
function x.soulsNear(D, Q)
	local f = {};
	local h = u:FindFirstChild("Debree");
	if not h then
		return f;
	end;
	local function P(u)
		local h = string.lower(u.Name);
		if not string.match(h, "soul$") then
			return;
		end;
		if string.find(h, "host", 1, true) or string.find(h, "grab", 1, true) then
			return;
		end;
		local P = u;
		if u.ClassName ~= "Part" and u.ClassName ~= "MeshPart" then
			P = u:FindFirstChild("Root") or u:FindFirstChildWhichIsA("BasePart");
		end;
		if not P then
			return;
		end;
		for D = 1, #f, 1 do
			if f[D].root == P then
				return;
			end;
		end;
		local S;
		if pcall(function()
			S = ((P.Position - D)).Magnitude;
		end) and (S and S <= Q) then
			f[#f + 1] = {
					root = P,
					get = function()
						return P.Position;
					end,
					soul = true,
					hold = math.max(m.HoldT or 2, 2.5),
					lift = 0,
					reach = 6,
				};
		end;
	end;
	for D, Q in ipairs(h:GetChildren()) do
		P(Q);
		if Q.ClassName == "Folder" or Q.ClassName == "Model" then
			for D, Q in ipairs(Q:GetChildren()) do
				P(Q);
			end;
		end;
	end;
	return f;
end;
function x.soulStep(D)
	if not m.AutoSouls then
		return false;
	end;
	local Q, f = x.char();
	if not f then
		return false;
	end;
	local u = m.soul;
	if D - ((u and u.chk or 0)) < .25 then
		if not u or not u.pos then
			return false;
		end;
	end;
	local h = x.soulsNear(f.Position, m.LootRange);
	if #h == 0 then
		if u then
			if u.held then
				pcall(function()
					keyrelease(84);
				end);
			end;
			m.soul = nil;
		end;
		return false;
	end;
	local P, S;
	for D = 1, #h, 1 do
		local Q;
		if pcall(function()
			Q = h[D].get();
		end) and Q then
			local D = ((Q - f.Position)).Magnitude;
			if not S or D < S then
				P, S = Q, D;
			end;
		end;
	end;
	if not P then
		return false;
	end;
	if not u then
		u = { t = D, tries = 0 };
		m.soul = u;
	end;
	u.chk = D;
	u.pos = P;
	if S > 6 then
		u.held = false;
		pcall(function()
			keyrelease(84);
		end);
		x.travelTo(Vector3.new(P.X, P.Y - 3, P.Z), D);
		if D - u.t > 20 then
			m.soul = nil;
		end;
		return true;
	end;
	pcall(function()
		f.AssemblyLinearVelocity = Vector3.new(0, 0, 0);
	end);
	pcall(x.aimStep, P, true);
	if not x.focused(D) then
		return true;
	end;
	local C = math.max(m.HoldT or 2, 2.5);
	if not u.held then
		if D - ((u.rel or 0)) < .15 then
			return true;
		end;
		u.held = true;
		u.pressT = D;
		pcall(function()
			keypress(84);
		end);
	elseif D - ((u.pressT or D)) > C then
		u.held = false;
		u.rel = D;
		u.tries = u.tries + 1;
		pcall(function()
			keyrelease(84);
		end);
		if u.tries >= 6 then
			m.soul = nil;
		end;
	end;
	return true;
end;
function x.partOf(D, Q)
	local f = D.ClassName;
	if f == "Part" or f == "MeshPart" then
		return D;
	end;
	return (Q and D:FindFirstChild(Q)) or D:FindFirstChildWhichIsA("BasePart");
end;
function x.refind(D, Q)
	if not D or not Q then
		return nil;
	end;
	local f = D:GetChildren();
	for D = 1, #f, 1 do
		if x.addr(f[D]) == Q then
			return f[D];
		end;
	end;
	return nil;
end;
function x.pickup(D, Q, f, u, h, P)
	local m = P or {};
	m.folderFn, m.prefer = Q, h;
	m.node, m.part = f, u;
	m.addr, m.pAddr = x.addr(f), x.addr(D);
	m.seen = os.clock();
	m.get = function()
			return x.pickupPos(m);
		end;
	return m;
end;
function x.pickupPos(D)
	if D.gone then
		return nil;
	end;
	local Q = os.clock();
	if Q - ((D.seen or 0)) > I then
		local Q = D.folderFn();
		local f = x.refind(Q, D.addr);
		local u = f and x.partOf(f, D.prefer);
		if not u then
			D.gone = true;
			return nil;
		end;
		D.node, D.part, D.pAddr = f, u, x.addr(Q);
	elseif not x.attached(D.node, D.pAddr) then
		D.gone = true;
		return nil;
	end;
	D.seen = Q;
	local f, u = pcall(function()
			return D.part.Position;
		end);
	if f and u then
		return u;
	end;
	return nil;
end;
local function a()
	return u:FindFirstChild("LootDrops");
end;
local function z()
	return u:FindFirstChild("Chests");
end;
function x.lootNear(D, Q)
	local f = {};
	local u = os.clock();
	local h = a();
	if h then
		for u, P in ipairs(h:GetChildren()) do
			local m = x.partOf(P);
			if m then
				local u;
				if pcall(function()
					u = ((m.Position - D)).Magnitude;
				end) and (u and u <= Q) then
					f[#f + 1] = x.pickup(h, a, P, m);
				end;
			end;
		end;
	end;
	local P = z();
	for h, S in ipairs(x.openChests()) do
		local C;
		if pcall(function()
			C = ((S.Root.Position - D)).Magnitude;
		end) and (C and C <= Q) then
			local D = S.Root.Position;
			local Q = S.Name .. ("@" .. (math.floor(D.X) .. ("," .. math.floor(D.Z))));
			local h = m.chestTap[Q];
			if not h or (u - h) > 45 then
				m.chestTap[Q] = u;
				table.insert(f, 1, x.pickup(P, z, S.Node, S.Root, "RootPart", { tap = true, side = true }));
			end;
		end;
	end;
	return f;
end;
function x.crowBossFromText(D)
	local Q = string.lower(tostring(D or ""));
	if Q == "" or not string.find(Q, "defeat", 1, true) then
		return nil;
	end;
	if string.find(Q, "mother bear", 1, true) then
		return nil;
	end;
	for D, f in ipairs(S.Bosses) do
		if f ~= "All Bosses" and (f ~= "Mother Bear" and string.find(Q, string.lower(f), 1, true)) then
			return f;
		end;
	end;
	return nil;
end;
function x.crowChoices()
	local D = os.clock();
	local Q = m.crowPicks;
	if Q and (D - Q.t) < .35 then
		return Q.list;
	end;
	local f = {};
	local u = h:FindFirstChild("PlayerGui");
	if not u then
		m.crowPicks = { t = D, list = f };
		return f;
	end;
	local function P(D, Q)
		if Q > 8 then
			return;
		end;
		for D, u in ipairs(D:GetChildren()) do
			local h = u.ClassName;
			if h == "TextButton" or h == "TextLabel" or h == "ImageButton" then
				local D;
				pcall(function()
					D = u.Text;
				end);
				local Q = x.crowBossFromText(D);
				if Q then
					local D;
					pcall(function()
						D = u.AbsolutePosition;
					end);
					if not ((D and (D.X > 0 and D.Y > 0))) then
						local Q = u.Parent;
						for f = 1, 4, 1 do
							if not Q then
								break;
							end;
							local u;
							pcall(function()
								u = Q.AbsolutePosition;
							end);
							if u and (u.X > 0 and u.Y > 0) then
								D = u;
								break;
							end;
							Q = Q.Parent;
						end;
					end;
					if D and (D.X > 0 and D.Y > 0) then
						local h, P = 0, 0;
						local m = x.absSize and x.absSize(u);
						if m then
							h = tonumber(m.X) or 0;
							P = tonumber(m.Y) or 0;
						end;
						local S, C, I, U;
						local Y = u.Parent;
						for D = 1, 4, 1 do
							if not Y then
								break;
							end;
							local Q = Y.ClassName;
							if Q == "TextButton" or Q == "ImageButton" then
								local D;
								pcall(function()
									D = Y.AbsolutePosition;
								end);
								local Q = x.absSize and x.absSize(Y);
								if D and (D.X > 0 and D.Y > 0) then
									S, C = D.X, D.Y;
									I = Q and tonumber(Q.X) or 0;
									U = Q and tonumber(Q.Y) or 0;
								end;
								break;
							end;
							Y = Y.Parent;
						end;
						f[#f + 1] = {
								x = D.X,
								y = D.Y,
								w = h,
								h = P,
								bx = S,
								by = C,
								bw = I,
								bh = U,
								boss = Q,
							};
					end;
				end;
			end;
			P(u, Q + 1);
		end;
	end;
	P(u, 0);
	table.sort(f, function(D, Q)
		return D.y < Q.y;
	end);
	local S, C = {}, {};
	for D, Q in ipairs(f) do
		local f = Q.boss .. (":" .. math.floor(Q.y / 20));
		if not S[f] then
			S[f] = true;
			C[#C + 1] = Q;
		end;
	end;
	m.crowPicks = { t = D, list = C };
	return C;
end;
function x.crowDump()
	if m.crowDumped then
		return;
	end;
	m.crowDumped = true;
	local D = { os.date("%H:%M:%S") .. " crow menu dump" };
	local function Q(Q, f)
		local u, h, P, m, S, C = "?", "?", nil, nil, nil, nil;
		pcall(function()
			u = Q.Name;
			h = Q.ClassName;
		end);
		pcall(function()
			P = Q.Text;
		end);
		pcall(function()
			m = Q.AbsolutePosition;
		end);
		pcall(function()
			S = Q.Visible;
			C = Q.Active;
		end);
		local I = x.absSize and x.absSize(Q);
		D[#D + 1] = string.rep(" ", f) .. (h .. (" " .. (u .. (" pos=" .. (((m and (math.floor(m.X) .. ("," .. math.floor(m.Y))) or "?")) .. (" size=" .. (((I and (math.floor(tonumber(I.X) or 0) .. ("x" .. math.floor(tonumber(I.Y) or 0))) or "?")) .. (" vis=" .. (tostring(S) .. (" active=" .. (tostring(C) .. ((P and (P ~= "" and (" text=" .. string.sub(tostring(P), 1, 40))) or "")))))))))))));
	end;
	local f = h:FindFirstChild("PlayerGui");
	local function u(f, h)
		if h > 9 then
			return;
		end;
		for f, P in ipairs(f:GetChildren()) do
			local m;
			pcall(function()
				m = P.Text;
			end);
			if x.crowBossFromText(m) then
				local f, u = {}, P;
				for D = 1, 6, 1 do
					if not u then
						break;
					end;
					f[#f + 1] = u;
					u = u.Parent;
				end;
				D[#D + 1] = "--- offer: " .. tostring(m);
				for D = #f, 1, -1 do
					Q(f[D], ((#f - D)) * 2);
				end;
				for D, f in ipairs(P.Parent and P.Parent:GetChildren() or {}) do
					Q(f, 14);
				end;
			end;
			u(P, h + 1);
		end;
	end;
	if f then
		pcall(u, f, 0);
	end;
	pcall(function()
		writefile("crow_ui.txt", table.concat(D, string.char(10)));
	end);
	x.notify("crow: wrote crow_ui.txt", 4);
end;
function x.crowPoint(D, Q)
	Q = Q or 1;
	if Q <= 1 and (((D.w or 0)) > 4 and ((D.h or 0)) > 4) then
		return D.x + D.w * .5, D.y + D.h * .5;
	end;
	if Q <= 2 then
		return D.x + 40, D.y + 14;
	end;
	if Q <= 3 and (D.bx and ((D.bw or 0)) > 4) then
		return D.bx + D.bw * .5, D.by + ((D.bh or 20)) * .5;
	end;
	if Q <= 4 then
		return D.x + 120, D.y + 14;
	end;
	return D.x + 8, D.y + 8;
end;
function x.crowClose(D)
	local Q = 48 + math.max(1, math.min(5, math.floor(m.CrowSlot or 2)));
	pcall(function()
		keypress(Q);
	end);
	m.crowKeyUp = { vk = Q, t = D + .15 };
end;
function x.crowUse(D)
	local Q = 48 + math.max(1, math.min(5, math.floor(m.CrowSlot or 2)));
	pcall(function()
		keypress(Q);
	end);
	m.crowKeyUp = { vk = Q, t = D + .2 };
	m.crow.clickAt = D + .6;
end;
function x.crowStep(D)
	local Q = m.crow;
	if not Q then
		Q = { phase = "idle", t = D };
		m.crow = Q;
	end;
	if not x.focused(D) then
		return nil;
	end;
	if not x.scrOffset(D) then
		return nil;
	end;
	if m.crowKeyUp and D >= m.crowKeyUp.t then
		local D = m.crowKeyUp.vk;
		m.crowKeyUp = nil;
		pcall(function()
			keyrelease(D);
		end);
	end;
	if Q.clickAt and D >= Q.clickAt then
		Q.clickAt = nil;
		x.dlgClickAt(((m.crowClickX or 960)) - ((m.scrOffX or 0)), ((m.crowClickY or 540)) - ((m.scrOffY or 0)));
	end;
	local f = x.activeQuest();
	if f and (not f.Complete and Q.phase ~= "fight") then
		local u = x.questTarget(f);
		if not u then
			for D = 1, #((f.Tasks or {})), 1 do
				u = u or x.codeToMob(f.Tasks[D].Code);
			end;
		end;
		if u and S.BossNames[string.lower(u)] then
			Q.target = u;
			Q.quest = f.Name;
			Q.phase = "fight";
			Q.lastAlive = D;
			x.notify("crow: already on " .. (f.Name .. (" - hunting " .. u)), 4);
			return u;
		end;
	end;
	if Q.phase == "idle" then
		if D < ((Q.nextAt or 0)) then
			return nil;
		end;
		Q.prevQuest = f and f.Name or nil;
		x.crowUse(D);
		Q.phase = "menu";
		Q.menuAt = D + 3.5;
		Q.deadline = D + 10;
		return nil;
	end;
	if Q.phase == "menu" then
		if D < ((Q.menuAt or 0)) then
			return nil;
		end;
		local f = x.crowChoices();
		if #f > 0 then
			local u = f[1];
			Q.aim = (Q.aim or (m.crowAim or 1));
			local h, P = x.crowPoint(u, Q.aim);
			x.dlgClickAt(h, P);
			Q.pick = u;
			Q.target = u.boss;
			Q.phase = "quest";
			Q.deadline = D + 5;
			Q.sawBoss = false;
			x.logErr("crow", "click " .. (u.boss .. (" aim " .. (tostring(Q.aim) .. (" at " .. (math.floor(h) .. ("," .. math.floor(P))))))));
			x.notify("crow: taking " .. u.boss, 3);
		elseif D >= ((Q.deadline or 0)) then
			Q.phase = "idle";
			Q.nextAt = D + 2;
			x.notify("crow: no boss offer found, retrying", 4);
		end;
		return nil;
	end;
	if Q.phase == "quest" then
		local u = f and f.Name ~= Q.prevQuest;
		if f and (not f.Complete and ((u or (Q.target and x.questMatchesEntry(f.Name, Q.target))))) then
			m.crowAim = Q.aim or 1;
			Q.quest = f.Name;
			Q.phase = "fight";
			Q.lastAlive = D;
			if #x.crowChoices() > 0 then
				x.crowClose(D);
			end;
			x.notify("crow: hunting " .. tostring(Q.target), 3);
		elseif D >= ((Q.deadline or 0)) then
			if #x.crowChoices() > 0 and ((Q.aim or 1)) < 5 then
				pcall(x.crowDump);
				Q.aim = ((Q.aim or 1)) + 1;
				Q.phase = "menu";
				Q.menuAt = D + .4;
				Q.deadline = D + 8;
				return nil;
			end;
			x.crowClose(D);
			Q.aim = nil;
			Q.target = nil;
			Q.phase = "idle";
			Q.nextAt = D + 3;
		end;
		return nil;
	end;
	if not f or (f.Name == Q.quest and f.Complete) then
		Q.target, Q.quest, Q.phase, Q.nextAt = nil, nil, "idle", D + 1;
		x.notify("crow: boss quest done, next one", 3);
		return nil;
	end;
	return Q.target;
end;
function x.scrOffset(D)
	if m.scrOffX then
		return true;
	end;
	local Q = h:GetMouse();
	if not m.scrCalT then
		m.scrMx, m.scrMy = Q.X, Q.Y;
		pcall(function()
			mousemoveabs(700, 450);
		end);
		m.scrCalT = D;
		return false;
	end;
	if D - m.scrCalT < .12 then
		return false;
	end;
	m.scrOffX, m.scrOffY = 700 - Q.X, 450 - Q.Y;
	pcall(function()
		mousemoveabs(m.scrMx + m.scrOffX, m.scrMy + m.scrOffY);
	end);
	return true;
end;
function x.dlgRoot()
	local D = h:FindFirstChild("PlayerGui");
	local Q = D and D:FindFirstChild("ComponentsHolder");
	return Q and Q:FindFirstChild("DialogueFrame") or nil;
end;
local function j(D, Q)
	Q = Q or 0;
	if Q > 6 then
		return nil;
	end;
	for D, f in ipairs(D:GetChildren()) do
		local u, h;
		pcall(function()
			u = f.Text;
		end);
		pcall(function()
			h = f.AbsolutePosition;
		end);
		if u and (u ~= "" and h) then
			return u, h;
		end;
		local P, m = j(f, Q + 1);
		if P then
			return P, m;
		end;
	end;
	return nil;
end;
function x.dlgAnswers(D)
	local Q = {};
	local f = D and D:FindFirstChild("ButtonHolder", true);
	if not f then
		local function Q(D, f)
			if f > 5 then
				return nil;
			end;
			for D, u in ipairs(D:GetChildren()) do
				if u.Name == "ButtonHolder" then
					return u;
				end;
				local h = Q(u, f + 1);
				if h then
					return h;
				end;
			end;
		end;
		f = D and Q(D, 0) or nil;
	end;
	if not f then
		return Q;
	end;
	for D, f in ipairs(f:GetChildren()) do
		local u = j(f, 0);
		local h;
		pcall(function()
			h = f.AbsolutePosition;
		end);
		if h and ((h.X > 0 or h.Y > 0)) then
			Q[#Q + 1] = { text = u or f.Name, x = h.X, y = h.Y };
		end;
	end;
	return Q;
end;
function x.dlgContinue(D)
	local Q;
	local function f(D, Q)
		if Q > 5 then
			return nil;
		end;
		for D, u in ipairs(D:GetChildren()) do
			if u.Name == "DialogueHolder" then
				return u;
			end;
			local h = f(u, Q + 1);
			if h then
				return h;
			end;
		end;
	end;
	Q = D and f(D, 0) or nil;
	if not Q then
		return nil;
	end;
	local u, h = j(Q, 0);
	if h and h.X < 0 then
		return nil;
	end;
	return h;
end;
function x.dlgClickAt(D, Q)
	m.crowPicks = nil;
	m.dlgClick = {
			x = D + ((m.scrOffX or 0)),
			y = Q + ((m.scrOffY or 0)),
			st = "move",
			t = os.clock(),
		};
end;
function x.dlgTalk(D, Q)
	local f = m.dlg;
	if not f or f.want ~= D then
		f = { want = D, t = Q, tries = 0 };
		m.dlg = f;
	end;
	if not x.focused(Q) then
		return "working";
	end;
	if not x.scrOffset(Q) then
		return "working";
	end;
	local u = x.dlgRoot();
	if not u then
		if f.opened then
			m.dlg = nil;
			return "done";
		end;
		if Q - f.t > 20 then
			m.dlg = nil;
			return "failed";
		end;
		if Q - ((f.openT or 0)) > 2.5 then
			f.openT = Q;
			f.tries = f.tries + 1;
			pcall(function()
				keypress(84);
			end);
			m.dlgKeyUp = Q + .15;
		end;
		return "working";
	end;
	f.opened = true;
	if not D then
		return "done";
	end;
	if Q - ((f.clickT or 0)) < .8 then
		return "working";
	end;
	f.clickT = Q;
	for Q, u in ipairs(x.dlgAnswers(u)) do
		if u.text == D then
			x.dlgClickAt(u.x + 40, u.y + 14);
			f.clicked = true;
			return "working";
		end;
	end;
	local h = x.dlgContinue(u);
	if h then
		x.dlgClickAt(h.X + 12, h.Y + 8);
	end;
	if Q - f.t > 25 then
		m.dlg = nil;
		return "failed";
	end;
	return "working";
end;
function x.dlgRelease(D)
	local Q = m.dlgClick;
	if Q then
		if Q.st == "move" then
			pcall(function()
				mousemoveabs(Q.x, Q.y);
			end);
			Q.st, Q.t = "press", D;
		elseif Q.st == "press" and D - Q.t > .1 then
			pcall(function()
				mouse1press();
			end);
			m.dlgUp = D + .08;
			Q.st, Q.t = "down", D;
		elseif Q.st == "down" and D - Q.t > .3 then
			m.dlgClick = nil;
		end;
	end;
	if m.dlgUp and D >= m.dlgUp then
		m.dlgUp = nil;
		pcall(function()
			mouse1release();
		end);
	end;
	if m.dlgKeyUp and D >= m.dlgKeyUp then
		m.dlgKeyUp = nil;
		pcall(function()
			keyrelease(84);
		end);
	end;
end;
function x.acoPath(D, Q)
	local f = #Q;
	if f == 0 then
		return {};
	end;
	if f == 1 then
		return { 1 };
	end;
	local u = { [0] = D };
	for D = 1, f, 1 do
		u[D] = Q[D];
	end;
	local h = {};
	for D = 0, f, 1 do
		h[D] = {};
		for Q = 0, f, 1 do
			local f, P = u[D], u[Q];
			local m, S, x = f.X - P.X, f.Y - P.Y, f.Z - P.Z;
			h[D][Q] = math.max(math.sqrt((m * m + S * S) + x * x), .01);
		end;
	end;
	local function P(D)
		local Q, f = 0, 0;
		for u = 1, #D, 1 do
			Q = Q + h[f][D[u]];
			f = D[u];
		end;
		return Q;
	end;
	local m, S, x = {}, {}, 0;
	for D = 1, f, 1 do
		local Q, u = nil, math.huge;
		for D = 1, f, 1 do
			if not S[D] and h[x][D] < u then
				u = h[x][D];
				Q = D;
			end;
		end;
		S[Q] = true;
		m[#m + 1] = Q;
		x = Q;
	end;
	local C = P(m);
	local I = 1 / ((f * C));
	local U = {};
	for D = 0, f, 1 do
		U[D] = {};
		for Q = 0, f, 1 do
			U[D][Q] = I;
		end;
	end;
	local Y, R, i, J = 1, 3, .2, .9;
	local q = math.min(20, f + 5);
	local t = f <= 8 and 40 or (f <= 20 and 30 or 15);
	for D = 1, t, 1 do
		for D = 1, q, 1 do
			local Q, u, S = {}, {}, 0;
			for D = 1, f, 1 do
				local P;
				if math.random() < J then
					local D = -1;
					for f = 1, f, 1 do
						if not Q[f] then
							local Q = U[S][f] ^ Y * ((1 / h[S][f])) ^ R;
							if Q > D then
								D = Q;
								P = f;
							end;
						end;
					end;
				else
					local D, u = 0, {};
					for f = 1, f, 1 do
						if not Q[f] then
							u[f] = U[S][f] ^ Y * ((1 / h[S][f])) ^ R;
							D = D + u[f];
						end;
					end;
					local m = math.random() * D;
					for D = 1, f, 1 do
						if u[D] then
							m = m - u[D];
							if m <= 0 then
								P = D;
								break;
							end;
						end;
					end;
					if not P then
						for D = 1, f, 1 do
							if not Q[D] then
								P = D;
								break;
							end;
						end;
					end;
				end;
				U[S][P] = ((1 - i)) * U[S][P] + i * I;
				Q[P] = true;
				u[#u + 1] = P;
				S = P;
			end;
			local x = P(u);
			if x < C then
				C = x;
				m = u;
			end;
		end;
		local Q = 0;
		for D = 1, #m, 1 do
			local f = m[D];
			U[Q][f] = ((1 - i)) * U[Q][f] + i / C;
			Q = f;
		end;
	end;
	return m, C;
end;
function x.routeStart(D)
	local Q, f = x.char();
	if not f or not D or #D == 0 then
		return false;
	end;
	local u, h = {}, {};
	for Q = 1, #D, 1 do
		local f, P = pcall(D[Q].get);
		if f and P then
			u[#u + 1] = P;
			h[#h + 1] = D[Q];
		end;
	end;
	if #u == 0 then
		return false;
	end;
	local P, S = x.acoPath(f.Position, u);
	local C = {};
	for D = 1, #P, 1 do
		C[D] = h[P[D]];
	end;
	m.route = {
			list = C,
			i = 1,
			t = os.clock(),
			tDown = false,
			nextT = 0,
		};
	x.notify(string.format("boss drops: %d, path %.0f studs", #C, S or 0), 3);
	return true;
end;
function x.routeStop()
	local D = m.route;
	m.route = nil;
	if D and D.tDown then
		pcall(function()
			keyrelease(84);
		end);
	end;
end;
function x.routeStep(D)
	local Q = m.route;
	if not Q then
		return false;
	end;
	local f, u = x.char();
	if not u then
		return true;
	end;
	if x.focused(D) and D >= Q.nextT then
		Q.nextT = D + m.RouteTap;
		Q.tDown = not Q.tDown;
		pcall(function()
			if Q.tDown then
				keypress(84);
			else
				keyrelease(84);
			end;
		end);
	end;
	local h = Q.list[Q.i];
	if not h then
		x.routeStop();
		return false;
	end;
	local P;
	if not pcall(function()
		P = h.get();
	end) or not P then
		Q.i = Q.i + 1;
		Q.t = D;
		Q.arrive = nil;
		return true;
	end;
	local S = u.Position;
	local C, I, U = P.X - S.X, ((P.Y + 2.5)) - S.Y, P.Z - S.Z;
	local Y = math.sqrt((C * C + I * I) + U * U);
	if Y <= 3 then
		Q.i = Q.i + 1;
		Q.t = D;
		if not Q.list[Q.i] then
			pcall(function()
				u.AssemblyLinearVelocity = Vector3.new(0, 0, 0);
			end);
		end;
		return true;
	end;
	if Y > m.WarpAbove then
		pcall(function()
			u.CFrame = CFrame.new(P.X, P.Y + 3, P.Z);
			u.AssemblyLinearVelocity = Vector3.new(0, 0, 0);
		end);
	else
		local D = m.MoveSpeed;
		if not Q.list[Q.i + 1] then
			D = math.min(Y * 5.0, m.MoveSpeed);
		end;
		local f = D / Y;
		pcall(function()
			u.AssemblyLinearVelocity = Vector3.new(C * f, I * f, U * f);
		end);
	end;
	if D - Q.t > 5 then
		Q.i = Q.i + 1;
		Q.t = D;
	end;
	return true;
end;
function x.activeQuest()
	local D = Q:FindFirstChild("Player_Service");
	local f = D and D:FindFirstChild("Data");
	local u = f and f:FindFirstChild(h.Name);
	if not u then
		return nil;
	end;
	local P = u:FindFirstChild("slotEquipped");
	P = P and P.Value or 1;
	local m = u:FindFirstChild("slots");
	local S = m and m:FindFirstChild("Slot" .. tostring(math.floor(P)));
	local x = S and S:FindFirstChild("Quests");
	local C = x and x:FindFirstChild("Holder");
	if not C then
		return nil;
	end;
	local I = nil;
	local U = C:GetChildren();
	for D = 1, #U, 1 do
		local Q = U[D]:FindFirstChild("Tasks");
		if Q then
			local f, u = {}, true;
			local h = Q:GetChildren();
			for D = 1, #h, 1 do
				local Q = h[D]:FindFirstChild("Value");
				local P = h[D]:FindFirstChild("Max");
				local m;
				local S = h[D]:FindFirstChild("Code");
				if S then
					local D = S.Value;
					if type(D) == "string" and string.match(D, "^[%w ]+$") then
						m = D;
					end;
				end;
				f[#f + 1] = {
						Name = h[D].Name,
						Value = Q and Q.Value or 0,
						Max = P and P.Value or 1,
						Code = m,
					};
				if ((Q and Q.Value or 0)) < ((P and P.Value or 1)) then
					u = false;
				end;
			end;
			local P = U[D]:FindFirstChild("QuestString");
			local m = nil;
			if P then
				local D = P.Value;
				if type(D) == "string" and #D > 0 then
					local Q = true;
					for f = 1, #D, 1 do
						local u = string.byte(D, f);
						if u < 32 or u > 126 then
							Q = false;
							break;
						end;
					end;
					if Q then
						m = D;
					end;
				end;
			end;
			local S = {
					Name = U[D].Name,
					Desc = m,
					Id = U[D].Name,
					Tasks = f,
					Complete = u,
				};
			if not u then
				return S;
			end;
			if not I then
				I = S;
			end;
		end;
	end;
	return I;
end;
local A = {
		"guard",
		"guards",
		"subordinate",
		"subordinates",
		"minion",
		"minions",
		"underling",
		"underlings",
		"escort",
	};
function x.questPickups()
	local D = {};
	for Q, f in ipairs(u:GetChildren()) do
		if string.match(f.Name, "%d+$") then
			local Q;
			if f.ClassName == "Part" or f.ClassName == "MeshPart" then
				Q = f;
			else
				Q = f:FindFirstChildWhichIsA("BasePart");
			end;
			if Q then
				local u = false;
				for D, Q in ipairs(f:GetDescendants()) do
					if Q.ClassName == "ProximityPrompt" then
						u = true;
						break;
					end;
				end;
				if u then
					D[#D + 1] = { Name = f.Name, Root = Q, Node = f };
				end;
			end;
		end;
	end;
	return D;
end;
local B = {
		"defeated",
		"remaining",
		"slain",
		"killed",
		"eliminated",
		"defeat",
		"hunted",
		"fell ",
	};
local Z = {
		"found",
		"collected",
		"delivered",
		"escorted",
		"caught",
		"return",
		"retrieve",
		"recovered",
		"gathered",
	};
function x.taskKind(D)
	if not D then
		return "unknown";
	end;
	local Q = " " .. (string.lower(D) .. " ");
	for D, f in ipairs(Z) do
		if string.find(Q, f, 1, true) then
			return "fetch";
		end;
	end;
	for D, f in ipairs(B) do
		if string.find(Q, f, 1, true) then
			return "kill";
		end;
	end;
	return "unknown";
end;
function x.questMatchesEntry(D, Q)
	if not D or not Q then
		return false;
	end;
	local function f(D)
		local Q = {};
		for D in string.gmatch(string.lower(D), "%a+") do
			if #D >= 4 then
				Q[D] = true;
			end;
		end;
		return Q;
	end;
	local u, h = f(D), f(Q);
	for D in pairs(u) do
		if h[D] then
			return true;
		end;
	end;
	return false;
end;
function x.codeToMob(D)
	if not D or D == "" then
		return nil;
	end;
	local Q = string.gsub(D, "(%l)(%u)", "%1 %2");
	Q = string.gsub(Q, " Sub$", " Subordinate");
	local f = string.lower(Q);
	local u = string.gsub(f, " ", "");
	for D, Q in ipairs({ S.Mobs, S.Bosses }) do
		for D = 1, #Q, 1 do
			local h = string.lower(Q[D]);
			if h == f or string.gsub(h, " ", "") == u then
				return Q[D];
			end;
		end;
	end;
	return nil;
end;
function x.questCompleted(D)
	local f = Q:FindFirstChild("Player_Service");
	local u = f and f:FindFirstChild("Data");
	local P = u and u:FindFirstChild(h.Name);
	if not P then
		return false;
	end;
	local m = P:FindFirstChild("slotEquipped");
	local S = P:FindFirstChild("slots");
	S = S and S:FindFirstChild("Slot" .. tostring(math.floor(m and m.Value or 1)));
	local x = S and S:FindFirstChild("Quests");
	local C = x and x:FindFirstChild("Completed");
	return C ~= nil and C:FindFirstChild(D) ~= nil;
end;
function x.questLastAge()
	local D = Q:FindFirstChild("Player_Service");
	local f = D and D:FindFirstChild("Data");
	local u = f and f:FindFirstChild(h.Name);
	if not u then
		return nil;
	end;
	local P = u:FindFirstChild("slotEquipped");
	local m = u:FindFirstChild("slots");
	m = m and m:FindFirstChild("Slot" .. tostring(math.floor(P and P.Value or 1)));
	local S = m and m:FindFirstChild("Quests");
	local x = S and S:FindFirstChild("LastTime");
	if not x then
		return nil;
	end;
	return os.time() - x.Value;
end;
function x.optLevel(D)
	if type(D) ~= "string" then
		return nil;
	end;
	local Q = string.match(D, "%(Lv%s*(%d+)%)");
	return Q and tonumber(Q) or nil;
end;
function x.questOpt(D)
	if not D then
		return nil;
	end;
	if m.QuestPick == "boss" then
		return D.BossOption or D.QuestOption;
	end;
	return D.QuestOption or D.BossOption;
end;
function x.pickQuest(D)
	m.qBack = m.qBack or {};
	m.qSent = m.qSent or {};
	local Q;
	local f = m.QuestSel or {};
	local u = #f;
	local h = 1;
	if m.lastDoneKey then
		local D = S.Quests[m.lastDoneKey];
		local Q = D and ((D.QuestOption or D.BossOption));
		for D = 1, u, 1 do
			local u = f[D];
			local P = S.Quests[u];
			local x = u == m.lastDoneKey;
			if not x and (P and (P.AutoDeliver and Q)) then
				for D, f in ipairs(S.DeliverGivers) do
					if f.QuestOption == Q then
						x = true;
						break;
					end;
				end;
			end;
			if x then
				h = D + 1;
				break;
			end;
		end;
	end;
	for P = 0, u - 1, 1 do
		local C = f[((((h - 1) + P)) % math.max(u, 1)) + 1];
		local I = S.Quests[C];
		local U = m.qSent[C];
		if U and D - U > 6 then
			m.qSent[C] = nil;
			m.qBack[C] = D + 60;
		end;
		if I and I.AutoDeliver then
			local f, u = x.pickDelivery(D);
			if f then
				return I, C;
			end;
			if u and ((not Q or u < Q)) then
				Q = u;
			end;
		elseif I and (not I.Auto and I.Pos) then
			local f = x.questOpt(I);
			if f and not x.questCompleted(f) then
				local f = m.qBack[C];
				if not f or D >= f then
					return I, C;
				end;
				if not Q or f < Q then
					Q = f;
				end;
			end;
		end;
	end;
	return nil, Q;
end;
function x.entryForQuest(D)
	if not D then
		return nil;
	end;
	if D.Desc then
		for Q, f in pairs(S.Quests) do
			if f.QuestOption == D.Desc or f.BossOption == D.Desc then
				return f, Q;
			end;
		end;
	end;
	for Q, f in ipairs(m.QuestSel or {}) do
		local u = S.Quests[f];
		if u and (not u.Auto and x.questMatchesEntry(D.Name, f)) then
			return u, f;
		end;
	end;
	return nil;
end;
function x.pickDelivery(D)
	m.delivBack = m.delivBack or {};
	local Q;
	for f, u in ipairs(S.DeliverGivers) do
		local h = u.QuestOption;
		local P = m.delivSent and m.delivSent[h];
		if P and (D - P) > 6 then
			m.delivSent[h] = nil;
			local Q = ((m.delivFails or {}))[h] or 0;
			m.delivFails = m.delivFails or {};
			m.delivFails[h] = Q + 1;
			m.delivBack[h] = D + 8;
		end;
		if not x.questCompleted(h) then
			local f = m.delivBack[h];
			if not f or D >= f then
				return u;
			end;
			if not Q or f < Q then
				Q = f;
			end;
		end;
	end;
	return nil, Q;
end;
function x.deliverStep(D, Q, f, u, h)
	if m.cur then
		m.cur = nil;
		x.aimRelease();
		pcall(x.skillsRelease);
		if x.clickRelease then
			pcall(x.clickRelease);
		end;
	end;
	local P = D .. ("|" .. Q);
	if m.deliverKey ~= P then
		m.deliverKey = P;
		m.deliverTries = 0;
		m.deliverAt = nil;
	end;
	local S = x.followNpc(f.npc, f.pos, h);
	if S > 8 then
		m.deliverAt = nil;
		return;
	end;
	m.deliverAt = m.deliverAt or (h + m.SettleT);
	if h < m.deliverAt then
		return;
	end;
	if not x.remote then
		local D = x.dlgTalk(f.answer, h);
		if D == "failed" and h - ((m.deliverWarn or 0)) > 12 then
			m.deliverWarn = h;
			x.notify("could not hand \'" .. (Q .. ("\' to " .. (f.npc .. " - do it by hand"))), 6);
		end;
		return;
	end;
	if (h - ((m.deliverT or 0))) < 3 then
		return;
	end;
	m.deliverT = h;
	m.deliverTries = ((m.deliverTries or 0)) + 1;
	if m.deliverTries > 4 then
		if h - ((m.deliverWarn or 0)) > 10 then
			m.deliverWarn = h;
			x.notify("\'" .. (Q .. "\' not accepted - missing the item?"), 6);
		end;
		return;
	end;
	pcall(function()
		x.remote:FireServer("QuestProgress", D, Q);
	end);
	x.notify("handing in: " .. Q, 3);
end;
function x.questTarget(D)
	if not D then
		return nil;
	end;
	local Q = string.lower(D.Name or "");
	if D.Desc then
		Q = Q .. (" " .. string.lower(D.Desc));
	end;
	for f = 1, #D.Tasks, 1 do
		Q = Q .. (" " .. string.lower(D.Tasks[f].Name));
	end;
	Q = " " .. (string.gsub(Q, "[^%a]+", " ") .. " ");
	local f = false;
	for D, u in ipairs(A) do
		if string.find(Q, " " .. (u .. " "), 1, true) then
			f = true;
			break;
		end;
	end;
	local u = { lost = true };
	local function h(D)
		local f, u = 0, 0;
		for D in string.gmatch(string.lower(D), "%a+") do
			u = u + 1;
			if #D >= 4 then
				if string.find(Q, " " .. (D .. " "), 1, true) or string.find(Q, " " .. (D .. "s "), 1, true) then
					f = f + 1;
				end;
			end;
		end;
		return f, u;
	end;
	local P, m, x = nil, 0, -1;
	local C = { S.Mobs, S.Bosses };
	for D = 1, #C, 1 do
		local Q = C[D];
		for D = 1, #Q, 1 do
			local S = Q[D];
			if S ~= "All Regular Mobs" and (S ~= "All Bosses" and not u[string.lower(S)]) then
				local D = h(S);
				if D > 0 then
					local Q = string.find(string.lower(S), "subordinate", 1, true) ~= nil;
					local u = D * 10;
					if f and Q then
						u = u + 5;
					end;
					if (not f) and (not Q) then
						u = u + 5;
					end;
					if D > m or (D == m and u > x) then
						P, m, x = S, D, u;
					end;
				end;
			end;
		end;
	end;
	return P;
end;
function x.chests()
	local D = {};
	local Q = u:FindFirstChild("Chests");
	if not Q then
		return D;
	end;
	local f = Q:GetChildren();
	for Q = 1, #f, 1 do
		local u = f[Q];
		local h;
		if u.ClassName == "Part" or u.ClassName == "MeshPart" then
			h = u;
		else
			h = u:FindFirstChild("RootPart") or u:FindFirstChildWhichIsA("BasePart");
		end;
		if h then
			D[#D + 1] = { Name = u.Name, Pos = h.Position };
		end;
	end;
	return D;
end;
local y = "__s2pool_slayers2";
local v = 48;
do
	local D = rawget(_G, y);
	_G[y] = nil;
	local Q;
	if type(D) == "table" and (#D == v and (type(D[1]) == "table" and (D[1].box and D[1].tag))) then
		D[1].box.Visible = false;
		D[1].tag.Visible = false;
		Q = D;
	end;
	if Q then
		m.pool = Q;
		for D = 1, v, 1 do
			local f = Q[D];
			f.vis = false;
			f.box.Visible = false;
			f.tag.Visible = false;
		end;
		_G[y] = Q;
	else
		for D = 1, v, 1 do
			local Q = Drawing.new("Square");
			Q.Thickness = 1;
			Q.Filled = false;
			Q.Visible = false;
			local f = Drawing.new("Text");
			f.Size = 13;
			f.Center = true;
			f.Outline = true;
			f.Visible = false;
			m.pool[D] = { box = Q, tag = f };
		end;
		_G[y] = m.pool;
	end;
end;
local X = {};
m.espList = function()
		return X;
	end;
local r = {
		Color3.fromRGB(230, 230, 230),
		Color3.fromRGB(255, 90, 90),
		Color3.fromRGB(255, 215, 90),
		Color3.fromRGB(255, 130, 200),
		Color3.fromRGB(170, 130, 255),
		Color3.fromRGB(140, 255, 200),
		Color3.fromRGB(255, 240, 90),
		Color3.fromRGB(255, 40, 70),
		Color3.fromRGB(120, 255, 80),
	};
function x.espOn()
	return m.EspMobs or m.EspBosses or m.EspChests or m.EspCrystals or m.EspLily or m.EspHorses or m.EspLevers or m.EspMuzan or m.EspFruits;
end;
function x.espClear()
	local D = m.pool;
	if not D then
		return;
	end;
	for Q = 1, #D, 1 do
		local f = D[Q];
		f.box.Visible = false;
		f.tag.Visible = false;
		f.vis, f.bx, f.by, f.bw, f.bh, f.tx, f.ty, f.text, f.col = false, nil, nil, nil, nil, nil, nil, nil, nil;
	end;
	X = {};
	x.npcCache = nil;
end;
function x.espBuild()
	local D, Q = x.char();
	if not Q then
		X = {};
		return;
	end;
	local f = Q.Position;
	local h = m.EspDist;
	local P = {};
	if m.EspMobs or m.EspBosses then
		local D = os.clock();
		if not x.npcCache or (D - x.npcCacheT) > 2.0 then
			x.npcCache = x.scan(nil, nil);
			x.npcCacheT = D;
		end;
		local Q = x.npcCache;
		for D = 1, #Q, 1 do
			if #P >= v then
				break;
			end;
			local u = Q[D];
			local S = (u.Boss and m.EspBosses) or ((not u.Boss) and m.EspMobs);
			if S and x.npcAlive(u, .6) then
				local D, Q = nil, 0;
				pcall(function()
					Q = u.Hum.Health;
					D = ((u.Root.Position - f)).Magnitude;
				end);
				if D and (Q > 0 and D <= h) then
					P[#P + 1] = {
							kind = "m",
							npc = u,
							root = u.Root,
							text = u.Boss and (u.Name .. (" " .. (math.floor(Q) .. "hp"))) or (u.Name .. (" " .. (math.floor(D) .. "m"))),
							col = u.Boss and 2 or 1,
						};
				end;
			end;
		end;
	end;
	if m.EspChests then
		local D = x.chests();
		for Q = 1, #D, 1 do
			if #P >= v then
				break;
			end;
			local u = ((D[Q].Pos - f)).Magnitude;
			if u <= h then
				P[#P + 1] = {
						kind = "p",
						pos = D[Q].Pos,
						text = D[Q].Name,
						col = 3,
					};
			end;
		end;
	end;
	if m.EspCrystals or m.EspLily then
		local D = u:FindFirstChild("Debree");
		if D then
			if m.EspLily then
				local Q = D:GetChildren();
				for D = 1, #Q, 1 do
					if #P >= v then
						break;
					end;
					if string.find(string.lower(Q[D].Name), "spider lily", 1, true) then
						local u = Q[D]:FindFirstChildWhichIsA("BasePart");
						if u then
							local D = ((u.Position - f)).Magnitude;
							if D <= h then
								P[#P + 1] = {
										kind = "p",
										pos = u.Position,
										text = "Spider Lily",
										col = 4,
									};
							end;
						end;
					end;
				end;
			end;
			local Q = D:FindFirstChild("Regions");
			if m.EspCrystals and Q then
				local D = Q:GetChildren();
				for Q = 1, #D, 1 do
					if #P >= v then
						break;
					end;
					local u = D[Q]:GetChildren();
					for m = 1, #u, 1 do
						if string.find(string.lower(u[m].Name), "spawncrystal", 1, true) then
							local S = u[m]:FindFirstChild("Root") or u[m]:FindFirstChildWhichIsA("BasePart");
							if S then
								local u = ((S.Position - f)).Magnitude;
								if u <= h then
									P[#P + 1] = {
											kind = "p",
											pos = S.Position,
											text = D[Q].Name,
											col = 5,
										};
								end;
							end;
							break;
						end;
					end;
				end;
			end;
		end;
	end;
	if m.EspHorses then
		local D = x.scan("horse", false);
		for Q = 1, #D, 1 do
			if #P >= v then
				break;
			end;
			local u = ((D[Q].Root.Position - f)).Magnitude;
			if u <= h then
				P[#P + 1] = {
						kind = "p",
						pos = D[Q].Root.Position,
						text = "Horse",
						col = 6,
					};
			end;
		end;
	end;
	if m.EspLevers and #P < v then
		local D = u:FindFirstChild("Map");
		local Q = D and D:FindFirstChild("Puzzles");
		if Q then
			for D, Q in ipairs(Q:GetDescendants()) do
				if #P >= v then
					break;
				end;
				if ((Q.ClassName == "MeshPart" or Q.ClassName == "Part")) and string.find(string.lower(Q.Name), "lever", 1, true) then
					local D = ((Q.Position - f)).Magnitude;
					if D <= h then
						P[#P + 1] = {
								kind = "p",
								pos = Q.Position,
								text = "LEVER " .. (math.floor(D) .. "m"),
								col = 7,
							};
					end;
				end;
			end;
		end;
	end;
	if m.EspMuzan and #P < v then
		local D = {};
		for Q, f in ipairs({ u:FindFirstChild("Humanoids"), u:FindFirstChild("Debree") }) do
			local u = f and f:FindFirstChild("Regions");
			for Q, f in ipairs(u and u:GetChildren() or {}) do
				D[#D + 1] = f:FindFirstChild("ActiveNpcs");
				D[#D + 1] = f:FindFirstChild("StationaryNpcs");
			end;
		end;
		for D, Q in ipairs(D) do
			if #P >= v then
				break;
			end;
			for D, Q in ipairs(Q and Q:GetChildren() or {}) do
				if #P >= v then
					break;
				end;
				if string.find(string.lower(Q.Name), "muzan", 1, true) then
					local D = Q:FindFirstChild(Q.Name) or Q:FindFirstChildOfClass("Model");
					local u = Q:FindFirstChild("HumanoidRootPart") or (D and D:FindFirstChild("HumanoidRootPart")) or Q:FindFirstChildWhichIsA("BasePart");
					local m = Q:FindFirstChildOfClass("Humanoid") or (D and D:FindFirstChildOfClass("Humanoid"));
					if u then
						local D = ((u.Position - f)).Magnitude;
						if D <= h then
							local f = "";
							if m then
								f = " " .. (math.floor(m.Health) .. ("/" .. math.floor(m.MaxHealth)));
							end;
							P[#P + 1] = {
									kind = "p",
									pos = u.Position,
									text = Q.Name .. (f .. (" " .. (math.floor(D) .. "m"))),
									col = 8,
								};
						end;
					end;
				end;
			end;
		end;
	end;
	if m.EspFruits and #P < v then
		local D = {
				"apple",
				"banana",
				"grape",
				"fruit",
				"peach",
				"mango",
				"melon",
				"berry",
				"cherry",
				"kiwi",
				"orange",
				"pineapple",
				"coconut",
			};
		for Q, u in ipairs(u:GetChildren()) do
			if #P >= v then
				break;
			end;
			local m = string.lower(u.Name);
			local S = false;
			for D, Q in ipairs(D) do
				if string.find(m, Q, 1, true) then
					S = true;
					break;
				end;
			end;
			if S then
				local D = u;
				if u.ClassName ~= "Part" and u.ClassName ~= "MeshPart" then
					D = u:FindFirstChildWhichIsA("BasePart");
				end;
				if D then
					local Q = ((D.Position - f)).Magnitude;
					if Q <= h then
						P[#P + 1] = {
								kind = "p",
								pos = D.Position,
								text = u.Name .. (" " .. (math.floor(Q) .. "m")),
								col = 9,
							};
					end;
				end;
			end;
		end;
	end;
	X = P;
end;
local K = { off = 260, at = 0 };
function K.calibrate()
	K.at = tick();
	local D, Q = pcall(function()
			return workspace.CurrentCamera.ViewportSize;
		end);
	if not ((D and (Q and Q.X > 1))) then
		return;
	end;
	local f, u = pcall(function()
			return (game:GetService("Players")).LocalPlayer.PlayerGui:GetChildren();
		end);
	for D, f in ipairs(f and u or {}) do
		local u, h = pcall(function()
				return f.Address;
			end);
		if u and (type(h) == "number" and h > 0) then
			for D = 128, 512, 4 do
				local f, u = pcall(memory_read, "float", h + D);
				local P, m = pcall(memory_read, "float", (h + D) + 4);
				if f and (P and (type(u) == "number" and (type(m) == "number" and (math.abs(u - Q.X) < 1 and (m > Q.Y - 120 and m <= Q.Y))))) then
					K.off = D;
					return;
				end;
			end;
		end;
	end;
end;
local function l(D)
	local Q, f = pcall(function()
			return D.AbsoluteSize;
		end);
	if Q and (f and ((f.X >= .5 or f.Y >= .5))) then
		return f;
	end;
	local u, h = pcall(function()
			return D.Address;
		end);
	if not ((u and (type(h) == "number" and h > 0))) then
		return f;
	end;
	local function P()
		local D, Q = pcall(memory_read, "float", h + K.off);
		local f, u = pcall(memory_read, "float", (h + K.off) + 4);
		if D and (f and (type(Q) == "number" and (type(u) == "number" and (Q == Q and (u == u and (Q >= 0 and (u >= 0 and (Q < 100000.0 and u < 100000.0)))))))) then
			return { X = Q, Y = u };
		end;
	end;
	local m = P();
	if m and ((m.X >= .5 or m.Y >= .5)) then
		return m;
	end;
	if tick() - K.at > 1 then
		pcall(K.calibrate);
		return P() or f;
	end;
	return m or f;
end;
x.absSize = l;
local function L(D, Q, f, u, h, P, m, S, x)
	if D.bx ~= Q or D.by ~= f then
		D.bx, D.by = Q, f;
		D.box.Position = Vector2.new(Q, f);
	end;
	if D.bw ~= u or D.bh ~= h then
		D.bw, D.bh = u, h;
		D.box.Size = Vector2.new(u, h);
	end;
	if D.col ~= x then
		D.col = x;
		local Q = r[x] or r[1];
		D.box.Color = Q;
		D.tag.Color = Q;
	end;
	if D.tx ~= P or D.ty ~= m then
		D.tx, D.ty = P, m;
		D.tag.Position = Vector2.new(P, m);
	end;
	if D.text ~= S then
		D.text = S;
		D.tag.Text = S;
	end;
	if not D.vis then
		D.vis = true;
		D.box.Visible = true;
		D.tag.Visible = true;
	end;
end;
function x.espDraw()
	local D = m.pool;
	if not D then
		return;
	end;
	local Q = 0;
	for f = 1, #X, 1 do
		if Q >= v then
			break;
		end;
		local u = X[f];
		if u.kind == "m" then
			local f = x.npcAlive(u.npc, .6) and u.root.Position;
			local h, P, m, S;
			if f then
				h, P = WorldToScreen(f + Vector3.new(0, 2.8, 0));
				m, S = WorldToScreen(f - Vector3.new(0, 3.2, 0));
			end;
			if P and S then
				Q = Q + 1;
				local f = m.Y - h.Y;
				local P = f * .5;
				L(D[Q], h.X - P * .5, h.Y, P, f, h.X, h.Y - 15, u.text, u.col);
			end;
		else
			local f, h = WorldToScreen(u.pos);
			if h then
				Q = Q + 1;
				L(D[Q], f.X - 7, f.Y - 7, 14, 14, f.X, f.Y - 17, u.text, u.col);
			end;
		end;
	end;
	for Q = Q + 1, v, 1 do
		local f = D[Q];
		if f.vis then
			f.vis = false;
			f.box.Visible = false;
			f.tag.Visible = false;
		end;
	end;
end;
function x.espSync()
	if x.espOn() then
		if not m.drawConn then
			local D = 0;
			m.drawConn = f.RenderStepped:Connect(function()
					if not m.Running or not m.pool then
						return;
					end;
					local Q = os.clock();
					if Q - D < .0166 then
						return;
					end;
					D = Q;
					x.mark("espdraw");
					local f, u = pcall(x.espDraw);
					if not f and Q - ((m.drawErrT or 0)) > 5 then
						m.drawErrT = Q;
						x.logErr("esp", u);
					end;
				end);
		end;
	else
		if m.drawConn then
			pcall(function()
				m.drawConn:Disconnect();
			end);
			m.drawConn = nil;
		end;
		x.espClear();
	end;
end;
local b = {
		One = 49,
		Two = 50,
		Three = 51,
		Four = 52,
		Five = 53,
		Six = 54,
		Seven = 55,
		Eight = 56,
		Nine = 57,
		Zero = 48,
	};
function x.onMenu()
	if not m.win then
		return false;
	end;
	local D, Q = pcall(function()
			return m.win:IsVisible();
		end);
	if not D or not Q then
		return false;
	end;
	return P ~= nil and P._overUI == true;
end;
function x.inputHeld()
	return m.lmbDown == true or m.rmbDown == true or m.dlgUp ~= nil or m.dlgClick ~= nil or (m.fish ~= nil and m.fish.lmb == true) or (m.bar ~= nil and m.bar.lmb == true);
end;
function x.trainBar()
	return m.AutoTrain == true and not x.remote;
end;
function x.fishWantsInput()
	local D = m.bar;
	if D and ((D.active or D.lmb)) then
		return true;
	end;
	local Q = m.fish;
	if not m.AutoFish or not Q then
		return false;
	end;
	return Q.lmb == true or Q.keyUp ~= nil or Q.phase == "game" or os.clock() < ((Q.inputUntil or 0));
end;
function x.wantsGameInput()
	return x.fishWantsInput() or m.dlg ~= nil or m.Mode ~= "off" or m.collect ~= nil or m.route ~= nil or m.AutoTrain == true;
end;
function x.fishInputReady(D)
	local Q = m.fish;
	Q.inputUntil = D + .6;
	return not ((P and P._inputSent == false));
end;
function x.waterAreas(D)
	local Q = m.waterC;
	if Q and D - Q.t < 5 then
		return Q.list;
	end;
	local f = {};
	local h = u:FindFirstChild("Map");
	h = h and h:FindFirstChild("Map");
	local P = h and h:FindFirstChild("WatersFunctions");
	local function S(D, Q)
		if Q > 3 then
			return;
		end;
		for D, u in ipairs(D:GetChildren()) do
			if u.Name == "TouchPart" then
				local D, Q;
				pcall(function()
					D = u.Position;
					Q = u.Size;
				end);
				if D and (Q and Q.X > 4) then
					f[#f + 1] = {
							top = D.Y + Q.Y * .5,
							x1 = D.X - Q.X * .5,
							x2 = D.X + Q.X * .5,
							z1 = D.Z - Q.Z * .5,
							z2 = D.Z + Q.Z * .5,
						};
				end;
			end;
			S(u, Q + 1);
		end;
	end;
	if P then
		S(P, 0);
	end;
	m.waterC = { t = D, list = f };
	return f;
end;
function x.waterPoint(D, Q)
	local f, u = x.char();
	if not u then
		return nil;
	end;
	local h = u.Position;
	local P = x.waterAreas(D);
	local m;
	pcall(function()
		m = u.CFrame.LookVector;
	end);
	local S = {};
	if m then
		local D = math.sqrt(m.X * m.X + m.Z * m.Z);
		if D > .01 then
			S[1] = { m.X / D, m.Z / D };
		end;
	end;
	for D = 0, 7, 1 do
		local Q = (D * math.pi) / 4;
		S[#S + 1] = { math.cos(Q), math.sin(Q) };
	end;
	local C = ((Q or 0)) % #S;
	for D = 1, #S, 1 do
		local Q = S[((((D - 1) + C)) % #S) + 1];
		for D, f in ipairs({ 10, 16, 22 }) do
			local u, m = h.X + Q[1] * f, h.Z + Q[2] * f;
			for D, Q in ipairs(P) do
				local f = h.Y - Q.top;
				if u > Q.x1 and (u < Q.x2 and (m > Q.z1 and (m < Q.z2 and (f > -4 and f < 45)))) then
					return Vector3.new(u, Q.top, m);
				end;
			end;
		end;
	end;
	local I = S[(C % #S) + 1];
	return Vector3.new(h.X + I[1] * 16, h.Y - 2, h.Z + I[2] * 16);
end;
function x.fishRod(D)
	local Q = m.fishRodC;
	if D and (Q and D - Q.t < .5) then
		return Q.v;
	end;
	local f = x.fishRodScan();
	if D then
		m.fishRodC = { t = D, v = f };
	end;
	return f;
end;
function x.fishRodScan()
	local D = h.Character;
	local Q = D and D:FindFirstChild("Tool_Accessories");
	if not Q then
		return nil;
	end;
	for D, Q in ipairs(Q:GetChildren()) do
		if string.find(Q.Name, "Fishing Rod", 1, true) then
			return Q;
		end;
	end;
	return nil;
end;
function x.fishRodKey()
	local D = nil;
	if not D then
		local f = Q:FindFirstChild("Player_Service");
		local u = f and f:FindFirstChild("Data");
		D = u and u:FindFirstChild(h.Name);
		local P = D and D:FindFirstChild("slotEquipped");
		local m = D and D:FindFirstChild("slots");
		D = m and m:FindFirstChild("Slot" .. tostring(math.floor(P and P.Value or 1)));
	end;
	local f = D and D:FindFirstChild("Inventory");
	local u = f and f:FindFirstChild("Toolbar");
	local P = f and f:FindFirstChild("Inventory");
	if not u or not P then
		return nil;
	end;
	local m = {};
	for D, Q in ipairs(P:GetChildren()) do
		if string.find(Q.Name, "Fishing Rod", 1, true) then
			local D = Q:FindFirstChild("Id");
			if D then
				m[D.Value] = true;
			end;
		end;
	end;
	for D, Q in ipairs(u:GetChildren()) do
		if m[Q.Value] and b[Q.Name] then
			return b[Q.Name];
		end;
	end;
	return nil;
end;
function x.fishGame(D)
	local Q = h:FindFirstChild("PlayerGui");
	local f = Q and Q:FindFirstChild("Misc");
	if not f then
		return nil;
	end;
	local u = f:GetChildren();
	if #u == 0 then
		return nil;
	end;
	for D, Q in ipairs(u) do
		for D, Q in ipairs(Q:GetChildren()) do
			local f = Q:FindFirstChild("tracker");
			if f then
				local D = Q:FindFirstChild("Bar");
				if D then
					return f, D, Q;
				end;
			end;
		end;
	end;
	return nil;
end;
function x.barMouse(D)
	local Q = m.bar;
	if D and x.onMenu() then
		D = false;
	end;
	if not Q or Q.lmb == D then
		return;
	end;
	Q.lmb = D;
	pcall(function()
		if D then
			mouse1press();
		else
			mouse1release();
		end;
	end);
end;
function x.barStep(D, Q)
	local f = m.bar;
	if not f then
		f = { lmb = false };
		m.bar = f;
	end;
	local u, h, S;
	if f.active or D - ((f.chk or 0)) >= .1 then
		f.chk = D;
		u, h, S = x.fishGame(D);
	end;
	if not u then
		if f.active then
			f.active = false;
			x.barMouse(false);
		end;
		return false;
	end;
	if not f.active then
		f.active = true;
		f.prevBy = nil;
		f.games = ((f.games or 0)) + 1;
	end;
	if not x.focused(D) then
		x.barMouse(false);
		return true;
	end;
	local C, I;
	pcall(function()
		C = u.AbsolutePosition.Y + (l(u)).Y * .5;
		I = h.AbsolutePosition.Y + (l(h)).Y * .5;
	end);
	if C and I then
		local D = 0;
		if f.prevBy and Q > 0 then
			D = ((I - f.prevBy)) / Q;
		end;
		f.prevBy = I;
		x.barMouse((I + D * m.FishLead) > C and not ((P and P._inputSent == false)));
	end;
	return true;
end;
function x.fishFind(D, Q)
	local f = u:FindFirstChild("Debree");
	local h, P = x.char();
	if not f or not P then
		return nil;
	end;
	local m = #D;
	local S, C = nil, Q;
	for Q, f in ipairs(f:GetChildren()) do
		if string.sub(f.Name, 1, m) == D then
			local D = f;
			if f.ClassName ~= "Part" and f.ClassName ~= "MeshPart" then
				D = f:FindFirstChildWhichIsA("BasePart");
			end;
			local Q, u = pcall(function()
					return D.Position;
				end);
			if Q and u then
				local D = ((u - P.Position)).Magnitude;
				if D < C then
					C = D;
					S = u;
				end;
			end;
		end;
	end;
	return S, C;
end;
function x.fishMouse(D)
	local Q = m.fish;
	if D and x.onMenu() then
		D = false;
	end;
	if not Q or Q.lmb == D then
		return;
	end;
	Q.lmb = D;
	pcall(function()
		if D then
			mouse1press();
		else
			mouse1release();
		end;
	end);
end;
function x.fishCast(D)
	if not x.remote then
		return false;
	end;
	pcall(function()
		x.remote:FireServer("Tool_Mouse", "Up", D);
	end);
	return true;
end;
function x.fishStep(D)
	local Q = m.fish;
	if not Q then
		return;
	end;
	if not m.AutoFish then
		return;
	end;
	local f = D - ((Q.last or D));
	Q.last = D;
	if x.barStep(D, f) then
		Q.phase = "game";
		return;
	end;
	if Q.phase == "game" then
		Q.badCasts = 0;
		Q.phase = "catch";
		Q.t = D;
		Q.tries = 0;
		Q.bites = ((Q.bites or 0)) + 1;
		Q.holdAt = nil;
		return;
	end;
	if D - ((Q.slow or 0)) < .1 then
		return;
	end;
	Q.slow = D;
	local u, h = x.char();
	if not h then
		return;
	end;
	if not x.fishRod(D) then
		if D - ((Q.eqT or 0)) > 3 then
			Q.eqT = D;
			local f = x.fishRodKey();
			if f and (x.focused(D) and not x.fishInputReady(D)) then
				Q.eqT = D - 2.8;
			elseif f and x.focused(D) then
				pcall(function()
					keypress(f);
				end);
				Q.keyUp = { vk = f, t = D + .08 };
			elseif D - ((Q.warn or 0)) > 10 then
				Q.warn = D;
				x.notify("auto fish: put a fishing rod on the hotbar", 5);
			end;
		end;
		Q.phase = "cast";
		Q.t = D;
		return;
	end;
	if Q.phase == "catch" then
		if Q.keyUp then
			return;
		end;
		if D - ((Q.catchChk or 0)) < .4 then
			return;
		end;
		Q.catchChk = D;
		local f = x.fishFind("FishingCatch_", 14);
		if not f then
			if D - Q.t > 2.5 or Q.tries > 0 then
				if Q.tries > 0 then
					Q.caught = ((Q.caught or 0)) + 1;
				end;
				Q.phase = "cast";
				Q.t = D;
			end;
			return;
		end;
		if D < ((Q.holdAt or 0)) or not x.focused(D) then
			return;
		end;
		if Q.tries >= 6 then
			x.notify("auto fish: could not pick up the catch", 4);
			Q.phase = "cast";
			Q.t = D;
			return;
		end;
		if not x.fishInputReady(D) then
			return;
		end;
		Q.tries = Q.tries + 1;
		local u = math.max(m.HoldT or 3, 3);
		pcall(function()
			keypress(84);
		end);
		Q.keyUp = { vk = 84, t = D + u };
		Q.holdAt = (D + u) + .4;
		return;
	end;
	if Q.phase == "cast" then
		if D - Q.t < m.FishPause then
			return;
		end;
		if x.fishFind("FishingCatch_", 12) then
			Q.phase = "catch";
			Q.t = D;
			Q.tries = 0;
			Q.holdAt = nil;
			return;
		end;
		local f = x.waterPoint(D, Q.dir or 0);
		if not f then
			return;
		end;
		Q.water = f;
		if x.fishCast(f) == false then
			return;
		end;
		Q.phase = "wait";
		Q.t = D;
		return;
	end;
	if Q.phase == "wait" then
		if D - ((Q.bobChk or 0)) < 1 then
			return;
		end;
		Q.bobChk = D;
		local f = x.fishFind("FishingLine_", 120);
		if f then
			Q.sawBob = true;
		end;
		if (not f and D - Q.t > 4) or D - Q.t > m.FishTimeout then
			if Q.sawBob and (not f and D - Q.t < 8) then
				Q.badCasts = ((Q.badCasts or 0)) + 1;
				Q.dir = ((Q.dir or 0)) + 1;
				if Q.badCasts % 6 == 0 then
					x.notify("auto fish: casts keep coming back - move closer to open water", 6);
				end;
			else
				Q.badCasts = 0;
			end;
			if f then
				x.fishCast(Q.water);
			end;
			Q.phase = "cast";
			Q.t = D;
			Q.sawBob = false;
		end;
		return;
	end;
	if Q.phase ~= "idle" then
		Q.phase = "cast";
		Q.t = D;
	end;
end;
function x.fishSync()
	if m.AutoFish or x.trainBar() then
		if m.AutoFish and not m.fish then
			m.fish = { phase = "cast", t = os.clock(), lmb = false };
		end;
		if not m.fishConn then
			local D = 0;
			local Q = os.clock();
			m.fishConn = f.RenderStepped:Connect(function()
					local f, u = pcall(function()
							if not m.Running or not ((m.AutoFish or x.trainBar())) then
								return;
							end;
							local f = os.clock();
							local u = m.fish;
							if u and (u.mouseUp and f >= u.mouseUp) then
								u.mouseUp = nil;
								x.fishMouse(false);
							end;
							if u and (u.keyUp and f >= u.keyUp.t) then
								local D = u.keyUp.vk;
								u.keyUp = nil;
								pcall(function()
									keyrelease(D);
								end);
							end;
							if f - D < .0166 then
								return;
							end;
							D = f;
							local h = f - Q;
							Q = f;
							local P, S;
							if m.AutoFish then
								x.mark("fish/" .. tostring(m.fish and m.fish.phase or "?"));
								x.markWrite(f);
								P, S = pcall(x.fishStep, f);
							else
								x.mark("barminigame");
								x.markWrite(f);
								P, S = pcall(x.barStep, f, h);
							end;
							if not P and f - ((m.fishErrT or 0)) > 5 then
								m.fishErrT = f;
								x.logErr("fish", S);
							end;
						end);
					if not f then
						x.logErr("fish loop", u);
					end;
				end);
		end;
	else
		if m.fishConn then
			pcall(function()
				m.fishConn:Disconnect();
			end);
			m.fishConn = nil;
		end;
		if m.fish then
			x.fishMouse(false);
			if m.fish.keyUp then
				pcall(function()
					keyrelease(m.fish.keyUp.vk);
				end);
				m.fish.keyUp = nil;
			end;
			m.fish.phase = "cast";
		end;
		if m.bar then
			x.barMouse(false);
			m.bar.active = false;
		end;
	end;
end;
pcall(function()
	P:SetTheme("monochrome");
	P:SetFont("Monospace", 13);
	P:SetAccentBar(false);
end);
local G = P:CreateWindow({
		title = "slayers 2",
		subtitle = "matcha",
		id = "slayers2",
		size = { 560, 600 },
		columns = 2,
		toggleKey = "RSHIFT",
		folder = "UI/slayers2",
		config = "slayers2.json",
		inputGuard = function()
			return x.inputHeld();
		end,
		captureInput = function()
			return not x.wantsGameInput();
		end,
		onUnload = function()
			m.Running = false;
			m.Mode = "off";
			pcall(function()
				if m.F then
					m.F.aimRelease();
					m.F.skillsRelease();
				end;
			end);
			pcall(function()
				if m.drawConn then
					m.drawConn:Disconnect();
				end;
			end);
			pcall(function()
				if m.tickConn then
					m.tickConn:Disconnect();
				end;
			end);
			pcall(function()
				if m.fishConn then
					m.fishConn:Disconnect();
				end;
			end);
			pcall(function()
				if m.deferConn then
					m.deferConn:Disconnect();
				end;
			end);
			m.deferConn = nil;
			pcall(function()
				if m.fish and m.fish.lmb then
					mouse1release();
				end;
			end);
			pcall(function()
				if m.bar and m.bar.lmb then
					mouse1release();
				end;
			end);
			pcall(function()
				mouse1release();
			end);
			pcall(function()
				mouse2release();
			end);
			pcall(function()
				keyrelease(84);
			end);
			pcall(x.skillsRelease);
			m.soul, m.dlg, m.dlgClick, m.crow, m.collect, m.route = nil, nil, nil, nil, nil, nil;
			local D = m.pool;
			m.pool = nil;
			_G[y] = nil;
			if D then
				for Q = 1, #D, 1 do
					pcall(function()
						D[Q].box.Visible = false;
						D[Q].box:Remove();
					end);
					pcall(function()
						D[Q].tag.Visible = false;
						D[Q].tag:Remove();
					end);
				end;
			end;
			pcall(function()
				delfile("slayers2_where.txt");
			end);
			_G.Slayers2 = nil;
		end,
	});
m.win = G;
local n = G:AddTab("farm");
local F = G:AddTab("teleport");
local k = G:AddTab("skills");
local o = G:AddTab("esp");
local E = G:AddTab("misc");
G:AddSettingsTab("menu");
local T = x.guard(n:AddGroup("target", 1));
T:AddToggle({
	text = "autofarm",
	flag = "autofarm",
	default = false,
	save = false,
	callback = function(D)
		m.Mode = D and ((m.FarmKind or "boss")) or "off";
		m.crow = nil;
		m.cur = nil;
		x.dropCache();
		x.sync();
	end,
});
T:AddDropdown({
	text = "mode",
	flag = "farmkind",
	values = {
		"mob",
		"boss",
		"crow",
		"quest",
	},
	default = "boss",
	callback = function(D)
		m.FarmKind = D;
		if m.Mode ~= "off" then
			m.Mode = D;
			m.cur = nil;
			x.dropCache();
			x.sync();
		end;
	end,
});
T:AddDropdown({
	text = "mob",
	flag = "mob",
	values = S.Mobs,
	default = "Bandit",
	search = true,
	callback = function(D)
		m.SelectedMob = D;
	end,
});
T:AddDropdown({
	text = "boss",
	flag = "boss",
	values = S.Bosses,
	default = { "Akazo" },
	multi = true,
	search = true,
	callback = function(D)
		m.SelectedBoss = D;
		x.updateBossOrder(D);
		m.cur = nil;
		x.dropCache();
	end,
});
do
	local D, Q, f = {}, {}, {};
	for u, h in pairs(S.Quests) do
		local P, m = u, nil;
		if not h.Auto then
			local D, Q = x.optLevel(h.QuestOption), x.optLevel(h.BossOption);
			if D and (Q and D ~= Q) then
				P = u .. (" (Lv " .. (math.min(D, Q) .. ("/" .. (math.max(D, Q) .. ")"))));
			elseif D or Q then
				P = u .. (" (Lv " .. (((D or Q)) .. ")"));
			end;
			m = math.min(D or 999, Q or 999);
		end;
		D[#D + 1] = P;
		Q[P] = u;
		f[P] = h.Auto and -1 or m;
	end;
	table.sort(D, function(D, Q)
		if f[D] ~= f[Q] then
			return f[D] < f[Q];
		end;
		return D < Q;
	end);
	local u = {};
	for D, f in ipairs(D) do
		u[Q[f]] = D;
	end;
	S.QuestByLabel = Q;
	T:AddDropdown({
		text = "quest",
		flag = "quests",
		values = D,
		default = { D[1] },
		multi = true,
		search = true,
		callback = function(D)
			local f = {};
			local function h(D)
				local u = Q[D] or (S.Quests[D] and D);
				if u then
					f[#f + 1] = u;
				end;
			end;
			if type(D) == "table" then
				for D, Q in pairs(D) do
					if Q == true then
						h(D);
					elseif type(Q) == "string" then
						h(Q);
					end;
				end;
			elseif type(D) == "string" then
				h(D);
			end;
			table.sort(f, function(D, Q)
				return ((u[D] or 999)) < ((u[Q] or 999));
			end);
			m.QuestSel = f;
			m.SelectedQuest = f[1] or "[Auto-Detect Active Quest]";
			m.acceptTries = 0;
			m.acceptAt = nil;
			m.acceptKey = nil;
		end,
	});
end;
T:AddDropdown({
	text = "quest type",
	flag = "questpick",
	values = { "normal", "boss" },
	default = "normal",
	callback = function(D)
		m.QuestPick = D;
		m.acceptTries = 0;
		m.acceptAt = nil;
	end,
});
T:AddSlider({
	text = "crow slot",
	flag = "crowslot",
	min = 1,
	max = 5,
	step = 1,
	default = 2,
	callback = function(D)
		m.CrowSlot = D;
	end,
});
T:AddSlider({
	text = "quest gap",
	flag = "questgap",
	min = 10,
	max = 120,
	step = 5,
	default = 30,
	suffix = "s",
	callback = function(D)
		m.QuestGap = D;
	end,
});
local c = x.guard(n:AddGroup("fight", 2));
c:AddDropdown({
	text = "stance",
	flag = "stance",
	values = {
		"Under Target",
		"Behind",
		"On Top",
		"Above (Hover)",
	},
	default = "Under Target",
	callback = function(D)
		m.Stance = D;
	end,
});
c:AddSlider({
	text = "offset",
	flag = "offset",
	min = 1,
	max = 15,
	step = .1,
	default = 3.8,
	callback = function(D)
		m.Offset = D;
	end,
});
c:AddSlider({
	text = "attack delay",
	flag = "atkdelay",
	min = .05,
	max = 1,
	step = .05,
	default = .3,
	suffix = "s",
	callback = function(D)
		m.AttackDelay = D;
	end,
});
c:AddSlider({
	text = "swing within",
	flag = "reach",
	min = 5,
	max = 40,
	default = 18,
	suffix = "studs",
	callback = function(D)
		m.Reach = D;
	end,
});
c:AddToggle({
	text = "hold when blocking",
	flag = "skipblock",
	default = true,
	callback = function(D)
		m.SkipBlocking = D;
	end,
});
c:AddToggle({
	text = "aimlock",
	flag = "aimlock",
	default = true,
	callback = function(D)
		m.AimLock = D;
		if not D then
			x.aimRelease();
		end;
	end,
});
c:AddSlider({
	text = "aim gain",
	flag = "aimgain",
	min = .1,
	max = 1,
	step = .05,
	default = .55,
	callback = function(D)
		m.AimGain = D;
	end,
});
c:AddToggle({
	text = "smooth movement",
	flag = "smooth",
	default = true,
	callback = function(D)
		m.Smooth = D;
	end,
});
c:AddSlider({
	text = "move speed",
	flag = "movespeed",
	min = 10,
	max = 150,
	default = 60,
	suffix = "s/s",
	callback = function(D)
		m.MoveSpeed = D;
	end,
});
c:AddSlider({
	text = "warp above",
	flag = "warpabove",
	min = 10,
	max = 200,
	default = 40,
	suffix = "studs",
	callback = function(D)
		m.WarpAbove = D;
	end,
});
c:AddToggle({
	text = "health guard",
	flag = "hguard",
	default = false,
	callback = function(D)
		m.HealthGuard = D;
	end,
});
c:AddSlider({
	text = "guard at",
	flag = "hpct",
	min = .1,
	max = .9,
	step = .05,
	default = .45,
	callback = function(D)
		m.GuardPct = D;
	end,
});
local g = {};
for D in pairs(S.Zones) do
	g[#g + 1] = D;
end;
table.sort(g);
local N = {};
for D in pairs(S.BossTP) do
	N[#N + 1] = D;
end;
table.sort(N);
local s = {};
for D in pairs(S.Trainers) do
	s[#s + 1] = D;
end;
table.sort(s);
local W = x.guard(F:AddGroup("regions", 1));
W:AddButton({ text = "black market", callback = function()
		x.tpBlackMarketer();
	end });
W:AddButton({ text = "find muzan", callback = function()
		x.findMuzan(true);
	end });
local V = g[1];
W:AddDropdown({
	text = "region",
	flag = "zone",
	values = g,
	default = V,
	search = true,
	callback = function(D)
		V = D;
	end,
});
W:AddButton({ text = "travel", callback = function()
		x.tpTo(S.Zones[V], 3);
	end });
local w = N[1];
W:AddDropdown({
	text = "boss",
	flag = "bosstp",
	values = N,
	default = w,
	search = true,
	callback = function(D)
		w = D;
	end,
});
W:AddButton({ text = "travel to boss", callback = function()
		x.tpNamed(w, S.BossTP[w]);
	end });
local d = x.guard(F:AddGroup("npcs", 2));
local O = s[1];
d:AddDropdown({
	text = "trainer",
	flag = "trainer",
	values = s,
	default = O,
	search = true,
	callback = function(D)
		O = D;
	end,
});
d:AddButton({ text = "travel to trainer", callback = function()
		x.tpNamed(O, S.Trainers[O]);
	end });
local M = S.Stations[1];
d:AddDropdown({
	text = "station",
	flag = "station",
	values = S.Stations,
	default = M,
	callback = function(D)
		M = D;
	end,
});
d:AddButton({ text = "travel to station", callback = function()
		local D = u:FindFirstChild("Training");
		local Q = D and D:FindFirstChild(M);
		local f = Q and Q:FindFirstChildWhichIsA("BasePart", true);
		if f then
			x.tpTo(f.Position, 3);
			x.notify("at " .. M, 2);
		else
			x.notify(M .. " not loaded", 2);
		end;
	end });
d:AddButton({ text = "nearest chest", callback = function()
		local D, Q = x.char();
		if not Q then
			return;
		end;
		local f = x.chests();
		local u, h = nil, 1000000000.0;
		for D = 1, #f, 1 do
			local P = ((f[D].Pos - Q.Position)).Magnitude;
			if P < h then
				h = P;
				u = f[D];
			end;
		end;
		if u then
			x.tpTo(u.Pos, 3);
			x.notify(u.Name, 2);
		else
			x.notify("no chests up", 2);
		end;
	end });
S.SkillKeys = {
		{ "z", 90 },
		{ "x", 88 },
		{ "c", 67 },
		{ "v", 86 },
		{ "b", 66 },
		{ "k", 75 },
		{ "n", 78 },
	};
do
	local D = x.guard(k:AddGroup("timing", 1));
	D:AddSlider({
		text = "gap between presses",
		flag = "sk_gap",
		min = .05,
		max = 5,
		step = .05,
		default = .3,
		suffix = "s",
		callback = function(D)
			m.SkillGap = D;
		end,
	});
end;
m.Skills = {};
for D = 1, #S.SkillKeys, 1 do
	local Q, f = S.SkillKeys[D][1], S.SkillKeys[D][2];
	local u = x.guard(k:AddGroup(Q, (D <= 3) and 1 or 2));
	local h = {
			on = false,
			breaker = false,
			hold = .08,
			dist = 15,
			vk = f,
			rel = 0,
			next = 0,
			name = Q,
		};
	m.Skills[D] = h;
	u:AddToggle({
		text = "enabled",
		flag = "sk_" .. (Q .. "_on"),
		default = false,
		callback = function(D)
			h.on = D;
		end,
	});
	u:AddToggle({
		text = "block breaker",
		flag = "sk_" .. (Q .. "_breaker"),
		default = false,
		callback = function(D)
			h.breaker = D;
		end,
	});
	u:AddSlider({
		text = "hold",
		flag = "sk_" .. (Q .. "_hold"),
		min = .02,
		max = 3,
		step = .02,
		default = .08,
		suffix = "s",
		callback = function(D)
			h.hold = D;
		end,
	});
	u:AddSlider({
		text = "range",
		flag = "sk_" .. (Q .. "_dist"),
		min = 1,
		max = 120,
		default = 15,
		suffix = "studs",
		callback = function(D)
			h.dist = D;
		end,
	});
end;
local Dn = x.guard(o:AddGroup("targets", 1));
Dn:AddToggle({
	text = "mobs",
	flag = "espmob",
	default = false,
	callback = function(D)
		m.EspMobs = D;
		x.sync();
	end,
});
Dn:AddToggle({
	text = "bosses",
	flag = "espboss",
	default = false,
	callback = function(D)
		m.EspBosses = D;
		x.sync();
	end,
});
Dn:AddToggle({
	text = "chests",
	flag = "espchest",
	default = false,
	callback = function(D)
		m.EspChests = D;
		x.sync();
	end,
});
local Qn = x.guard(o:AddGroup("world", 2));
Qn:AddToggle({
	text = "spawn crystals",
	flag = "espcry",
	default = false,
	callback = function(D)
		m.EspCrystals = D;
		x.sync();
	end,
});
Qn:AddToggle({
	text = "spider lily",
	flag = "esplily",
	default = false,
	callback = function(D)
		m.EspLily = D;
		x.sync();
	end,
});
Qn:AddToggle({
	text = "horses",
	flag = "esphorse",
	default = false,
	callback = function(D)
		m.EspHorses = D;
		x.sync();
	end,
});
Qn:AddToggle({
	text = "levers",
	flag = "esplever",
	default = false,
	callback = function(D)
		m.EspLevers = D;
		x.sync();
	end,
});
Qn:AddToggle({
	text = "muzan",
	flag = "espmuzan",
	default = false,
	callback = function(D)
		m.EspMuzan = D;
		x.sync();
	end,
});
Qn:AddToggle({
	text = "fruits",
	flag = "espfruit",
	default = false,
	callback = function(D)
		m.EspFruits = D;
		x.sync();
	end,
});
Qn:AddSlider({
	text = "distance",
	flag = "espdist",
	min = 100,
	max = 3000,
	default = 1200,
	suffix = "m",
	callback = function(D)
		m.EspDist = D;
	end,
});
local fn = x.guard(E:AddGroup("automation", 1));
fn:AddToggle({
	text = "auto pick up loot",
	flag = "autoloot",
	default = true,
	callback = function(D)
		m.AutoLoot = D;
	end,
});
fn:AddToggle({
	text = "collect boss drops",
	flag = "lootboss",
	default = true,
	callback = function(D)
		m.LootBoss = D;
	end,
});
fn:AddToggle({
	text = "claim souls",
	flag = "autosouls",
	default = true,
	callback = function(D)
		m.AutoSouls = D;
		x.sync();
	end,
});
fn:AddSlider({
	text = "settle before press",
	flag = "settlet",
	min = 0,
	max = 2,
	step = .1,
	default = .6,
	suffix = "s",
	callback = function(D)
		m.SettleT = D;
	end,
});
fn:AddSlider({
	text = "wait for drops",
	flag = "lootwait",
	min = 1,
	max = 20,
	step = .5,
	default = 5,
	suffix = "s",
	callback = function(D)
		m.LootWait = D;
	end,
});
fn:AddSlider({
	text = "hold T for",
	flag = "holdt",
	min = .5,
	max = 4,
	step = .1,
	default = 2.0,
	suffix = "s",
	callback = function(D)
		m.HoldT = D;
	end,
});
fn:AddToggle({
	text = "auto train",
	flag = "autotrain",
	default = false,
	callback = function(D)
		m.AutoTrain = D;
		x.sync();
	end,
});
fn:AddToggle({
	text = "noclip",
	flag = "noclip",
	default = false,
	callback = function(D)
		m.NoClip = D;
		x.sync();
	end,
});
fn:AddToggle({
	text = "anti afk",
	flag = "antiafk",
	default = false,
	callback = function(D)
		m.AntiAFK = D;
		x.sync();
	end,
});
local un = x.guard(E:AddGroup("panic", 2));
un:AddKeybind({
	text = "panic stop",
	flag = "panickey",
	default = "F2",
	mode = "always",
	onClick = function()
		x.panic();
	end,
	changed = function(D)
 
	end,
});
un:AddButton({ text = "stop everything", callback = function()
		x.panic();
	end });
un:AddButton({ text = "unload", confirm = true, callback = function()
		G:Unload();
	end });
local hn = x.guard(E:AddGroup("fishing", 2));
hn:AddToggle({
	text = "auto fish",
	flag = "autofish",
	default = false,
	callback = function(D)
		m.AutoFish = D;
		x.fishSync();
	end,
});
hn:AddSlider({
	text = "bar lead",
	flag = "fishlead",
	min = 0,
	max = .4,
	step = .01,
	default = .12,
	suffix = "s",
	callback = function(D)
		m.FishLead = D;
	end,
});
hn:AddSlider({
	text = "no bite recast",
	flag = "fishtimeout",
	min = 10,
	max = 60,
	step = 1,
	default = 30,
	suffix = "s",
	callback = function(D)
		m.FishTimeout = D;
	end,
});
function x.panic()
	m.Mode = "off";
	m.AutoTrain = false;
	m.AutoFish = false;
	pcall(function()
		G:SetValue("autofarm", false);
		G:SetValue("autotrain", false);
		G:SetValue("autofish", false);
	end);
	pcall(x.fishSync);
	pcall(x.skillsRelease);
	pcall(function()
		keyrelease(84);
	end);
	m.cur = nil;
	x.aimRelease();
	pcall(x.skillsRelease);
	m.pendingLoot = nil;
	m.lootPhase = nil;
	m.follow = nil;
	m.dlg = nil;
	m.soul = nil;
	m.crow = nil;
	x.routeStop();
	x.collectStop();
	x.dropCache();
	x.sync();
	x.notify("stopped", 3);
end;
local Pn = {
		farm = 0,
		esp = 0,
		chest = 0,
		train = 0,
		clip = 0,
		afk = 0,
		zx = 0,
		guard = 0,
		key = 0,
		soul = 0,
	};
local function mn(D)
	if m.Mode == "off" then
		return;
	end;
	local Q, f, h = x.char();
	if not Q or h.Health <= 0 then
		return;
	end;
	if m.HealthGuard and h.Health < h.MaxHealth * m.GuardPct then
		if D - Pn.guard > 1 then
			Pn.guard = D;
			pcall(function()
				f.CFrame = f.CFrame + Vector3.new(0, 60, 0);
				f.AssemblyLinearVelocity = Vector3.new(0, 0, 0);
			end);
		end;
		return;
	end;
	local P, C;
	if m.Mode == "mob" then
		P, C = m.SelectedMob, false;
	elseif m.Mode == "crow" then
		local Q = x.crowStep(D);
		if not Q then
			if m.cur then
				m.cur = nil;
				x.aimRelease();
			end;
			return;
		end;
		P, C = Q, true;
	elseif m.Mode == "boss" then
		local D = x.bossSet();
		P, C = (D or "All Bosses"), true;
	else
		local Q = x.activeQuest();
		if not Q then
			if m.questInfoKey then
				m.lastDoneKey = m.questInfoKey;
				m.questInfoKey = nil;
			end;
			local Q, f = x.pickQuest(D);
			if not Q then
				local Q, f = x.pickQuest(D);
				if m.cur then
					m.cur = nil;
					x.aimRelease();
				end;
				if D - ((Pn.qwarn or 0)) > 12 then
					Pn.qwarn = D;
					if f then
						x.notify("ticked quests cooling down - retry in " .. (math.ceil(f - D) .. "s"), 5);
					else
						x.notify("no quest to take - tick some in the quest list (or they are all done)", 6);
					end;
				end;
				return;
			end;
			if m.acceptKey ~= f then
				m.acceptKey = f;
				m.acceptTries = 0;
				m.acceptAt = nil;
				m.atGiver = false;
			end;
			if Q and Q.AutoDeliver then
				local f, u = x.pickDelivery(D);
				if not f then
					if m.cur then
						m.cur = nil;
						x.aimRelease();
					end;
					if u then
						for Q, f in ipairs(S.DeliverGivers) do
							if not x.questCompleted(f.QuestOption) then
								x.followNpc(f.Npc, f.Pos, D);
								break;
							end;
						end;
					end;
					if D - ((Pn.qwarn or 0)) > 15 then
						Pn.qwarn = D;
						if u then
							x.notify("delivery quests cooling down - retry in " .. (math.ceil(u - D) .. "s"), 5);
						else
							x.notify("every delivery quest is done", 6);
						end;
					end;
					return;
				end;
				if m.delivGiver ~= f.QuestOption then
					m.delivGiver = f.QuestOption;
					m.acceptTries = 0;
					m.acceptAt = nil;
					m.atGiver = false;
				end;
				if not ((m.delivSent and m.delivSent[f.QuestOption])) then
					m.acceptTries = 0;
				end;
				Q = f;
			end;
			if not ((Q and Q.Pos)) then
				if D - ((Pn.qwarn or 0)) > 8 then
					Pn.qwarn = D;
					x.notify("no active quest - pick one in the quest list to accept it", 5);
				end;
				return;
			end;
			if m.cur then
				m.cur = nil;
				x.aimRelease();
				pcall(x.skillsRelease);
				if x.clickRelease then
					pcall(x.clickRelease);
				end;
			end;
			local u = x.followNpc(Q.Npc, Q.Pos, D);
			local h = m.atGiver and 12 or 8;
			if u > h then
				m.atGiver = false;
				m.acceptAt = nil;
				return;
			end;
			m.atGiver = true;
			local P = x.questOpt(Q);
			if not P then
				if D - ((Pn.qwarn or 0)) > 8 then
					Pn.qwarn = D;
					x.notify("at " .. (tostring(Q.Npc) .. (": this quest has no " .. (m.QuestPick .. " option"))), 5);
				end;
				return;
			end;
			if x.questCompleted(P) then
				if D - ((Pn.qwarn or 0)) > 10 then
					Pn.qwarn = D;
					x.notify("already completed: " .. (P .. " - pick another quest"), 6);
				end;
				return;
			end;
			m.acceptAt = m.acceptAt or (D + m.SettleT);
			if D < m.acceptAt then
				return;
			end;
			m.hadQuest = false;
			local C = m.lastAcceptAt and (D - m.lastAcceptAt);
			if not C and Q.QuestOption then
				local D = x.questLastAge();
				if D then
					C = D;
				end;
			end;
			if C and C < m.QuestGap then
				if D - ((Pn.qwarn or 0)) > 10 then
					Pn.qwarn = D;
					x.notify("next quest in " .. (math.ceil(m.QuestGap - C) .. "s"), 3);
				end;
				return;
			end;
			if not x.remote then
				local f = x.dlgTalk(P, D);
				if f == "failed" and D - ((Pn.qwarn or 0)) > 12 then
					Pn.qwarn = D;
					x.notify("could not take \'" .. (P .. ("\' from " .. (tostring(Q.Npc) .. " - do it by hand"))), 6);
				end;
				return;
			end;
			if x.remote and (D - ((Pn.accept or 0))) > 8 then
				Pn.accept = D;
				m.acceptTries = ((m.acceptTries or 0)) + 1;
				if m.acceptTries > 3 then
					if D - ((Pn.qwarn or 0)) > 10 then
						Pn.qwarn = D;
						x.notify("server keeps refusing \'" .. (P .. "\' (level or quest limit?) - skipping it"), 6);
					end;
					if f then
						m.qBack = m.qBack or {};
						m.qBack[f] = D + 120;
					end;
					m.acceptTries = 0;
					return;
				end;
				pcall(function()
					x.remote:FireServer("AddQuest", P);
				end);
				if f and not Q.AutoDeliver then
					m.qSent = m.qSent or {};
					m.qSent[f] = m.qSent[f] or D;
				end;
				if Q.QuestOption == P and S.DeliverGivers then
					m.delivSent = m.delivSent or {};
					m.delivSent[P] = m.delivSent[P] or D;
				end;
				x.notify("accepting: " .. P, 3);
			end;
			return;
		end;
		m.acceptAt = nil;
		m.acceptTries = 0;
		m.atGiver = false;
		m.dlg = nil;
		if not m.hadQuest then
			m.lastAcceptAt = D;
		end;
		m.hadQuest = true;
		if Q.Desc and m.delivSent then
			m.delivSent[Q.Desc] = nil;
			if m.delivFails then
				m.delivFails[Q.Desc] = nil;
			end;
		end;
		local h, I = x.entryForQuest(Q);
		m.questInfo = h;
		m.questDesc = Q.Desc;
		m.questInfoKey = I;
		if I and m.qSent then
			m.qSent[I] = nil;
		end;
		if I and m.qBack then
			m.qBack[I] = nil;
		end;
		if Q.Complete then
			if m.cur then
				m.cur = nil;
				x.aimRelease();
				pcall(x.skillsRelease);
				if x.clickRelease then
					pcall(x.clickRelease);
				end;
			end;
			local Q = h;
			if Q and Q.Pos then
				x.followNpc(Q.Npc, Q.Pos, D);
			end;
			return;
		end;
		local U = h;
		local Y;
		for D = 1, #Q.Tasks, 1 do
			if ((Q.Tasks[D].Value or 0)) < ((Q.Tasks[D].Max or 1)) then
				Y = Q.Tasks[D];
				break;
			end;
		end;
		local R = S.Deliver[Q.Name];
		local i = R and (Y and R.tasks[Y.Name]);
		if i then
			x.deliverStep(R.opt, Y.Name, i, f, D);
			return;
		end;
		m.deliverKey = nil;
		local J = x.taskKind(Y and Y.Name);
		P = nil;
		if J ~= "fetch" then
			local D = {};
			local function f(Q)
				if type(Q) == "string" and Q ~= "" then
					D[string.lower(Q)] = true;
				end;
			end;
			f(x.codeToMob(Y and Y.Code));
			f(x.questTarget(Q));
			if J == "kill" and (U and not U.Auto) then
				local D = Q.Desc and U.BossOption == Q.Desc;
				if D == nil then
					D = (m.QuestPick == "boss");
				end;
				f(D and ((U.BossTarget or U.Target)) or U.Target);
			end;
			local u = 0;
			for D in pairs(D) do
				u = u + 1;
			end;
			if u > 1 then
				P = D;
			elseif u == 1 then
				for D in pairs(D) do
					P = D;
				end;
			end;
		end;
		if not P then
			local D = x.questPickups();
			if #D > 0 then
				local Q = {};
				for f = 1, #D, 1 do
					Q[#Q + 1] = x.pickup(u, function()
							return u;
						end, D[f].Node, D[f].Root, nil, { tap = true });
				end;
				x.collectStart(Q, "quest pickup: " .. D[1].Name);
				return;
			end;
			if J == "kill" and (U and (not U.Auto and U.Target)) then
				P = U.Target;
			end;
		end;
		if not P and J == "fetch" then
			if D - ((Pn.qwarn or 0)) > 10 then
				Pn.qwarn = D;
				x.notify("\'" .. (tostring(Y and Y.Name) .. "\' is not a kill objective and no prop is spawned - nothing to farm"), 6);
			end;
			return;
		end;
		C = false;
		if not P then
			if D - ((Pn.qwarn or 0)) > 8 then
				Pn.qwarn = D;
				x.notify("nothing to farm or pick up in: " .. tostring(Q.Name), 5);
			end;
			return;
		end;
	end;
	local I = (type(P) == "table") and x.setKey(P) or tostring(P);
	local U = m.cur;
	if U and U.wantKey == I then
		local Q = nil;
		local f = x.npcAlive(U) and pcall(function()
				Q = U.Hum.Health;
			end);
		if not U.stale and (((not f or not Q or Q <= 0)) and (m.LootBoss and (U.Boss and U.lastPos))) then
			m.pendingLoot = { pos = U.lastPos, t = D };
		end;
		if Q and Q > 0 then
			if U.lastHp == nil or Q < U.lastHp then
				U.lastHp = Q;
				U.t = D;
			end;
			pcall(function()
				U.lastPos = U.Root.Position;
			end);
			if (D - U.t) < 8.0 then
				x.attack(D);
				return;
			end;
			x.notify("target stuck, re-picking", 2);
		end;
		m.cur = nil;
		x.dropCache();
	end;
	local Y = x.scanCached(P, C, D, .5, m.Mode == "boss" or m.Mode == "crow");
	if #Y == 0 and m.Mode == "quest" then
		local D = m.questInfo;
		local Q = D and (D.BossOption and m.questDesc == D.BossOption);
		if D and (D.Alt and not Q) then
			Y = x.scan(D.Alt, false);
		end;
	end;
	if #Y == 0 then
		if D - Pn.guard > m.Retarget * 3 then
			Pn.guard = D;
			local Q = x.wantSpawn(P);
			if Q then
				x.tpTo(Q, 3);
			elseif D - ((Pn.nodest or 0)) > 12 then
				Pn.nodest = D;
				local Q = (type(P) == "table") and x.bossLabel() or tostring(P);
				x.notify("no known spot for " .. (Q .. " - find it once and it is remembered"), 6);
			end;
		end;
		return;
	end;
	local R = x.nearest(Y, f.Position);
	if not R then
		x.dropCache();
		return;
	end;
	R.want = P;
	R.wantKey = I;
	R.t = D;
	if R.Head == nil then
		R.MAddr = x.addr(R.Model);
		pcall(function()
			R.Head = R.Model:FindFirstChild("Head") or false;
		end);
	end;
	m.cur = R;
	x.attack(D);
end;
local function Sn()
	if not m.Running then
		return;
	end;
	local D = os.clock();
	x.mark("tick");
	x.markWrite(D);
	if m.AutoSouls and not m.route then
		x.mark("souls");
		local Q, f = pcall(x.soulStep, D);
		if Q and f then
			return;
		end;
	end;
	if m.route then
		x.mark("route");
		local Q, f = pcall(x.routeStep, D);
		if Q and f then
			return;
		end;
		if not Q then
			x.routeStop();
		end;
	end;
	if m.collect then
		x.mark("collect");
		if pcall(x.collectStep, D) then
			return;
		end;
	end;
	if ((m.AutoLoot or m.AutoSouls)) and (not m.collect and (not m.lootPhase and (not m.route and (D - ((Pn.loot or 0))) > 2.0))) then
		Pn.loot = D;
		local Q, f = x.char();
		if f then
			if m.AutoLoot then
				local D = x.lootNear(f.Position, m.LootRange);
				if #D > 0 then
					x.collectStart(D, "loot");
				end;
			end;
		end;
	end;
	if m.pendingLoot and (D - m.pendingLoot.t) > 1.0 then
		m.lootPhase = { pos = m.pendingLoot.pos, t0 = D, last = D };
		m.pendingLoot = nil;
		x.notify("boss down, collecting drops", 3);
	end;
	if m.lootPhase then
		x.mark("bossloot");
		local Q = m.lootPhase;
		if D - ((Q.scanT or 0)) < .25 then
			x.travelTo(Q.pos, D);
			return;
		end;
		Q.scanT = D;
		local f = x.lootNear(Q.pos, 90);
		local u, h = {}, {};
		for D = 1, #f, 1 do
			if f[D].tap then
				u[#u + 1] = f[D];
			else
				h[#h + 1] = f[D];
			end;
		end;
		if #u > 0 or #h > 0 then
			Q.sawChest = true;
		end;
		if #u > 0 then
			Q.last = D;
			x.collectStart(u, "boss chest");
			return;
		end;
		if #h > 0 and ((Q.passes or 0)) < 4 then
			Q.last = D;
			Q.passes = ((Q.passes or 0)) + 1;
			if x.routeStart(h) then
				return;
			end;
		end;
		local P = (D - Q.last) > m.LootWait;
		if not Q.sawChest then
			P = (D - Q.t0) > 25;
		end;
		if (D - Q.t0) > 60 then
			P = true;
		end;
		if P then
			if not Q.sawChest then
				x.notify("no boss chest appeared, moving on", 4);
			end;
			m.lootPhase = nil;
			x.notify("drops done, next boss", 2);
		else
			x.travelTo(Q.pos, D);
			return;
		end;
	end;
	if D - Pn.key >= .1 then
		Pn.key = D;
		if iskeypressed and iskeypressed(m.PanicVK) then
			if m.Mode ~= "off" or m.AutoTrain or m.collect then
				x.panic();
			end;
		end;
	end;
	pcall(x.dlgRelease, D);
	if m.follow and not m.cur then
		pcall(x.followStep, D);
	end;
	if m.Mode ~= "off" and (m.cur and x.npcAlive(m.cur)) then
		pcall(x.steer);
		local Q;
		pcall(function()
			local D = m.cur;
			local f = D.Head;
			if f and not x.attached(f, D.MAddr) then
				f = nil;
				D.Head = false;
			end;
			Q = (f and f.Position) or D.Root.Position;
		end);
		pcall(x.aimStep, Q);
		local f, u = x.char();
		if u and Q then
			local f = ((Q - u.Position)).Magnitude;
			pcall(x.skillStep, D, f);
		end;
	else
		x.aimRelease();
		pcall(x.skillsRelease);
	end;
	if D - Pn.farm >= m.AttackDelay then
		Pn.farm = D;
		x.mark("farm/" .. tostring(m.Mode));
		pcall(mn, D);
	end;
	if x.espOn() and D - Pn.esp >= .35 then
		Pn.esp = D;
		x.mark("espbuild");
		pcall(x.espBuild);
	end;
	if m.AutoTrain and (not m.AutoFish and (not m.collect and D - Pn.train >= 1.5)) then
		Pn.train = D;
		pcall(function()
			local f = Q:FindFirstChild("Player_Service");
			local u = f and f:FindFirstChild("Values");
			local P = u and u:FindFirstChild(h.Name);
			if not P then
				return;
			end;
			if P:FindFirstChild("Training") then
				if x.remote then
					x.remote:FireServer("training_signaler", "Stop", true);
				else
					m.trainSince = m.trainSince or D;
					local Q = m.bar;
					if not ((Q and Q.active)) and (D - m.trainSince > 5 and D - ((m.trainWarn or 0)) > 30) then
						m.trainWarn = D;
						x.notify("click mode only plays the Squat bar - finish this station yourself", 6);
					end;
				end;
			else
				m.trainSince = nil;
			end;
		end);
	end;
	if m.NoClip and D - Pn.clip >= .25 then
		Pn.clip = D;
		pcall(function()
			local D = h.Character;
			if not D then
				return;
			end;
			local Q = D:GetChildren();
			for D = 1, #Q, 1 do
				local f = Q[D];
				if ((f.ClassName == "Part" or f.ClassName == "MeshPart")) and f.Name ~= "HumanoidRootPart" then
					f.CanCollide = false;
				end;
			end;
		end);
	end;
	if m.Mode ~= "off" and (D - ((Pn.survey or 0))) > 10 then
		Pn.survey = D;
		pcall(function()
			x.scan(nil, nil);
		end);
	end;
	pcall(x.seenSave, D);
	if m.AntiAFK and D - Pn.afk >= 120 then
		Pn.afk = D;
		pcall(function()
			mousemoverel(2, 0);
			mousemoverel(-2, 0);
		end);
	end;
end;
function x.busy()
	return m.Mode ~= "off" or m.collect ~= nil or m.route ~= nil or m.lootPhase ~= nil or m.AutoLoot or m.AutoSouls or m.AutoTrain or m.NoClip or m.AntiAFK or x.espOn();
end;
function x.sync()
	x.espSync();
	pcall(x.fishSync);
	pcall(x.syncSkills);
	if x.busy() then
		if not m.tickConn then
			m.tickConn = f.Heartbeat:Connect(function()
					local D, Q = pcall(Sn);
					if not D then
						x.logErr("tick", Q);
					end;
				end);
		end;
	elseif m.tickConn then
		pcall(function()
			m.tickConn:Disconnect();
		end);
		m.tickConn = nil;
		x.aimRelease();
		pcall(x.skillsRelease);
		m.cur = nil;
		x.dropCache();
	end;
end;
pcall(x.markPrev);
pcall(x.seenLoad);
pcall(function()
	G:LoadSettings();
end);
x.sync();
x.notify("loaded - rshift for the menu", 4);
print("[Slayers 2] loaded");
