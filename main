local h = game:GetService("Players") or game.Players;
local Y = game:GetService("ReplicatedStorage") or game.ReplicatedStorage;
local c = game:GetService("RunService") or game.RunService;
local D = workspace;
local A = h.LocalPlayer;
if _G.Slayers2 then
	local h = _G.Slayers2;
	_G.Slayers2 = nil;
	h.Running = false;
	pcall(function()
		if h.drawConn then
			h.drawConn:Disconnect();
		end;
	end);
	pcall(function()
		if h.tickConn then
			h.tickConn:Disconnect();
		end;
	end);
	pcall(function()
		if h.fishConn then
			h.fishConn:Disconnect();
		end;
	end);
	pcall(function()
		if h.bmConn then
			h.bmConn:Disconnect();
		end;
	end);
	pcall(function()
		if h.deferConn then
			h.deferConn:Disconnect();
		end;
	end);
	h.fishConn, h.tickConn, h.drawConn, h.bmConn = nil, nil, nil, nil;
	h.deferConn = nil;
	h.fish, h.bar, h.route, h.collect, h.follow = nil, nil, nil, nil, nil;
	h.soul, h.dlg, h.dlgClick, h.crow, h.lootPhase = nil, nil, nil, nil, nil;
	pcall(function()
		mouse1release();
	end);
	pcall(function()
		mouse2release();
	end);
	pcall(function()
		keyrelease(84);
	end);
	h.pool = nil;
	if true then
 
	end;
	pcall(function()
		if h.win then
			h.win:Destroy();
		end;
	end);
end;
local l;
do
	local h = rawget(_G, "UILib");
	if type(h) == "table" then
		local Y = tonumber(rawget(h, "_now"));
		if Y and ((tick() - Y) > 3 and type(rawget(h, "_rehome")) ~= "function") then
			_G.UI_RELOAD = true;
		end;
	end;
	local Y;
	pcall(function()
		Y = readfile("UILib.lua");
	end);
	if type(Y) ~= "string" or #Y < 1000 then
		Y = nil;
		pcall(function()
			Y = game:HttpGet("https://raw.githubusercontent.com/Shoobie-code/linoria-inspired/refs/heads/main/main");
		end);
		if type(Y) == "string" and #Y > 1000 then
			pcall(writefile, "UILib.lua", Y);
		else
			Y = nil;
		end;
	end;
	if type(Y) == "string" then
		pcall(function()
			(loadstring(Y))();
		end);
	end;
	_G.UI_RELOAD = nil;
	l = _G.UILib;
end;
if not l then
	notify("UILib not found in workspace", "Slayers 2", 6);
	return;
end;
local I = {
		Running = true,
		pool = {},
		drawConn = nil,
		tickConn = nil,
		win = nil,
		Mode = "off",
		FarmKind = "boss",
		CrowSlot = 2,
		WeapSlot = 1,
		crow = nil,
		SelectedMob = "Bandit",
		SelectedBoss = { Akazo = true },
		SelectedQuest = "[Auto-Detect Active Quest]",
		QuestSel = { "[Auto-Detect Active Quest]" },
		Stance = "Under Target",
		Offset = 3.8,
		AttackDelay = .3,
		Retarget = 1.0,
		HealthGuard = false,
		GuardPct = .45,
		Smooth = true,
		MoveSpeed = 60,
		WarpAbove = 40,
		camT = 0,
		AimLock = true,
		SkillGap = .3,
		atkCount = 0,
		SkipBlocking = true,
		Reach = 18,
		blockedT = 0,
		AimGain = .55,
		rmbDown = false,
		HoldT = 2.0,
		SettleT = .6,
		AutoLoot = true,
		AutoSouls = true,
		LootRange = 300,
		LootWait = 5,
		lootPhase = nil,
		LootBoss = true,
		RouteTap = .05,
		collect = nil,
		pendingLoot = nil,
		seen = {},
		seenDirty = false,
		seenT = 0,
		chestTap = {},
		clearKey = nil,
		warnedFocus = 0,
		AutoTrain = false,
		NoClip = false,
		AntiAFK = false,
		EspMobs = false,
		EspBosses = false,
		EspChests = false,
		EspCrystals = false,
		EspLily = false,
		EspHorses = false,
		EspLevers = false,
		EspMuzan = false,
		EspFruits = false,
		QuestPick = "normal",
		QuestGap = 30,
		AutoFish = false,
		FishLead = .12,
		FishPause = 1.0,
		FishTimeout = 30,
		BossOrder = { "Akazo" },
		npcPosCache = {},
		acceptAt = nil,
		EspDist = 1200,
		PanicVK = 113,
	};
_G.Slayers2 = I;
local f = {};
f.Presets = {
		Combat = true,
		["Regular Katana"] = true,
		["Insect Katana"] = true,
		["Sound Katanas"] = true,
		Scythe = true,
		["Bladed Wagasa"] = true,
		Gauntlet = true,
		Soryu = true,
		["Tai Chi"] = true,
		Spear = true,
		Tanto = true,
		["War Fans"] = true,
		Shotgun = true,
		Sickles = true,
		Claws = true,
		["Blood Manipulation"] = true,
		Cutlass = true,
		["Axe and Mace"] = true,
		Reaper = true,
		["Obi Manipulation"] = true,
		Tamari = true,
		Dream = true,
	};
do
	local h = Y:FindFirstChild("Assets");
	h = h and h:FindFirstChild("Animations");
	for h, Y in ipairs(h and h:GetChildren() or {}) do
		local c = string.match(Y.Name, "^(.*)_Combat_Anims$");
		if c and c ~= "" then
			f.Presets[c] = true;
		end;
	end;
end;
f.StyleWords = {
		cleaver = "Sound Katanas",
		fan = "War Fans",
		sickle = "Sickles",
		claw = "Claws",
		wagasa = "Bladed Wagasa",
	};
f.StyleMap = {};
do
	local h = {
			Gauntlets = "Gauntlet",
			Warfans = "War Fans",
			["Axe & Mace"] = "Axe and Mace",
			Katanas = "Regular Katana",
		};
	local function c(Y)
		local c = string.gsub(Y, "%s*Category%s*$", "");
		c = string.gsub(c, "^%s*(.-)%s*$", "%1");
		c = h[c] or c;
		if f.Presets[c] then
			return c;
		end;
		return nil;
	end;
	local D = Y:FindFirstChild("ItemAssets");
	D = D and D:FindFirstChild("Tools");
	for h, Y in ipairs(D and D:GetChildren() or {}) do
		local D = c(Y.Name);
		for h, Y in ipairs(Y:GetChildren()) do
			local c = Y.Name;
			local A = string.lower(c);
			local l;
			if f.Presets[c] then
				l = c;
			elseif string.find(A, "sound", 1, true) or string.find(A, "cleaver", 1, true) then
				l = "Sound Katanas";
			elseif string.find(A, "insect", 1, true) then
				l = "Insect Katana";
			else
				local h = string.match(c, "[-]%s*(.+)$");
				local Y = string.match(c, "^(.+)%s+Orb$");
				if h and f.Presets[h] then
					l = h;
				elseif Y and f.Presets[Y] then
					l = Y;
				else
					l = D;
				end;
			end;
			if l and f.Presets[l] then
				f.StyleMap[A] = l;
			end;
		end;
	end;
end;
do
	local h = Y:FindFirstChild("CAM");
	h = h and h:FindFirstChild("Global");
	h = h and h:FindFirstChild("Powers");
	h = h and h:FindFirstChild("FightingStyles");
	if h then
		for h, Y in ipairs(h:GetChildren()) do
			f.Presets[Y.Name] = true;
		end;
	end;
end;
f.Spawns = {
		Bandit = Vector3.new(-287, 1224, -1008),
		Zuko = Vector3.new(-450, 1245, -950),
		Civilian = Vector3.new(-650, 1245, -1100),
		["*Civilian*"] = Vector3.new(-650, 1245, -1100),
		Mizunoto = Vector3.new(-888, 965, -26),
		["Beast Born Demon"] = Vector3.new(-850, 970, -120),
		["Blood Hounded Demon"] = Vector3.new(-870, 970, -100),
		["Bear Cub"] = Vector3.new(540, 1122, -1023),
		["Mother Bear"] = Vector3.new(550, 1122, -1035),
		Hoyuzo = Vector3.new(480, 1122, -1010),
		["Hoyuzo Subordinate"] = Vector3.new(470, 1122, -1010),
		Kaiden = Vector3.new(510, 1122, -1000),
		["Kaiden Subordinate"] = Vector3.new(500, 1122, -1000),
		["Lesser Demon"] = Vector3.new(-715, 220, 456),
		["Greater Demon"] = Vector3.new(-650, 235, 380),
		["High Demon"] = Vector3.new(518, 1223, -1785),
		["Ice Profound Demon"] = Vector3.new(312, 1223, -1778),
		["Fire Profound Demon"] = Vector3.new(230, 1254, -1983),
		["Kanoe Demon Slayer"] = Vector3.new(373, 1286, -2031),
		["Mizunoe Demon Slayer"] = Vector3.new(-2626.7, 284, -191.2),
		Fujiko = Vector3.new(-2626.7, 284, -191.2),
		Akazo = Vector3.new(-1132.6, 1381.1, -1742.5),
		Gyutai = Vector3.new(-263.1, 1043.5, -1150.5),
		Datai = Vector3.new(-163.2, 1043.5, -1136.9),
		Saneri = Vector3.new(-375.2, 1093.6, -438.8),
		Obari = Vector3.new(742.85, 1124.07, -1018.62),
		["Serpent Trainee"] = Vector3.new(-270.3, 1292.49, -1533.8),
		Reaper = Vector3.new(99.86, 1043.49, -576.31),
		Yahari = Vector3.new(823.2, 1019.1, -640.0),
		Sumari = Vector3.new(395.4, 1018.5, -622.7),
		Nezura = Vector3.new(-1456.63, 278.45, 927.69),
		Muzan = Vector3.new(2466.37, 1079.63, 2334.54),
	};
f.Mobs = {
		"All Regular Mobs",
		"Bandit",
		"Civilian",
		"*Civilian*",
		"Mizunoto",
		"Village Spy",
		"Karu Village Bandit",
		"Beast Born Demon",
		"Blood Hounded Demon",
		"Lesser Demon",
		"Greater Demon",
		"High Demon",
		"Ice Profound Demon",
		"Fire Profound Demon",
		"Rogue Demon",
		"Lost",
		"Iceveil Road Pikeman",
		"Iceveil Road Bandit",
		"Iceveil Road Marauder",
		"Bear Cub",
		"Mother Bear",
		"Hoyuzo Subordinate",
		"Kaiden Subordinate",
		"Kanoe Demon Slayer",
		"Mizunoe Demon Slayer",
		"Cache Lancer",
		"Cache Prowler",
		"Grove Raider",
		"Lancer Captain",
		"Prowler Captain",
		"Raid Captain",
		"Serpent Trainee",
		"Water Trainee",
		"Thunder Trainee",
		"Flame Trainee",
		"Wind Trainee",
		"Stone Trainee",
		"Insect Trainee",
		"Sound Trainee",
		"Soryu Trainee",
		"Tai Chi Trainee",
		"Reaper Trainee",
	};
f.Bosses = {
		"All Bosses",
		"Akazo",
		"Gyutai",
		"Datai",
		"Domae",
		"Reaper",
		"Nezura",
		"Yahari",
		"Sumari",
		"Enru",
		"Yeti Demon",
		"Small Yeti",
		"Hand Demon",
		"Zuko",
		"Kaiden",
		"Hoyuzo",
		"Mother Bear",
		"Fujiko",
		"Shinora",
		"Obari",
		"Saneri",
		"Giyen",
		"Tengai",
		"Zentaro",
		"Gyorei",
		"Rengu",
		"Muzan",
	};
f.BossNames = {};
for h = 1, #f.Bosses, 1 do
	f.BossNames[string.lower(f.Bosses[h])] = true;
end;
f.BossNames["all bosses"] = nil;
f.Zones = {
		["Windy Peak (Village)"] = Vector3.new(-650, 1245, -1100),
		["Windy Peak (Bandit Camp)"] = Vector3.new(-287, 1224, -1008),
		["Mistfall Harbor (Docks)"] = Vector3.new(-888, 965, -26),
		["Mistfall Harbor (Crystal)"] = Vector3.new(135.8, 873.7, 734.1),
		["Bamboo Grove (Bear Woods)"] = Vector3.new(540, 1122, -1023),
		["Bamboo Grove Sanctuary"] = Vector3.new(657.4, 1021.2, 139.7),
		["Butterfly Estate"] = Vector3.new(-1809.3, 311.8, -101.1),
		["Hidden Mist Village"] = Vector3.new(1793.08, 661.3, -231.72),
		["Iceveil Valley"] = Vector3.new(485.3, 1222.9, -1813.4),
		["Iceveil Settlement"] = Vector3.new(-154.62, 1351.5, -2556.21),
		["Final Selection Plains"] = Vector3.new(-2626.7, 284, -191.2),
		["Workout Grounds"] = Vector3.new(-311.6, 1071.6, -579.6),
	};
f.BossTP = {
		Akazo = Vector3.new(-1132.6, 1381.1, -1742.5),
		Gyutai = Vector3.new(-263.1, 1043.5, -1150.5),
		Datai = Vector3.new(-163.2, 1043.5, -1136.9),
		Saneri = Vector3.new(-383.03, 1096.07, -405.25),
		Zuko = Vector3.new(-288.51, 1226.7, -1027.99),
		Kaiden = Vector3.new(558.56, 1148.67, -1322.1),
		Hoyuzo = Vector3.new(758.57, 1003.51, -1406.92),
		["Mother Bear"] = Vector3.new(550, 1122, -1035),
		Fujiko = Vector3.new(-2626.7, 284, -191.2),
		["Hand Demon"] = Vector3.new(-2626.7, 284, -191.2),
		["Yeti Demon"] = Vector3.new(485.3, 1222.9, -1813.4),
		["Small Yeti"] = Vector3.new(485.3, 1222.9, -1813.4),
		Domae = Vector3.new(-297.34, 1352.99, -3448.14),
		Reaper = Vector3.new(104.73, 1045.5, -579.95),
		Enru = Vector3.new(819.73, 798.15, 557.06),
		Shinora = Vector3.new(-463.59, 967.0, -7.85),
		Obari = Vector3.new(742.85, 1124.07, -1018.62),
		Giyen = Vector3.new(405.07, 1020.5, -74.58),
		Tengai = Vector3.new(-132.74, 1351.48, -2616.27),
		Zentaro = Vector3.new(1329.34, 823.5, -1022.86),
		Gyorei = Vector3.new(2569.72, 1091.5, -730.2),
		Nezura = Vector3.new(-1456.63, 278.45, 927.69),
		Yahari = Vector3.new(809.68, 1021.01, -632.32),
		Sumari = Vector3.new(400.17, 1020.5, -596.86),
		["Serpent Trainee"] = Vector3.new(-270.3, 1292.49, -1533.8),
		["Reaper Trainee Kuzan"] = Vector3.new(-1231.91, 1376.61, -3013.45),
		Muzan = Vector3.new(2466.37, 1079.63, 2334.54),
	};
f.Trainers = {
		["Fisherman Jeso"] = Vector3.new(-192.0, 807.0, 602.7),
		["Alchemist Meku"] = Vector3.new(-138.1, 798.9, 518.7),
		["Dock Master Sofen"] = Vector3.new(-160.8, 798.8, 703.3),
		["Estate Worker Niko"] = Vector3.new(341.1, 939.0, 580.6),
		Ginzo = Vector3.new(273.8, 944.0, 528.2),
		Elara = Vector3.new(427.8, 943.6, 507.4),
		Jugg = Vector3.new(487.7, 876.5, 1007.8),
		Liv = Vector3.new(657.4, 1021.2, 139.7),
		["Serpent Trainer Obari"] = Vector3.new(37.0, 1307.5, -1179.5),
		["Soryu Trainee Goki"] = Vector3.new(-424.5, 289.1, 540.4),
		["Lamplighter Isamu"] = Vector3.new(1082.2, 1425.7, -749.0),
	};
f.Stations = {
		"Boulder Push",
		"Squat Rack",
		"Aim Training",
		"Boulder Split",
		"Cup Game",
		"Meditation",
		"Pushups",
		"Parkour Dungeon",
	};
f.Deliver = {
		["Deliver Niko\'s Supply Box"] = { opt = "Ill deliver the supply box(Lv 70)", tasks = { ["Deliver to Shiori"] = { npc = "Shiori", pos = Vector3.new(-1814.34, 311.8, -101.07), answer = "Niko sent it up" }, ["Report back to Niko"] = { npc = "Estate Worker Niko", pos = Vector3.new(341.07, 938.91, 580.59), answer = "Shiori says thank you" } } },
		["Restock the Infirmary"] = { opt = "Ill restock the infirmary(Lv 70)", tasks = { ["Return to Shiori"] = { npc = "Shiori", pos = Vector3.new(-1814.34, 311.8, -101.07), answer = "The infirmary is stocked" } } },
		["The Full Pantry"] = { opt = "Ill stock the reserves(Lv 75)", tasks = { ["Return to Shiori"] = { npc = "Shiori", pos = Vector3.new(-1814.34, 311.8, -101.07), answer = "The stores are stocked" } } },
		["Locate Ren\'s Lost Nichirin"] = { opt = "Ill look for your blade(Lv 75)", tasks = { ["Return to Ren"] = { npc = "Ren", pos = Vector3.new(-1795, 311.8, -85) } } },
		["The Morning Haul"] = { opt = "Ill fill your crates(Lv 45)", tasks = { ["Return to Runo"] = { npc = "Angler Runo", pos = Vector3.new(-561.1, 796.25, 683.7) } } },
		["The Good Catch"] = { opt = "Ill land the good catch(Lv 60)", tasks = { ["Return to Runo"] = { npc = "Angler Runo", pos = Vector3.new(-561.1, 796.25, 683.7) } } },
		["Retrieve Ginzo\'s Jewelry Box"] = { opt = "Ill find the jewelry box(Lv 45)", tasks = { ["Return to Ginzo"] = { npc = "Ginzo", pos = Vector3.new(273.8, 941.5, 528.19) } } },
		["Earn a Fishing Permit"] = { opt = "Ill find the permit stamp(Lv 45)", tasks = { ["Return to Sofen"] = { npc = "Dock Master Sofen", pos = Vector3.new(-160.81, 796.25, 703.29) } } },
		["Acquire Bear Meat"] = { opt = "Ill restock the pantry(Lv 10)", tasks = { ["Bring bear meat back to Lucy"] = { npc = "Lucy", pos = Vector3.new(-615.5, 1258.5, -1177.5), answer = "Hand over the meat" } } },
		["Deliver Package to Elara"] = { opt = "Ill deliver the package", tasks = { ["Package delivered"] = { npc = "Elara", pos = Vector3.new(427.82, 941.0, 507.45) } } },
		["Report to Noote"] = { opt = "Ill bring him the notes", tasks = { ["Speak with Noote"] = { npc = "Noote", pos = Vector3.new(-515.5, 1243.2, -1251.2) } } },
		["Deliver the Coded Letter"] = { opt = "Ill get this letter delivered", tasks = { ["Deliver the Letter to Chaka"] = { npc = "Chaka", pos = Vector3.new(471, 1146, -1260) } } },
	};
f.DeliverGivers = { { Npc = "Estate Worker Niko", Pos = Vector3.new(341.1, 938.9, 580.6), QuestOption = "Ill deliver the supply box(Lv 70)" }, { Npc = "MoldySugar", Pos = Vector3.new(-701.8, 1243.3, -982.7), QuestOption = "Ill deliver the package" }, { Npc = "Noote", Pos = Vector3.new(-515.5, 1243.2, -1251.2), QuestOption = "Ill get this letter delivered" } };
f.Quests = {
		["[Auto-Detect Active Quest]"] = { Auto = true },
		["[Auto] All delivery quests"] = { Auto = true, AutoDeliver = true },
		["[Windy Peak] Krue: 3 Bandits"] = {
			Npc = "Krue",
			Pos = Vector3.new(-425.9, 1240.7, -952.5),
			Target = "Bandit",
			QuestOption = "Ill take 3 bandits",
		},
		["[Windy Peak] Krue: Bandit Leader Zuko"] = {
			Npc = "Krue",
			Pos = Vector3.new(-425.9, 1240.7, -952.5),
			Target = "Zuko",
			BossOption = "Ill take the bandit boss(Lv 7)",
		},
		["[Windy Peak] Kazu: Village Spies"] = {
			Npc = "Kazu",
			Pos = Vector3.new(-626, 1242.5, -1138),
			Target = "*Civilian*",
			Alt = "Village Spy",
			QuestOption = "Ill help clear them out",
		},
		["[Windy Peak] Kazu: Notes to Noote"] = { Npc = "Kazu", Pos = Vector3.new(-626, 1242.5, -1138), QuestOption = "Ill bring him the notes" },
		["[Windy Peak] Lucy: Bear Meat"] = {
			Npc = "Lucy",
			Pos = Vector3.new(-615.5, 1260.2, -1177.5),
			Target = "Mother Bear",
			Alt = "Bear Cub",
		},
		["[Mistfall] Niko: Eliminate Mizunoto"] = { Npc = "Estate Worker Niko", Pos = Vector3.new(341.1, 938.0, 581.9), Target = "Mizunoto" },
		["[Mistfall] Niko: 5 Broken Blades"] = { Npc = "Estate Worker Niko", Pos = Vector3.new(341.1, 938.0, 581.9), Target = "Mizunoto" },
		["[Mistfall] Ginzo: Lost Jewelry Box"] = { Npc = "Ginzo", Pos = Vector3.new(273.8, 944.0, 528.2), Target = "Mizunoto" },
		["[Bamboo] Betty: Betty\'s Gemstone"] = { Npc = "Betty", Pos = Vector3.new(714.0, 1125.1, -808.0), QuestOption = "Ill look for it(Lv 10)" },
		["[Bamboo] Wagwan: Hoyuzo\'s guard"] = {
			Npc = "Wagwan",
			Pos = Vector3.new(723.76, 1019.2, -801.98),
			Target = "Hoyuzo Subordinate",
			QuestOption = "I will clear out his guards(Lv 40)",
			BossOption = "I will take care of Hoyuzo(Lv 50)",
			BossTarget = "Hoyuzo",
		},
		["[Bamboo] Liv: Lucky Penny / coins"] = {
			Npc = "Liv",
			Pos = Vector3.new(657.4, 1021.2, 139.7),
			QuestOption = "Ill look for the penny(Lv 14)",
			BossOption = "Ill find the coins(Lv 21)",
		},
		["[Hidden Mist] Togane: the forge"] = { Npc = "Blacksmith Togane", Pos = Vector3.new(1793.08, 661.3, -231.72), QuestOption = "Ill find the forge(Lv 65)" },
		["[Bamboo] Tom: Wood & Bamboo"] = {
			Npc = "Tom",
			Pos = Vector3.new(508.5, 1124.3, -970.5),
			Target = "Bear Cub",
			Alt = "Mother Bear",
			QuestOption = "Ill drive the bears back(Lv 10)",
			BossOption = "Ill fell the Mother Bear(Lv 18)",
			BossTarget = "Mother Bear",
		},
		["[Bamboo] Chaka: Kaiden and the bandits"] = {
			Npc = "Chaka",
			Pos = Vector3.new(471, 1148.5, -1260),
			Target = "Kaiden Subordinate",
			QuestOption = "Ill clear out his subordinates(Lv 26)",
			BossOption = "Ill deal with Kaiden(Lv 34)",
			BossTarget = "Kaiden",
		},
		["[Butterfly] Shiori: Restock Infirmary"] = {
			Npc = "Shiori",
			Pos = Vector3.new(-1814.3, 311.8, -101.1),
			Target = "Lesser Demon",
			Alt = "Greater Demon",
			QuestOption = "Ill restock the infirmary(Lv 70)",
			BossOption = "Ill stock the reserves(Lv 75)",
		},
		["[Butterfly] Goro: Lesser / Greater Demons"] = {
			Npc = "Demon Slayer Goro",
			Pos = Vector3.new(-871.97, 237.25, 318.47),
			Target = "Lesser Demon",
			QuestOption = "Ill thin them out(Lv 75)",
			BossOption = "Ill go up after the greater ones(Lv 83)",
			BossTarget = "Greater Demon",
		},
		["[Butterfly] Ren: Lost Nichirin"] = {
			Npc = "Ren",
			Pos = Vector3.new(-1795, 311.8, -85),
			Target = "Greater Demon",
			Alt = "Lesser Demon",
			QuestOption = "Ill look for your blade(Lv 75)",
		},
		["[Mistfall] Niko: Supply Box (delivery)"] = { Npc = "Estate Worker Niko", Pos = Vector3.new(341.1, 938.9, 580.6), QuestOption = "Ill deliver the supply box(Lv 70)" },
		["[Windy Peak] MoldySugar: Package (delivery)"] = { Npc = "MoldySugar", Pos = Vector3.new(-701.8, 1243.3, -982.7), QuestOption = "Ill deliver the package" },
		["[Windy Peak] Noote: Coded Letter (delivery)"] = { Npc = "Noote", Pos = Vector3.new(-515.5, 1243.2, -1251.2), QuestOption = "Ill get this letter delivered" },
		["[Iceveil] Tomoi: Ice Demons"] = {
			Npc = "Wounded Slayer Tomoi",
			Pos = Vector3.new(485.3, 1222.9, -1813.4),
			Target = "Ice Profound Demon",
			Alt = "High Demon",
		},
		["[Iceveil] Tomoi: Fire Demons"] = {
			Npc = "Wounded Slayer Tomoi",
			Pos = Vector3.new(485.3, 1222.9, -1813.4),
			Target = "Fire Profound Demon",
			Alt = "High Demon",
		},
		["[Final Selection] Exam"] = {
			Npc = "Demon Mokuro",
			Pos = Vector3.new(-2626.7, 284, -191.2),
			Target = "Mizunoe Demon Slayer",
			Alt = "Fujiko",
		},
		["[Demon Only] Mokuro: Mizunoto slayer"] = {
			Npc = "Demon Mokuro",
			Pos = Vector3.new(-2626.7, 284, -191.2),
			Target = "Mizunoto Demon Slayer",
			BossOption = "Ill break their watch(Lv 75)",
		},
	};
local N = {};
I.F = N;
N.remote = nil;
do
	local h = Y:FindFirstChild("Communication");
	local c = h and h:FindFirstChild("ServerAndClient");
	local D = c and c:FindFirstChild("Signals");
	local A = D and D:FindFirstChild("SignalEvent");
	N.remote = A and A:FindFirstChild("Event");
end;
function N.logErr(h, Y)
	local c = os.date("%H:%M:%S") .. (" " .. (tostring(h) .. (": " .. tostring(Y))));
	warn("[Slayers 2] " .. c);
	if (os.clock() - ((I.errT or 0))) < 1 then
		return;
	end;
	I.errT = os.clock();
	pcall(function()
		appendfile("slayers2_errors.txt", c .. string.char(10));
	end);
end;
local n = {};
function N.defer(h, Y, c)
	if type(h) ~= "function" then
		return;
	end;
	n[#n + 1] = { h, Y, c };
end;
function N.deferRun()
	if I.bmDone then
		I.bmDone = nil;
		local h = I.bmConn;
		I.bmConn = nil;
		if h then
			pcall(function()
				h:Disconnect();
			end);
		end;
	end;
	if #n > 0 then
		local h = n;
		n = {};
		if I.Running then
			for Y = 1, #h, 1 do
				local c, D = pcall(h[Y][1], h[Y][2], h[Y][3]);
				if not c then
					N.logErr("ui", D);
				end;
			end;
		end;
	end;
	if I.syncWant then
		I.syncWant = nil;
		local h, Y = pcall(N.syncNow);
		if not h then
			N.logErr("sync", Y);
		end;
	end;
end;
function N.guard(h)
	if type(h) ~= "table" and type(h) ~= "userdata" then
		return h;
	end;
	return setmetatable({}, { __index = function(Y, c)
			local D = h[c];
			if type(D) ~= "function" then
				return D;
			end;
			return function(Y, c, ...)
				if type(c) == "table" and type(c.callback) == "function" then
					local h = c.callback;
					c.callback = function(Y, c)
							N.defer(h, Y, c);
						end;
				end;
				return D(h, c, ...);
			end;
		end });
end;
local q = {};
function N.mark(h)
	I.where = h;
	if I.markBurst then
		local Y = os.clock();
		if Y < I.markBurst then
			pcall(function()
				appendfile("slayers2_trail.txt", "! " .. (tostring(h) .. string.char(10)));
			end);
		else
			I.markBurst = nil;
			pcall(function()
				appendfile("slayers2_trail.txt", "-- burst over" .. string.char(10));
			end);
		end;
	end;
	local Y = q[#q];
	if Y and Y.w == h then
		Y.n = Y.n + 1;
	else
		q[#q + 1] = { w = h, n = 1 };
		if #q > 60 then
			table.remove(q, 1);
		end;
	end;
end;
function N.markWrite(h)
	if #q == 0 then
		return;
	end;
	if h - ((I.markT or 0)) < .2 then
		return;
	end;
	I.markT = h;
	local Y = {};
	for h = 1, #q, 1 do
		local c = q[h];
		Y[#Y + 1] = c.n > 1 and (c.w .. (" x" .. c.n)) or c.w;
	end;
	q = {};
	local c = os.date("%H:%M:%S") .. (" " .. table.concat(Y, " > "));
	pcall(function()
		appendfile("slayers2_trail.txt", c .. string.char(10));
	end);
end;
function N.markPrev()
	local h;
	pcall(function()
		if isfile("slayers2_trail.txt") then
			h = readfile("slayers2_trail.txt");
		end;
	end);
	if type(h) == "string" and #h > 0 then
		local Y = h;
		for h in string.gmatch(h, "[^" .. (string.char(10) .. "]+")) do
			if #h > 0 then
				Y = h;
			end;
		end;
		pcall(function()
			appendfile("slayers2_errors.txt", os.date("%H:%M:%S") .. (" previous run ended at: " .. (Y .. string.char(10))));
		end);
		warn("[Slayers 2] previous run ended at: " .. Y);
	end;
	pcall(function()
		delfile("slayers2_trail.txt");
	end);
	pcall(function()
		delfile("slayers2_where.txt");
	end);
end;
function N.notify(h, Y)
	pcall(function()
		l:Notify({ title = "Slayers 2", text = tostring(h), duration = Y or 3 });
	end);
end;
function N.char()
	local h = A.Character;
	if not h then
		return nil, nil, nil;
	end;
	local Y = h:FindFirstChild("HumanoidRootPart");
	local c = h:FindFirstChildOfClass("Humanoid");
	if not Y or not c then
		return nil, nil, nil;
	end;
	return h, Y, c;
end;
local g = .25;
function N.addr(h)
	local Y, c = pcall(function()
			return h.Address;
		end);
	if Y and type(c) == "number" then
		return c;
	end;
	return nil;
end;
function N.attached(h, Y)
	if h == nil then
		return false;
	end;
	local c, D = pcall(function()
			local c = h.Parent;
			if c == nil then
				return false;
			end;
			if Y == nil then
				return true;
			end;
			return c.Address == Y;
		end);
	return c and D == true;
end;
function N.npcAlive(h, Y)
	if not h or h.dead or h.stale then
		return false;
	end;
	local c = os.clock();
	local D = c - ((h.chkT or 0));
	if D < .005 then
		return true;
	end;
	if D > ((Y or g)) then
		h.stale = true;
		return false;
	end;
	if N.attached(h.Model, h.PAddr) then
		h.chkT = c;
		return true;
	end;
	h.dead = true;
	return false;
end;
function N.curAlive(h)
	if not h or h.dead then
		return false;
	end;
	if not ((h.Src and (h.Region and (h.IAddr and h.MAddr)))) then
		return N.npcAlive(h);
	end;
	local Y = os.clock();
	if Y - ((h.freshT or 0)) < .005 then
		return true;
	end;
	local c = D:FindFirstChild(h.Src);
	local A = c and c:FindFirstChild("Regions");
	local l = A and A:FindFirstChild(h.Region);
	local I = l and l:FindFirstChild("ActiveNpcs");
	local f = I and N.refind(I, h.IAddr);
	local n = f and (((h.IAddr == h.MAddr) and f or N.refind(f, h.MAddr)));
	if not n then
		h.dead = true;
		return false;
	end;
	h.freshT, h.chkT = Y, Y;
	h.Model = n;
	local q = n:FindFirstChild("HumanoidRootPart");
	if q then
		h.Root = q;
	end;
	local g = n:FindFirstChildOfClass("Humanoid");
	if g then
		h.Hum = g;
	end;
	h.Head = n:FindFirstChild("Head") or false;
	return true;
end;
local S = {};
function N.refreshPlayers()
	S = {};
	local Y = h:GetPlayers();
	for h = 1, #Y, 1 do
		S[Y[h].Name] = true;
	end;
end;
N.refreshPlayers();
function N.isBoss(h)
	if h:FindFirstChild("BossInfo") then
		return true;
	end;
	return f.BossNames[string.lower(h.Name)] == true;
end;
function N.scan(h, Y, c)
	N.mark("scan/walk");
	local A = {};
	local l = nil;
	if type(h) == "table" then
		l = h;
		h = nil;
	end;
	local I = h and string.lower(h) or nil;
	local f = (not l) and (((h == nil) or (h == "All Regular Mobs") or (h == "All Bosses")));
	local n = { D:FindFirstChild("Humanoids"), D:FindFirstChild("Debree") };
	for h = 1, #n, 1 do
		local D = n[h] and n[h]:FindFirstChild("Regions");
		if D then
			local q = D:GetChildren();
			for D = 1, #q, 1 do
				local g = q[D]:FindFirstChild("ActiveNpcs");
				if g then
					local k = g:GetChildren();
					for g = 1, #k, 1 do
						local H = k[g];
						local R = H.Name;
						if not S[R] then
							local g = false;
							if l then
								g = l[string.lower(R)] == true;
							elseif f then
								g = true;
							elseif I then
								local h = string.lower(R);
								if c then
									g = (h == I);
								else
									g = (h == I) or (string.find(h, I, 1, true) ~= nil);
								end;
							end;
							if g then
								local h = N.isBoss(H);
								if c and (Y and not h) then
									g = false;
								end;
							end;
							if g then
								local c = N.isBoss(H);
								if Y == nil or not f or Y == c then
									local Y = H:FindFirstChild(R) or H:FindFirstChildOfClass("Model");
									if Y then
										local l = Y:FindFirstChildOfClass("Humanoid");
										local f = Y:FindFirstChild("HumanoidRootPart");
										if l and (f and l.Health > 0) then
											local g = {
													Model = Y,
													Hum = l,
													Root = f,
													Name = R,
													Boss = c,
												};
											g.exactName = I ~= nil and string.lower(R) == I;
											g.PAddr = N.addr(H);
											g.IAddr = g.PAddr;
											g.MAddr = N.addr(Y);
											g.Src = n[h] and n[h].Name or nil;
											g.Region = q[D].Name;
											g.chkT = os.clock();
											A[#A + 1] = g;
											pcall(function()
												N.noteSeen(R, f.Position);
											end);
										end;
									end;
								end;
							end;
						end;
					end;
				end;
			end;
		end;
	end;
	N.mark("scan/done");
	if I and not c then
		local h = {};
		for Y = 1, #A, 1 do
			if A[Y].exactName then
				h[#h + 1] = A[Y];
			end;
		end;
		if #h > 0 then
			return h;
		end;
	end;
	return A;
end;
local k = { key = "", t = -99, list = {} };
function N.setKey(h)
	local Y = {};
	for h in pairs(h) do
		Y[#Y + 1] = h;
	end;
	table.sort(Y);
	return table.concat(Y, "|");
end;
function N.scanCached(h, Y, c, D, A)
	local l;
	if type(h) == "table" then
		l = N.setKey(h);
	else
		l = tostring(h);
	end;
	l = l .. (tostring(Y) .. tostring(A));
	if k.key == l and (c - k.t) < ((D or 1.0)) then
		return k.list;
	end;
	k.key = l;
	k.t = c;
	k.list = N.scan(h, Y, A);
	return k.list;
end;
function N.dropCache()
	k.key = "";
	k.t = -99;
end;
function N.bossSet()
	local h = I.SelectedBoss;
	local Y = false;
	local c, D = {}, 0;
	if type(h) == "string" then
		if h == "All Bosses" then
			Y = true;
		else
			c[string.lower(h)] = true;
			D = 1;
		end;
	else
		for h, A in pairs(h or {}) do
			if A then
				if h == "All Bosses" then
					Y = true;
				else
					c[string.lower(h)] = true;
					D = D + 1;
				end;
			end;
		end;
	end;
	if Y then
		c, D = {}, 0;
		for h = 1, #f.Bosses, 1 do
			local Y = f.Bosses[h];
			if Y ~= "All Bosses" then
				c[string.lower(Y)] = true;
				D = D + 1;
			end;
		end;
	end;
	if D == 0 then
		return nil, false;
	end;
	return c, Y;
end;
function N.updateBossOrder(h)
	local Y = {};
	if type(h) == "table" then
		for h, c in pairs(h) do
			if c then
				Y[h] = true;
			end;
		end;
	elseif type(h) == "string" then
		Y[h] = true;
	end;
	local c = {};
	if Y["All Bosses"] then
		for h, Y in ipairs(f.Bosses) do
			if Y ~= "All Bosses" then
				c[#c + 1] = Y;
			end;
		end;
	else
		for h, D in ipairs(I.BossOrder or {}) do
			if Y[D] then
				c[#c + 1] = D;
				Y[D] = nil;
			end;
		end;
		for h, D in ipairs(f.Bosses) do
			if Y[D] then
				c[#c + 1] = D;
			end;
		end;
	end;
	I.BossOrder = c;
	I.spawnIdx = 0;
end;
function N.bossLabel()
	local h, Y = N.bossSet();
	if Y then
		return "all bosses";
	end;
	if not h then
		return "none selected";
	end;
	local c = {};
	for h in pairs(h) do
		c[#c + 1] = h;
	end;
	table.sort(c);
	return table.concat(c, ", ");
end;
f.SharedOK = { ["-2627,-192"] = true, ["485,-1814"] = true };
local H = {};
do
	local h = {};
	for Y, c in ipairs({ f.Spawns, f.BossTP }) do
		for Y, c in pairs(c) do
			local D = math.floor(c.X) .. ("," .. math.floor(c.Z));
			h[D] = h[D] or {};
			h[D][string.lower(Y)] = true;
		end;
	end;
	for h, Y in pairs(h) do
		local c = 0;
		for h in pairs(Y) do
			c = c + 1;
		end;
		if c > 1 and not f.SharedOK[h] then
			for h in pairs(Y) do
				H[h] = true;
			end;
		end;
	end;
end;
local function R(h)
	local Y = string.lower(h);
	local c = I.seen[Y];
	if c then
		return Vector3.new(c[1], c[2], c[3]);
	end;
	if H[Y] then
		return nil;
	end;
	for c, D in ipairs({ f.Spawns, f.BossTP }) do
		local A = D[h];
		if A then
			return A;
		end;
		for h, c in pairs(D) do
			if string.lower(h) == Y then
				return c;
			end;
		end;
	end;
	return nil;
end;
function N.wantSpawn(h)
	if type(h) ~= "table" then
		return R(h);
	end;
	local Y = {};
	if I.Mode == "boss" and I.BossOrder then
		for c, D in ipairs(I.BossOrder) do
			local A = string.lower(D);
			if h[A] and R(A) then
				Y[#Y + 1] = A;
			end;
		end;
	end;
	if #Y == 0 then
		for h in pairs(h) do
			if R(h) then
				Y[#Y + 1] = h;
			end;
		end;
		table.sort(Y);
	end;
	if #Y == 0 then
		return nil;
	end;
	I.spawnIdx = ((I.spawnIdx or 0)) % #Y + 1;
	return R(Y[I.spawnIdx]);
end;
local b = "slayers2_seen.json";
function N.seenLoad()
	local h;
	if not pcall(function()
		h = readfile(b);
	end) or type(h) ~= "string" then
		return;
	end;
	local Y, c = pcall(function()
			return (game:GetService("HttpService")):JSONDecode(h);
		end);
	if Y and type(c) == "table" then
		I.seen = c;
	end;
end;
function N.seenSave(h)
	if not I.seenDirty then
		return;
	end;
	if h and (h - ((I.seenT or 0))) < 30 then
		return;
	end;
	I.seenT = h or 0;
	I.seenDirty = false;
	pcall(function()
		writefile(b, (game:GetService("HttpService")):JSONEncode(I.seen));
	end);
end;
function N.noteSeen(h, Y)
	local c = string.lower(h);
	local D = I.seen[c];
	if D and (math.abs(D[1] - Y.X) < 8 and math.abs(D[3] - Y.Z) < 8) then
		return;
	end;
	I.seen[c] = { Y.X, Y.Y, Y.Z };
	I.seenDirty = true;
end;
function N.nearest(h, Y)
	local c, D = nil, 1000000000.0;
	for A = 1, #h, 1 do
		local l = h[A];
		if N.npcAlive(l, .6) then
			local h, A = pcall(function()
					return l.Root.Position;
				end);
			if h and A then
				local h = ((A - Y)).Magnitude;
				if h < D then
					D = h;
					c = l;
				end;
			end;
		end;
	end;
	return c, D;
end;
function N.tpTo(h, Y)
	local c, D = N.char();
	if not D or not h then
		return false;
	end;
	pcall(function()
		D.CFrame = CFrame.new(h.X, h.Y + ((Y or 2.5)), h.Z);
		D.AssemblyLinearVelocity = Vector3.new(0, 0, 0);
	end);
	return true;
end;
function N.findNamed(h)
	local Y = string.lower(h or "");
	if Y == "" then
		return nil;
	end;
	local c = nil;
	local A = { D:FindFirstChild("Humanoids"), D:FindFirstChild("Debree") };
	for h = 1, #A, 1 do
		local D = A[h] and A[h]:FindFirstChild("Regions");
		if D then
			for h, D in ipairs(D:GetChildren()) do
				for h, D in ipairs({ D:FindFirstChild("ActiveNpcs"), D:FindFirstChild("StationaryNpcs") }) do
					for h, D in ipairs(D and D:GetChildren() or {}) do
						local A = string.lower(D.Name);
						local l = (A == Y);
						if l or (not c and string.find(A, Y, 1, true)) then
							local h = D:FindFirstChildOfClass("Model") or D;
							local Y = h:FindFirstChild("HumanoidRootPart") or h:FindFirstChildWhichIsA("BasePart");
							if Y then
								if l then
									return Y.Position;
								end;
								c = Y.Position;
							end;
						end;
					end;
				end;
			end;
		end;
	end;
	return c;
end;
function N.npcPos(h, Y, c)
	local D = I.npcPosCache[h];
	if D and (Y - D.t) < ((c or 2.0)) then
		return D.pos;
	end;
	local A = N.findNamed(h);
	I.npcPosCache[h] = { pos = A, t = Y };
	return A;
end;
function N.followNpc(h, Y, c)
	local D, A = N.char();
	local l = N.npcPos(h, c, .1) or Y;
	local f = I.follow;
	if f and f.name == h then
		f.t = c;
		f.fb = Y;
	else
		I.follow = { name = h, fb = Y, t = c };
	end;
	if not A or not l then
		return math.huge;
	end;
	local n = A.Position;
	local q, g = n.X - l.X, n.Z - l.Z;
	local S = math.sqrt(q * q + g * g);
	if math.abs(n.Y - l.Y) > 15 then
		return math.max(S, 99);
	end;
	return S;
end;
function N.followStep(h)
	local Y = I.follow;
	if not Y then
		return;
	end;
	if h - Y.t > .6 then
		I.follow = nil;
		return;
	end;
	local c, D = N.char();
	local A = N.npcPos(Y.name, h, .1) or Y.fb;
	if not D or not A then
		return;
	end;
	if Y.lastPos and Y.lastPos ~= A then
		local c = h - ((Y.lastT or h));
		if c > .02 then
			local D, l = ((A.X - Y.lastPos.X)) / c, ((A.Z - Y.lastPos.Z)) / c;
			if D * D + l * l < 900 then
				Y.vx, Y.vz = D, l;
			end;
			Y.lastPos, Y.lastT = A, h;
		end;
	elseif not Y.lastPos then
		Y.lastPos, Y.lastT = A, h;
	end;
	local l, f = Y.vx or 0, Y.vz or 0;
	if I.dlg or (I.dlgClick ~= nil) then
		N.aimRelease();
	else
		pcall(N.aimStep, Vector3.new(A.X, A.Y + 2, A.Z), true);
	end;
	local n = D.Position;
	local q, g = n.X - A.X, n.Z - A.Z;
	local S = math.sqrt(q * q + g * g);
	local k, H = 1, 0;
	if S > .5 then
		k, H = q / S, g / S;
	end;
	local R = (A.X + k * 2.5) + l * .2;
	local b = (A.Z + H * 2.5) + f * .2;
	local w, W, t = R - n.X, A.Y - n.Y, b - n.Z;
	local r = math.sqrt((w * w + W * W) + t * t);
	if r > I.WarpAbove then
		N.travelTo(Vector3.new(R, A.Y - 3, b), h);
		return;
	end;
	local m, i, X = l + w * 6, W * 6, f + t * 6;
	local J = math.sqrt((m * m + i * i) + X * X);
	if J > I.MoveSpeed then
		local h = I.MoveSpeed / J;
		m, i, X = m * h, i * h, X * h;
	end;
	if r < .6 and l * l + f * f < 1 then
		m, i, X = 0, 0, 0;
	end;
	pcall(function()
		D.AssemblyLinearVelocity = Vector3.new(m, i, X);
	end);
end;
f.MuzanRoute = {
		Vector3.new(55.0, 826.5, 781.5),
		Vector3.new(197.2, 871.4, 812.2),
		Vector3.new(330.0, 873.0, 824.0),
		Vector3.new(182.4, 873.0, 703.7),
		Vector3.new(187.6, 914.8, 558.3),
		Vector3.new(237.8, 940.5, 389.6),
		Vector3.new(227.8, 963.5, 250.1),
		Vector3.new(98.9, 963.5, 210.2),
		Vector3.new(232.7, 963.5, 267.0),
		Vector3.new(212.2, 940.5, 452.1),
		Vector3.new(181.8, 909.0, 590.1),
		Vector3.new(183.6, 873.0, 714.7),
		Vector3.new(115.8, 826.5, 808.6),
		Vector3.new(1920.6, 599.0, -881.0),
		Vector3.new(1816.3, 599.0, -719.9),
		Vector3.new(1748.2, 599.0, -549.1),
		Vector3.new(1771.7, 599.0, -425.8),
		Vector3.new(1787.4, 599.0, -295.5),
		Vector3.new(1810.5, 599.0, -145.7),
		Vector3.new(1667.8, 599.0, -92.7),
		Vector3.new(1811.8, 599.0, -146.8),
		Vector3.new(1776.7, 599.0, -332.5),
		Vector3.new(1772.2, 599.0, -452.5),
		Vector3.new(1762.0, 599.0, -632.5),
		Vector3.new(1877.4, 599.0, -794.8),
		Vector3.new(-687.0, 1380.5, -2606.5),
		Vector3.new(-531.8, 1368.2, -2594.2),
		Vector3.new(-410.5, 1348.5, -2603.5),
		Vector3.new(-392.3, 1348.5, -2794.1),
		Vector3.new(-413.6, 1380.5, -2992.6),
		Vector3.new(-463.5, 1380.5, -3136.3),
		Vector3.new(-451.1, 1350.0, -3301.2),
		Vector3.new(-398.7, 1350.0, -3438.5),
		Vector3.new(-251.6, 1350.0, -3452.8),
		Vector3.new(-191.2, 1350.0, -3327.3),
		Vector3.new(-182.9, 1380.5, -3188.0),
		Vector3.new(-171.5, 1380.5, -2996.0),
		Vector3.new(-173.1, 1373.7, -2862.2),
		Vector3.new(-163.6, 1348.5, -2702.0),
		Vector3.new(-172.5, 1348.5, -2548.0),
		Vector3.new(-362.7, 1348.5, -2551.9),
		Vector3.new(-499.4, 1352.7, -2594.5),
		Vector3.new(-643.2, 1380.5, -2607.4),
	};
function N.muzanSpots()
	local h = {};
	for Y, c in ipairs({ D:FindFirstChild("Humanoids"), D:FindFirstChild("Debree") }) do
		local D = c and c:FindFirstChild("Regions");
		for Y, c in ipairs(D and D:GetChildren() or {}) do
			for Y, D in ipairs({ c:FindFirstChild("ActiveNpcs"), c:FindFirstChild("StationaryNpcs") }) do
				for Y, D in ipairs(D and D:GetChildren() or {}) do
					if string.find(string.lower(D.Name), "muzan", 1, true) then
						local Y = D:FindFirstChild(D.Name) or D:FindFirstChildOfClass("Model");
						local A = D:FindFirstChild("HumanoidRootPart") or (Y and Y:FindFirstChild("HumanoidRootPart")) or D:FindFirstChildWhichIsA("BasePart");
						local l;
						if A then
							pcall(function()
								l = A.Position;
							end);
						end;
						h[#h + 1] = {
								name = D.Name,
								pos = l,
								region = c.Name,
								npc = true,
							};
					end;
				end;
			end;
		end;
	end;
	return h;
end;
function N.findMuzan(h)
	local Y, c = N.char();
	local D, A;
	for h, Y in ipairs(N.muzanSpots()) do
		local l = ((c and Y.pos)) and ((Y.pos - c.Position)).Magnitude or nil;
		if not D or (l and (A and l < A)) then
			D, A = Y, l;
		end;
	end;
	if D and D.pos then
		if h then
			N.tpTo(Vector3.new(D.pos.X + 5, D.pos.Y, D.pos.Z), 3);
			N.notify("at muzan" .. ((D.region and " (" .. (D.region .. ")") or "")), 4);
		else
			N.notify(string.format("muzan at %d, %d, %d - %d studs", D.pos.X, D.pos.Y, D.pos.Z, A or 0), 6);
		end;
		return D;
	end;
	if D then
		N.notify("muzan is walking in " .. (tostring(D.region) .. " - travel there to see him"), 6);
		return D;
	end;
	local l, I;
	for h, Y in ipairs(f.MuzanRoute) do
		local D = c and ((Y - c.Position)).Magnitude or 0;
		if not I or D < I then
			l, I = Y, D;
		end;
	end;
	if h and l then
		N.tpTo(l, 3);
	end;
	N.notify("muzan is not walking the map - waiting spot set on his patrol route", 6);
	return nil;
end;
f.BlackMarketer = { Vector3.new(-132.68, 804, 101.24), Vector3.new(2161.96, 823.46, -900.68) };
function N.tpBlackMarketer()
	if I.bmConn then
		pcall(function()
			I.bmConn:Disconnect();
		end);
		I.bmConn = nil;
	end;
	local function h(h)
		N.tpTo(Vector3.new(h.X + 4, h.Y, h.Z), 2.5);
		N.notify("at the Black Marketer", 3);
	end;
	local Y = N.findNamed("Black Marketer");
	if Y then
		h(Y);
		return;
	end;
	local D, A = 1, os.clock();
	N.tpTo(f.BlackMarketer[1], 3);
	I.bmConn = c.Heartbeat:Connect(function()
			local Y, c = pcall(function()
					local Y = os.clock();
					if Y - A < 1.5 then
						return;
					end;
					local c = N.findNamed("Black Marketer");
					if c then
						I.bmDone = true;
						h(c);
					elseif D < #f.BlackMarketer then
						D = D + 1;
						A = Y;
						N.tpTo(f.BlackMarketer[D], 3);
					else
						I.bmDone = true;
						N.notify("Black Marketer not at either spawn", 4);
					end;
				end);
			if not Y then
				I.bmDone = true;
				N.logErr("black marketer", c);
			end;
		end);
end;
function N.tpNamed(h, Y)
	local c = N.findNamed(h);
	local D = c or Y;
	if D then
		N.tpTo(D, 2.5);
		N.notify(((c and "Reached " or "Teleported to ")) .. h, 2);
	else
		N.notify("No location for " .. tostring(h), 2);
	end;
end;
local w = { t = -99, style = "Combat", item = nil };
function N.style()
	local h = os.clock();
	if h - w.t < 2.0 then
		return w.style, w.item;
	end;
	w.t = h;
	local Y, c = N.styleRaw();
	w.style, w.item = Y, c;
	return Y, c;
end;
function N.styleRaw()
	local h = A.Character;
	if not h then
		return "Combat", nil;
	end;
	local c = Y:FindFirstChild("Assets");
	c = c and c:FindFirstChild("Animations");
	local D = nil;
	local l = h:GetChildren();
	for h = 1, #l, 1 do
		if l[h].ClassName == "Tool" then
			D = l[h];
			break;
		end;
	end;
	if not D then
		local Y = h:FindFirstChild("Tool_Accessories");
		if Y then
			D = (Y:GetChildren())[1];
		end;
	end;
	local I = nil;
	local N = Y:FindFirstChild("CAM");
	local n = N and (N.Client and N.Client.Controllers);
	n = n and n:FindFirstChild("Skills_Provider");
	n = n and n:FindFirstChild("CurPower");
	if n and (type(n.Value) == "string" and (n.Value ~= "" and c)) then
		for h in string.gmatch(n.Value, "[^,]+") do
			if c:FindFirstChild(h .. "_Combat_Anims") then
				I = h;
				break;
			end;
		end;
	end;
	if not I and (D and (c and c:FindFirstChild(D.Name .. "_Combat_Anims"))) then
		I = D.Name;
	end;
	if not I and D then
		I = D.Name;
	end;
	if not I then
		I = "Combat";
	end;
	if f.Presets[I] then
		return I, nil;
	end;
	local q = f.StyleMap[string.lower(I)];
	if q then
		return q, nil;
	end;
	local g = string.lower(I);
	local S;
	for h, Y in pairs(f.StyleWords) do
		if string.find(g, h, 1, true) and f.Presets[Y] then
			return Y, nil;
		end;
	end;
	for h in pairs(f.Presets) do
		if h ~= "Combat" then
			local Y = string.lower(h);
			local c = string.find(g, Y, 1, true) ~= nil;
			if not c and string.sub(Y, -1) == "s" then
				c = (g == string.sub(Y, 1, -2)) or string.find(g, string.sub(Y, 1, -2), 1, true) ~= nil;
			end;
			if not c then
				c = (g .. "s" == Y);
			end;
			if c and ((not S or #h > #S)) then
				S = h;
			end;
		end;
	end;
	if S then
		return S, nil;
	end;
	return "Regular Katana", I;
end;
function N.targetBlocking()
	if not I.SkipBlocking then
		return false;
	end;
	local h = I.cur;
	if not h or not h.Model or not N.curAlive(h) then
		return false;
	end;
	local Y;
	local c = pcall(function()
			Y = h.Model:FindFirstChild("Blocking");
		end);
	return c and Y ~= nil;
end;
function N.curBlocking()
	local h = I.cur;
	if not h or not h.Model or not N.curAlive(h) then
		return false;
	end;
	local Y;
	local c = pcall(function()
			Y = h.Model:FindFirstChild("Blocking");
		end);
	return c and Y ~= nil;
end;
function N.attack(h)
	if not N.remote then
		return;
	end;
	local Y = A.Character;
	if not Y then
		return;
	end;
	h = h or os.clock();
	if h < ((I.atkNext or 0)) then
		return;
	end;
	local c = I.cur;
	if c and I.Reach > 0 then
		local h;
		local D = pcall(function()
				h = ((c.Root.Position - Y.HumanoidRootPart.Position)).Magnitude;
			end);
		if D and (h and h > I.Reach) then
			return;
		end;
	end;
	if N.targetBlocking() then
		I.blockedT = h;
		if I.cur then
			I.cur.t = h;
		end;
		return;
	end;
	local D = tonumber(Y:GetAttribute("last_combo")) or 0;
	if I.atkSent then
		if D == I.atkSent then
			I.atkMiss = 0;
		else
			I.atkMiss = ((I.atkMiss or 0)) + 1;
		end;
	end;
	local l = D + 1;
	if l > 5 or l < 1 then
		l = 1;
	end;
	local f, n = N.style();
	pcall(function()
		N.remote:FireServer("Combat_Service", f, l, false, 0, false, n);
	end);
	I.atkSent = l;
	I.atkCount = ((I.atkCount or 0)) + 1;
	local q = I.AttackDelay;
	if l == 1 then
		q = q * 2.0;
	end;
	if ((I.atkMiss or 0)) > 0 then
		q = q + .15 * I.atkMiss;
	end;
	if q > 1.6 then
		q = 1.6;
	end;
	I.atkNext = h + q;
end;
function N.goalFor(h)
	local Y = h.Position;
	local c = I.Offset;
	if I.Stance == "On Top" then
		return Y + Vector3.new(0, c, 0);
	elseif I.Stance == "Above (Hover)" then
		return Y + Vector3.new(0, c + 4, 0);
	elseif I.Stance == "Behind" then
		local D;
		if pcall(function()
			D = h.CFrame.LookVector;
		end) and D then
			local h, A = D.X, D.Z;
			local l = math.sqrt(h * h + A * A);
			if l > .001 then
				return Vector3.new(Y.X - (h / l) * c, Y.Y + .8, Y.Z - (A / l) * c);
			end;
		end;
		return Y + Vector3.new(0, .8, c);
	end;
	return Y - Vector3.new(0, c, 0);
end;
function N.steer()
	local h = I.cur;
	if not h or not N.curAlive(h) then
		return;
	end;
	local Y, c = N.char();
	if not c then
		return;
	end;
	local D, A;
	local l = pcall(function()
			A = h.Root.Position;
			D = N.goalFor(h.Root);
		end);
	if not l or not D then
		return;
	end;
	local f = c.Position;
	local n, q, g = D.X - f.X, D.Y - f.Y, D.Z - f.Z;
	local S = math.sqrt((n * n + q * q) + g * g);
	if I.Smooth and S > I.WarpAbove then
		pcall(function()
			c.CFrame = CFrame.new(D.X, D.Y + 2, D.Z);
			c.AssemblyLinearVelocity = Vector3.new(0, 0, 0);
		end);
		return;
	end;
	if not I.Smooth then
		pcall(function()
			c.CFrame = CFrame.lookAt(D, A);
			c.AssemblyLinearVelocity = Vector3.new(0, 0, 0);
		end);
		return;
	end;
	if S < .4 then
		pcall(function()
			c.AssemblyLinearVelocity = Vector3.new(0, 0, 0);
		end);
		if A then
			local h, Y = A.X - f.X, A.Z - f.Z;
			if (h * h + Y * Y) > .02 then
				pcall(function()
					c.CFrame = CFrame.lookAt(f, Vector3.new(A.X, f.Y, A.Z));
				end);
			end;
		end;
		return;
	end;
	local k = S * 5.0;
	if k > I.MoveSpeed then
		k = I.MoveSpeed;
	end;
	local H = k / S;
	pcall(function()
		c.AssemblyLinearVelocity = Vector3.new(n * H, q * H, g * H);
	end);
end;
local W = .001747;
function N.camYaw()
	local h = D.CurrentCamera;
	if not h then
		return nil;
	end;
	local Y;
	local c = pcall(function()
			Y = h.CFrame.LookVector;
		end);
	if not c or not Y then
		return nil;
	end;
	return math.atan2(-Y.X, -Y.Z);
end;
local t = -0.001747;
local r = -0.001345;
function N.aimRelease()
	local h = I.rmbDown;
	if not h and ismouse2pressed then
		local Y, c = pcall(ismouse2pressed);
		h = Y and c;
	end;
	if h then
		pcall(function()
			mouse2release();
		end);
	end;
	I.rmbDown = false;
end;
function N.aimStep(h, Y)
	if (not I.AimLock and not Y) or not h then
		return N.aimRelease();
	end;
	if I.win and I.win:IsVisible() then
		return N.aimRelease();
	end;
	if isrbxactive then
		local h, Y = pcall(isrbxactive);
		if h and not Y then
			return N.aimRelease();
		end;
	end;
	local c = D.CurrentCamera;
	if not c then
		return N.aimRelease();
	end;
	local A;
	if not pcall(function()
		A = c.CFrame;
	end) or not A then
		return;
	end;
	local l, f = A.Position, A.LookVector;
	local n = h.X - l.X;
	local q = h.Y - l.Y;
	local g = h.Z - l.Z;
	local S = math.sqrt(n * n + g * g);
	if S < .01 then
		return;
	end;
	local k = math.atan2(-n, -g) - math.atan2(-f.X, -f.Z);
	while k > math.pi do
		k = k - 2 * math.pi;
	end;
	while k < -math.pi do
		k = k + 2 * math.pi;
	end;
	local H = math.atan2(q, S) - math.asin(f.Y);
	if math.abs(k) < .008 and math.abs(H) < .008 then
		return;
	end;
	if not I.rmbDown then
		pcall(function()
			mouse2press();
		end);
		I.rmbDown = true;
	end;
	local R = (k / t) * I.AimGain;
	local b = (H / r) * I.AimGain;
	if R > 900 then
		R = 900;
	elseif R < -900 then
		R = -900;
	end;
	if b > 600 then
		b = 600;
	elseif b < -600 then
		b = -600;
	end;
	pcall(function()
		mousemoverel(R, b);
	end);
end;
function N.syncSkills()
	if not I.Skills or not I.win then
		return;
	end;
	for h = 1, #I.Skills, 1 do
		local Y = I.Skills[h];
		local c;
		if pcall(function()
			c = I.win:GetValue("sk_" .. (Y.name .. "_on"));
		end) and c ~= nil then
			Y.on = c and true or false;
		end;
	end;
end;
function N.skillsRelease()
	for h = 1, #((I.Skills or {})), 1 do
		local Y = I.Skills[h];
		if Y.vk and Y.vk > 0 then
			pcall(function()
				keyrelease(Y.vk);
			end);
		end;
		Y.rel = 0;
	end;
end;
function N.skillStep(h, Y)
	if not I.Skills then
		return;
	end;
	if (h - ((I.skillSyncT or 0))) > 1.0 then
		I.skillSyncT = h;
		pcall(N.syncSkills);
	end;
	local c;
	for D = 1, #I.Skills, 1 do
		local A = I.Skills[D];
		if A.rel > 0 then
			if h >= A.rel then
				pcall(function()
					keyrelease(A.vk);
				end);
				A.rel = 0;
				A.next = h + I.SkillGap;
			end;
		elseif A.on and (Y and (Y <= A.dist and (h >= ((A.next or 0)) and ((not A.breaker or ((function()
			if c == nil then
				local h, Y = pcall(N.curBlocking);
				c = h and Y == true;
			end;
			return c;
		end))()))))) then
			A.rel = h + A.hold;
			pcall(function()
				keypress(A.vk);
			end);
		end;
	end;
end;
function N.travelTo(h, Y)
	local c, D = N.char();
	if not D then
		return;
	end;
	local A = D.Position;
	local l, f, n = h.X - A.X, ((h.Y + 3)) - A.Y, h.Z - A.Z;
	local q = math.sqrt((l * l + f * f) + n * n);
	if q > I.WarpAbove or not I.Smooth then
		pcall(function()
			D.CFrame = CFrame.new(h.X, h.Y + 3, h.Z);
			D.AssemblyLinearVelocity = Vector3.new(0, 0, 0);
		end);
		return;
	end;
	if q < 1 then
		return;
	end;
	local g = q * 5.0;
	if g > I.MoveSpeed then
		g = I.MoveSpeed;
	end;
	local S = g / q;
	pcall(function()
		D.AssemblyLinearVelocity = Vector3.new(l * S, f * S, n * S);
	end);
end;
function N.focused(h)
	if not isrbxactive then
		return true;
	end;
	local Y, c = pcall(isrbxactive);
	if not Y then
		return true;
	end;
	if c then
		return true;
	end;
	if h and (h - ((I.warnedFocus or 0))) > 15 then
		I.warnedFocus = h;
		N.notify("roblox not focused - T pickups paused", 4);
	end;
	return false;
end;
local function m(h)
	local Y = string.lower(h);
	return string.find(Y, "mound", 1, true) ~= nil or string.find(Y, "snow", 1, true) ~= nil;
end;
function N.openChests()
	local h = {};
	local Y = D:FindFirstChild("Chests");
	if not Y then
		return h;
	end;
	for Y, c in ipairs(Y:GetChildren()) do
		if not c:FindFirstChild("SealVFX") and not m(c.Name) then
			local Y = c;
			if c.ClassName ~= "Part" and c.ClassName ~= "MeshPart" then
				Y = c:FindFirstChild("RootPart") or c:FindFirstChildWhichIsA("BasePart");
			end;
			if Y then
				h[#h + 1] = { Name = c.Name, Root = Y, Node = c };
			end;
		end;
	end;
	return h;
end;
function N.collectStart(h, Y)
	if not h or #h == 0 then
		return false;
	end;
	I.collect = {
			list = h,
			i = 1,
			phase = "move",
			t = os.clock(),
		};
	N.notify(((Y or "collecting")) .. (": " .. (#h .. " item(s)")), 3);
	return true;
end;
function N.collectStop()
	I.collect = nil;
	N.aimRelease();
	pcall(function()
		keyrelease(84);
	end);
end;
function N.collectStep(h)
	local Y = I.collect;
	if not Y then
		return false;
	end;
	local c, D = N.char();
	if not D then
		return true;
	end;
	if not N.focused(h) then
		Y.blocked = Y.blocked or h;
		if (h - Y.blocked) > 6 then
			N.collectStop();
			return false;
		end;
		return true;
	end;
	Y.blocked = nil;
	local A = Y.list[Y.i];
	if not A then
		N.collectStop();
		N.notify("loot collected", 2);
		return false;
	end;
	local l;
	if not pcall(function()
		l = A.get();
	end) or not l then
		Y.i = Y.i + 1;
		Y.phase = "move";
		Y.t = h;
		pcall(function()
			keyrelease(84);
		end);
		return true;
	end;
	pcall(N.aimStep, Vector3.new(l.X, l.Y + 1.5, l.Z), true);
	local f, n, q = l.X, l.Y + ((A.lift or 2.5)), l.Z;
	if A.side then
		f = f + 6;
		n = l.Y + 3;
	end;
	local g = D.Position;
	local S, k, H = f - g.X, n - g.Y, q - g.Z;
	local R = math.sqrt((S * S + k * k) + H * H);
	if Y.phase == "move" then
		if R > I.WarpAbove then
			pcall(function()
				D.CFrame = CFrame.new(l.X, l.Y + 3, l.Z);
				D.AssemblyLinearVelocity = Vector3.new(0, 0, 0);
			end);
		elseif R > ((A.reach or 3.5)) then
			local h = R * 5.0;
			if h > I.MoveSpeed then
				h = I.MoveSpeed;
			end;
			local Y = h / R;
			pcall(function()
				D.AssemblyLinearVelocity = Vector3.new(S * Y, k * Y, H * Y);
			end);
		else
			pcall(function()
				D.AssemblyLinearVelocity = Vector3.new(0, 0, 0);
			end);
			Y.settle = Y.settle or (h + I.SettleT);
			if h < Y.settle then
				return true;
			end;
			Y.settle = nil;
			Y.phase = "hold";
			Y.t = h;
			pcall(function()
				keypress(84);
			end);
		end;
		if h - Y.t > 10 then
			Y.i = Y.i + 1;
			Y.phase = "move";
			Y.t = h;
			Y.settle = nil;
		end;
	else
		pcall(function()
			D.AssemblyLinearVelocity = Vector3.new(0, 0, 0);
		end);
		local c = A.hold or (A.tap and .2 or I.HoldT);
		if h - Y.t >= c then
			pcall(function()
				keyrelease(84);
			end);
			local c = false;
			if A.soul and ((A.tries or 0)) < 2 then
				local h;
				if pcall(function()
					h = A.get();
				end) and h then
					local Y = ((h - D.Position)).Magnitude;
					if Y < ((A.reach or 6)) + 3 then
						A.tries = ((A.tries or 0)) + 1;
						c = true;
					end;
				end;
			end;
			if not c then
				Y.i = Y.i + 1;
			end;
			Y.phase = "move";
			Y.t = h;
			Y.settle = nil;
		end;
	end;
	return true;
end;
function N.soulsNear(h, Y)
	local c = {};
	local A = D:FindFirstChild("Debree");
	if not A then
		return c;
	end;
	local function l(D)
		local A = string.lower(D.Name);
		if not string.match(A, "soul$") then
			return;
		end;
		if string.find(A, "host", 1, true) or string.find(A, "grab", 1, true) then
			return;
		end;
		local l = D;
		if D.ClassName ~= "Part" and D.ClassName ~= "MeshPart" then
			l = D:FindFirstChild("Root") or D:FindFirstChildWhichIsA("BasePart");
		end;
		if not l then
			return;
		end;
		for h = 1, #c, 1 do
			if c[h].root == l then
				return;
			end;
		end;
		local f;
		if pcall(function()
			f = ((l.Position - h)).Magnitude;
		end) and (f and f <= Y) then
			c[#c + 1] = {
					root = l,
					get = function()
						return l.Position;
					end,
					soul = true,
					hold = math.max(I.HoldT or 2, 2.5),
					lift = 0,
					reach = 6,
				};
		end;
	end;
	for h, Y in ipairs(A:GetChildren()) do
		l(Y);
		if Y.ClassName == "Folder" or Y.ClassName == "Model" then
			for h, Y in ipairs(Y:GetChildren()) do
				l(Y);
			end;
		end;
	end;
	return c;
end;
function N.soulStep(h)
	if not I.AutoSouls then
		return false;
	end;
	local Y, c = N.char();
	if not c then
		return false;
	end;
	local D = I.soul;
	if h - ((D and D.chk or 0)) < .25 then
		if not D or not D.pos then
			return false;
		end;
	end;
	local A = N.soulsNear(c.Position, I.LootRange);
	if #A == 0 then
		if D then
			if D.held then
				pcall(function()
					keyrelease(84);
				end);
			end;
			I.soul = nil;
		end;
		return false;
	end;
	local l, f;
	for h = 1, #A, 1 do
		local Y;
		if pcall(function()
			Y = A[h].get();
		end) and Y then
			local h = ((Y - c.Position)).Magnitude;
			if not f or h < f then
				l, f = Y, h;
			end;
		end;
	end;
	if not l then
		return false;
	end;
	if not D then
		D = { t = h, tries = 0 };
		I.soul = D;
	end;
	D.chk = h;
	D.pos = l;
	if f > 6 then
		D.held = false;
		pcall(function()
			keyrelease(84);
		end);
		N.travelTo(Vector3.new(l.X, l.Y - 3, l.Z), h);
		if h - D.t > 20 then
			I.soul = nil;
		end;
		return true;
	end;
	pcall(function()
		c.AssemblyLinearVelocity = Vector3.new(0, 0, 0);
	end);
	pcall(N.aimStep, l, true);
	if not N.focused(h) then
		return true;
	end;
	local n = math.max(I.HoldT or 2, 2.5);
	if not D.held then
		if h - ((D.rel or 0)) < .15 then
			return true;
		end;
		D.held = true;
		D.pressT = h;
		pcall(function()
			keypress(84);
		end);
	elseif h - ((D.pressT or h)) > n then
		D.held = false;
		D.rel = h;
		D.tries = D.tries + 1;
		pcall(function()
			keyrelease(84);
		end);
		if D.tries >= 6 then
			I.soul = nil;
		end;
	end;
	return true;
end;
function N.partOf(h, Y)
	local c = h.ClassName;
	if c == "Part" or c == "MeshPart" then
		return h;
	end;
	return (Y and h:FindFirstChild(Y)) or h:FindFirstChildWhichIsA("BasePart");
end;
function N.refind(h, Y)
	if not h or not Y then
		return nil;
	end;
	local c = h:GetChildren();
	for h = 1, #c, 1 do
		if N.addr(c[h]) == Y then
			return c[h];
		end;
	end;
	return nil;
end;
function N.pickup(h, Y, c, D, A, l)
	local I = l or {};
	I.folderFn, I.prefer = Y, A;
	I.node, I.part = c, D;
	I.addr, I.pAddr = N.addr(c), N.addr(h);
	I.seen = os.clock();
	I.get = function()
			return N.pickupPos(I);
		end;
	return I;
end;
function N.pickupPos(h)
	if h.gone then
		return nil;
	end;
	local Y = os.clock();
	if Y - ((h.seen or 0)) > .016 then
		local c = h.folderFn();
		local D = N.refind(c, h.addr);
		local A = D and N.partOf(D, h.prefer);
		if not A then
			h.gone = true;
			return nil;
		end;
		h.node, h.part, h.pAddr = D, A, N.addr(c);
		h.seen = Y;
	end;
	local c, D = pcall(function()
			return h.part.Position;
		end);
	if c and D then
		return D;
	end;
	return nil;
end;
local function i()
	return D:FindFirstChild("LootDrops");
end;
local function X()
	return D:FindFirstChild("Chests");
end;
function N.lootNear(h, Y)
	local c = {};
	local D = os.clock();
	local A = i();
	if A then
		for D, l in ipairs(A:GetChildren()) do
			local I = N.partOf(l);
			if I then
				local D;
				if pcall(function()
					D = ((I.Position - h)).Magnitude;
				end) and (D and D <= Y) then
					c[#c + 1] = N.pickup(A, i, l, I);
				end;
			end;
		end;
	end;
	local l = X();
	for A, f in ipairs(N.openChests()) do
		local n;
		if pcall(function()
			n = ((f.Root.Position - h)).Magnitude;
		end) and (n and n <= Y) then
			local h = f.Root.Position;
			local Y = f.Name .. ("@" .. (math.floor(h.X) .. ("," .. math.floor(h.Z))));
			local A = I.chestTap[Y];
			if not A or (D - A) > 45 then
				I.chestTap[Y] = D;
				table.insert(c, 1, N.pickup(l, X, f.Node, f.Root, "RootPart", { tap = true, side = true }));
			end;
		end;
	end;
	return c;
end;
function N.dAttr(h, Y, c)
	local D, A = pcall(function()
			return h:GetAttribute(Y);
		end);
	if D and A ~= nil then
		return A;
	end;
	return c;
end;
function N.dungeonMult(h, Y)
	for Y, c in ipairs({ "PointMultiplier", "PointsMultiplier", "ScoreMultiplier" }) do
		local D = tonumber(N.dAttr(h, c, nil));
		if D and D > 0 then
			if D > 100 then
				return D / 100;
			end;
			if D > 10 then
				return 1 + D / 100;
			end;
			return D;
		end;
	end;
	if not string.find(Y, "point", 1, true) and not string.find(Y, "score", 1, true) then
		return nil;
	end;
	local c = string.gsub(Y, "\195\151", "x");
	local D = tonumber(string.match(c, "x%s*(%d+%.?%d*)")) or tonumber(string.match(c, "(%d+%.?%d*)%s*x"));
	if D then
		return D;
	end;
	local A = tonumber(string.match(c, "(%d+%.?%d*)%s*%%"));
	if A then
		return 1 + A / 100;
	end;
	return nil;
end;
function N.dungeonScore(h, Y)
	local c = tostring(N.dAttr(h, "Title", h.Name) or "");
	local D = tostring(N.dAttr(h, "Description", "") or "");
	local A = tostring(N.dAttr(h, "Type", "") or "");
	local l = tostring(N.dAttr(h, "Stat", "") or "");
	local I = string.lower(c .. (" " .. (D .. (" " .. (A .. (" " .. l))))));
	local n = string.gsub(I, "[%s_%-]", "");
	if string.find(n, "secondwind", 1, true) then
		return Y and 2000000 or -50000;
	end;
	local q = N.dungeonMult(h, I);
	if q then
		return 1000000 + math.min(math.max(q, 0), 9) * 100000;
	end;
	if string.find(n, "bossrush", 1, true) then
		return 960000;
	end;
	if string.find(I, "ascension", 1, true) then
		return 950000;
	end;
	if string.find(n, "luckydraw", 1, true) then
		return 900000;
	end;
	local g = N.dAttr(h, "Rarity", 1);
	local S = tonumber(g) or 1;
	local k = string.lower(tostring(g));
	local H = string.find(I, "legendary", 1, true) ~= nil or S == 5 or string.find(k, "legendary", 1, true) ~= nil;
	local R = S == 6 or string.find(k, "mythic", 1, true) ~= nil or k == "red";
	local b = S == 3 or string.find(k, "rare", 1, true) ~= nil or k == "blue";
	local w = false;
	for h, Y in ipairs(f.DungeonBad) do
		if string.find(I, Y, 1, true) then
			w = true;
			break;
		end;
	end;
	if not w and ((R or H or b)) then
		local h = string.find(I, "damage", 1, true) or string.find(I, "dmg", 1, true);
		local Y = string.find(I, "health", 1, true) or string.find(I, "heal", 1, true) or string.find(I, "hp", 1, true) or string.find(I, "life", 1, true);
		local c = string.find(n, "attackspeed", 1, true) or string.find(I, "faster attacks", 1, true);
		if h or Y or c then
			return R and 880000 or (H and 870000 or 860000);
		end;
	end;
	local W = 0;
	if R then
		W = W + 70000;
	elseif H then
		W = W + 60000;
	elseif b then
		W = W + 50000;
	end;
	if w then
		W = W - 100000;
	else
		W = W + 10000;
		if string.find(I, "heal", 1, true) or string.find(I, "health", 1, true) or string.find(I, "regen", 1, true) or string.find(I, "points", 1, true) then
			W = W + 10000;
		end;
	end;
	return W;
end;
f.DungeonBad = {
		"cannot dash",
		"can\'t dash",
		"no dash",
		"double jump",
		"no jumping",
		"cannot jump",
		"less health",
		"reduced health",
		"max health reduced",
		"lose health",
		"health is reduced",
		"take more damage",
		"damage taken",
		"slower",
		"reduced damage",
		"less damage",
		"enemies deal more",
		"reduced attack speed",
		"less attack speed",
		"slower attacks",
	};
function N.dungeonOfferButton(h)
	local Y = A:FindFirstChild("PlayerGui");
	local c = Y and Y:FindFirstChild("ComponentsHolder");
	local D = c and c:FindFirstChild("MainNotificationFrame");
	local l = D and D:FindFirstChild("OuwigaharaOffers");
	if not l then
		return nil;
	end;
	local I = l:FindFirstChild(h);
	if not I then
		for Y, c in ipairs(l:GetDescendants()) do
			if c.Name == h and c.ClassName == "Frame" then
				I = c;
				break;
			end;
		end;
	end;
	if not I then
		return nil;
	end;
	for h, Y in ipairs(I:GetDescendants()) do
		if Y.ClassName == "TextButton" or Y.ClassName == "ImageButton" then
			return Y;
		end;
	end;
	return I;
end;
function N.dungeonCardsShown()
	local h = A:FindFirstChild("OuwigaharaOffers");
	if not h or #h:GetChildren() == 0 then
		return false;
	end;
	if N.dAttr(h, "Picked", false) == true then
		return false;
	end;
	local Y = A:FindFirstChild("PlayerGui");
	local c = Y and Y:FindFirstChild("ComponentsHolder");
	local D = c and c:FindFirstChild("MainNotificationFrame");
	local l = D and D:FindFirstChild("OuwigaharaOffers");
	return l ~= nil and #l:GetChildren() > 0;
end;
function N.dungeonCard(h)
	local Y = I.dungeon;
	if h < ((Y.cardAt or 0)) then
		return false;
	end;
	Y.cardAt = h + .25;
	local c = A:FindFirstChild("OuwigaharaOffers");
	if not c then
		return false;
	end;
	if N.dAttr(c, "Picked", false) == true then
		return false;
	end;
	local D = c:GetChildren();
	if #D == 0 then
		return false;
	end;
	if not N.focused(h) or not N.scrOffset(h) then
		return false;
	end;
	local l, f;
	for h, c in ipairs(D) do
		local D = tostring(N.dAttr(c, "Title", c.Name) or c.Name);
		local A = tostring(N.dAttr(c, "Description", "") or "");
		local I = string.lower(c.Name .. (" " .. (D .. (" " .. A))));
		if not string.find(I, "mother bear", 1, true) and not string.find(I, "reroll", 1, true) then
			local h = N.dungeonScore(c, ((Y.swRounds or 0)) > 0);
			if not f or h > f then
				l, f = c, h;
			end;
		end;
	end;
	if not l then
		return false;
	end;
	local n = N.dungeonOfferButton(l.Name);
	if not n then
		return false;
	end;
	local q;
	pcall(function()
		q = n.AbsolutePosition;
	end);
	if not q then
		return false;
	end;
	local g = N.absSize and N.absSize(n);
	local S, k = g and tonumber(g.X) or 0, g and tonumber(g.Y) or 0;
	N.dlgClickAt(q.X + ((S > 4 and S * .5 or 40)), q.Y + ((k > 4 and k * .5 or 14)));
	Y.cardAt = h + .75;
	if ((Y.swRounds or 0)) > 0 then
		Y.swRounds = Y.swRounds - 1;
	end;
	N.notify("dungeon: took " .. tostring(N.dAttr(l, "Title", l.Name) or l.Name), 3);
	return true;
end;
function N.dungeonStep(h)
	local Y = I.dungeon;
	if not Y then
		Y = {};
		I.dungeon = Y;
	end;
	local c, D, A = N.char();
	if A then
		local h, c = 0, 0;
		pcall(function()
			h = A.Health;
			c = A.MaxHealth;
		end);
		if c > 0 and h > 0 then
			local D = h / c;
			if Y.lowHp then
				if D >= .85 then
					Y.lowHp = false;
				end;
			elseif D <= .75 then
				Y.lowHp = true;
				Y.swRounds = 2;
				N.notify("dungeon: health low - taking Second Wind for the next 2 picks", 4);
			end;
		end;
	end;
	return N.dungeonCard(h);
end;
function N.dungeonNames()
	if f.DungeonNames then
		return f.DungeonNames;
	end;
	local h = { ["cache trainee"] = true };
	for Y, c in ipairs(f.Mobs) do
		if not string.find(c, "^All ") then
			h[string.lower(c)] = true;
		end;
	end;
	for Y, c in ipairs(f.Bosses) do
		if not string.find(c, "^All ") then
			h[string.lower(c)] = true;
		end;
	end;
	f.DungeonNames = h;
	return h;
end;
function N.dungeonTargets(h)
	local Y = I.dungeon;
	if not Y then
		Y = {};
		I.dungeon = Y;
	end;
	if Y.listAt and h - Y.listAt < .3 then
		return Y.list or {};
	end;
	Y.listAt = h;
	local c = {};
	local A = N.dungeonNames();
	for Y, l in ipairs({ "Humanoids", "Debree" }) do
		local I = D:FindFirstChild(l);
		local f = I and I:FindFirstChild("Regions");
		for Y, D in ipairs({ "Ouwigahara", "Temporary" }) do
			local I = f and f:FindFirstChild(D);
			local n = I and I:FindFirstChild("ActiveNpcs");
			for Y, I in ipairs(n and n:GetChildren() or {}) do
				local f = I.Name;
				if not S[f] then
					local Y = I:FindFirstChildOfClass("Model");
					if not Y and I.ClassName == "Model" then
						Y = I;
					end;
					if Y then
						local n = D ~= "Temporary" or A[string.lower(f)] == true;
						if not n then
							n = ((Y:FindFirstChild("OuwigaharaMark") or I:FindFirstChild("OuwigaharaMark"))) ~= nil;
						end;
						local q = n and Y:FindFirstChildOfClass("Humanoid");
						local g = q and Y:FindFirstChild("HumanoidRootPart");
						local S = false;
						if g then
							pcall(function()
								S = q.Health > 0;
							end);
						end;
						if S then
							local A = {
									Model = Y,
									Hum = q,
									Root = g,
									Name = f,
									Boss = N.isBoss(I),
								};
							A.PAddr = N.addr(I);
							A.IAddr = A.PAddr;
							A.MAddr = N.addr(Y);
							A.Src, A.Region, A.chkT = l, D, h;
							c[#c + 1] = A;
						end;
					end;
				end;
			end;
		end;
	end;
	Y.list = c;
	return c;
end;
function N.crowBossFromText(h)
	local Y = string.lower(tostring(h or ""));
	if Y == "" or not string.find(Y, "defeat", 1, true) then
		return nil;
	end;
	if string.find(Y, "mother bear", 1, true) then
		return nil;
	end;
	for h, c in ipairs(f.Bosses) do
		if c ~= "All Bosses" and (c ~= "Mother Bear" and string.find(Y, string.lower(c), 1, true)) then
			return c;
		end;
	end;
	return nil;
end;
function N.crowChoices()
	local h = os.clock();
	local Y = I.crowPicks;
	if Y and (h - Y.t) < .35 then
		return Y.list;
	end;
	local c = N.dlgRoot and N.dlgRoot();
	if c then
		local Y = {};
		for h, c in ipairs(N.dlgAnswers(c)) do
			local D = N.crowBossFromText(c.text);
			if D then
				Y[#Y + 1] = {
						x = c.x,
						y = c.y,
						boss = D,
						dlg = true,
					};
			end;
		end;
		if #Y > 0 then
			table.sort(Y, function(h, Y)
				return h.y < Y.y;
			end);
			I.crowPicks = { t = h, list = Y };
			return Y;
		end;
	end;
	local D = {};
	local l = A:FindFirstChild("PlayerGui");
	if not l then
		I.crowPicks = { t = h, list = D };
		return D;
	end;
	local function f(h, Y)
		if Y > 8 then
			return;
		end;
		for h, c in ipairs(h:GetChildren()) do
			local A = c.ClassName;
			if A == "TextButton" or A == "TextLabel" or A == "ImageButton" then
				local h;
				pcall(function()
					h = c.Text;
				end);
				local Y = N.crowBossFromText(h);
				if Y then
					local h;
					pcall(function()
						h = c.AbsolutePosition;
					end);
					if not ((h and (h.X > 0 and h.Y > 0))) then
						local Y = c.Parent;
						for c = 1, 4, 1 do
							if not Y then
								break;
							end;
							local D;
							pcall(function()
								D = Y.AbsolutePosition;
							end);
							if D and (D.X > 0 and D.Y > 0) then
								h = D;
								break;
							end;
							Y = Y.Parent;
						end;
					end;
					if h and (h.X > 0 and h.Y > 0) then
						local A, l = 0, 0;
						local I = N.absSize and N.absSize(c);
						if I then
							A = tonumber(I.X) or 0;
							l = tonumber(I.Y) or 0;
						end;
						local f, n, q, g;
						local S = c.Parent;
						for h = 1, 4, 1 do
							if not S then
								break;
							end;
							local Y = S.ClassName;
							if Y == "TextButton" or Y == "ImageButton" then
								local h;
								pcall(function()
									h = S.AbsolutePosition;
								end);
								local Y = N.absSize and N.absSize(S);
								if h and (h.X > 0 and h.Y > 0) then
									f, n = h.X, h.Y;
									q = Y and tonumber(Y.X) or 0;
									g = Y and tonumber(Y.Y) or 0;
								end;
								break;
							end;
							S = S.Parent;
						end;
						D[#D + 1] = {
								x = h.X,
								y = h.Y,
								w = A,
								h = l,
								bx = f,
								by = n,
								bw = q,
								bh = g,
								boss = Y,
							};
					end;
				end;
			end;
			f(c, Y + 1);
		end;
	end;
	f(l, 0);
	table.sort(D, function(h, Y)
		return h.y < Y.y;
	end);
	local n, q = {}, {};
	for h, Y in ipairs(D) do
		local c = Y.boss .. (":" .. math.floor(Y.y / 20));
		if not n[c] then
			n[c] = true;
			q[#q + 1] = Y;
		end;
	end;
	I.crowPicks = { t = h, list = q };
	return q;
end;
function N.crowDump()
	if I.crowDumped then
		return;
	end;
	I.crowDumped = true;
	local h = { os.date("%H:%M:%S") .. " crow menu dump" };
	local function Y(Y, c)
		local D, A, l, I, f, n = "?", "?", nil, nil, nil, nil;
		pcall(function()
			D = Y.Name;
			A = Y.ClassName;
		end);
		pcall(function()
			l = Y.Text;
		end);
		pcall(function()
			I = Y.AbsolutePosition;
		end);
		pcall(function()
			f = Y.Visible;
			n = Y.Active;
		end);
		local q = N.absSize and N.absSize(Y);
		h[#h + 1] = string.rep(" ", c) .. (A .. (" " .. (D .. (" pos=" .. (((I and (math.floor(I.X) .. ("," .. math.floor(I.Y))) or "?")) .. (" size=" .. (((q and (math.floor(tonumber(q.X) or 0) .. ("x" .. math.floor(tonumber(q.Y) or 0))) or "?")) .. (" vis=" .. (tostring(f) .. (" active=" .. (tostring(n) .. ((l and (l ~= "" and (" text=" .. string.sub(tostring(l), 1, 40))) or "")))))))))))));
	end;
	local c = A:FindFirstChild("PlayerGui");
	local function D(c, A)
		if A > 9 then
			return;
		end;
		for c, l in ipairs(c:GetChildren()) do
			local I;
			pcall(function()
				I = l.Text;
			end);
			if N.crowBossFromText(I) then
				local c, D = {}, l;
				for h = 1, 6, 1 do
					if not D then
						break;
					end;
					c[#c + 1] = D;
					D = D.Parent;
				end;
				h[#h + 1] = "--- offer: " .. tostring(I);
				for h = #c, 1, -1 do
					Y(c[h], ((#c - h)) * 2);
				end;
				for h, c in ipairs(l.Parent and l.Parent:GetChildren() or {}) do
					Y(c, 14);
				end;
			end;
			D(l, A + 1);
		end;
	end;
	if c then
		pcall(D, c, 0);
	end;
	pcall(function()
		writefile("crow_ui.txt", table.concat(h, string.char(10)));
	end);
	N.notify("crow: wrote crow_ui.txt", 4);
end;
function N.crowPoint(h, Y)
	Y = Y or 1;
	if h.dlg and Y <= 1 then
		return h.x + 40, h.y + 14;
	end;
	if Y <= 1 and (((h.w or 0)) > 4 and ((h.h or 0)) > 4) then
		return h.x + h.w * .5, h.y + h.h * .5;
	end;
	if Y <= 2 then
		return h.x + 40, h.y + 14;
	end;
	if Y <= 3 and (h.bx and ((h.bw or 0)) > 4) then
		return h.bx + h.bw * .5, h.by + ((h.bh or 20)) * .5;
	end;
	if Y <= 4 then
		return h.x + 120, h.y + 14;
	end;
	return h.x + 8, h.y + 8;
end;
function N.equipSlot(h, Y)
	local c = 48 + math.max(1, math.min(5, math.floor(h or 1)));
	pcall(function()
		keypress(c);
	end);
	I.crowKeyUp = { vk = c, t = Y + .15 };
end;
function N.heldItem()
	local h = A.Character;
	if not h then
		return nil;
	end;
	local Y = h:GetChildren();
	for h = 1, #Y, 1 do
		if Y[h].ClassName == "Tool" then
			return Y[h].Name;
		end;
	end;
	local c = h:FindFirstChild("Tool_Accessories");
	local D = c and (c:GetChildren())[1];
	return D and D.Name or nil;
end;
function N.weaponHeld()
	local h = N.heldItem();
	if not h then
		return false;
	end;
	local Y = string.lower(h);
	if f.StyleMap[Y] then
		return true;
	end;
	if f.Presets[h] and h ~= "Combat" then
		return true;
	end;
	for h in pairs(f.StyleWords) do
		if string.find(Y, h, 1, true) then
			return true;
		end;
	end;
	return string.find(Y, "katana", 1, true) ~= nil;
end;
function N.crowClose(h)
	N.equipSlot(I.WeapSlot or 1, h);
end;
function N.crowUse(h)
	N.equipSlot(I.CrowSlot or 2, h);
	I.crow.clickAt = h + .6;
end;
function N.crowStep(h)
	local Y = I.crow;
	if not Y then
		Y = { phase = "idle", t = h };
		I.crow = Y;
	end;
	if not N.focused(h) then
		return nil;
	end;
	if not N.scrOffset(h) then
		return nil;
	end;
	if I.crowKeyUp and h >= I.crowKeyUp.t then
		local h = I.crowKeyUp.vk;
		I.crowKeyUp = nil;
		pcall(function()
			keyrelease(h);
		end);
	end;
	if Y.clickAt and h >= Y.clickAt then
		Y.clickAt = nil;
		N.dlgClickAt(((I.crowClickX or 960)) - ((I.scrOffX or 0)), ((I.crowClickY or 540)) - ((I.scrOffY or 0)));
	end;
	local c = N.activeQuest();
	if c and (not c.Complete and Y.phase ~= "fight") then
		local D = N.questTarget(c);
		if not D then
			for h = 1, #((c.Tasks or {})), 1 do
				D = D or N.codeToMob(c.Tasks[h].Code);
			end;
		end;
		if D and f.BossNames[string.lower(D)] then
			Y.target = D;
			Y.quest = c.Name;
			Y.phase = "fight";
			Y.lastAlive = h;
			N.notify("crow: already on " .. (c.Name .. (" - hunting " .. D)), 4);
			return D;
		end;
	end;
	if Y.phase == "idle" then
		if h < ((Y.nextAt or 0)) then
			return nil;
		end;
		Y.prevQuest = c and c.Name or nil;
		N.crowUse(h);
		Y.phase = "menu";
		Y.menuAt = h + 3.5;
		Y.deadline = h + 10;
		return nil;
	end;
	if Y.phase == "menu" then
		if h < ((Y.menuAt or 0)) then
			return nil;
		end;
		local c = N.crowChoices();
		if #c > 0 then
			local D = c[1];
			Y.aim = (Y.aim or (I.crowAim or 1));
			local A, l = N.crowPoint(D, Y.aim);
			N.dlgClickAt(A, l);
			Y.pick = D;
			Y.target = D.boss;
			Y.phase = "quest";
			Y.deadline = h + 5;
			Y.sawBoss = false;
			N.mark("crow click " .. (D.boss .. (" aim " .. (tostring(Y.aim) .. (" at " .. (math.floor(A) .. ("," .. math.floor(l))))))));
			N.notify("crow: taking " .. D.boss, 3);
		elseif h >= ((Y.deadline or 0)) then
			Y.phase = "idle";
			Y.nextAt = h + 2;
			N.notify("crow: no boss offer found, retrying", 4);
		end;
		return nil;
	end;
	if Y.phase == "quest" then
		local D = c and c.Name ~= Y.prevQuest;
		if c and (not c.Complete and ((D or (Y.target and N.questMatchesEntry(c.Name, Y.target))))) then
			I.crowAim = Y.aim or 1;
			Y.quest = c.Name;
			Y.phase = "fight";
			Y.lastAlive = h;
			Y.eqT = 0;
			if #N.crowChoices() > 0 then
				N.crowClose(h);
			end;
			N.notify("crow: hunting " .. tostring(Y.target), 3);
		elseif h >= ((Y.deadline or 0)) then
			if #N.crowChoices() > 0 and ((Y.aim or 1)) < 5 then
				pcall(N.crowDump);
				Y.aim = ((Y.aim or 1)) + 1;
				Y.phase = "menu";
				Y.menuAt = h + .4;
				Y.deadline = h + 8;
				return nil;
			end;
			N.crowClose(h);
			Y.aim = nil;
			Y.target = nil;
			Y.phase = "idle";
			Y.nextAt = h + 3;
		end;
		return nil;
	end;
	if not N.weaponHeld() and h - ((Y.eqT or 0)) > 1.5 then
		Y.eqT = h;
		N.equipSlot(I.WeapSlot or 1, h);
	end;
	if not c or (c.Name == Y.quest and c.Complete) then
		Y.target, Y.quest, Y.phase, Y.nextAt = nil, nil, "idle", h + 1;
		N.notify("crow: boss quest done, next one", 3);
		return nil;
	end;
	return Y.target;
end;
f.Crystals = {
		{ Region = "Mistfall Harbor", Name = "Mistfall Harbor", Pos = Vector3.new(135.8, 873.7, 734.1) },
		{ Region = "Windy Peak", Name = "Windy Peak", Pos = Vector3.new(-650, 1245, -1100) },
		{ Region = "Bamboo Grove", Name = "Bamboo Grove Sanctuary", Pos = Vector3.new(540, 1122, -1023) },
		{ Region = "Bamboo Grove", Name = "Bamboo Grove", Pos = Vector3.new(657.4, 1021.2, 139.7) },
		{ Region = "Butterfly Estate", Name = "Butterfly Estate", Pos = Vector3.new(-1767.39, 314.51, -121.67) },
		{ Region = "Hidden Mist Village", Name = "Hidden Mist Village", Pos = Vector3.new(-263.1, 1043.5, -1150.5) },
		{ Region = "Iceveil Valley", Name = "Iceveil Settlement", Pos = Vector3.new(485.3, 1222.9, -1813.4) },
		{ Region = "Final Selection Plains", Name = "Final Selection", Pos = Vector3.new(-2626.7, 284, -191.2) },
	};
function N.unlockStart()
	if I.unlock then
		N.notify("already unlocking locations", 3);
		return;
	end;
	I.unlock = { i = 1, phase = "go", t = os.clock() };
	N.notify("unlocking " .. (#f.Crystals .. " locations - panic key stops it"), 4);
	N.sync();
end;
function N.unlockStop()
	if I.unlock and I.unlock.held then
		pcall(function()
			keyrelease(84);
		end);
	end;
	I.unlock = nil;
end;
function N.crystalPart(h)
	local Y = D:FindFirstChild("Debree");
	local c = Y and Y:FindFirstChild("Regions");
	local A = c and c:FindFirstChild(h);
	for h, Y in ipairs(A and A:GetChildren() or {}) do
		local c = string.lower(Y.Name);
		if string.find(c, "crystal", 1, true) or string.find(c, "spawn", 1, true) then
			local h = Y:FindFirstChild("Root") or Y:FindFirstChild("Cube") or Y:FindFirstChildWhichIsA("BasePart");
			if Y.ClassName == "Part" or Y.ClassName == "MeshPart" then
				h = Y;
			end;
			if h then
				return h;
			end;
		end;
	end;
	return nil;
end;
function N.unlockStep(h)
	local Y = I.unlock;
	if not Y then
		return false;
	end;
	local c = f.Crystals[Y.i];
	if not c then
		N.unlockStop();
		N.notify("every location visited", 4);
		N.sync();
		return false;
	end;
	if not N.focused(h) then
		return true;
	end;
	if Y.phase == "go" then
		if h < Y.t then
			return true;
		end;
		N.notify("unlocking " .. (Y.i .. ("/" .. (#f.Crystals .. (": " .. c.Name)))), 2);
		N.tpTo(c.Pos, 3);
		Y.phase, Y.t = "find", h;
	elseif Y.phase == "find" then
		if h - Y.t < 1.0 then
			return true;
		end;
		local D = N.crystalPart(c.Region);
		local A;
		if D then
			pcall(function()
				A = D.Position;
			end);
		end;
		if A then
			N.tpTo(Vector3.new(A.X, A.Y, A.Z + 2.5), 1.5);
		end;
		Y.phase, Y.t = "hold", h;
	elseif Y.phase == "hold" then
		if not Y.held then
			if h - Y.t < .3 then
				return true;
			end;
			pcall(function()
				keypress(84);
			end);
			Y.held, Y.t = true, h;
		elseif h - Y.t >= 1.5 then
			pcall(function()
				keyrelease(84);
			end);
			Y.held = false;
			Y.i = Y.i + 1;
			Y.phase, Y.t = "go", h + .5;
		end;
	end;
	return true;
end;
function N.scrOffset(h)
	if I.scrOffX then
		return true;
	end;
	local Y = A:GetMouse();
	if not I.scrCalT then
		I.scrMx, I.scrMy = Y.X, Y.Y;
		pcall(function()
			mousemoveabs(700, 450);
		end);
		I.scrCalT = h;
		return false;
	end;
	if h - I.scrCalT < .12 then
		return false;
	end;
	I.scrOffX, I.scrOffY = 700 - Y.X, 450 - Y.Y;
	pcall(function()
		mousemoveabs(I.scrMx + I.scrOffX, I.scrMy + I.scrOffY);
	end);
	return true;
end;
function N.dlgRoot()
	local h = A:FindFirstChild("PlayerGui");
	local Y = h and h:FindFirstChild("ComponentsHolder");
	return Y and Y:FindFirstChild("DialogueFrame") or nil;
end;
local function J(h, Y)
	Y = Y or 0;
	if Y > 6 then
		return nil;
	end;
	for h, c in ipairs(h:GetChildren()) do
		local D, A;
		pcall(function()
			D = c.Text;
		end);
		pcall(function()
			A = c.AbsolutePosition;
		end);
		if D and (D ~= "" and A) then
			return D, A;
		end;
		local l, I = J(c, Y + 1);
		if l then
			return l, I;
		end;
	end;
	return nil;
end;
function N.dlgAnswers(h)
	local Y = {};
	local c = h and h:FindFirstChild("ButtonHolder", true);
	if not c then
		local function Y(h, c)
			if c > 5 then
				return nil;
			end;
			for h, D in ipairs(h:GetChildren()) do
				if D.Name == "ButtonHolder" then
					return D;
				end;
				local A = Y(D, c + 1);
				if A then
					return A;
				end;
			end;
		end;
		c = h and Y(h, 0) or nil;
	end;
	if not c then
		return Y;
	end;
	for h, c in ipairs(c:GetChildren()) do
		local D = J(c, 0);
		local A;
		pcall(function()
			A = c.AbsolutePosition;
		end);
		if A and ((A.X > 0 or A.Y > 0)) then
			Y[#Y + 1] = { text = D or c.Name, x = A.X, y = A.Y };
		end;
	end;
	return Y;
end;
function N.dlgContinue(h)
	local Y;
	local function c(h, Y)
		if Y > 5 then
			return nil;
		end;
		for h, D in ipairs(h:GetChildren()) do
			if D.Name == "DialogueHolder" then
				return D;
			end;
			local A = c(D, Y + 1);
			if A then
				return A;
			end;
		end;
	end;
	Y = h and c(h, 0) or nil;
	if not Y then
		return nil;
	end;
	local D, A = J(Y, 0);
	if A and A.X < 0 then
		return nil;
	end;
	return A;
end;
function N.dlgClickAt(h, Y)
	I.crowPicks = nil;
	I.dlgClick = {
			x = h + ((I.scrOffX or 0)),
			y = Y + ((I.scrOffY or 0)),
			st = "move",
			t = os.clock(),
		};
end;
function N.dlgTalk(h, Y)
	local c = I.dlg;
	if not c or c.want ~= h then
		c = { want = h, t = Y, tries = 0 };
		I.dlg = c;
	end;
	if not N.focused(Y) then
		return "working";
	end;
	if not N.scrOffset(Y) then
		return "working";
	end;
	local D = N.dlgRoot();
	if not D then
		if c.opened then
			I.dlg = nil;
			return "done";
		end;
		if Y - c.t > 20 then
			I.dlg = nil;
			return "failed";
		end;
		if Y - ((c.openT or 0)) > 2.5 then
			c.openT = Y;
			c.tries = c.tries + 1;
			pcall(function()
				keypress(84);
			end);
			I.dlgKeyUp = Y + .15;
		end;
		return "working";
	end;
	c.opened = true;
	if not h then
		return "done";
	end;
	if Y - ((c.clickT or 0)) < .8 then
		return "working";
	end;
	c.clickT = Y;
	for Y, D in ipairs(N.dlgAnswers(D)) do
		if D.text == h then
			N.dlgClickAt(D.x + 40, D.y + 14);
			c.clicked = true;
			return "working";
		end;
	end;
	local A = N.dlgContinue(D);
	if A then
		N.dlgClickAt(A.X + 12, A.Y + 8);
	end;
	if Y - c.t > 25 then
		I.dlg = nil;
		return "failed";
	end;
	return "working";
end;
function N.dlgRelease(h)
	local Y = I.dlgClick;
	if Y then
		if Y.st == "move" then
			pcall(function()
				mousemoveabs(Y.x, Y.y);
			end);
			Y.st, Y.t = "press", h;
		elseif Y.st == "press" and h - Y.t > .1 then
			pcall(function()
				mouse1press();
			end);
			I.dlgUp = h + .08;
			Y.st, Y.t = "down", h;
		elseif Y.st == "down" and h - Y.t > .3 then
			I.dlgClick = nil;
		end;
	end;
	if I.dlgUp and h >= I.dlgUp then
		I.dlgUp = nil;
		pcall(function()
			mouse1release();
		end);
	end;
	if I.crowKeyUp and h >= I.crowKeyUp.t then
		local h = I.crowKeyUp.vk;
		I.crowKeyUp = nil;
		pcall(function()
			keyrelease(h);
		end);
	end;
	if I.dlgKeyUp and h >= I.dlgKeyUp then
		I.dlgKeyUp = nil;
		pcall(function()
			keyrelease(84);
		end);
	end;
end;
function N.acoPath(h, Y)
	local c = #Y;
	if c == 0 then
		return {};
	end;
	if c == 1 then
		return { 1 };
	end;
	local D = { [0] = h };
	for h = 1, c, 1 do
		D[h] = Y[h];
	end;
	local A = {};
	for h = 0, c, 1 do
		A[h] = {};
		for Y = 0, c, 1 do
			local c, l = D[h], D[Y];
			local I, f, N = c.X - l.X, c.Y - l.Y, c.Z - l.Z;
			A[h][Y] = math.max(math.sqrt((I * I + f * f) + N * N), .01);
		end;
	end;
	local function l(h)
		local Y, c = 0, 0;
		for D = 1, #h, 1 do
			Y = Y + A[c][h[D]];
			c = h[D];
		end;
		return Y;
	end;
	local I, f, N = {}, {}, 0;
	for h = 1, c, 1 do
		local Y, D = nil, math.huge;
		for h = 1, c, 1 do
			if not f[h] and A[N][h] < D then
				D = A[N][h];
				Y = h;
			end;
		end;
		f[Y] = true;
		I[#I + 1] = Y;
		N = Y;
	end;
	local n = l(I);
	local q = 1 / ((c * n));
	local g = {};
	for h = 0, c, 1 do
		g[h] = {};
		for Y = 0, c, 1 do
			g[h][Y] = q;
		end;
	end;
	local S, k, H, R = 1, 3, .2, .9;
	local b = math.min(20, c + 5);
	local w = c <= 8 and 40 or (c <= 20 and 30 or 15);
	for h = 1, w, 1 do
		for h = 1, b, 1 do
			local Y, D, f = {}, {}, 0;
			for h = 1, c, 1 do
				local l;
				if math.random() < R then
					local h = -1;
					for c = 1, c, 1 do
						if not Y[c] then
							local Y = g[f][c] ^ S * ((1 / A[f][c])) ^ k;
							if Y > h then
								h = Y;
								l = c;
							end;
						end;
					end;
				else
					local h, D = 0, {};
					for c = 1, c, 1 do
						if not Y[c] then
							D[c] = g[f][c] ^ S * ((1 / A[f][c])) ^ k;
							h = h + D[c];
						end;
					end;
					local I = math.random() * h;
					for h = 1, c, 1 do
						if D[h] then
							I = I - D[h];
							if I <= 0 then
								l = h;
								break;
							end;
						end;
					end;
					if not l then
						for h = 1, c, 1 do
							if not Y[h] then
								l = h;
								break;
							end;
						end;
					end;
				end;
				g[f][l] = ((1 - H)) * g[f][l] + H * q;
				Y[l] = true;
				D[#D + 1] = l;
				f = l;
			end;
			local N = l(D);
			if N < n then
				n = N;
				I = D;
			end;
		end;
		local Y = 0;
		for h = 1, #I, 1 do
			local c = I[h];
			g[Y][c] = ((1 - H)) * g[Y][c] + H / n;
			Y = c;
		end;
	end;
	return I, n;
end;
function N.routeStart(h)
	local Y, c = N.char();
	if not c or not h or #h == 0 then
		return false;
	end;
	local D, A = {}, {};
	for Y = 1, #h, 1 do
		local c, l = pcall(h[Y].get);
		if c and l then
			D[#D + 1] = l;
			A[#A + 1] = h[Y];
		end;
	end;
	if #D == 0 then
		return false;
	end;
	local l, f = N.acoPath(c.Position, D);
	local n = {};
	for h = 1, #l, 1 do
		n[h] = A[l[h]];
	end;
	I.route = {
			list = n,
			i = 1,
			t = os.clock(),
			tDown = false,
			nextT = 0,
		};
	N.notify(string.format("boss drops: %d, path %.0f studs", #n, f or 0), 3);
	return true;
end;
function N.routeStop()
	local h = I.route;
	I.route = nil;
	if h and h.tDown then
		pcall(function()
			keyrelease(84);
		end);
	end;
end;
function N.routeStep(h)
	local Y = I.route;
	if not Y then
		return false;
	end;
	local c, D = N.char();
	if not D then
		return true;
	end;
	if N.focused(h) and h >= Y.nextT then
		Y.nextT = h + I.RouteTap;
		Y.tDown = not Y.tDown;
		pcall(function()
			if Y.tDown then
				keypress(84);
			else
				keyrelease(84);
			end;
		end);
	end;
	local A = Y.list[Y.i];
	if not A then
		N.routeStop();
		return false;
	end;
	local l;
	if not pcall(function()
		l = A.get();
	end) or not l then
		Y.i = Y.i + 1;
		Y.t = h;
		Y.arrive = nil;
		return true;
	end;
	local f = D.Position;
	local n, q, g = l.X - f.X, ((l.Y + 2.5)) - f.Y, l.Z - f.Z;
	local S = math.sqrt((n * n + q * q) + g * g);
	if S <= 3 then
		Y.i = Y.i + 1;
		Y.t = h;
		if not Y.list[Y.i] then
			pcall(function()
				D.AssemblyLinearVelocity = Vector3.new(0, 0, 0);
			end);
		end;
		return true;
	end;
	if S > I.WarpAbove then
		pcall(function()
			D.CFrame = CFrame.new(l.X, l.Y + 3, l.Z);
			D.AssemblyLinearVelocity = Vector3.new(0, 0, 0);
		end);
	else
		local h = I.MoveSpeed;
		if not Y.list[Y.i + 1] then
			h = math.min(S * 5.0, I.MoveSpeed);
		end;
		local c = h / S;
		pcall(function()
			D.AssemblyLinearVelocity = Vector3.new(n * c, q * c, g * c);
		end);
	end;
	if h - Y.t > 5 then
		Y.i = Y.i + 1;
		Y.t = h;
	end;
	return true;
end;
function N.activeQuest()
	local h = Y:FindFirstChild("Player_Service");
	local c = h and h:FindFirstChild("Data");
	local D = c and c:FindFirstChild(A.Name);
	if not D then
		return nil;
	end;
	local l = D:FindFirstChild("slotEquipped");
	l = l and l.Value or 1;
	local I = D:FindFirstChild("slots");
	local f = I and I:FindFirstChild("Slot" .. tostring(math.floor(l)));
	local N = f and f:FindFirstChild("Quests");
	local n = N and N:FindFirstChild("Holder");
	if not n then
		return nil;
	end;
	local q = nil;
	local g = n:GetChildren();
	for h = 1, #g, 1 do
		local Y = g[h]:FindFirstChild("Tasks");
		if Y then
			local c, D = {}, true;
			local A = Y:GetChildren();
			for h = 1, #A, 1 do
				local Y = A[h]:FindFirstChild("Value");
				local l = A[h]:FindFirstChild("Max");
				local I;
				local f = A[h]:FindFirstChild("Code");
				if f then
					local h = f.Value;
					if type(h) == "string" and string.match(h, "^[%w ]+$") then
						I = h;
					end;
				end;
				c[#c + 1] = {
						Name = A[h].Name,
						Value = Y and Y.Value or 0,
						Max = l and l.Value or 1,
						Code = I,
					};
				if ((Y and Y.Value or 0)) < ((l and l.Value or 1)) then
					D = false;
				end;
			end;
			local l = g[h]:FindFirstChild("QuestString");
			local I = nil;
			if l then
				local h = l.Value;
				if type(h) == "string" and #h > 0 then
					local Y = true;
					for c = 1, #h, 1 do
						local D = string.byte(h, c);
						if D < 32 or D > 126 then
							Y = false;
							break;
						end;
					end;
					if Y then
						I = h;
					end;
				end;
			end;
			local f = {
					Name = g[h].Name,
					Desc = I,
					Id = g[h].Name,
					Tasks = c,
					Complete = D,
				};
			if not D then
				return f;
			end;
			if not q then
				q = f;
			end;
		end;
	end;
	return q;
end;
local d = {
		"guard",
		"guards",
		"subordinate",
		"subordinates",
		"minion",
		"minions",
		"underling",
		"underlings",
		"escort",
	};
function N.questPickups()
	local h = {};
	for Y, c in ipairs(D:GetChildren()) do
		if string.match(c.Name, "%d+$") then
			local Y;
			if c.ClassName == "Part" or c.ClassName == "MeshPart" then
				Y = c;
			else
				Y = c:FindFirstChildWhichIsA("BasePart");
			end;
			if Y then
				local D = false;
				for h, Y in ipairs(c:GetDescendants()) do
					if Y.ClassName == "ProximityPrompt" then
						D = true;
						break;
					end;
				end;
				if D then
					h[#h + 1] = { Name = c.Name, Root = Y, Node = c };
				end;
			end;
		end;
	end;
	return h;
end;
local z = {
		"defeated",
		"remaining",
		"slain",
		"killed",
		"eliminated",
		"defeat",
		"hunted",
		"fell ",
	};
local O = {
		"found",
		"collected",
		"delivered",
		"escorted",
		"caught",
		"return",
		"retrieve",
		"recovered",
		"gathered",
	};
function N.taskKind(h)
	if not h then
		return "unknown";
	end;
	local Y = " " .. (string.lower(h) .. " ");
	for h, c in ipairs(O) do
		if string.find(Y, c, 1, true) then
			return "fetch";
		end;
	end;
	for h, c in ipairs(z) do
		if string.find(Y, c, 1, true) then
			return "kill";
		end;
	end;
	return "unknown";
end;
function N.questMatchesEntry(h, Y)
	if not h or not Y then
		return false;
	end;
	local function c(h)
		local Y = {};
		for h in string.gmatch(string.lower(h), "%a+") do
			if #h >= 4 then
				Y[h] = true;
			end;
		end;
		return Y;
	end;
	local D, A = c(h), c(Y);
	for h in pairs(D) do
		if A[h] then
			return true;
		end;
	end;
	return false;
end;
function N.codeToMob(h)
	if not h or h == "" then
		return nil;
	end;
	local Y = string.gsub(h, "(%l)(%u)", "%1 %2");
	Y = string.gsub(Y, " Sub$", " Subordinate");
	local c = string.lower(Y);
	local D = string.gsub(c, " ", "");
	for h, Y in ipairs({ f.Mobs, f.Bosses }) do
		for h = 1, #Y, 1 do
			local A = string.lower(Y[h]);
			if A == c or string.gsub(A, " ", "") == D then
				return Y[h];
			end;
		end;
	end;
	return nil;
end;
function N.questCompleted(h)
	local c = Y:FindFirstChild("Player_Service");
	local D = c and c:FindFirstChild("Data");
	local l = D and D:FindFirstChild(A.Name);
	if not l then
		return false;
	end;
	local I = l:FindFirstChild("slotEquipped");
	local f = l:FindFirstChild("slots");
	f = f and f:FindFirstChild("Slot" .. tostring(math.floor(I and I.Value or 1)));
	local N = f and f:FindFirstChild("Quests");
	local n = N and N:FindFirstChild("Completed");
	return n ~= nil and n:FindFirstChild(h) ~= nil;
end;
function N.questLastAge()
	local h = Y:FindFirstChild("Player_Service");
	local c = h and h:FindFirstChild("Data");
	local D = c and c:FindFirstChild(A.Name);
	if not D then
		return nil;
	end;
	local l = D:FindFirstChild("slotEquipped");
	local I = D:FindFirstChild("slots");
	I = I and I:FindFirstChild("Slot" .. tostring(math.floor(l and l.Value or 1)));
	local f = I and I:FindFirstChild("Quests");
	local N = f and f:FindFirstChild("LastTime");
	if not N then
		return nil;
	end;
	return os.time() - N.Value;
end;
function N.optLevel(h)
	if type(h) ~= "string" then
		return nil;
	end;
	local Y = string.match(h, "%(Lv%s*(%d+)%)");
	return Y and tonumber(Y) or nil;
end;
function N.questOpt(h)
	if not h then
		return nil;
	end;
	if I.QuestPick == "boss" then
		return h.BossOption or h.QuestOption;
	end;
	return h.QuestOption or h.BossOption;
end;
function N.pickQuest(h)
	I.qBack = I.qBack or {};
	I.qSent = I.qSent or {};
	local Y;
	local c = I.QuestSel or {};
	local D = #c;
	local A = 1;
	if I.lastDoneKey then
		local h = f.Quests[I.lastDoneKey];
		local Y = h and ((h.QuestOption or h.BossOption));
		for h = 1, D, 1 do
			local D = c[h];
			local l = f.Quests[D];
			local N = D == I.lastDoneKey;
			if not N and (l and (l.AutoDeliver and Y)) then
				for h, c in ipairs(f.DeliverGivers) do
					if c.QuestOption == Y then
						N = true;
						break;
					end;
				end;
			end;
			if N then
				A = h + 1;
				break;
			end;
		end;
	end;
	for l = 0, D - 1, 1 do
		local n = c[((((A - 1) + l)) % math.max(D, 1)) + 1];
		local q = f.Quests[n];
		local g = I.qSent[n];
		if g and h - g > 6 then
			I.qSent[n] = nil;
			I.qBack[n] = h + 60;
		end;
		if q and q.AutoDeliver then
			local c, D = N.pickDelivery(h);
			if c then
				return q, n;
			end;
			if D and ((not Y or D < Y)) then
				Y = D;
			end;
		elseif q and (not q.Auto and q.Pos) then
			local c = N.questOpt(q);
			if c and not N.questCompleted(c) then
				local c = I.qBack[n];
				if not c or h >= c then
					return q, n;
				end;
				if not Y or c < Y then
					Y = c;
				end;
			end;
		end;
	end;
	return nil, Y;
end;
function N.entryForQuest(h)
	if not h then
		return nil;
	end;
	if h.Desc then
		for Y, c in pairs(f.Quests) do
			if c.QuestOption == h.Desc or c.BossOption == h.Desc then
				return c, Y;
			end;
		end;
	end;
	for Y, c in ipairs(I.QuestSel or {}) do
		local D = f.Quests[c];
		if D and (not D.Auto and N.questMatchesEntry(h.Name, c)) then
			return D, c;
		end;
	end;
	return nil;
end;
function N.pickDelivery(h)
	I.delivBack = I.delivBack or {};
	local Y;
	for c, D in ipairs(f.DeliverGivers) do
		local A = D.QuestOption;
		local l = I.delivSent and I.delivSent[A];
		if l and (h - l) > 6 then
			I.delivSent[A] = nil;
			local Y = ((I.delivFails or {}))[A] or 0;
			I.delivFails = I.delivFails or {};
			I.delivFails[A] = Y + 1;
			I.delivBack[A] = h + 8;
		end;
		if not N.questCompleted(A) then
			local c = I.delivBack[A];
			if not c or h >= c then
				return D;
			end;
			if not Y or c < Y then
				Y = c;
			end;
		end;
	end;
	return nil, Y;
end;
function N.deliverStep(h, Y, c, D, A)
	if I.cur then
		I.cur = nil;
		N.aimRelease();
		pcall(N.skillsRelease);
		if N.clickRelease then
			pcall(N.clickRelease);
		end;
	end;
	local l = h .. ("|" .. Y);
	if I.deliverKey ~= l then
		I.deliverKey = l;
		I.deliverTries = 0;
		I.deliverAt = nil;
	end;
	local f = N.followNpc(c.npc, c.pos, A);
	if f > 8 then
		I.deliverAt = nil;
		return;
	end;
	I.deliverAt = I.deliverAt or (A + I.SettleT);
	if A < I.deliverAt then
		return;
	end;
	if not N.remote then
		local h = N.dlgTalk(c.answer, A);
		if h == "failed" and A - ((I.deliverWarn or 0)) > 12 then
			I.deliverWarn = A;
			N.notify("could not hand \'" .. (Y .. ("\' to " .. (c.npc .. " - do it by hand"))), 6);
		end;
		return;
	end;
	if (A - ((I.deliverT or 0))) < 3 then
		return;
	end;
	I.deliverT = A;
	I.deliverTries = ((I.deliverTries or 0)) + 1;
	if I.deliverTries > 4 then
		if A - ((I.deliverWarn or 0)) > 10 then
			I.deliverWarn = A;
			N.notify("\'" .. (Y .. "\' not accepted - missing the item?"), 6);
		end;
		return;
	end;
	pcall(function()
		N.remote:FireServer("QuestProgress", h, Y);
	end);
	N.notify("handing in: " .. Y, 3);
end;
function N.questTarget(h)
	if not h then
		return nil;
	end;
	local Y = string.lower(h.Name or "");
	if h.Desc then
		Y = Y .. (" " .. string.lower(h.Desc));
	end;
	for c = 1, #h.Tasks, 1 do
		Y = Y .. (" " .. string.lower(h.Tasks[c].Name));
	end;
	Y = " " .. (string.gsub(Y, "[^%a]+", " ") .. " ");
	local c = false;
	for h, D in ipairs(d) do
		if string.find(Y, " " .. (D .. " "), 1, true) then
			c = true;
			break;
		end;
	end;
	local D = { lost = true };
	local function A(h)
		local c, D = 0, 0;
		for h in string.gmatch(string.lower(h), "%a+") do
			D = D + 1;
			if #h >= 4 then
				if string.find(Y, " " .. (h .. " "), 1, true) or string.find(Y, " " .. (h .. "s "), 1, true) then
					c = c + 1;
				end;
			end;
		end;
		return c, D;
	end;
	local l, I, N = nil, 0, -1;
	local n = { f.Mobs, f.Bosses };
	for h = 1, #n, 1 do
		local Y = n[h];
		for h = 1, #Y, 1 do
			local f = Y[h];
			if f ~= "All Regular Mobs" and (f ~= "All Bosses" and not D[string.lower(f)]) then
				local h = A(f);
				if h > 0 then
					local Y = string.find(string.lower(f), "subordinate", 1, true) ~= nil;
					local D = h * 10;
					if c and Y then
						D = D + 5;
					end;
					if (not c) and (not Y) then
						D = D + 5;
					end;
					if h > I or (h == I and D > N) then
						l, I, N = f, h, D;
					end;
				end;
			end;
		end;
	end;
	return l;
end;
function N.chests()
	local h = {};
	local Y = D:FindFirstChild("Chests");
	if not Y then
		return h;
	end;
	local c = Y:GetChildren();
	for Y = 1, #c, 1 do
		local D = c[Y];
		local A;
		if D.ClassName == "Part" or D.ClassName == "MeshPart" then
			A = D;
		else
			A = D:FindFirstChild("RootPart") or D:FindFirstChildWhichIsA("BasePart");
		end;
		if A then
			h[#h + 1] = { Name = D.Name, Pos = A.Position };
		end;
	end;
	return h;
end;
local s = "__s2pool_slayers2";
local e = 48;
do
	local h = rawget(_G, s);
	_G[s] = nil;
	local Y;
	if type(h) == "table" and (#h == e and (type(h[1]) == "table" and (h[1].box and h[1].tag))) then
		h[1].box.Visible = false;
		h[1].tag.Visible = false;
		Y = h;
	end;
	if Y then
		I.pool = Y;
		for h = 1, e, 1 do
			local c = Y[h];
			c.vis = false;
			c.box.Visible = false;
			c.tag.Visible = false;
		end;
		_G[s] = Y;
	else
		for h = 1, e, 1 do
			local Y = Drawing.new("Square");
			Y.Thickness = 1;
			Y.Filled = false;
			Y.Visible = false;
			local c = Drawing.new("Text");
			c.Size = 13;
			c.Center = true;
			c.Outline = true;
			c.Visible = false;
			I.pool[h] = { box = Y, tag = c };
		end;
		_G[s] = I.pool;
	end;
end;
local j = {};
I.espList = function()
		return j;
	end;
local E = {
		Color3.fromRGB(230, 230, 230),
		Color3.fromRGB(255, 90, 90),
		Color3.fromRGB(255, 215, 90),
		Color3.fromRGB(255, 130, 200),
		Color3.fromRGB(170, 130, 255),
		Color3.fromRGB(140, 255, 200),
		Color3.fromRGB(255, 240, 90),
		Color3.fromRGB(255, 40, 70),
		Color3.fromRGB(120, 255, 80),
		Color3.fromRGB(60, 255, 150),
	};
function N.espOn()
	return I.EspMobs or I.EspBosses or I.EspChests or I.EspCrystals or I.EspLily or I.EspHorses or I.EspLevers or I.EspMuzan or I.EspFruits or I.EspPlants;
end;
function N.espClear()
	local h = I.pool;
	if not h then
		return;
	end;
	for Y = 1, #h, 1 do
		local c = h[Y];
		c.box.Visible = false;
		c.tag.Visible = false;
		c.vis, c.bx, c.by, c.bw, c.bh, c.tx, c.ty, c.text, c.col = false, nil, nil, nil, nil, nil, nil, nil, nil;
	end;
	j = {};
	N.npcCache = nil;
end;
function N.espBuild()
	local h, Y = N.char();
	if not Y then
		j = {};
		return;
	end;
	local c = Y.Position;
	local A = I.EspDist;
	local l = {};
	if I.EspMobs or I.EspBosses then
		local h = os.clock();
		if not N.npcCache or (h - N.npcCacheT) > 2.0 then
			N.npcCache = N.scan(nil, nil);
			N.npcCacheT = h;
		end;
		local Y = N.npcCache;
		for h = 1, #Y, 1 do
			if #l >= e then
				break;
			end;
			local D = Y[h];
			local f = (D.Boss and I.EspBosses) or ((not D.Boss) and I.EspMobs);
			if f and N.npcAlive(D, .6) then
				local h, Y = nil, 0;
				pcall(function()
					Y = D.Hum.Health;
					h = ((D.Root.Position - c)).Magnitude;
				end);
				if h and (Y > 0 and h <= A) then
					l[#l + 1] = {
							kind = "m",
							npc = D,
							root = D.Root,
							text = D.Boss and (D.Name .. (" " .. (math.floor(Y) .. "hp"))) or (D.Name .. (" " .. (math.floor(h) .. "m"))),
							col = D.Boss and 2 or 1,
						};
				end;
			end;
		end;
	end;
	if I.EspChests then
		local h = N.chests();
		for Y = 1, #h, 1 do
			if #l >= e then
				break;
			end;
			local D = ((h[Y].Pos - c)).Magnitude;
			if D <= A then
				l[#l + 1] = {
						kind = "p",
						pos = h[Y].Pos,
						text = h[Y].Name,
						col = 3,
					};
			end;
		end;
	end;
	if I.EspCrystals or I.EspLily then
		local h = D:FindFirstChild("Debree");
		if h then
			if I.EspLily then
				local Y = h:GetChildren();
				for h = 1, #Y, 1 do
					if #l >= e then
						break;
					end;
					if string.find(string.lower(Y[h].Name), "spider lily", 1, true) then
						local D = Y[h]:FindFirstChildWhichIsA("BasePart");
						if D then
							local h = ((D.Position - c)).Magnitude;
							if h <= A then
								l[#l + 1] = {
										kind = "p",
										pos = D.Position,
										text = "Spider Lily",
										col = 4,
									};
							end;
						end;
					end;
				end;
			end;
			local Y = h:FindFirstChild("Regions");
			if I.EspCrystals and Y then
				local h = Y:GetChildren();
				for Y = 1, #h, 1 do
					if #l >= e then
						break;
					end;
					local D = h[Y]:GetChildren();
					for I = 1, #D, 1 do
						if string.find(string.lower(D[I].Name), "spawncrystal", 1, true) then
							local f = D[I]:FindFirstChild("Root") or D[I]:FindFirstChildWhichIsA("BasePart");
							if f then
								local D = ((f.Position - c)).Magnitude;
								if D <= A then
									l[#l + 1] = {
											kind = "p",
											pos = f.Position,
											text = h[Y].Name,
											col = 5,
										};
								end;
							end;
							break;
						end;
					end;
				end;
			end;
		end;
	end;
	if I.EspHorses then
		local h = N.scan("horse", false);
		for Y = 1, #h, 1 do
			if #l >= e then
				break;
			end;
			local D = ((h[Y].Root.Position - c)).Magnitude;
			if D <= A then
				l[#l + 1] = {
						kind = "p",
						pos = h[Y].Root.Position,
						text = "Horse",
						col = 6,
					};
			end;
		end;
	end;
	if I.EspLevers and #l < e then
		local h = D:FindFirstChild("Map");
		local Y = h and h:FindFirstChild("Puzzles");
		if Y then
			for h, Y in ipairs(Y:GetDescendants()) do
				if #l >= e then
					break;
				end;
				if ((Y.ClassName == "MeshPart" or Y.ClassName == "Part")) and string.find(string.lower(Y.Name), "lever", 1, true) then
					local h = ((Y.Position - c)).Magnitude;
					if h <= A then
						l[#l + 1] = {
								kind = "p",
								pos = Y.Position,
								text = "LEVER " .. (math.floor(h) .. "m"),
								col = 7,
							};
					end;
				end;
			end;
		end;
	end;
	if I.EspMuzan and #l < e then
		local h = {};
		for Y, c in ipairs({ D:FindFirstChild("Humanoids"), D:FindFirstChild("Debree") }) do
			local D = c and c:FindFirstChild("Regions");
			for Y, c in ipairs(D and D:GetChildren() or {}) do
				h[#h + 1] = c:FindFirstChild("ActiveNpcs");
				h[#h + 1] = c:FindFirstChild("StationaryNpcs");
			end;
		end;
		for h, Y in ipairs(h) do
			if #l >= e then
				break;
			end;
			for h, Y in ipairs(Y and Y:GetChildren() or {}) do
				if #l >= e then
					break;
				end;
				if string.find(string.lower(Y.Name), "muzan", 1, true) then
					local h = Y:FindFirstChild(Y.Name) or Y:FindFirstChildOfClass("Model");
					local D = Y:FindFirstChild("HumanoidRootPart") or (h and h:FindFirstChild("HumanoidRootPart")) or Y:FindFirstChildWhichIsA("BasePart");
					local I = Y:FindFirstChildOfClass("Humanoid") or (h and h:FindFirstChildOfClass("Humanoid"));
					if D then
						local h = ((D.Position - c)).Magnitude;
						if h <= A then
							local c = "";
							if I then
								c = " " .. (math.floor(I.Health) .. ("/" .. math.floor(I.MaxHealth)));
							end;
							l[#l + 1] = {
									kind = "p",
									pos = D.Position,
									text = Y.Name .. (c .. (" " .. (math.floor(h) .. "m"))),
									col = 8,
								};
						end;
					end;
				end;
			end;
		end;
	end;
	if I.EspPlants and #l < e then
		local h = D:FindFirstChild("Debree");
		for h, Y in ipairs(h and h:GetChildren() or {}) do
			if #l >= e then
				break;
			end;
			local D = string.lower(Y.Name);
			if ((string.find(D, "flower", 1, true) or string.find(D, "herb", 1, true))) and not string.find(D, "spider lily", 1, true) then
				local h = Y;
				if Y.ClassName ~= "Part" and Y.ClassName ~= "MeshPart" then
					h = Y:FindFirstChildWhichIsA("BasePart");
				end;
				if h then
					local D = ((h.Position - c)).Magnitude;
					if D <= A then
						l[#l + 1] = {
								kind = "p",
								pos = h.Position,
								text = Y.Name .. (" " .. (math.floor(D) .. "m")),
								col = 10,
							};
					end;
				end;
			end;
		end;
		local Y = D:FindFirstChild("Map");
		Y = Y and Y:FindFirstChild("Map");
		local I = Y and Y:FindFirstChild("Assets");
		local f = I and I:FindFirstChild("Clovers");
		for h, Y in ipairs(f and f:GetChildren() or {}) do
			if #l >= e then
				break;
			end;
			local D = Y;
			if Y.ClassName ~= "MeshPart" then
				D = Y:FindFirstChildWhichIsA("BasePart");
			end;
			if D then
				local h = ((D.Position - c)).Magnitude;
				if h <= A then
					l[#l + 1] = {
							kind = "p",
							pos = D.Position,
							text = "Clover " .. (math.floor(h) .. "m"),
							col = 10,
						};
				end;
			end;
		end;
	end;
	if I.EspFruits and #l < e then
		local h = {
				"apple",
				"banana",
				"grape",
				"fruit",
				"peach",
				"mango",
				"melon",
				"berry",
				"cherry",
				"kiwi",
				"orange",
				"pineapple",
				"coconut",
			};
		for Y, D in ipairs(D:GetChildren()) do
			if #l >= e then
				break;
			end;
			local I = string.lower(D.Name);
			local f = false;
			for h, Y in ipairs(h) do
				if string.find(I, Y, 1, true) then
					f = true;
					break;
				end;
			end;
			if f then
				local h = D;
				if D.ClassName ~= "Part" and D.ClassName ~= "MeshPart" then
					h = D:FindFirstChildWhichIsA("BasePart");
				end;
				if h then
					local Y = ((h.Position - c)).Magnitude;
					if Y <= A then
						l[#l + 1] = {
								kind = "p",
								pos = h.Position,
								text = D.Name .. (" " .. (math.floor(Y) .. "m")),
								col = 9,
							};
					end;
				end;
			end;
		end;
	end;
	j = l;
end;
local Z = { off = 260, at = 0 };
function Z.calibrate()
	Z.at = tick();
	local h, Y = pcall(function()
			return workspace.CurrentCamera.ViewportSize;
		end);
	if not ((h and (Y and Y.X > 1))) then
		return;
	end;
	local c, D = pcall(function()
			return (game:GetService("Players")).LocalPlayer.PlayerGui:GetChildren();
		end);
	for h, c in ipairs(c and D or {}) do
		local D, A = pcall(function()
				return c.Address;
			end);
		if D and (type(A) == "number" and A > 0) then
			for h = 128, 512, 4 do
				local c, D = pcall(memory_read, "float", A + h);
				local l, I = pcall(memory_read, "float", (A + h) + 4);
				if c and (l and (type(D) == "number" and (type(I) == "number" and (math.abs(D - Y.X) < 1 and (I > Y.Y - 120 and I <= Y.Y))))) then
					Z.off = h;
					return;
				end;
			end;
		end;
	end;
end;
local function u(h)
	local Y, c = pcall(function()
			return h.AbsoluteSize;
		end);
	if Y and (c and ((c.X >= .5 or c.Y >= .5))) then
		return c;
	end;
	local D, A = pcall(function()
			return h.Address;
		end);
	if not ((D and (type(A) == "number" and A > 0))) then
		return c;
	end;
	local function l()
		local h, Y = pcall(memory_read, "float", A + Z.off);
		local c, D = pcall(memory_read, "float", (A + Z.off) + 4);
		if h and (c and (type(Y) == "number" and (type(D) == "number" and (Y == Y and (D == D and (Y >= 0 and (D >= 0 and (Y < 100000.0 and D < 100000.0)))))))) then
			return { X = Y, Y = D };
		end;
	end;
	local I = l();
	if I and ((I.X >= .5 or I.Y >= .5)) then
		return I;
	end;
	if tick() - Z.at > 1 then
		pcall(Z.calibrate);
		return l() or c;
	end;
	return I or c;
end;
N.absSize = u;
local function o(h, Y, c, D, A, l, I, f, N)
	if h.bx ~= Y or h.by ~= c then
		h.bx, h.by = Y, c;
		h.box.Position = Vector2.new(Y, c);
	end;
	if h.bw ~= D or h.bh ~= A then
		h.bw, h.bh = D, A;
		h.box.Size = Vector2.new(D, A);
	end;
	if h.col ~= N then
		h.col = N;
		local Y = E[N] or E[1];
		h.box.Color = Y;
		h.tag.Color = Y;
	end;
	if h.tx ~= l or h.ty ~= I then
		h.tx, h.ty = l, I;
		h.tag.Position = Vector2.new(l, I);
	end;
	if h.text ~= f then
		h.text = f;
		h.tag.Text = f;
	end;
	if not h.vis then
		h.vis = true;
		h.box.Visible = true;
		h.tag.Visible = true;
	end;
end;
function N.espDraw()
	local h = I.pool;
	if not h then
		return;
	end;
	local Y = 0;
	for c = 1, #j, 1 do
		if Y >= e then
			break;
		end;
		local D = j[c];
		if D.kind == "m" then
			local c = N.npcAlive(D.npc, .6) and D.root.Position;
			local A, l, I, f;
			if c then
				A, l = WorldToScreen(c + Vector3.new(0, 2.8, 0));
				I, f = WorldToScreen(c - Vector3.new(0, 3.2, 0));
			end;
			if l and f then
				Y = Y + 1;
				local c = I.Y - A.Y;
				local l = c * .5;
				o(h[Y], A.X - l * .5, A.Y, l, c, A.X, A.Y - 15, D.text, D.col);
			end;
		else
			local c, A = WorldToScreen(D.pos);
			if A then
				Y = Y + 1;
				o(h[Y], c.X - 7, c.Y - 7, 14, 14, c.X, c.Y - 17, D.text, D.col);
			end;
		end;
	end;
	for Y = Y + 1, e, 1 do
		local c = h[Y];
		if c.vis then
			c.vis = false;
			c.box.Visible = false;
			c.tag.Visible = false;
		end;
	end;
end;
function N.espSync()
	if N.espOn() then
		if not I.drawConn then
			local h = 0;
			I.drawConn = c.RenderStepped:Connect(function()
					if not I.Running or not I.pool then
						return;
					end;
					local Y = os.clock();
					if Y - h < .0166 then
						return;
					end;
					h = Y;
					N.mark("espdraw");
					local c, D = pcall(N.espDraw);
					if not c and Y - ((I.drawErrT or 0)) > 5 then
						I.drawErrT = Y;
						N.logErr("esp", D);
					end;
				end);
		end;
	else
		if I.drawConn then
			pcall(function()
				I.drawConn:Disconnect();
			end);
			I.drawConn = nil;
		end;
		N.espClear();
	end;
end;
local v = {
		One = 49,
		Two = 50,
		Three = 51,
		Four = 52,
		Five = 53,
		Six = 54,
		Seven = 55,
		Eight = 56,
		Nine = 57,
		Zero = 48,
	};
function N.onMenu()
	if not I.win then
		return false;
	end;
	local h, Y = pcall(function()
			return I.win:IsVisible();
		end);
	if not h or not Y then
		return false;
	end;
	return l ~= nil and l._overUI == true;
end;
function N.inputHeld()
	return I.lmbDown == true or I.rmbDown == true or I.dlgUp ~= nil or I.dlgClick ~= nil or (I.fish ~= nil and I.fish.lmb == true) or (I.bar ~= nil and I.bar.lmb == true);
end;
function N.trainBar()
	return I.AutoTrain == true and not N.remote;
end;
function N.fishWantsInput()
	local h = I.bar;
	if h and ((h.active or h.lmb)) then
		return true;
	end;
	local Y = I.fish;
	if not I.AutoFish or not Y then
		return false;
	end;
	return Y.lmb == true or Y.keyUp ~= nil or Y.phase == "game" or os.clock() < ((Y.inputUntil or 0));
end;
function N.wantsGameInput()
	return N.fishWantsInput() or I.dlg ~= nil or I.Mode ~= "off" or I.collect ~= nil or I.route ~= nil or I.AutoTrain == true;
end;
function N.fishInputReady(h)
	local Y = I.fish;
	Y.inputUntil = h + .6;
	return not ((l and l._inputSent == false));
end;
function N.waterAreas(h)
	local Y = I.waterC;
	if Y and h - Y.t < 5 then
		return Y.list;
	end;
	local c = {};
	local A = D:FindFirstChild("Map");
	A = A and A:FindFirstChild("Map");
	local l = A and A:FindFirstChild("WatersFunctions");
	local function f(h, Y)
		if Y > 3 then
			return;
		end;
		for h, D in ipairs(h:GetChildren()) do
			if D.Name == "TouchPart" then
				local h, Y;
				pcall(function()
					h = D.Position;
					Y = D.Size;
				end);
				if h and (Y and Y.X > 4) then
					c[#c + 1] = {
							top = h.Y + Y.Y * .5,
							x1 = h.X - Y.X * .5,
							x2 = h.X + Y.X * .5,
							z1 = h.Z - Y.Z * .5,
							z2 = h.Z + Y.Z * .5,
						};
				end;
			end;
			f(D, Y + 1);
		end;
	end;
	if l then
		f(l, 0);
	end;
	I.waterC = { t = h, list = c };
	return c;
end;
function N.waterPoint(h, Y)
	local c, D = N.char();
	if not D then
		return nil;
	end;
	local A = D.Position;
	local l = N.waterAreas(h);
	local I;
	pcall(function()
		I = D.CFrame.LookVector;
	end);
	local f = {};
	if I then
		local h = math.sqrt(I.X * I.X + I.Z * I.Z);
		if h > .01 then
			f[1] = { I.X / h, I.Z / h };
		end;
	end;
	for h = 0, 7, 1 do
		local Y = (h * math.pi) / 4;
		f[#f + 1] = { math.cos(Y), math.sin(Y) };
	end;
	local n = ((Y or 0)) % #f;
	for h = 1, #f, 1 do
		local Y = f[((((h - 1) + n)) % #f) + 1];
		for h, c in ipairs({ 10, 16, 22 }) do
			local D, I = A.X + Y[1] * c, A.Z + Y[2] * c;
			for h, Y in ipairs(l) do
				local c = A.Y - Y.top;
				if D > Y.x1 and (D < Y.x2 and (I > Y.z1 and (I < Y.z2 and (c > -4 and c < 45)))) then
					return Vector3.new(D, Y.top, I);
				end;
			end;
		end;
	end;
	local q = f[(n % #f) + 1];
	return Vector3.new(A.X + q[1] * 16, A.Y - 2, A.Z + q[2] * 16);
end;
function N.fishRod(h)
	local Y = I.fishRodC;
	if h and (Y and h - Y.t < .5) then
		return Y.v;
	end;
	local c = N.fishRodScan();
	if h then
		I.fishRodC = { t = h, v = c };
	end;
	return c;
end;
function N.fishRodScan()
	local h = A.Character;
	local Y = h and h:FindFirstChild("Tool_Accessories");
	if not Y then
		return nil;
	end;
	for h, Y in ipairs(Y:GetChildren()) do
		if string.find(Y.Name, "Fishing Rod", 1, true) then
			return Y;
		end;
	end;
	return nil;
end;
function N.fishRodKey()
	local h = nil;
	if not h then
		local c = Y:FindFirstChild("Player_Service");
		local D = c and c:FindFirstChild("Data");
		h = D and D:FindFirstChild(A.Name);
		local l = h and h:FindFirstChild("slotEquipped");
		local I = h and h:FindFirstChild("slots");
		h = I and I:FindFirstChild("Slot" .. tostring(math.floor(l and l.Value or 1)));
	end;
	local c = h and h:FindFirstChild("Inventory");
	local D = c and c:FindFirstChild("Toolbar");
	local l = c and c:FindFirstChild("Inventory");
	if not D or not l then
		return nil;
	end;
	local I = {};
	for h, Y in ipairs(l:GetChildren()) do
		if string.find(Y.Name, "Fishing Rod", 1, true) then
			local h = Y:FindFirstChild("Id");
			if h then
				I[h.Value] = true;
			end;
		end;
	end;
	for h, Y in ipairs(D:GetChildren()) do
		if I[Y.Value] and v[Y.Name] then
			return v[Y.Name];
		end;
	end;
	return nil;
end;
function N.fishGame(h)
	local Y = A:FindFirstChild("PlayerGui");
	local c = Y and Y:FindFirstChild("Misc");
	if not c then
		return nil;
	end;
	local D = c:GetChildren();
	if #D == 0 then
		return nil;
	end;
	for h, Y in ipairs(D) do
		for h, Y in ipairs(Y:GetChildren()) do
			local c = Y:FindFirstChild("tracker");
			if c then
				local h = Y:FindFirstChild("Bar");
				if h then
					return c, h, Y;
				end;
			end;
		end;
	end;
	return nil;
end;
function N.barMouse(h)
	local Y = I.bar;
	if h and N.onMenu() then
		h = false;
	end;
	if not Y or Y.lmb == h then
		return;
	end;
	Y.lmb = h;
	pcall(function()
		if h then
			mouse1press();
		else
			mouse1release();
		end;
	end);
end;
function N.barStep(h, Y)
	local c = I.bar;
	if not c then
		c = { lmb = false };
		I.bar = c;
	end;
	local D, A, f;
	if c.active or h - ((c.chk or 0)) >= .1 then
		c.chk = h;
		D, A, f = N.fishGame(h);
	end;
	if not D then
		if c.active then
			c.active = false;
			N.barMouse(false);
		end;
		return false;
	end;
	if not c.active then
		c.active = true;
		c.prevBy = nil;
		c.games = ((c.games or 0)) + 1;
	end;
	if not N.focused(h) then
		N.barMouse(false);
		return true;
	end;
	local n, q;
	pcall(function()
		n = D.AbsolutePosition.Y + (u(D)).Y * .5;
		q = A.AbsolutePosition.Y + (u(A)).Y * .5;
	end);
	if n and q then
		local h = 0;
		if c.prevBy and Y > 0 then
			h = ((q - c.prevBy)) / Y;
		end;
		c.prevBy = q;
		N.barMouse((q + h * I.FishLead) > n and not ((l and l._inputSent == false)));
	end;
	return true;
end;
function N.fishFind(h, Y)
	local c = D:FindFirstChild("Debree");
	local A, l = N.char();
	if not c or not l then
		return nil;
	end;
	local I = #h;
	local f, n = nil, Y;
	for Y, c in ipairs(c:GetChildren()) do
		if string.sub(c.Name, 1, I) == h then
			local h = c;
			if c.ClassName ~= "Part" and c.ClassName ~= "MeshPart" then
				h = c:FindFirstChildWhichIsA("BasePart");
			end;
			local Y, D = pcall(function()
					return h.Position;
				end);
			if Y and D then
				local h = ((D - l.Position)).Magnitude;
				if h < n then
					n = h;
					f = D;
				end;
			end;
		end;
	end;
	return f, n;
end;
function N.fishMouse(h)
	local Y = I.fish;
	if h and N.onMenu() then
		h = false;
	end;
	if not Y or Y.lmb == h then
		return;
	end;
	Y.lmb = h;
	pcall(function()
		if h then
			mouse1press();
		else
			mouse1release();
		end;
	end);
end;
function N.fishCast(h)
	if not N.remote then
		return false;
	end;
	pcall(function()
		N.remote:FireServer("Tool_Mouse", "Up", h);
	end);
	return true;
end;
function N.fishStep(h)
	local Y = I.fish;
	if not Y then
		return;
	end;
	if not I.AutoFish then
		return;
	end;
	local c = h - ((Y.last or h));
	Y.last = h;
	if N.barStep(h, c) then
		Y.phase = "game";
		return;
	end;
	if Y.phase == "game" then
		Y.badCasts = 0;
		Y.phase = "catch";
		Y.t = h;
		Y.tries = 0;
		Y.bites = ((Y.bites or 0)) + 1;
		Y.holdAt = nil;
		return;
	end;
	if h - ((Y.slow or 0)) < .1 then
		return;
	end;
	Y.slow = h;
	local D, A = N.char();
	if not A then
		return;
	end;
	if not N.fishRod(h) then
		if h - ((Y.eqT or 0)) > 3 then
			Y.eqT = h;
			local c = N.fishRodKey();
			if c and (N.focused(h) and not N.fishInputReady(h)) then
				Y.eqT = h - 2.8;
			elseif c and N.focused(h) then
				pcall(function()
					keypress(c);
				end);
				Y.keyUp = { vk = c, t = h + .08 };
			elseif h - ((Y.warn or 0)) > 10 then
				Y.warn = h;
				N.notify("auto fish: put a fishing rod on the hotbar", 5);
			end;
		end;
		Y.phase = "cast";
		Y.t = h;
		return;
	end;
	if Y.phase == "catch" then
		if Y.keyUp then
			return;
		end;
		if h - ((Y.catchChk or 0)) < .4 then
			return;
		end;
		Y.catchChk = h;
		local c = N.fishFind("FishingCatch_", 14);
		if not c then
			if h - Y.t > 2.5 or Y.tries > 0 then
				if Y.tries > 0 then
					Y.caught = ((Y.caught or 0)) + 1;
				end;
				Y.phase = "cast";
				Y.t = h;
			end;
			return;
		end;
		if h < ((Y.holdAt or 0)) or not N.focused(h) then
			return;
		end;
		if Y.tries >= 6 then
			N.notify("auto fish: could not pick up the catch", 4);
			Y.phase = "cast";
			Y.t = h;
			return;
		end;
		if not N.fishInputReady(h) then
			return;
		end;
		Y.tries = Y.tries + 1;
		local D = math.max(I.HoldT or 3, 3);
		pcall(function()
			keypress(84);
		end);
		Y.keyUp = { vk = 84, t = h + D };
		Y.holdAt = (h + D) + .4;
		return;
	end;
	if Y.phase == "cast" then
		if h - Y.t < I.FishPause then
			return;
		end;
		if N.fishFind("FishingCatch_", 12) then
			Y.phase = "catch";
			Y.t = h;
			Y.tries = 0;
			Y.holdAt = nil;
			return;
		end;
		local c = N.waterPoint(h, Y.dir or 0);
		if not c then
			return;
		end;
		Y.water = c;
		if N.fishCast(c) == false then
			return;
		end;
		Y.phase = "wait";
		Y.t = h;
		return;
	end;
	if Y.phase == "wait" then
		if h - ((Y.bobChk or 0)) < 1 then
			return;
		end;
		Y.bobChk = h;
		local c = N.fishFind("FishingLine_", 120);
		if c then
			Y.sawBob = true;
		end;
		if (not c and h - Y.t > 4) or h - Y.t > I.FishTimeout then
			if Y.sawBob and (not c and h - Y.t < 8) then
				Y.badCasts = ((Y.badCasts or 0)) + 1;
				Y.dir = ((Y.dir or 0)) + 1;
				if Y.badCasts % 6 == 0 then
					N.notify("auto fish: casts keep coming back - move closer to open water", 6);
				end;
			else
				Y.badCasts = 0;
			end;
			if c then
				N.fishCast(Y.water);
			end;
			Y.phase = "cast";
			Y.t = h;
			Y.sawBob = false;
		end;
		return;
	end;
	if Y.phase ~= "idle" then
		Y.phase = "cast";
		Y.t = h;
	end;
end;
function N.fishSync()
	if I.AutoFish or N.trainBar() then
		if I.AutoFish and not I.fish then
			I.fish = { phase = "cast", t = os.clock(), lmb = false };
		end;
		if not I.fishConn then
			local h = 0;
			local Y = os.clock();
			I.fishConn = c.RenderStepped:Connect(function()
					local c, D = pcall(function()
							if not I.Running or not ((I.AutoFish or N.trainBar())) then
								return;
							end;
							local c = os.clock();
							local D = I.fish;
							if D and (D.mouseUp and c >= D.mouseUp) then
								D.mouseUp = nil;
								N.fishMouse(false);
							end;
							if D and (D.keyUp and c >= D.keyUp.t) then
								local h = D.keyUp.vk;
								D.keyUp = nil;
								pcall(function()
									keyrelease(h);
								end);
							end;
							if c - h < .0166 then
								return;
							end;
							h = c;
							local A = c - Y;
							Y = c;
							local l, f;
							if I.AutoFish then
								N.mark("fish/" .. tostring(I.fish and I.fish.phase or "?"));
								N.markWrite(c);
								l, f = pcall(N.fishStep, c);
							else
								N.mark("barminigame");
								N.markWrite(c);
								l, f = pcall(N.barStep, c, A);
							end;
							if not l and c - ((I.fishErrT or 0)) > 5 then
								I.fishErrT = c;
								N.logErr("fish", f);
							end;
						end);
					if not c then
						N.logErr("fish loop", D);
					end;
				end);
		end;
	else
		if I.fishConn then
			pcall(function()
				I.fishConn:Disconnect();
			end);
			I.fishConn = nil;
		end;
		if I.fish then
			N.fishMouse(false);
			if I.fish.keyUp then
				pcall(function()
					keyrelease(I.fish.keyUp.vk);
				end);
				I.fish.keyUp = nil;
			end;
			I.fish.phase = "cast";
		end;
		if I.bar then
			N.barMouse(false);
			I.bar.active = false;
		end;
	end;
end;
pcall(function()
	l:SetTheme("monochrome");
	l:SetFont("Monospace", 13);
	l:SetAccentBar(false);
end);
local x = l:CreateWindow({
		title = "slayers 2",
		subtitle = "matcha",
		id = "slayers2",
		size = { 560, 600 },
		columns = 2,
		toggleKey = "RSHIFT",
		folder = "UI/slayers2",
		config = "slayers2.json",
		inputGuard = function()
			return N.inputHeld();
		end,
		captureInput = function()
			return not N.wantsGameInput();
		end,
		onUnload = function()
			I.Running = false;
			I.Mode = "off";
			pcall(function()
				if I.F then
					I.F.aimRelease();
					I.F.skillsRelease();
				end;
			end);
			pcall(function()
				if I.drawConn then
					I.drawConn:Disconnect();
				end;
			end);
			pcall(function()
				if I.tickConn then
					I.tickConn:Disconnect();
				end;
			end);
			pcall(function()
				if I.fishConn then
					I.fishConn:Disconnect();
				end;
			end);
			pcall(function()
				if I.deferConn then
					I.deferConn:Disconnect();
				end;
			end);
			I.deferConn = nil;
			pcall(function()
				if I.fish and I.fish.lmb then
					mouse1release();
				end;
			end);
			pcall(function()
				if I.bar and I.bar.lmb then
					mouse1release();
				end;
			end);
			pcall(function()
				mouse1release();
			end);
			pcall(function()
				mouse2release();
			end);
			pcall(function()
				keyrelease(84);
			end);
			pcall(N.skillsRelease);
			I.soul, I.dlg, I.dlgClick, I.crow, I.collect, I.route = nil, nil, nil, nil, nil, nil;
			local h = I.pool;
			I.pool = nil;
			_G[s] = nil;
			if h then
				for Y = 1, #h, 1 do
					pcall(function()
						h[Y].box.Visible = false;
						h[Y].box:Remove();
					end);
					pcall(function()
						h[Y].tag.Visible = false;
						h[Y].tag:Remove();
					end);
				end;
			end;
			pcall(function()
				delfile("slayers2_trail.txt");
			end);
			_G.Slayers2 = nil;
		end,
	});
I.win = x;
local L = x:AddTab("farm");
local B = x:AddTab("teleport");
local p = x:AddTab("skills");
local P = x:AddTab("esp");
local Q = x:AddTab("misc");
x:AddSettingsTab("menu");
local K = N.guard(L:AddGroup("target", 1));
K:AddToggle({
	text = "autofarm",
	flag = "autofarm",
	default = false,
	save = false,
	callback = function(h)
		I.Mode = h and ((I.FarmKind or "boss")) or "off";
		I.crow = nil;
		I.cur = nil;
		N.dropCache();
		I.markBurst = h and (os.clock() + 6) or nil;
		N.mark(h and "autofarm on" or "autofarm off");
		N.sync();
	end,
});
K:AddDropdown({
	text = "mode",
	flag = "farmkind",
	values = {
		"mob",
		"boss",
		"crow",
		"quest",
		"dungeon",
	},
	default = "boss",
	callback = function(h)
		I.FarmKind = h;
		if I.Mode ~= "off" then
			I.Mode = h;
			I.cur = nil;
			N.dropCache();
			N.sync();
		end;
	end,
});
K:AddDropdown({
	text = "mob",
	flag = "mob",
	values = f.Mobs,
	default = "Bandit",
	search = true,
	callback = function(h)
		I.SelectedMob = h;
	end,
});
K:AddDropdown({
	text = "boss",
	flag = "boss",
	values = f.Bosses,
	default = { "Akazo" },
	multi = true,
	search = true,
	callback = function(h)
		I.SelectedBoss = h;
		N.updateBossOrder(h);
		I.cur = nil;
		N.dropCache();
	end,
});
do
	local h, Y, c = {}, {}, {};
	for D, A in pairs(f.Quests) do
		local l, I = D, nil;
		if not A.Auto then
			local h, Y = N.optLevel(A.QuestOption), N.optLevel(A.BossOption);
			if h and (Y and h ~= Y) then
				l = D .. (" (Lv " .. (math.min(h, Y) .. ("/" .. (math.max(h, Y) .. ")"))));
			elseif h or Y then
				l = D .. (" (Lv " .. (((h or Y)) .. ")"));
			end;
			I = math.min(h or 999, Y or 999);
		end;
		h[#h + 1] = l;
		Y[l] = D;
		c[l] = A.Auto and -1 or I;
	end;
	table.sort(h, function(h, Y)
		if c[h] ~= c[Y] then
			return c[h] < c[Y];
		end;
		return h < Y;
	end);
	local D = {};
	for h, c in ipairs(h) do
		D[Y[c]] = h;
	end;
	f.QuestByLabel = Y;
	K:AddDropdown({
		text = "quest",
		flag = "quests",
		values = h,
		default = { h[1] },
		multi = true,
		search = true,
		callback = function(h)
			local c = {};
			local function A(h)
				local D = Y[h] or (f.Quests[h] and h);
				if D then
					c[#c + 1] = D;
				end;
			end;
			if type(h) == "table" then
				for h, Y in pairs(h) do
					if Y == true then
						A(h);
					elseif type(Y) == "string" then
						A(Y);
					end;
				end;
			elseif type(h) == "string" then
				A(h);
			end;
			table.sort(c, function(h, Y)
				return ((D[h] or 999)) < ((D[Y] or 999));
			end);
			I.QuestSel = c;
			I.SelectedQuest = c[1] or "[Auto-Detect Active Quest]";
			I.acceptTries = 0;
			I.acceptAt = nil;
			I.acceptKey = nil;
		end,
	});
end;
K:AddDropdown({
	text = "quest type",
	flag = "questpick",
	values = { "normal", "boss" },
	default = "normal",
	callback = function(h)
		I.QuestPick = h;
		I.acceptTries = 0;
		I.acceptAt = nil;
	end,
});
K:AddSlider({
	text = "crow slot",
	flag = "crowslot",
	min = 1,
	max = 5,
	step = 1,
	default = 2,
	callback = function(h)
		I.CrowSlot = h;
	end,
});
K:AddSlider({
	text = "weapon slot",
	flag = "weapslot",
	min = 1,
	max = 5,
	step = 1,
	default = 1,
	callback = function(h)
		I.WeapSlot = h;
	end,
});
K:AddSlider({
	text = "quest gap",
	flag = "questgap",
	min = 10,
	max = 120,
	step = 5,
	default = 30,
	suffix = "s",
	callback = function(h)
		I.QuestGap = h;
	end,
});
local y = N.guard(L:AddGroup("fight", 2));
y:AddDropdown({
	text = "stance",
	flag = "stance",
	values = {
		"Under Target",
		"Behind",
		"On Top",
		"Above (Hover)",
	},
	default = "Under Target",
	callback = function(h)
		I.Stance = h;
	end,
});
y:AddSlider({
	text = "offset",
	flag = "offset",
	min = 1,
	max = 15,
	step = .1,
	default = 3.8,
	callback = function(h)
		I.Offset = h;
	end,
});
y:AddSlider({
	text = "attack delay",
	flag = "atkdelay",
	min = .05,
	max = 1,
	step = .05,
	default = .3,
	suffix = "s",
	callback = function(h)
		I.AttackDelay = h;
	end,
});
y:AddSlider({
	text = "swing within",
	flag = "reach",
	min = 5,
	max = 40,
	default = 18,
	suffix = "studs",
	callback = function(h)
		I.Reach = h;
	end,
});
y:AddToggle({
	text = "hold when blocking",
	flag = "skipblock",
	default = true,
	callback = function(h)
		I.SkipBlocking = h;
	end,
});
y:AddToggle({
	text = "aimlock",
	flag = "aimlock",
	default = true,
	callback = function(h)
		I.AimLock = h;
		if not h then
			N.aimRelease();
		end;
	end,
});
y:AddSlider({
	text = "aim gain",
	flag = "aimgain",
	min = .1,
	max = 1,
	step = .05,
	default = .55,
	callback = function(h)
		I.AimGain = h;
	end,
});
y:AddToggle({
	text = "smooth movement",
	flag = "smooth",
	default = true,
	callback = function(h)
		I.Smooth = h;
	end,
});
y:AddSlider({
	text = "move speed",
	flag = "movespeed",
	min = 10,
	max = 150,
	default = 60,
	suffix = "s/s",
	callback = function(h)
		I.MoveSpeed = h;
	end,
});
y:AddSlider({
	text = "warp above",
	flag = "warpabove",
	min = 10,
	max = 200,
	default = 40,
	suffix = "studs",
	callback = function(h)
		I.WarpAbove = h;
	end,
});
y:AddToggle({
	text = "health guard",
	flag = "hguard",
	default = false,
	callback = function(h)
		I.HealthGuard = h;
	end,
});
y:AddSlider({
	text = "guard at",
	flag = "hpct",
	min = .1,
	max = .9,
	step = .05,
	default = .45,
	callback = function(h)
		I.GuardPct = h;
	end,
});
local M = {};
for h in pairs(f.Zones) do
	M[#M + 1] = h;
end;
table.sort(M);
local V = {};
for h in pairs(f.BossTP) do
	V[#V + 1] = h;
end;
table.sort(V);
local T = {};
for h in pairs(f.Trainers) do
	T[#T + 1] = h;
end;
table.sort(T);
local U = N.guard(B:AddGroup("regions", 1));
U:AddButton({ text = "black market", callback = function()
		N.tpBlackMarketer();
	end });
U:AddButton({ text = "find muzan", callback = function()
		N.findMuzan(true);
	end });
U:AddButton({ text = "unlock all locations", callback = function()
		N.unlockStart();
	end });
local C = M[1];
U:AddDropdown({
	text = "region",
	flag = "zone",
	values = M,
	default = C,
	search = true,
	callback = function(h)
		C = h;
	end,
});
U:AddButton({ text = "travel", callback = function()
		N.tpTo(f.Zones[C], 3);
	end });
local G = V[1];
U:AddDropdown({
	text = "boss",
	flag = "bosstp",
	values = V,
	default = G,
	search = true,
	callback = function(h)
		G = h;
	end,
});
U:AddButton({ text = "travel to boss", callback = function()
		N.tpNamed(G, f.BossTP[G]);
	end });
local a = N.guard(B:AddGroup("npcs", 2));
local F = T[1];
a:AddDropdown({
	text = "trainer",
	flag = "trainer",
	values = T,
	default = F,
	search = true,
	callback = function(h)
		F = h;
	end,
});
a:AddButton({ text = "travel to trainer", callback = function()
		N.tpNamed(F, f.Trainers[F]);
	end });
local ht = f.Stations[1];
a:AddDropdown({
	text = "station",
	flag = "station",
	values = f.Stations,
	default = ht,
	callback = function(h)
		ht = h;
	end,
});
a:AddButton({ text = "travel to station", callback = function()
		local h = D:FindFirstChild("Training");
		local Y = h and h:FindFirstChild(ht);
		local c = Y and Y:FindFirstChildWhichIsA("BasePart", true);
		if c then
			N.tpTo(c.Position, 3);
			N.notify("at " .. ht, 2);
		else
			N.notify(ht .. " not loaded", 2);
		end;
	end });
a:AddButton({ text = "nearest chest", callback = function()
		local h, Y = N.char();
		if not Y then
			return;
		end;
		local c = N.chests();
		local D, A = nil, 1000000000.0;
		for h = 1, #c, 1 do
			local l = ((c[h].Pos - Y.Position)).Magnitude;
			if l < A then
				A = l;
				D = c[h];
			end;
		end;
		if D then
			N.tpTo(D.Pos, 3);
			N.notify(D.Name, 2);
		else
			N.notify("no chests up", 2);
		end;
	end });
f.SkillKeys = {
		{ "z", 90 },
		{ "x", 88 },
		{ "c", 67 },
		{ "v", 86 },
		{ "b", 66 },
		{ "k", 75 },
		{ "n", 78 },
	};
do
	local h = N.guard(p:AddGroup("timing", 1));
	h:AddSlider({
		text = "gap between presses",
		flag = "sk_gap",
		min = .05,
		max = 5,
		step = .05,
		default = .3,
		suffix = "s",
		callback = function(h)
			I.SkillGap = h;
		end,
	});
end;
I.Skills = {};
for h = 1, #f.SkillKeys, 1 do
	local Y, c = f.SkillKeys[h][1], f.SkillKeys[h][2];
	local D = N.guard(p:AddGroup(Y, (h <= 3) and 1 or 2));
	local A = {
			on = false,
			breaker = false,
			hold = .08,
			dist = 15,
			vk = c,
			rel = 0,
			next = 0,
			name = Y,
		};
	I.Skills[h] = A;
	D:AddToggle({
		text = "enabled",
		flag = "sk_" .. (Y .. "_on"),
		default = false,
		callback = function(h)
			A.on = h;
		end,
	});
	D:AddToggle({
		text = "block breaker",
		flag = "sk_" .. (Y .. "_breaker"),
		default = false,
		callback = function(h)
			A.breaker = h;
		end,
	});
	D:AddSlider({
		text = "hold",
		flag = "sk_" .. (Y .. "_hold"),
		min = .02,
		max = 3,
		step = .02,
		default = .08,
		suffix = "s",
		callback = function(h)
			A.hold = h;
		end,
	});
	D:AddSlider({
		text = "range",
		flag = "sk_" .. (Y .. "_dist"),
		min = 1,
		max = 120,
		default = 15,
		suffix = "studs",
		callback = function(h)
			A.dist = h;
		end,
	});
end;
local Yt = N.guard(P:AddGroup("targets", 1));
Yt:AddToggle({
	text = "mobs",
	flag = "espmob",
	default = false,
	callback = function(h)
		I.EspMobs = h;
		N.sync();
	end,
});
Yt:AddToggle({
	text = "bosses",
	flag = "espboss",
	default = false,
	callback = function(h)
		I.EspBosses = h;
		N.sync();
	end,
});
Yt:AddToggle({
	text = "chests",
	flag = "espchest",
	default = false,
	callback = function(h)
		I.EspChests = h;
		N.sync();
	end,
});
local ct = N.guard(P:AddGroup("world", 2));
ct:AddToggle({
	text = "spawn crystals",
	flag = "espcry",
	default = false,
	callback = function(h)
		I.EspCrystals = h;
		N.sync();
	end,
});
ct:AddToggle({
	text = "spider lily",
	flag = "esplily",
	default = false,
	callback = function(h)
		I.EspLily = h;
		N.sync();
	end,
});
ct:AddToggle({
	text = "plants",
	flag = "espplant",
	default = false,
	callback = function(h)
		I.EspPlants = h;
		N.sync();
	end,
});
ct:AddToggle({
	text = "horses",
	flag = "esphorse",
	default = false,
	callback = function(h)
		I.EspHorses = h;
		N.sync();
	end,
});
ct:AddToggle({
	text = "levers",
	flag = "esplever",
	default = false,
	callback = function(h)
		I.EspLevers = h;
		N.sync();
	end,
});
ct:AddToggle({
	text = "muzan",
	flag = "espmuzan",
	default = false,
	callback = function(h)
		I.EspMuzan = h;
		N.sync();
	end,
});
ct:AddToggle({
	text = "fruits",
	flag = "espfruit",
	default = false,
	callback = function(h)
		I.EspFruits = h;
		N.sync();
	end,
});
ct:AddSlider({
	text = "distance",
	flag = "espdist",
	min = 100,
	max = 3000,
	default = 1200,
	suffix = "m",
	callback = function(h)
		I.EspDist = h;
	end,
});
local Dt = N.guard(Q:AddGroup("automation", 1));
Dt:AddToggle({
	text = "auto pick up loot",
	flag = "autoloot",
	default = true,
	callback = function(h)
		I.AutoLoot = h;
	end,
});
Dt:AddToggle({
	text = "collect boss drops",
	flag = "lootboss",
	default = true,
	callback = function(h)
		I.LootBoss = h;
	end,
});
Dt:AddToggle({
	text = "claim souls",
	flag = "autosouls",
	default = true,
	callback = function(h)
		I.AutoSouls = h;
		N.sync();
	end,
});
Dt:AddSlider({
	text = "settle before press",
	flag = "settlet",
	min = 0,
	max = 2,
	step = .1,
	default = .6,
	suffix = "s",
	callback = function(h)
		I.SettleT = h;
	end,
});
Dt:AddSlider({
	text = "wait for drops",
	flag = "lootwait",
	min = 1,
	max = 20,
	step = .5,
	default = 5,
	suffix = "s",
	callback = function(h)
		I.LootWait = h;
	end,
});
Dt:AddSlider({
	text = "hold T for",
	flag = "holdt",
	min = .5,
	max = 4,
	step = .1,
	default = 2.0,
	suffix = "s",
	callback = function(h)
		I.HoldT = h;
	end,
});
Dt:AddToggle({
	text = "auto train",
	flag = "autotrain",
	default = false,
	callback = function(h)
		I.AutoTrain = h;
		N.sync();
	end,
});
Dt:AddToggle({
	text = "noclip",
	flag = "noclip",
	default = false,
	callback = function(h)
		I.NoClip = h;
		N.sync();
	end,
});
Dt:AddToggle({
	text = "anti afk",
	flag = "antiafk",
	default = false,
	callback = function(h)
		I.AntiAFK = h;
		N.sync();
	end,
});
local At = N.guard(Q:AddGroup("panic", 2));
At:AddKeybind({
	text = "panic stop",
	flag = "panickey",
	default = "F2",
	mode = "always",
	onClick = function()
		N.panic();
	end,
	changed = function(h)
 
	end,
});
At:AddButton({ text = "stop everything", callback = function()
		N.panic();
	end });
At:AddButton({ text = "unload", confirm = true, callback = function()
		x:Unload();
	end });
local lt = N.guard(Q:AddGroup("fishing", 2));
lt:AddToggle({
	text = "auto fish",
	flag = "autofish",
	default = false,
	callback = function(h)
		I.AutoFish = h;
		N.fishSync();
	end,
});
lt:AddSlider({
	text = "bar lead",
	flag = "fishlead",
	min = 0,
	max = .4,
	step = .01,
	default = .12,
	suffix = "s",
	callback = function(h)
		I.FishLead = h;
	end,
});
lt:AddSlider({
	text = "no bite recast",
	flag = "fishtimeout",
	min = 10,
	max = 60,
	step = 1,
	default = 30,
	suffix = "s",
	callback = function(h)
		I.FishTimeout = h;
	end,
});
function N.panic()
	I.Mode = "off";
	I.AutoTrain = false;
	I.AutoFish = false;
	pcall(function()
		x:SetValue("autofarm", false);
		x:SetValue("autotrain", false);
		x:SetValue("autofish", false);
	end);
	pcall(N.fishSync);
	pcall(N.skillsRelease);
	pcall(function()
		keyrelease(84);
	end);
	I.cur = nil;
	N.aimRelease();
	pcall(N.skillsRelease);
	I.pendingLoot = nil;
	I.lootPhase = nil;
	I.follow = nil;
	I.dlg = nil;
	I.soul = nil;
	I.crow = nil;
	I.dungeon = nil;
	N.unlockStop();
	N.routeStop();
	N.collectStop();
	N.dropCache();
	N.sync();
	N.notify("stopped", 3);
end;
local It = {
		farm = 0,
		esp = 0,
		chest = 0,
		train = 0,
		clip = 0,
		afk = 0,
		zx = 0,
		guard = 0,
		key = 0,
		soul = 0,
	};
local function ft(h)
	if I.Mode == "off" then
		return;
	end;
	local Y, c, A = N.char();
	if not Y or A.Health <= 0 then
		return;
	end;
	if I.HealthGuard and A.Health < A.MaxHealth * I.GuardPct then
		if h - It.guard > 1 then
			It.guard = h;
			pcall(function()
				c.CFrame = c.CFrame + Vector3.new(0, 60, 0);
				c.AssemblyLinearVelocity = Vector3.new(0, 0, 0);
			end);
		end;
		return;
	end;
	local l, n;
	if I.Mode == "mob" then
		l, n = I.SelectedMob, false;
	elseif I.Mode == "crow" then
		local Y = N.crowStep(h);
		if not Y then
			if I.cur then
				I.cur = nil;
				N.aimRelease();
			end;
			return;
		end;
		l, n = Y, true;
	elseif I.Mode == "boss" then
		local h = N.bossSet();
		l, n = (h or "All Bosses"), true;
	elseif I.Mode == "dungeon" then
		N.mark("dungeon");
		if N.dungeonStep(h) then
			if I.cur then
				I.cur = nil;
				N.aimRelease();
			end;
			return;
		end;
		l, n = "__dungeon", nil;
	else
		local Y = N.activeQuest();
		if not Y then
			if I.questInfoKey then
				I.lastDoneKey = I.questInfoKey;
				I.questInfoKey = nil;
			end;
			local Y, c = N.pickQuest(h);
			if not Y then
				local Y, c = N.pickQuest(h);
				if I.cur then
					I.cur = nil;
					N.aimRelease();
				end;
				if h - ((It.qwarn or 0)) > 12 then
					It.qwarn = h;
					if c then
						N.notify("ticked quests cooling down - retry in " .. (math.ceil(c - h) .. "s"), 5);
					else
						N.notify("no quest to take - tick some in the quest list (or they are all done)", 6);
					end;
				end;
				return;
			end;
			if I.acceptKey ~= c then
				I.acceptKey = c;
				I.acceptTries = 0;
				I.acceptAt = nil;
				I.atGiver = false;
			end;
			if Y and Y.AutoDeliver then
				local c, D = N.pickDelivery(h);
				if not c then
					if I.cur then
						I.cur = nil;
						N.aimRelease();
					end;
					if D then
						for Y, c in ipairs(f.DeliverGivers) do
							if not N.questCompleted(c.QuestOption) then
								N.followNpc(c.Npc, c.Pos, h);
								break;
							end;
						end;
					end;
					if h - ((It.qwarn or 0)) > 15 then
						It.qwarn = h;
						if D then
							N.notify("delivery quests cooling down - retry in " .. (math.ceil(D - h) .. "s"), 5);
						else
							N.notify("every delivery quest is done", 6);
						end;
					end;
					return;
				end;
				if I.delivGiver ~= c.QuestOption then
					I.delivGiver = c.QuestOption;
					I.acceptTries = 0;
					I.acceptAt = nil;
					I.atGiver = false;
				end;
				if not ((I.delivSent and I.delivSent[c.QuestOption])) then
					I.acceptTries = 0;
				end;
				Y = c;
			end;
			if not ((Y and Y.Pos)) then
				if h - ((It.qwarn or 0)) > 8 then
					It.qwarn = h;
					N.notify("no active quest - pick one in the quest list to accept it", 5);
				end;
				return;
			end;
			if I.cur then
				I.cur = nil;
				N.aimRelease();
				pcall(N.skillsRelease);
				if N.clickRelease then
					pcall(N.clickRelease);
				end;
			end;
			local D = N.followNpc(Y.Npc, Y.Pos, h);
			local A = I.atGiver and 12 or 8;
			if D > A then
				I.atGiver = false;
				I.acceptAt = nil;
				return;
			end;
			I.atGiver = true;
			local l = N.questOpt(Y);
			if not l then
				if h - ((It.qwarn or 0)) > 8 then
					It.qwarn = h;
					N.notify("at " .. (tostring(Y.Npc) .. (": this quest has no " .. (I.QuestPick .. " option"))), 5);
				end;
				return;
			end;
			if N.questCompleted(l) then
				if h - ((It.qwarn or 0)) > 10 then
					It.qwarn = h;
					N.notify("already completed: " .. (l .. " - pick another quest"), 6);
				end;
				return;
			end;
			I.acceptAt = I.acceptAt or (h + I.SettleT);
			if h < I.acceptAt then
				return;
			end;
			I.hadQuest = false;
			local n = I.lastAcceptAt and (h - I.lastAcceptAt);
			if not n and Y.QuestOption then
				local h = N.questLastAge();
				if h then
					n = h;
				end;
			end;
			if n and n < I.QuestGap then
				if h - ((It.qwarn or 0)) > 10 then
					It.qwarn = h;
					N.notify("next quest in " .. (math.ceil(I.QuestGap - n) .. "s"), 3);
				end;
				return;
			end;
			if not N.remote then
				local c = N.dlgTalk(l, h);
				if c == "failed" and h - ((It.qwarn or 0)) > 12 then
					It.qwarn = h;
					N.notify("could not take \'" .. (l .. ("\' from " .. (tostring(Y.Npc) .. " - do it by hand"))), 6);
				end;
				return;
			end;
			if N.remote and (h - ((It.accept or 0))) > 8 then
				It.accept = h;
				I.acceptTries = ((I.acceptTries or 0)) + 1;
				if I.acceptTries > 3 then
					if h - ((It.qwarn or 0)) > 10 then
						It.qwarn = h;
						N.notify("server keeps refusing \'" .. (l .. "\' (level or quest limit?) - skipping it"), 6);
					end;
					if c then
						I.qBack = I.qBack or {};
						I.qBack[c] = h + 120;
					end;
					I.acceptTries = 0;
					return;
				end;
				pcall(function()
					N.remote:FireServer("AddQuest", l);
				end);
				if c and not Y.AutoDeliver then
					I.qSent = I.qSent or {};
					I.qSent[c] = I.qSent[c] or h;
				end;
				if Y.QuestOption == l and f.DeliverGivers then
					I.delivSent = I.delivSent or {};
					I.delivSent[l] = I.delivSent[l] or h;
				end;
				N.notify("accepting: " .. l, 3);
			end;
			return;
		end;
		I.acceptAt = nil;
		I.acceptTries = 0;
		I.atGiver = false;
		I.dlg = nil;
		if not I.hadQuest then
			I.lastAcceptAt = h;
		end;
		I.hadQuest = true;
		if Y.Desc and I.delivSent then
			I.delivSent[Y.Desc] = nil;
			if I.delivFails then
				I.delivFails[Y.Desc] = nil;
			end;
		end;
		local A, q = N.entryForQuest(Y);
		I.questInfo = A;
		I.questDesc = Y.Desc;
		I.questInfoKey = q;
		if q and I.qSent then
			I.qSent[q] = nil;
		end;
		if q and I.qBack then
			I.qBack[q] = nil;
		end;
		if Y.Complete then
			if I.cur then
				I.cur = nil;
				N.aimRelease();
				pcall(N.skillsRelease);
				if N.clickRelease then
					pcall(N.clickRelease);
				end;
			end;
			local Y = A;
			if Y and Y.Pos then
				N.followNpc(Y.Npc, Y.Pos, h);
			end;
			return;
		end;
		local g = A;
		local S;
		for h = 1, #Y.Tasks, 1 do
			if ((Y.Tasks[h].Value or 0)) < ((Y.Tasks[h].Max or 1)) then
				S = Y.Tasks[h];
				break;
			end;
		end;
		local k = f.Deliver[Y.Name];
		local H = k and (S and k.tasks[S.Name]);
		if H then
			N.deliverStep(k.opt, S.Name, H, c, h);
			return;
		end;
		I.deliverKey = nil;
		local R = N.taskKind(S and S.Name);
		l = nil;
		if R ~= "fetch" then
			local h = {};
			local function c(Y)
				if type(Y) == "string" and Y ~= "" then
					h[string.lower(Y)] = true;
				end;
			end;
			c(N.codeToMob(S and S.Code));
			c(N.questTarget(Y));
			if R == "kill" and (g and not g.Auto) then
				local h = Y.Desc and g.BossOption == Y.Desc;
				if h == nil then
					h = (I.QuestPick == "boss");
				end;
				c(h and ((g.BossTarget or g.Target)) or g.Target);
			end;
			local D = 0;
			for h in pairs(h) do
				D = D + 1;
			end;
			if D > 1 then
				l = h;
			elseif D == 1 then
				for h in pairs(h) do
					l = h;
				end;
			end;
		end;
		if not l then
			local h = N.questPickups();
			if #h > 0 then
				local Y = {};
				for c = 1, #h, 1 do
					Y[#Y + 1] = N.pickup(D, function()
							return D;
						end, h[c].Node, h[c].Root, nil, { tap = true });
				end;
				N.collectStart(Y, "quest pickup: " .. h[1].Name);
				return;
			end;
			if R == "kill" and (g and (not g.Auto and g.Target)) then
				l = g.Target;
			end;
		end;
		if not l and R == "fetch" then
			if h - ((It.qwarn or 0)) > 10 then
				It.qwarn = h;
				N.notify("\'" .. (tostring(S and S.Name) .. "\' is not a kill objective and no prop is spawned - nothing to farm"), 6);
			end;
			return;
		end;
		n = false;
		if not l then
			if h - ((It.qwarn or 0)) > 8 then
				It.qwarn = h;
				N.notify("nothing to farm or pick up in: " .. tostring(Y.Name), 5);
			end;
			return;
		end;
	end;
	local q = (type(l) == "table") and N.setKey(l) or tostring(l);
	local g = I.cur;
	if g and g.wantKey == q then
		local Y = nil;
		local c = N.curAlive(g) and pcall(function()
				Y = g.Hum.Health;
			end);
		if not g.stale and (((not c or not Y or Y <= 0)) and (I.LootBoss and (g.Boss and (g.lastPos and I.Mode ~= "dungeon")))) then
			I.pendingLoot = { pos = g.lastPos, t = h };
		end;
		if Y and Y > 0 then
			if g.lastHp == nil or Y < g.lastHp then
				g.lastHp = Y;
				g.t = h;
			end;
			pcall(function()
				g.lastPos = g.Root.Position;
			end);
			if (h - g.t) < 8.0 then
				N.attack(h);
				return;
			end;
			N.notify("target stuck, re-picking", 2);
		end;
		I.cur = nil;
		N.dropCache();
	end;
	N.mark("farm/scan");
	local S;
	if I.Mode == "dungeon" then
		S = N.dungeonTargets(h);
	else
		S = N.scanCached(l, n, h, .5, I.Mode == "boss" or I.Mode == "crow");
	end;
	N.mark("farm/pick n=" .. #S);
	if #S == 0 and I.Mode == "quest" then
		local h = I.questInfo;
		local Y = h and (h.BossOption and I.questDesc == h.BossOption);
		if h and (h.Alt and not Y) then
			S = N.scan(h.Alt, false);
		end;
	end;
	if #S == 0 then
		if I.Mode == "dungeon" then
			return;
		end;
		if h - It.guard > I.Retarget * 3 then
			It.guard = h;
			local Y = N.wantSpawn(l);
			if Y then
				N.tpTo(Y, 3);
			elseif h - ((It.nodest or 0)) > 12 then
				It.nodest = h;
				local Y = (type(l) == "table") and N.bossLabel() or tostring(l);
				N.notify("no known spot for " .. (Y .. " - find it once and it is remembered"), 6);
			end;
		end;
		return;
	end;
	local k = N.nearest(S, c.Position);
	if not k then
		N.dropCache();
		return;
	end;
	k.want = l;
	k.wantKey = q;
	k.t = h;
	if k.Head == nil then
		k.MAddr = N.addr(k.Model);
		pcall(function()
			k.Head = k.Model:FindFirstChild("Head") or false;
		end);
	end;
	I.cur = k;
	N.attack(h);
end;
local function Nt()
	if not I.Running then
		return;
	end;
	local h = os.clock();
	N.markWrite(h);
	N.mark("tick");
	N.deathCheck(h);
	if I.unlock then
		N.mark("unlock");
		local Y, c = pcall(N.unlockStep, h);
		if Y and c then
			return;
		end;
		if not Y then
			N.unlockStop();
		end;
	end;
	if I.AutoSouls and (N.farming() and not I.route) then
		N.mark("souls");
		local Y, c = pcall(N.soulStep, h);
		if Y and c then
			return;
		end;
	end;
	if I.route then
		N.mark("route");
		local Y, c = pcall(N.routeStep, h);
		if Y and c then
			return;
		end;
		if not Y then
			N.routeStop();
		end;
	end;
	if I.collect then
		N.mark("collect");
		if pcall(N.collectStep, h) then
			return;
		end;
	end;
	if ((I.AutoLoot or I.AutoSouls)) and (N.farming() and (not I.collect and (not I.lootPhase and (not I.route and (h - ((It.loot or 0))) > 2.0)))) then
		It.loot = h;
		local Y, c = N.char();
		if c then
			if I.AutoLoot then
				local h = N.lootNear(c.Position, I.LootRange);
				if #h > 0 then
					N.collectStart(h, "loot");
				end;
			end;
		end;
	end;
	if I.pendingLoot and (h - I.pendingLoot.t) > 1.0 then
		I.lootPhase = { pos = I.pendingLoot.pos, t0 = h, last = h };
		I.pendingLoot = nil;
		N.notify("boss down, collecting drops", 3);
	end;
	if I.lootPhase then
		N.mark("bossloot");
		local Y = I.lootPhase;
		if h - ((Y.scanT or 0)) < .25 then
			N.travelTo(Y.pos, h);
			return;
		end;
		Y.scanT = h;
		local c = N.lootNear(Y.pos, 90);
		local D, A = {}, {};
		for h = 1, #c, 1 do
			if c[h].tap then
				D[#D + 1] = c[h];
			else
				A[#A + 1] = c[h];
			end;
		end;
		if #D > 0 or #A > 0 then
			Y.sawChest = true;
		end;
		if #D > 0 then
			Y.last = h;
			N.collectStart(D, "boss chest");
			return;
		end;
		if #A > 0 and ((Y.passes or 0)) < 4 then
			Y.last = h;
			Y.passes = ((Y.passes or 0)) + 1;
			if N.routeStart(A) then
				return;
			end;
		end;
		local l = (h - Y.last) > I.LootWait;
		if not Y.sawChest then
			l = (h - Y.t0) > 25;
		end;
		if (h - Y.t0) > 60 then
			l = true;
		end;
		if l then
			if not Y.sawChest then
				N.notify("no boss chest appeared, moving on", 4);
			end;
			I.lootPhase = nil;
			N.notify("drops done, next boss", 2);
		else
			N.travelTo(Y.pos, h);
			return;
		end;
	end;
	if h - It.key >= .1 then
		It.key = h;
		if iskeypressed and iskeypressed(I.PanicVK) then
			if I.Mode ~= "off" or I.AutoTrain or I.collect then
				N.panic();
			end;
		end;
	end;
	N.mark("dlg");
	pcall(N.dlgRelease, h);
	if I.follow and not I.cur then
		N.mark("follow");
		pcall(N.followStep, h);
	end;
	if I.Mode ~= "off" and (I.cur and N.curAlive(I.cur)) then
		N.mark("steer");
		pcall(N.steer);
		local Y;
		pcall(function()
			local h = I.cur;
			local c = h.Head;
			Y = (c and c.Position) or h.Root.Position;
		end);
		N.mark("aim");
		pcall(N.aimStep, Y);
		local c, D = N.char();
		if D and Y then
			local c = ((Y - D.Position)).Magnitude;
			N.mark("skills");
			pcall(N.skillStep, h, c);
		end;
	else
		N.aimRelease();
		pcall(N.skillsRelease);
	end;
	if h - It.farm >= I.AttackDelay then
		It.farm = h;
		N.mark("farm/" .. tostring(I.Mode));
		pcall(ft, h);
		N.mark("farm/done");
	end;
	if N.espOn() and h - It.esp >= .35 then
		It.esp = h;
		N.mark("espbuild");
		pcall(N.espBuild);
	end;
	if I.AutoTrain and (not I.AutoFish and (not I.collect and h - It.train >= 1.5)) then
		It.train = h;
		pcall(function()
			local c = Y:FindFirstChild("Player_Service");
			local D = c and c:FindFirstChild("Values");
			local l = D and D:FindFirstChild(A.Name);
			if not l then
				return;
			end;
			if l:FindFirstChild("Training") then
				if N.remote then
					N.remote:FireServer("training_signaler", "Stop", true);
				else
					I.trainSince = I.trainSince or h;
					local Y = I.bar;
					if not ((Y and Y.active)) and (h - I.trainSince > 5 and h - ((I.trainWarn or 0)) > 30) then
						I.trainWarn = h;
						N.notify("click mode only plays the Squat bar - finish this station yourself", 6);
					end;
				end;
			else
				I.trainSince = nil;
			end;
		end);
	end;
	if I.NoClip and h - It.clip >= .25 then
		It.clip = h;
		pcall(function()
			local h = A.Character;
			if not h then
				return;
			end;
			local Y = h:GetChildren();
			for h = 1, #Y, 1 do
				local c = Y[h];
				if ((c.ClassName == "Part" or c.ClassName == "MeshPart")) and c.Name ~= "HumanoidRootPart" then
					c.CanCollide = false;
				end;
			end;
		end);
	end;
	if I.Mode ~= "off" and (h - ((It.survey or 0))) > 10 then
		It.survey = h;
		pcall(function()
			N.scan(nil, nil);
		end);
	end;
	pcall(N.seenSave, h);
	if I.AntiAFK and h - It.afk >= 120 then
		It.afk = h;
		pcall(function()
			mousemoverel(2, 0);
			mousemoverel(-2, 0);
		end);
	end;
end;
function N.deathCheck(h)
	if h - ((I.deathT or 0)) < .25 then
		return;
	end;
	I.deathT = h;
	local Y, c, D = N.char();
	local A = true;
	if D then
		pcall(function()
			A = D.Health <= 0;
		end);
	end;
	if A and not I.wasDead then
		I.wasDead = true;
		pcall(function()
			mouse1release();
		end);
		pcall(function()
			mouse2release();
		end);
		for h, Y in ipairs({
			70,
			84,
			90,
			88,
			67,
			86,
			66,
			78,
			75,
		}) do
			pcall(function()
				keyrelease(Y);
			end);
		end;
		I.rmbDown, I.lmbDown = false, false;
		pcall(N.skillsRelease);
		I.cur, I.collect, I.route, I.soul = nil, nil, nil, nil;
		if I.unlock then
			I.unlock.held = false;
		end;
		N.mark("died");
	elseif not A and I.wasDead then
		I.wasDead = nil;
	end;
end;
function N.farming()
	return I.Mode ~= "off" or I.lootPhase ~= nil or I.pendingLoot ~= nil;
end;
function N.busy()
	return I.Mode ~= "off" or I.collect ~= nil or I.route ~= nil or I.lootPhase ~= nil or I.unlock ~= nil or I.pendingLoot ~= nil or I.AutoTrain or I.NoClip or I.AntiAFK or N.espOn();
end;
function N.sync()
	I.syncWant = true;
end;
function N.syncNow()
	N.espSync();
	pcall(N.fishSync);
	pcall(N.syncSkills);
	if N.busy() then
		if not I.tickConn then
			I.tickConn = c.Heartbeat:Connect(function()
					local h, Y = pcall(Nt);
					if not h then
						N.logErr("tick", Y);
					end;
				end);
		end;
	elseif I.tickConn then
		pcall(function()
			I.tickConn:Disconnect();
		end);
		I.tickConn = nil;
		N.aimRelease();
		pcall(N.skillsRelease);
		I.cur = nil;
		N.dropCache();
	end;
end;
pcall(N.markPrev);
pcall(N.seenLoad);
pcall(function()
	x:LoadSettings();
end);
I.deferConn = c.Heartbeat:Connect(function()
		local h, Y = pcall(N.deferRun);
		if not h then
			N.logErr("defer", Y);
		end;
	end);
pcall(N.syncNow);
N.notify("loaded - rshift for the menu", 4);
print("[Slayers 2] loaded");
